heron-agent: HERON-REVIT-ELE-010
heron-step: 14
heron-status: PROVEN
heron-since: 0.1.0
heron-layer: brain

id: FRG-GEO-013
semantic-identity: "space copies of these out evenly along a direction"
kind: action
domain: revit.geometry
capability: ARRAY_ELEMENTS
version: 1
source: OFFICIAL
risk: MODIFY

purpose: >
  Copies elements at an even spacing along a direction — diffusers down a
  corridor, sprinklers along a branch, lights on a grid line.

  **The count and the spacing are both stated, and neither is derived from the
  other.** "Every 3 metres" and "eight of them" are different instructions that
  give different answers in the same corridor, and a fragment that computed one
  from a length would be choosing which the user meant.

  **The originals are not moved.** An array of eight means the original plus
  seven copies, which is what Revit's own array does and what a modeller
  expects when they count what appeared.

  **It is a plain copy, not a Revit Array element.** A real Array is a
  constrained group that stays linked and renumbers itself; independent copies
  are what somebody wants when they intend to adjust them individually
  afterwards. Nothing here creates the linked kind — that is a different
  capability and it changes what every later edit does.

  ASSUMES AN OPEN TRANSACTION and does not open one (Golden Rule 16).

contract:
  needs:
    - name: doc
      type: Document
    - name: elements
      type: IList<Element>
    - name: direction
      type: XYZ
      source: request
    - name: spacingMm
      type: double
      source: request
    - name: count
      type: int
      source: request
  provides:
    - name: created
      role: result
      type: IList<ElementId>
    - name: copiesEach
      role: result
      type: int
    - name: refused
      # Why it declined, in the user's words - the evidence it looked and
      # said no. Never a thing it FOUND, so it must not count toward
      # "did this come back empty" (D-52).
      role: accounting
      type: IList<ElementId>

proof:
  date: '2026-09-10'
  by: Ajmal PS
  model: Project1 work_ajmal.al (3,471 elements), Revit 2024, session 24688
  positive_case: run as it would normally be run - the arrangement was re-made first by running
    select-by-category-name, set-selection with this phase's own values - run inside a transaction,
    and Revit reported the transaction group ROLLED BACK afterwards. returned MillimetresPerFoot
    304.8, copiesEach 2, count 3, created 10 item(s) [926619, 926622, 926625, ...], length
    0.00328083989501312, refused 0 item(s), spacingMm 2000. inputs came elements from the
    selection (5); direction as given; spacingMm as given; count as given. on Project1 work_ajmal.al
  negative_case: run with categoryName=Ducts, count=1, direction=1,0,0, inViewOnly=1 - Mech,
    spacingMm=2000 instead - chosen because it should not contain what this fragment reports
    - the arrangement was re-made first by running select-by-category-name, set-selection
    with this phase's own values - run inside a transaction, and Revit reported the transaction
    group ROLLED BACK afterwards. returned MillimetresPerFoot 304.8, copiesEach 0, count 1,
    created 0 item(s), length 0.00328083989501312, refused 5 item(s) [925641, 925646, 925658,
    ...], spacingMm 2000. inputs came elements from the selection (5); direction as given;
    spacingMm as given; count as given. on Project1 work_ajmal.al
  second_route: NOT ESTABLISHED - no second route was run. D-30 asks for one 'where one exists';
    whether one exists here has not been settled, and a declared absence is not the same as
    none
  fingerprint: aab5a4a849bae2fe

revit: ["2020", "2021", "2022", "2023", "2024", "2025", "2026", "2027"]
runtime: [net472, net48, net8.0-windows, net10.0-windows]
tests: tests/

compatibility-note: >
  One implementation for every release. `ElementTransformUtils.CopyElement` and
  `XYZ.Normalize` are unchanged 2020 to 2027. Millimetres to feet is
  arithmetic (D-20).

# ROUTING - repeated in COPY_ELEMENTS and SNAP_TO_GRID.
#
#   "array them along the corridor"  -> here
#   "space these out evenly"         -> here
#   "copy these to the other floors" -> COPY_ELEMENTS - one offset, not a run
#   "line them up on the grid"       -> SNAP_TO_GRID
#
# ARRAYING AND COPYING ARE THE SAME WORD HALF THE TIME. One offset repeated N
# times is this; one offset once is COPY_ELEMENTS.

# ROUTING - repeated in ARRAY_ELEMENTS_RADIAL.
#
#   "space these out along the corridor"  -> here. A DIRECTION and a spacing
#   "array these round a circle"          -> ARRAY_ELEMENTS_RADIAL. A CENTRE
#                                            point and a sweep angle. The two
#                                            share no arithmetic - one adds a
#                                            vector repeatedly, the other
#                                            rotates about an axis - which is
#                                            why they are two fragments rather
#                                            than one with a mode flag
#
# THE WORD "ARRAY" MEANS BOTH SHAPES. Which one is meant shows in the rest of
# the sentence: a direction or a spacing is linear, a centre or a circle is
# radial.

utterances:
  - array them along the corridor
  - space these out evenly
  - repeat this every 3 metres
  - put eight of them along here
  - copy this along the branch
