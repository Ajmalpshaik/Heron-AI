heron-agent: HERON-REVIT-VIE-013
heron-step: 17
heron-status: PROVEN
heron-since: 0.1.0
heron-layer: brain

id: FRG-VIEW-026
semantic-identity: "make an empty drafting view to draw a detail in"
kind: action
domain: revit.views
capability: CREATE_DRAFTING_VIEW
version: 1
source: OFFICIAL
risk: MODIFY

purpose: >
  Creates a drafting view - a sheet-ready view with no model in it, for a
  standard detail, a schematic or an imported CAD detail.

  **It shows no model at all, and that is what it is for.** A drafting view is
  drawn by hand: it does not update when the model changes, and nothing in it is
  ever wrong because the ductwork moved. Every standard detail in a set is one.
  Somebody expecting it to show the model has asked for a section instead.

contract:
  needs:
    - name: doc
      type: Document
    - name: viewName
      type: string
      source: request
    - name: viewScale
      type: int
      source: request
  provides:
    - name: created
      type: ElementId
    - name: refused
      # Why it declined, in the user's words - the evidence it looked and
      # said no. Never a thing it FOUND, so it must not count toward
      # "did this come back empty" (D-52).
      role: accounting
      type: string

proof:
  date: '2026-09-08'
  by: Ajmal PS
  model: Snowdon-scratch_ajmal.al (9,628 elements), Revit 2024, session 1680
  positive_case: run as it would normally be run - run inside a transaction and ROLLED BACK,
    so the model was left exactly as it was. returned clash (null), created ElementId, draftingType
    ViewFamilyType, name HERON TEST DV9, refused (null), viewScale 100. inputs came viewName
    as given; viewScale as given. on Snowdon-scratch_ajmal.al
  negative_case: run with viewName=How to use this project 1, viewScale=100 instead - chosen
    because it should not contain what this fragment reports - run inside a transaction and
    ROLLED BACK, so the model was left exactly as it was. returned clash ViewDrafting, created
    (null), draftingType ViewFamilyType, name How to use this project 1, refused a view called
    "How to use this project 1" already exists - Revit refuses a duplicate name, and creating
    it first and renaming after leaves an unnamed view in the browser, viewScale 100. inputs
    came viewName as given; viewScale as given. on Snowdon-scratch_ajmal.al
  second_route: NOT ESTABLISHED - no second route was run. D-30 asks for one 'where one exists';
    whether one exists here has not been settled, and a declared absence is not the same as
    none
  fingerprint: da46df2b06accd03

revit: ["2020", "2021", "2022", "2023", "2024", "2025", "2026", "2027"]
runtime: [net472, net48, net8.0-windows, net10.0-windows]
tests: tests/

compatibility-note: >
  `ViewDrafting.Create(Document, ElementId)` and the `ViewFamily` enumeration
  are unchanged 2020 to 2027 - read off the reference assemblies at both ends.
  No ElementId is built from an int, so 2024's 64-bit ids do not reach this
  code.

# ROUTING - repeated in CREATE_SECTION_VIEW and CREATE_PLAN_VIEW.
#
#   "make a drafting view"       -> here
#   "I need a detail view"       -> here - for a hand-drawn standard detail
#   "create a section through this" -> CREATE_SECTION_VIEW. Shows the MODEL and
#                                      follows it; a drafting view shows nothing
#                                      and never changes
#   "make the plan for this level"  -> CREATE_PLAN_VIEW
#   "create a callout"           -> CREATE_CALLOUT - an enlarged part of a view
#                                   that already exists
#   "make another legend like this one" -> CREATE_LEGEND_VIEW
#   "hatch this area on the drawing" -> CREATE_FILLED_REGION

utterances:
  - make a drafting view
  - I need a detail view
  - create an empty view for a detail
  - add a drafting view for this detail
  - make a view to draw the detail in
