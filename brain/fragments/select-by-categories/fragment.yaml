heron-agent: HERON-REVIT-CAT-009
heron-step: 17
heron-status: PROVEN
heron-since: 0.1.0
heron-layer: brain

id: FRG-SEL-024
semantic-identity: "all the ductwork - the ducts, the fittings, the accessories and the flex"
kind: filter
domain: revit.selection
capability: SELECT_BY_CATEGORIES
version: 1
source: OFFICIAL
risk: READ

purpose: >
  Collects several categories into one set in a single pass. What somebody
  means by *"the ductwork"* is five Revit categories - ducts, fittings,
  accessories, flex duct and the insulation on them - and by *"the pipework"*,
  five more. One category is almost never the whole answer.

  **ONE COLLECTOR, NOT ONE PER CATEGORY.** Running a separate collector for
  each category walks the document that many times and then needs the results
  de-duplicated by hand. A multi-category filter asks once, which is both
  faster and the only version that cannot return the same element twice.

  **IT REPORTS THE BREAKDOWN, WHICH IS WHERE THE MISTAKE SHOWS.** A total of
  480 says nothing about whether the flex duct was included; 300 ducts, 150
  fittings, 30 accessories and no flex says the model has no flex duct or the
  category was left out of the list. One of those is worth knowing before a
  takeoff goes out.

  **A CATEGORY WITH NOTHING IN IT IS REPORTED AS ZERO, NOT LEFT OUT.** The
  absence is the finding: a duct system with no accessories is either a model
  with no dampers in it yet, or a filter that missed them.

  ASSUMES NOTHING ABOUT TRANSACTIONS - it opens none and needs none.

contract:
  needs:
    - name: doc
      type: Document
    - name: categories
      type: IList<BuiltInCategory>
      source: request
    # Null means the whole model; a view narrows it to what that view draws.
    - name: inViewOnly
      type: View
      source: request
  provides:
    - name: elements
      role: result
      type: IList<Element>
    - name: perCategory
      # One entry per category ASKED FOR, with what each contributed. Its
      # length mirrors the request, so it can never be zero when a category
      # was named - the counts inside are the answer, not the entry count.
      role: accounting
      type: IDictionary<string, int>
    - name: found
      role: result
      type: int

proof:
  date: '2026-09-08'
  by: Ajmal PS
  model: Snowdon-scratch_ajmal.al (9,628 elements), Revit 2024, session 1680
  positive_case: run as it would normally be run. returned elements 307 item(s) [Tees, Tees,
    Tees, ...], found 307, perCategory 1 entry(ies), wanted 1 item(s) [OST_DuctCurves]. inputs
    came categories as given (1); inViewOnly as given. on Snowdon-scratch_ajmal.al
  negative_case: run with categories=Ducts, inViewOnly=How to use this project 1 instead -
    chosen because it should not contain what this fragment reports. returned elements 0 item(s),
    found 0, perCategory 1 entry(ies), wanted 1 item(s) [OST_DuctCurves]. inputs came categories
    as given (1); inViewOnly as given. on Snowdon-scratch_ajmal.al
  second_route: NOT ESTABLISHED - no second route was run. D-30 asks for one 'where one exists';
    whether one exists here has not been settled, and a declared absence is not the same as
    none
  fingerprint: 79105382d7806412

revit: ["2020", "2021", "2022", "2023", "2024", "2025", "2026", "2027"]
runtime: [net472, net48, net8.0-windows, net10.0-windows]
tests: tests/

compatibility-note: >
  One implementation for every release. The multi-category filter and the
  view-scoped collector are unchanged 2020 to 2027, and categories are named
  through the element's own category rather than by converting an id back to a
  number.

# ROUTING - the same rows are in the fragments named here.
#
#   "all the ductwork"                  -> here: ducts, fittings, accessories,
#                                          flex and insulation are five
#                                          categories and one answer
#   "all the pipework"                  -> here
#   "all the ducts"                     -> FILTER_ELEMENTS_BY_CATEGORY when it
#                                          really is the one category
#   "find all the VCDs"                 -> SELECT_BY_FAMILY, by family name
#   "everything of this type"           -> FILTER_ELEMENTS_BY_TYPE
#   "what is visible in this view"      -> SELECT_VISIBLE_IN_VIEW, which is
#                                          about visibility rather than
#                                          category

utterances:
  - all the ductwork
  - select the whole pipe system
  - ducts and fittings and accessories together
  - everything in these categories
  - all the mep in this view
