heron-agent: HERON-REVIT-DIM-031
heron-step: 17
heron-status: PROVEN
heron-since: 0.1.0
heron-layer: brain

id: FRG-VIEW-087
semantic-identity: "stack these tags in a tidy column at the side"
kind: action
domain: revit.annotation
capability: STACK_TAGS
version: 1
source: OFFICIAL
risk: MODIFY

purpose: >
  Arranges tags into a neat vertical column at one point, each still leadered
  back to its own element. What a drafter does by hand beside a busy area, when
  there is no room for the tags where the elements are.

  **THE STACK ORDER IS NEAREST-ELEMENT-FIRST, NOT THE ORDER THE SET ARRIVED
  IN.** Tags are sorted by where their own element sits along the stack, so the
  leaders fan out without crossing. Stacking them in the order a filter
  happened to return produces a plate of spaghetti that looks like the tool is
  broken - and it is the same tags, in the same places, with the same leaders.

  **THE GAP IS A PAPER DISTANCE AND HAS TO BE MULTIPLIED BY THE VIEW SCALE.**
  Every millimetre in tag work is a paper millimetre: 5 mm between tags is 500
  model mm at 1:100 and 250 at 1:50. Using the number as a model distance is
  the commonest mistake in this whole subject, and it produces a stack that is
  either unreadable or off the sheet.

  **IT MOVES HEADS AND LEAVES THE LEADERS TO FOLLOW.** An attached leader end
  stays on its element; a free one is carried back to where it was, so the
  arrows do not come off the things they point at.

  ASSUMES AN OPEN TRANSACTION and does not open one (Golden Rule 16).

contract:
  needs:
    - name: doc
      type: Document
    - name: view
      type: View
      source: request
    - name: elements
      type: IList<Element>
    # Where the top of the stack goes, in the view's plane.
    - name: stackAt
      type: XYZ
      source: request
    # Paper millimetres between tags - multiplied by the view scale here.
    - name: gapMm
      type: double
      source: request
  provides:
    - name: stacked
      role: result
      type: int
    - name: noHead
      # Tags with no head to move, so it could not be stacked. The evidence it
      # looked, never a thing it found (D-52).
      role: accounting
      type: IList<ElementId>
    - name: notTags
      # Handed in but not tags, so there was nothing here to act on. A count
      # of what it was GIVEN and could not use, never a thing it found (D-52).
      role: accounting
      type: IList<ElementId>
    - name: refused
      # Why it declined, in the user's words - the evidence it looked and
      # said no. Never a thing it FOUND, so it must not count toward
      # "did this come back empty" (D-52).
      role: accounting
      type: IList<ElementId>

proof:
  date: '2026-09-14'
  by: Ajmal PS
  model: Snowdon-scratch_ajmal.al (9,638 elements), Revit 2024, session 33220
  positive_case: run as it would normally be run - the arrangement was re-made first by running
    select-by-category-name, set-selection with this phase's own values - run inside a transaction,
    and Revit reported the transaction group ROLLED BACK afterwards. returned MmToFeet 0.00328083989501312,
    down XYZ, gap 157.48031496063, gapMm 500, headOf Func`2, index 9, movable 9 item(s) [[-37.7595552681191,
    Autodesk.Revit.DB.IndependentTag], [-10.4245837343251, Autodesk.Revit.DB.IndependentTag],
    [-11.0047397696025, Autodesk.Revit.DB.IndependentTag], ...], noHead 0 item(s), notTags
    0 item(s), refused 0 item(s), scale 96, stacked 9, targetOf Func`2. inputs came view as
    given; elements from the selection (9); stackAt as given; gapMm as given. on Snowdon-scratch_ajmal.al
  negative_case: 'run with categoryName=Ducts, inViewOnly=FloorPlan: M1, view=FloorPlan: M1,
    stackAt=-22000,8000,0, gapMm=500 instead - chosen because it should not contain what this
    fragment reports - the arrangement was re-made first by running select-by-category-name,
    set-selection with this phase''s own values - run inside a transaction, and Revit reported
    the transaction group ROLLED BACK afterwards. returned MmToFeet 0.00328083989501312, down
    XYZ, gap 157.48031496063, gapMm 500, headOf Func`2, index 0, movable 0 item(s), noHead
    0 item(s), notTags 22 item(s) [1447716, 1447725, 1447768, ...], refused 0 item(s), scale
    96, stacked 0, targetOf Func`2. inputs came view as given; elements from the selection
    (22); stackAt as given; gapMm as given. on Snowdon-scratch_ajmal.al'
  second_route: NOT ESTABLISHED - no second route was run. D-30 asks for one 'where one exists';
    whether one exists here has not been settled, and a declared absence is not the same as
    none
  fingerprint: a025c8f0653108d1

revit: ["2020", "2021", "2022", "2023", "2024", "2025", "2026", "2027"]
runtime: [net472, net48, net8.0-windows, net10.0-windows]
tests: tests/

compatibility-note: >
  A compile-time split at 2022 for the leader end, which became a method taking
  the reference it belongs to. Tag classes share no base that exposes a head
  position, so an independent tag and a spatial tag - room, space and area -
  are handled in their own branches rather than by one cast: casting to the
  independent kind silently skips every room tag in the set.

# TRAP - OBSERVED ELSEWHERE, NOT YET PROVEN HERE. Added 2026-09-23.
#
# Another tool, on real models and mostly on Revit 2020, found that a ROOM TAG
# moved out of its room needs a LEADER: without one Revit flags the tag as
# outside its room, and on the sheet the tag reads as belonging to whatever
# room it now sits over. This fragment moves room tags (`SpatialElementTag`)
# into a stack along with element tags, and a stack is rarely inside every
# room it lists - and it does not switch a room tag's leader on. Not
# reproduced here. A note only: the fragment is PROVEN, and a change to its
# code would make the proof stale. After stacking room tags, check each one
# still points at its room. NEEDS-CHECKING AH9.

# ROUTING - the same rows are in the fragments named here.
#
#   "stack these tags at the side"       -> here
#   "put the tags in a column"           -> here
#   "the tags are on top of each other"  -> FIND_OVERLAPPING_TAGS to see them,
#                                           ARRANGE_TAGS to push them apart
#                                           where they are
#   "spread the tags out"                -> ARRANGE_TAGS. This one gathers them
#                                           instead, which is the answer when
#                                           there is no room at all
#   "centre the room tags"               -> CENTER_ROOM_TAGS
#   "make the leaders L-shaped"          -> FORCE_TAG_LEADER_LSHAPE
#   "put tags on these"                  -> TAG_ELEMENTS_IN_VIEW. This one
#                                           only moves tags that exist

utterances:
  - stack these tags in a column
  - put the tags at the side of the plan
  - there is no room for the tags here
  - line the tags up vertically
  - stack the tags
  - put the tags in a column
