heron-agent: HERON-REVIT-SYS-030
heron-step: 17
heron-status: DRAFT
heron-since: 0.1.0
heron-layer: brain

id: FRG-MEP-036
semantic-identity: "what connectors are on this thing, where they point, and what is really plugged into them"
kind: action
domain: revit.mep
capability: REPORT_CONNECTORS
version: 2
source: OFFICIAL
risk: READ

purpose: >
  Lists every MEP connector on the given elements - domain and, on a duct or
  pipe connector, its system classification (Supply Air, Return Air and the
  rest), shape, size, where it sits and which way it faces - and, for each
  one, the elements REALLY joined to it. Read-only.

  **THE WHOLE LIST ALSO COMES BACK AS ONE LINE OF TEXT**, `connectorSummary`,
  because a chat is sent only how many entries a dictionary holds: through a
  chat this answered `connectorFacts 1 entry(ies)` and nothing else (Project1,
  Revit 2024, 2026-09-23). It stops after 50 connectors, at a whole element,
  and says how many it left out.

  **`IsConnected` IS AN INTENT FLAG, NOT A FACT, AND IT HAS BEEN WRONG IN A
  REAL MODEL.** A connector can report `true` with nothing physically joined to
  it, and a run traced from that flag comes back complete while the air stops
  at a gap. So the flag is read and reported, and it is never what decides:
  the partners come from walking `AllRefs` and keeping only refs owned by
  ANOTHER element. Where the two disagree the element is named in
  `flagDisagrees`, which is the finding, not a footnote.

  **A SYSTEM IS NOT A PARTNER.** `AllRefs` also carries the logical MEP system
  the connector belongs to. Counting that as a connection makes every connector
  on a named system look joined, which is the exact failure the flag already
  causes - so refs owned by an `MEPSystem`, and refs back to the element
  itself, are dropped before anything is counted.

  **IT IS THE LOOK BEFORE THE DRAW.** Before a duct can be run off a spigot,
  four things have to be known and only one of them is in a schedule: is there
  a connector, what domain and size is it, which way does it face, and is it
  already taken. That is this fragment, and the direction is why it exists in
  geometry rather than as a count - a spigot facing into the wall is a
  different job from one facing the corridor.

  Positions and sizes are handed on in Revit's internal feet, not millimetres
  (D-20 keeps the conversion at the edge, where the user is).

  ASSUMES NOTHING ABOUT TRANSACTIONS - it opens none and needs none.

contract:
  needs:
    - name: elements
      type: IList<Element>
  provides:
    - name: connectorFacts
      role: result
      type: IDictionary<ElementId, IList<string>>
    - name: connections
      role: result
      type: IDictionary<ElementId, IList<ElementId>>
    - name: openConnectors
      role: result
      type: IDictionary<ElementId, int>
    - name: flagDisagrees
      # Connectors whose IsConnected flag does not match what is physically
      # joined. ISCONNECTED IS AN INTENT FLAG, NOT A FACT, AND IT HAS BEEN
      # WRONG IN A REAL MODEL - a run traced from it comes back complete while
      # the air stops at a gap.
      role: result
      type: IList<ElementId>
    - name: connectorSummary
      # Every line of `connectorFacts`, element by element, as ONE string. A
      # reply prints a dictionary as its size alone, so through a chat the
      # three above arrived as `1 entry(ies)` each and the connectors
      # themselves never arrived at all (Project1, Revit 2024, 2026-09-23).
      # A RESULT, and EMPTY when nothing had a connector: an empty string is
      # what the proving tool reads as nothing found, where any sentence is a
      # shape it cannot count. Stops after 50 connectors, at a whole element,
      # and its last part says how many it left out (D-26).
      role: result
      type: string
    - name: noConnectors
      # Elements with no connectors to list, so it could not be reported on.
      # The evidence it looked, never a thing it found (D-52).
      role: accounting
      type: IList<ElementId>

proof:
  date: '2026-09-07'
  by: Ajmal PS
  model: Snowdon Towers Sample HVAC (9,628 elements), Revit 2024, session 42676
  positive_case: 28 duct taps selected in Snowdon Towers Sample HVAC - run as it would normally
    be run. returned connections 28 entry(ies), connectorFacts 28 entry(ies), flagDisagrees
    0 item(s), noConnectors 0 item(s), openConnectors 28 entry(ies). inputs came elements
    from the selection (28). on Snowdon Towers Sample HVAC
  negative_case: 16 spaces selected in Snowdon Towers Sample HVAC (Residential Lobby 106,
    Two Story Studio Unit 204, and others) - none of which is what this fragment reports,
    so the answer had to be empty. returned connections 0 entry(ies), connectorFacts 0 entry(ies),
    flagDisagrees 0 item(s), noConnectors 16 item(s) [1410865, 1410870, 1410963, ...], openConnectors
    0 entry(ies). inputs came elements from the selection (16). on Snowdon Towers Sample HVAC
  second_route: NOT ESTABLISHED - no second route was run. D-30 asks for one 'where one exists';
    whether one exists here has not been settled, and a declared absence is not the same as
    none
  fingerprint: e1a32e3e98d7154e

revit: ["2020", "2021", "2022", "2023", "2024", "2025", "2026", "2027"]
runtime: [net472, net48, net8.0-windows, net10.0-windows]
tests: tests/

compatibility-note: >
  One implementation for every release. `ConnectorManager`, `Connector.AllRefs`,
  `Domain`, `Shape`, `Origin` and `CoordinateSystem` are unchanged 2020 to 2027,
  and so are `DuctSystemType` and `PipeSystemType` - each documented, on every
  release, as throwing InvalidOperationException on a connector whose domain is
  not its own, so each is read only inside its own domain and guarded there.
  The size properties are the version-independent risk instead: `Radius` is only
  there on a round connector and `Width`/`Height` only on a rectangular one, and
  the wrong one THROWS rather than returning zero - on every release. Each is
  guarded, and an electrical connector, which has no profile at all, is reported
  with its domain and no size rather than being dropped.

# BACK TO DRAFT ON 2026-09-23, AND THE PROOF ABOVE IS KEPT AS THE RECORD OF
# WHAT WAS RUN.
#
# The proof was taken on the code as it stood. The code then changed: every
# connector line now carries its duct or pipe system type after the domain -
# "DomainHvac SupplyAir" - and `connectorSummary` was added, the whole answer
# as one string, because through a chat the three dictionaries arrived as
# counts and the connectors never arrived at all. Nothing the proof recorded
# is contradicted, but it describes code that no longer runs, so its
# fingerprint no longer matches, and it must NOT be re-stamped: re-taking it on
# a named model is the only thing that makes this PROVEN again (D-30).
# NEEDS-CHECKING AM1 says what to arrange, and
# tools/jobs/report-connectors-project1-2026-09-23.yaml is that arrangement.
# FRAGMENT-ISSUES 5b-195.

# ROUTING - the same rows are in the fragments named here.
#
#   "what connectors does this have"      -> here. The inventory, with
#                                            direction and size, before
#                                            anything is drawn
#   "which spigots are open"              -> here - `openConnectors`
#   "does what is plugged in MATCH the    -> CHECK_EQUIPMENT_CONNECTORS. It
#    equipment"                              judges size, shape and domain
#                                            against the partner and reports
#                                            mismatches. This one describes;
#                                            that one grades
#   "what flow is on the connectors"      -> REPORT_CONNECTOR_LOADS - the
#                                            numbers the connector CARRIES,
#                                            not its geometry
#   "what is connected to what"           -> TRACE_CONNECTIVITY, which walks
#                                            the whole run. This stops at one
#                                            element's own partners
#   "where does the system stop"          -> FIND_DEAD_ENDS
#   "narrow this set to the connected     -> SELECT_BY_CONNECTION_STATUS,
#    ones"                                   which hands on a set to act on
#                                            rather than describing each one

utterances:
  - what connectors does this AHU have
  - which spigots are open
  - which way is the connector facing
  - show me the connectors on these
  - is anything connected to this
  - what size is the spigot
  - report the connectors
  - is this connector supply or return
