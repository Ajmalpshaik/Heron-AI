heron-agent: HERON-REVIT-SYS-030
heron-step: 15
heron-status: PROVEN
heron-since: 0.1.0
heron-layer: brain

id: FRG-MEP-010
semantic-identity: "join these duct or pipe ends with the fitting the geometry calls for"
kind: action
domain: revit.mep
capability: PLACE_MEP_FITTING
version: 1
source: OFFICIAL
risk: MODIFY

purpose: >
  Joins the open ends of two, three or four ducts or pipes with a fitting -
  elbow, transition, union, tee or cross - chosen from what the geometry
  actually is.

  **The fitting is not a choice the user should have to make.** Somebody says
  *"join these two"*; whether that is an elbow, a union or a transition is
  decided by whether the runs turn and whether they change size, and Revit's own
  behaviour when you drag one duct onto another is exactly this. Asking the
  modeller to name the fitting is asking them to do the API's job.

  **Before this, "join these two" resolved to CREATE_DUCT and "connect these two
  ducts" to TRACE_CONNECTIVITY** - one drawing a new duct nobody asked for, the
  other a read-only report answering a request to change the model.

  **It does not move anything to make the join work.** Revit requires the two
  ends to be at the same point already; ends that are apart are reported with
  the distance, not dragged together. Moving a run to close a gap changes where
  services are, which is a decision, not a detail.

contract:
  needs:
    - name: doc
      type: Document
    - name: elements
      type: IList<Element>
    - name: toleranceMm
      type: double
      source: request
  provides:
    - name: created
      role: result
      type: ElementId
    - name: refused
      # Why it declined, in the user's words - the evidence it looked and
      # said no. Never a thing it FOUND, so it must not count toward
      # "did this come back empty" (D-52).
      role: accounting
      type: string
    - name: notMepCurve
      # Handed in but not duct or pipe, so there was nothing here to act on. A
      # count of what it was GIVEN and could not use, never a thing it found
      # (D-52).
      role: accounting
      type: IList<ElementId>

proof:
  date: '2026-09-13'
  by: Ajmal PS
  model: test projject (3,509 elements), Revit 2024, session 40932
  positive_case: run as it would normally be run - the arrangement was re-made first by running
    select-by-category-name, set-selection with this phase's own values - run inside a transaction,
    and Revit reported the transaction group ROLLED BACK afterwards. returned created 1 item(s)
    [id 926327], notMepCurve 0 item(s), openEnds 2 item(s) [System.Collections.Generic.List`1[Autodesk.Revit.DB.Connecto...,
    System.Collections.Generic.List`1[Autodesk.Revit.DB.Connecto...], refused (null), runs
    2 item(s) [Standard, Standard], tolerance 1.64041994750656, toleranceMm 500. inputs came
    elements from the selection (2); toleranceMm as given. on test projject
  negative_case: run with categoryName=Levels, inViewOnly={3D}, toleranceMm=500 instead -
    chosen because it should not contain what this fragment reports - the arrangement was
    re-made first by running select-by-category-name, set-selection with this phase's own
    values - run inside a transaction, and Revit reported the transaction group ROLLED BACK
    afterwards. returned created (null), notMepCurve 2 item(s) [311, 694], openEnds 0 item(s),
    refused a fitting joins two, three or four runs - 0 were given. Two makes an elbow, a
    union or a transition, three a tee, four a cross, runs 0 item(s), tolerance 1.64041994750656,
    toleranceMm 500. inputs came elements from the selection (2); toleranceMm as given. on
    test projject
  second_route: NOT ESTABLISHED - no second route was run. D-30 asks for one 'where one exists';
    whether one exists here has not been settled, and a declared absence is not the same as
    none
  fingerprint: 8bfec35c261bd5e7

revit: ["2020", "2021", "2022", "2023", "2024", "2025", "2026", "2027"]
runtime: [net472, net48, net8.0-windows, net10.0-windows]
tests: tests/

compatibility-note: >
  `Document.Create.NewElbowFitting`, `NewTransitionFitting`, `NewUnionFitting`,
  `NewTeeFitting` and `NewCrossFitting` all take `Connector` arguments and are
  unchanged 2020 to 2027, as are `MEPCurve.ConnectorManager`,
  `Connector.Origin`, `Connector.IsConnected` and `Connector.Radius` /
  `Connector.Width`.

  Millimetres to feet is arithmetic - divide by 304.8, per D-20.

# ROUTING - repeated in CREATE_DUCT, TRACE_CONNECTIVITY and SPLIT_MEP_RUN.
#
#   "join these two"             -> here
#   "connect these two ducts"    -> here
#   "put an elbow here"          -> here - the elbow is CHOSEN, not named
#   "add a duct fitting"         -> here
#   "is this duct connected"     -> TRACE_CONNECTIVITY - a READ, and it must
#                                   not answer a request to change the model
#   "draw a duct from here"      -> CREATE_DUCT - makes a new run
#   "split this duct"            -> SPLIT_MEP_RUN - the opposite job
#   "they are touching but not connected" -> CONNECT_OPEN_ENDS - it joins ends
#                                       ALREADY at one point and adds no fitting
#   "connect the open ends"          -> CONNECT_OPEN_ENDS
#   "join these walls together"      -> JOIN_GEOMETRY. This row said nothing in
#                                       the library covered it, which was true
#                                       for one batch and is not now
#   "join these two ducts"           -> here. A FITTING between two runs
#   "connect the diffusers to the duct" -> CONNECT_AIR_TERMINALS

utterances:
  - join these two
  - connect these two ducts
  - put an elbow here
  - add a duct fitting
  - join the pipes up
  - connect them together
  - add a tee here
  - join these two ducts
