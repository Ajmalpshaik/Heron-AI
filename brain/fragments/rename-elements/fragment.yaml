heron-agent: HERON-REVIT-PAR-011
heron-step: 14
heron-status: PROVEN
heron-since: 0.1.0
heron-layer: brain

id: FRG-PAR-005
semantic-identity: "find and replace part of these elements' names"
kind: action
domain: revit.parameters
capability: RENAME_ELEMENTS
version: 1
source: OFFICIAL
risk: MODIFY

purpose: >
  Renames elements by replacing a piece of text inside the existing name, rather
  than by writing a whole new one. That is the shape the job almost always
  takes - *"change SUP to SUPPLY on all of them"* - and a fragment that only set
  a whole name would rename forty elements to one identical string.

  **A rename is not a parameter write.** `Element.Name` maps to different
  underlying parameters for different kinds of element, and on a TYPE it renames
  the type **everywhere it is used in the model**, not just on the instances
  handed in. That is the one thing about this fragment that surprises people.

  **Duplicate names are refused, not warned about.** Revit throws on a duplicate
  type name and quietly accepts duplicate instance names; neither is what
  somebody wants halfway through a batch, so intended names are checked against
  each other first and a clash stops the whole batch.

  ASSUMES AN OPEN TRANSACTION and does not open one (Golden Rule 16).

contract:
  needs:
    - name: elements
      type: IList<Element>
    - name: find
      type: string
      source: request
    - name: replaceWith
      type: string
      source: request
  provides:
    - name: renamed
      role: result
      type: int
    - name: notMatched
      # Elements whose name did not contain the search term, which is the
      # normal case across a mixed set. Never a rename it made (D-52).
      role: accounting
      type: IList<ElementId>
    - name: refused
      # Why it declined, in the user's words - the evidence it looked and
      # said no. Never a thing it FOUND, so it must not count toward
      # "did this come back empty" (D-52).
      role: accounting
      type: IList<ElementId>
    - name: collisions
      # Intended names that clash with each other or with a name already in
      # use, CHECKED FIRST so a clash stops the whole batch rather than
      # leaving it half done. Never a rename it made (D-52).
      role: accounting
      type: IList<string>

proof:
  date: '2026-09-13'
  by: Ajmal PS
  model: Snowdon-scratch_ajmal.al (9,638 elements), Revit 2024, session 47740
  positive_case: run as it would normally be run - the arrangement was re-made first by running
    find-views with this phase's own values - run inside a transaction, and Revit reported
    the transaction group ROLLED BACK afterwards. returned collisions 0 item(s), notMatched
    3 item(s) [1363453, 1363463, 1363483], planned 8 item(s) [[Autodesk.Revit.DB.ViewPlan,
    HERONHERONL1 - Block 35], [Autodesk.Revit.DB.ViewPlan, HERONHERONL1 - Block 37], [Autodesk.Revit.DB.ViewPlan,
    HERONHERONL1 - Block 43], ...], refused 0 item(s), renamed 8. inputs came elements from
    find-views (11); find as given; replaceWith as given. on Snowdon-scratch_ajmal.al
  negative_case: run with find=ZZZNOTHINGHERE, nameContains=, replaceWith=HERONL, viewType=FloorPlan
    instead - chosen because it should not contain what this fragment reports - the arrangement
    was re-made first by running find-views with this phase's own values - run inside a transaction,
    and Revit reported the transaction group ROLLED BACK afterwards. returned collisions 0
    item(s), notMatched 11 item(s) [1363403, 1363413, 32, ...], planned 0 item(s), refused
    0 item(s), renamed 0. inputs came elements from find-views (11); find as given; replaceWith
    as given. on Snowdon-scratch_ajmal.al
  caveat: THE SENTENCE ABOVE SAYING THE TRANSACTION GROUP ROLLED BACK IS WHAT REVIT REPORTED,
    AND IT WAS NOT TRUE OF THE FIRST RUN. An earlier run of this same job renamed 8 views
    L1 - Block 35 to HERONL1 - Block 35 and they STAYED renamed - read back minutes later
    from a separate process, with the element count unchanged at 9,638 so no count could
    see it. This run, starting from those names, planned HERONHERONL1 - Block 35 (see the
    planned list above) and DID roll back. One rename survived and the next did not.
    What this proof establishes is what the fragment DOES - 8 renamed with 0 collisions,
    and 0 renamed when nothing matched. What it does NOT establish is that nothing was
    kept. See docs/FRAGMENT-ISSUES.md row 19.
  second_route: NOT ESTABLISHED - no second route was run. D-30 asks for one 'where one exists';
    whether one exists here has not been settled, and a declared absence is not the same as
    none
  fingerprint: 9760e6dfb27cc2fd

revit: ["2020", "2021", "2022", "2023", "2024", "2025", "2026", "2027"]
runtime: [net472, net48, net8.0-windows, net10.0-windows]
tests: tests/

compatibility-note: >
  One implementation for every release. `Element.Name` is a get/set string on
  every release 2020 to 2027 and nothing here touches a unit or builds an
  ElementId from an integer.

# ROUTING - repeated in WRITE_ELEMENT_PARAMETERS and RENUMBER_SEQUENTIAL.
#
#   "rename these"                     -> here
#   "change SUP to SUPPLY"             -> here
#   "set the comments on these"        -> WRITE_ELEMENT_PARAMETERS, a named
#                                         parameter that is not the name
#   "number them in order"             -> RENUMBER_SEQUENTIAL
#
# THE NAME IS NOT THE MARK AND NOT THE TYPE NAME, and a modeller says "name"
# for all three. Ask which before running this on a type: renaming a type
# reaches every instance in the model, including the ones nobody selected.
#   "duplicate this type"            -> DUPLICATE_TYPE - a COPY under a new name.
#                                       Renaming a type here changes it EVERYWHERE
#                                       it is used, which is the opposite
#   "make a new duct type"           -> DUPLICATE_TYPE
#   "rename the family"              -> RENAME_FAMILY - the loaded component
#                                       itself, one level above the type name
#                                       this fragment edits

#   "rename this workset"                -> RENAME_WORKSET - a workset is NOT
#                                        an element. It lives in the workset
#                                        table, which nothing here reaches
#   "put a prefix on the mark or the     -> EDIT_PARAMETER_TEXT. A name is
#    comments"                              not a parameter, and the two
#                                           are written differently

utterances:
  - rename these
  - change the name of these
  - find and replace in the names
  - change SUP to SUPPLY on these
  - fix the naming on these
