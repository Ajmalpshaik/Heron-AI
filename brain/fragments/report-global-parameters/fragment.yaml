heron-agent: HERON-REVIT-PAR-011
heron-step: 17
heron-status: DRAFT
heron-since: 0.1.0
heron-layer: brain

id: FRG-PAR-011
semantic-identity: "list the global parameters and what each one drives"
kind: filter
domain: revit.parameters
capability: REPORT_GLOBAL_PARAMETERS
version: 1
source: OFFICIAL
risk: READ

purpose: >
  Lists every GLOBAL parameter in the project - its value, whether a formula
  drives it, whether it only reports, and WHICH DIMENSIONS IT DRIVES. Read-only.

  **A GLOBAL PARAMETER IS NOT A PROJECT PARAMETER, AND MIXING THEM UP WASTES A
  SESSION.** A project parameter is a COLUMN: every door carries its own value.
  A global parameter is ONE VALUE for the whole model, attachable to dimensions
  and to element parameters, so changing it MOVES GEOMETRY. *"Corridor width =
  1800"* as one number that eight dimensions obey is a global parameter.

  **THE INTERESTING PART IS WHAT IT DRIVES, NOT ITS VALUE.** The value is one
  number. The list of DIMENSIONS labelled by it is the list of things that move
  when somebody edits that number - and it is the only answer to *"why did that
  wall shift when I changed a figure in a dialog"*. Without this fragment a
  model driven by globals looks, from here, like a model where things move for
  no reason.

  **NOT EVERY DOCUMENT ALLOWS THEM.** A family document does not, and neither do
  some template states. That is asked first and said plainly, because an empty
  list otherwise reads as *"this model has none"* when the truth is *"this kind
  of document cannot have any"* - two different answers that look identical.

  **Creating or setting one is deliberately not here.** Setting a global
  parameter moves geometry, which makes it a MODIFY job and not a report, and
  the create call changed shape at Revit 2022.

  **SETTING ONE IS NOW `SET_GLOBAL_PARAMETER`**, built 2026-09-06. That sentence
  above said where it did NOT belong and nothing said where it DID, so for a
  while nothing in the library could write a global at all - and
  `WRITE_ELEMENT_PARAMETERS` cannot stand in, because a global's value is not a
  parameter on an element. Read this fragment's `drives` list before writing:
  it is exactly what will move. Creating one is `CREATE_GLOBAL_PARAMETER`,
  written 2026-09-07 - not here, but no longer nowhere. **Neither can RUN yet:**
  the executor has only `run_fragment_read`, so nothing that writes can be
  executed as a fragment at all, whatever its risk says.

contract:
  needs:
    - name: doc
      type: Document
  provides:
    - name: findings
      type: IList<string>
    - name: globalCount
      type: int
    - name: allowed
      type: bool

proof:
  date: '2026-09-07'
  by: Ajmal PS
  model: Project1 work_ajmal.al (3,436 elements), Revit 2024, session 42676
  positive_case: run as it would normally be run. returned allowed true, findings 2 item(s)
    [1 global parameter(s) in this project. A global is ONE value..., 'Length' = 0.6562 ft  (200
    mm)  - typed in  - drives 0 dimen...], globalCount 1. on Project1 work_ajmal.al
  negative_case: run after the state was arranged by hand so the answer had to be empty. returned
    allowed true, findings 2 item(s) [0 global parameter(s) in this project. A global is ONE
    value..., this project has no global parameters. Nothing here is drive...], globalCount
    0. on Project1 work_ajmal.al
  second_route: NOT ESTABLISHED - no second route was run. D-30 asks for one 'where one exists';
    whether one exists here has not been settled, and a declared absence is not the same as
    none
  fingerprint: 38c7a2284811eb6b

revit: ["2020", "2021", "2022", "2023", "2024", "2025", "2026", "2027"]
runtime: [net472, net48, net8.0-windows, net10.0-windows]
tests: tests/

compatibility-note: >
  One implementation for every release. `GlobalParametersManager`,
  `GetAllGlobalParameters`, `AreGlobalParametersAllowed`, `GetValue`,
  `GetFormula`, `GetLabeledDimensions`, `IsDrivenByFormula` and `IsReporting`
  are all present 2020 through 2027 - read off the reference assemblies at both
  ends. `GlobalParameter.Create` is NOT used: its signature changed at 2022, and
  `IsValidDataType` is NOT used either - it exists on 2020 and is gone by 2024,
  which is exactly the shape of member that turns one build red.

# ROUTING - repeated in ADD_PROJECT_PARAMETER and READ_ELEMENT_PARAMETERS.
#
#   "show me the global parameters in this model" -> here
#   "why did that wall move when I changed a number" -> here - a global
#                                                       parameter drives
#                                                       dimensions, and this
#                                                       lists which
#   "what is driving these dimensions"            -> here
#   "make a global parameter"                     -> CREATE_GLOBAL_PARAMETER -
#                                                    which MAKES one. This lists
#                                                    what is already there, and
#                                                    that list is what to read
#                                                    before making another
#   "set the global to 3000"                      -> SET_GLOBAL_PARAMETER -
#                                                    which WRITES a value and
#                                                    re-drives every dimension
#                                                    labelled by it. This
#                                                    fragment's purpose says
#                                                    outright that setting one
#                                                    is not a report's job
#   "add a parameter to all the doors"            -> ADD_PROJECT_PARAMETER - a
#                                                    COLUMN, one value per
#                                                    element. A global is ONE
#                                                    value for the whole model
#   "what parameters does this duct have"         -> REPORT_PARAMETER_INVENTORY -
#                                                    the FULL LIST on one
#                                                    element, before you know any
#                                                    names. Both that sentence
#                                                    and "what parameters can I
#                                                    filter on" resolved HERE
#                                                    until 2026-09-05, which is
#                                                    the mix-up this fragment's
#                                                    own purpose warns about
#   "what is the Mark on these"                   -> READ_ELEMENT_PARAMETERS -
#                                                    one parameter you can
#                                                    already NAME

utterances:
  - show me the global parameters in this model
  - why did that wall move when I changed a number
  - what is driving these dimensions
  - list the global parameters
  - what global parameters does this project use
