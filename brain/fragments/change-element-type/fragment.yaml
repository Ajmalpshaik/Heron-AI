heron-agent: HERON-REVIT-FAM-012
heron-step: 14
heron-status: PROVEN
heron-since: 0.1.0
heron-layer: brain

id: FRG-ELE-008
semantic-identity: "swap these elements to a different type"
kind: action
domain: revit.elements
capability: CHANGE_ELEMENT_TYPE
version: 1
source: OFFICIAL
risk: MODIFY

purpose: >
  Changes which type an element is - swapping every 600x600 diffuser on a floor
  for a different one, moving a run onto a different duct type, replacing a
  discontinued fitting.

  **The new type is an input and is never guessed.** Revit accepts a type change
  it cannot really honour, and the result is not an error - it is geometry that
  looks wrong on a drawing nobody re-checks.

  **Revit will accept the call and leave the element on its old type.** This is
  the case worth knowing: `ChangeTypeId` throws for some mismatches and, for
  others, simply does not take. So the type is read back after the change and
  only a real move is counted - the same read-back the parameter path needed
  after Revit returned TRUE and stored something else.

  ASSUMES AN OPEN TRANSACTION and does not open one (Golden Rule 16).

contract:
  needs:
    - name: elements
      type: IList<Element>
    - name: newTypeId
      type: ElementId
      source: request
  provides:
    - name: changed
      role: result
      type: int
    - name: alreadyThatType
      # Elements that were ALREADY in the state asked for, so there was
      # nothing to do. The evidence it looked, never a change it made
      # (D-52).
      role: accounting
      type: int
    - name: refused
      # Why it declined, in the user's words - the evidence it looked and
      # said no. Never a thing it FOUND, so it must not count toward
      # "did this come back empty" (D-52).
      role: accounting
      type: IList<ElementId>

proof:
  date: '2026-09-13'
  by: Ajmal PS
  model: test projject (3,432 elements), Revit 2024, session 40932
  positive_case: run as it would normally be run - the arrangement was re-made first by running
    select-in-region, set-selection with this phase's own values - run inside a transaction,
    and Revit reported the transaction group ROLLED BACK afterwards. returned alreadyThatType
    0, changed 4, refused 0 item(s). inputs came elements from the selection (4); newTypeId
    as given. on test projject
  negative_case: 'run with minMm=-1000,-1000,-1000, maxMm=9000,7000,4000, exact=false, categories=Walls,
    newTypeId=Floor: Generic 300mm instead - chosen because it should not contain what this
    fragment reports - the arrangement was re-made first by running select-in-region, set-selection
    with this phase''s own values - run inside a transaction, and Revit reported the transaction
    group ROLLED BACK afterwards. returned alreadyThatType 0, changed 0, refused 4 item(s)
    [925629, 925630, 925631, ...]. inputs came elements from the selection (4); newTypeId
    as given. on test projject'
  second_route: NOT ESTABLISHED - no second route was run. D-30 asks for one 'where one exists';
    whether one exists here has not been settled, and a declared absence is not the same as
    none
  fingerprint: 8473e87ce5b20e4c

revit: ["2020", "2021", "2022", "2023", "2024", "2025", "2026", "2027"]
runtime: [net472, net48, net8.0-windows, net10.0-windows]
tests: tests/

compatibility-note: >
  One implementation for every release. `Element.ChangeTypeId(ElementId)` and
  `Element.GetTypeId()` are unchanged 2020 to 2027, and the new type arrives as
  an ElementId object taken from a live element, so nothing here builds one
  from an integer and 2024's 64-bit ids do not reach this code.

# ROUTING - repeated in SET_MEP_SIZE, which is what is usually meant when the
# words are about size.
#
#   "change the type"                  -> here
#   "swap them for the other diffuser" -> here
#   "make the duct 300 wide"           -> SET_MEP_SIZE - a dimension on the
#                                         element, not a different type
#   "rename the type"                  -> RENAME_ELEMENTS, and note it reaches
#                                         every instance of that type
#
# SIZE AND TYPE ARE THE SAME SENTENCE IN A MODELLER'S MOUTH. "Change it to 600
# by 600" can mean either, and on a family whose sizes are types it means this
# one. Ask which before running on a batch.
#   "make a new duct type"           -> DUPLICATE_TYPE - the type does not exist
#                                       yet. This one moves instances onto a type
#                                       that already does
#   "duplicate this type"            -> DUPLICATE_TYPE

# SENTENCES ADDED 2026-09-01 BECAUSE THE ROUTING TABLE ABOVE ALREADY CLAIMED
# THEM. A routing table is a COMMENT and comments are not indexed - the search
# reads semantic-identity, the utterances, the capability, the domain and the
# purpose, and nothing else. So a table saying "-> here" recorded a decision
# that retrieval could not act on, and these resolved elsewhere.

utterances:
  - change the type
  - swap them to a different type
  - change these to the other family type
  - replace this type with another one
  - use a different diffuser type
  - swap them for the other diffuser
  # ADDED 2026-09-25 (FRAGMENT-ISSUES 5b-220). "change the type of these air
  # terminals to another type" reached SET_FAMILY_CATEGORY - which changes a
  # FAMILY's category - with this fragment fourth. Declared here, so the
  # identity route answers it before any ranking runs.
  - change the type of these air terminals to another type
  - change these elements to a different type
