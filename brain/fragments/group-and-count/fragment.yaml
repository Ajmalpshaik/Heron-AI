heron-agent: HERON-REVIT-CMP-021
heron-step: 14
heron-status: DRAFT
heron-since: 0.1.0
heron-layer: brain

id: FRG-PAR-002
semantic-identity: "turns a set of parameter values into a schedule-style table"
kind: action
domain: revit.reporting
capability: GROUP_AND_COUNT
version: 1
source: OFFICIAL
risk: READ

purpose: >
  Turns a set of parameter values into a schedule-style table: each distinct
  value, and how many carried it, largest group first.

contract:
  needs:
    - name: values
      type: IDictionary<ElementId, string>
  provides:
    - name: groups
      role: result
      type: IList<KeyValuePair<string, int>>

proof:
  date: '2026-09-10'
  by: Ajmal PS
  model: Project1 work_ajmal.al (3,445 elements), Revit 2024, session 24688
  positive_case: run as it would normally be run - the arrangement was re-made first by running
    select-by-category-name, set-selection, read-element-parameters with this phase's own
    values. returned groups 3 item(s) [[Supply Air, 9], [Exhaust Air, 8], [Return Air, 5]].
    inputs came values from read-element-parameters (22). on Snowdon-scratch_ajmal.al
  negative_case: 'run with categoryName=Ducts, inViewOnly=FloorPlan: M1, parameterName=Comments
    instead - chosen because it should not contain what this fragment reports - the arrangement
    was re-made first by running select-by-category-name, set-selection, read-element-parameters
    with this phase''s own values. returned groups 0 item(s). inputs came values from read-element-parameters
    (0). on Snowdon-scratch_ajmal.al'
  second_route: NOT ESTABLISHED - no second route was run. D-30 asks for one 'where one exists';
    whether one exists here has not been settled, and a declared absence is not the same as
    none
  fingerprint: 24c77e51d94e98e6

revit: ["2020", "2021", "2022", "2023", "2024", "2025", "2026", "2027"]
runtime: [net472, net48, net8.0-windows, net10.0-windows]
tests: tests/

# ROUTING - repeated in CREATE_SCHEDULE and COUNT_ELEMENTS.
#
#   "how many of each size"     -> here - numbers in the REPLY, and it
#                                  makes nothing in the model
#   "break it down by type"     -> here
#   "make a schedule"           -> CREATE_SCHEDULE - a VIEW in the Project
#                                  Browser that updates itself and prints on
#                                  a drawing. Both are right answers to
#                                  different sentences
#   "how many ducts are there"  -> COUNT_ELEMENTS

# SENTENCES ADDED 2026-09-01 BECAUSE THE ROUTING TABLE ABOVE ALREADY CLAIMED
# THEM. A routing table is a COMMENT and comments are not indexed - the search
# reads semantic-identity, the utterances, the capability, the domain and the
# purpose, and nothing else. So a table saying "-> here" recorded a decision
# that retrieval could not act on, and these resolved elsewhere.

utterances:
  - what sizes are they
  - break it down by size
  - group them and count
  - give me a schedule of the types
  - break it down by type
