heron-agent: HERON-REVIT-PAR-011
heron-step: 14
heron-status: PROVEN
heron-since: 0.1.0
heron-layer: brain

id: FRG-PAR-006
semantic-identity: "copy one parameter's value into another parameter on the same elements"
kind: action
domain: revit.parameters
capability: COPY_PARAMETER_VALUE
version: 1
source: OFFICIAL
risk: MODIFY

purpose: >
  Copies the value of one parameter into another, on each element. Getting a
  system name into a shared parameter a schedule can use, filling a client's
  required field from one Revit already holds, populating a COBie field before
  handover.

  **Both parameters must hold the same KIND of value.** A number into a text
  field is a silent reformat; text into a number is a refusal. The storage
  types are compared before anything is written and a mismatch stops the whole
  batch — one wrong-typed pair discovered halfway leaves a set that is half
  converted with no record of where.

  **It copies what is STORED, not what is displayed.** A length displayed as
  "2400 mm" is stored as feet, and copying the displayed string into a text
  field puts a unit-suffixed string somewhere that a later calculation cannot
  use. Same storage type means the same internal value moves.

  ASSUMES AN OPEN TRANSACTION and does not open one (Golden Rule 16).

contract:
  needs:
    - name: elements
      type: IList<Element>
    - name: fromName
      type: string
      source: request
    - name: toName
      type: string
      source: request
  provides:
    - name: copied
      role: result
      type: int
    - name: sourceEmpty
      # Elements whose source parameter held nothing, so there was nothing to
      # copy. Given and unusable, never a write it made (D-52).
      role: accounting
      type: IList<ElementId>
    - name: refused
      # Why it declined, in the user's words - the evidence it looked and
      # said no. Never a thing it FOUND, so it must not count toward
      # "did this come back empty" (D-52).
      role: accounting
      type: IList<ElementId>
    - name: typeMismatch
      # That the two parameters store different types, which stops the whole
      # batch before anything is written. A refusal, never a copy (D-52).
      role: accounting
      type: bool

proof:
  date: '2026-09-12'
  by: Ajmal PS
  model: Project1 work_ajmal.al (3,447 elements), Revit 2024, session 65196
  positive_case: run as it would normally be run - the arrangement was re-made first by running
    select-by-category-name, set-selection, write-element-parameters with this phase's own
    values - run inside a transaction, and Revit reported the transaction group ROLLED BACK
    afterwards. returned copied 9, pairs 9 item(s) [[Autodesk.Revit.DB.Parameter, Autodesk.Revit.DB.Parameter],
    [Autodesk.Revit.DB.Parameter, Autodesk.Revit.DB.Parameter], [Autodesk.Revit.DB.Parameter,
    Autodesk.Revit.DB.Parameter], ...], refused 0 item(s), sourceEmpty 0 item(s), typeMismatch
    false. inputs came elements from set-selection (9); fromName as given; toName as given.
    on Project1 work_ajmal.al
  negative_case: run with categoryName=Duct Fittings, fromName=HERON_NO_SUCH_PARAMETER, inViewOnly=1
    - Mech, parameterName=Comments, toName=Mark, value=HERON-TEST-VALUE instead - chosen because
    it should not contain what this fragment reports - the arrangement was re-made first by
    running select-by-category-name, set-selection, write-element-parameters with this phase's
    own values - run inside a transaction, and Revit reported the transaction group ROLLED
    BACK afterwards. returned copied 0, pairs 0 item(s), refused 8 item(s) [925657, 925663,
    925708, ...], sourceEmpty 0 item(s), typeMismatch false. inputs came elements from set-selection
    (8); fromName as given; toName as given. on Project1 work_ajmal.al
  second_route: NOT ESTABLISHED - no second route was run. D-30 asks for one 'where one exists';
    whether one exists here has not been settled, and a declared absence is not the same as
    none
  fingerprint: 62ed358f278d7b82
revit: ["2020", "2021", "2022", "2023", "2024", "2025", "2026", "2027"]
runtime: [net472, net48, net8.0-windows, net10.0-windows]
tests: tests/

compatibility-note: >
  One implementation for every release. `Element.LookupParameter`,
  `Parameter.StorageType` and the typed `Set` overloads are unchanged 2020 to
  2027. Nothing here converts a unit - the internal value is moved as it is,
  which is what makes the copy exact.

# ROUTING - repeated in WRITE_ELEMENT_PARAMETERS.
#
#   "copy the parameter across"     -> here
#   "put the system name into Mark" -> here
#   "set the comments to X"         -> WRITE_ELEMENT_PARAMETERS - one stated
#                                      value, not a value taken from elsewhere
#   "renumber them"                 -> RENUMBER_SEQUENTIAL
#
# WRITING A VALUE AND COPYING ONE ARE DIFFERENT JOBS. This one never invents a
# value; it only moves one that already exists.
#   "add a project parameter"        -> ADD_PROJECT_PARAMETER - a different
#                                       write: it creates the parameter itself
#   "create a shared parameter"      -> ADD_PROJECT_PARAMETER

utterances:
  - copy the parameter from one to the other
  - put the system name into the mark
  - copy this parameter across
  - fill the shared parameter from the built-in one
  - move the value into another field
