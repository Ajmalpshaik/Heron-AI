heron-agent: 'none'
heron-step: '17'
heron-status: 'DRAFT'
heron-since: '0.1.0'
heron-layer: 'brain'
agent: 'HERON-REVIT-VIE-013'
name: 'Revit View Agent'
operation: 'list_views'
date: '2026-09-18'
by: 'Ajmal PS'
source: 'revit/Heron.Revit.Addin/RevitViews.cs'
fingerprint: '460bfe09e2329b42'
first_model: 'Project1 work_ajmal.al (3,513 elements), session 36860'
second_model: 'test projject (3,565 elements), session 71340'
moved: 'templateCount: 16 -> 18, templatesNothingUses: 16 -> 17, viewCount: 24 -> 26, viewsOnNoSheet: 23 -> 21'
held_same: 'activeViewFilters: 3, viewsWithNoTemplate: 24'
gaps:
  - 'a negative case in D-30''s original sense was not run, and could not be: this agent describes whatever model it is handed, so no input makes it correctly return nothing. D-53 tracking is the substitute and it is what the two runs above are'
  - 'TWO INPUTS, AND THE FRAGMENT GATE ASKS THREE. This proof compares two models. brain/heron_validate.py, judging the SAME decision for a fragment, refuses two: ''the tracking set has only 2 row(s). D-53 asks for the answer to follow the input across SEVERAL different inputs; two cannot show that''. prove-agent.py track takes exactly two sessions by design, so one decision is implemented at two bars. This agent reads the WHOLE MODEL rather than an argument, so the `vary` mode added 2026-09-19 cannot raise it - a THIRD MODEL is the only route, and only one Revit was free the day this was signed. Recorded rather than withdrawn: the evidence that the answer moved is real, and it is thinner than the library''s own standard. Group U, row U6.'
