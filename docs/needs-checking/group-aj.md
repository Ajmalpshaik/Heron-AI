# Needs checking — Group AJ

> One group of [the register](../NEEDS-CHECKING.md), in its own file since 2026-09-23 so that it can be read
> alone. **The register's rules, and every group's place in it, are on that page.** A new row
> for this group goes in this file. [`tools/needs-checking-register.py`](../../tools/needs-checking-register.py)
> reads it back into the register for every tool that reads the register, so a row here is seen
> exactly as it was seen there. Written by
> [`tools/split-needs-checking.py`](../../tools/split-needs-checking.py).

## Group AJ - Revit traps seen elsewhere, written into the fragments they affect (earlier-brain plan, package C4)

Nine traps another tool met on real models, mostly on Revit 2020, that Heron's fragments can meet too. **Every one is OBSERVED ELSEWHERE AND NOT YET PROVEN HERE.** A row below is the check that turns it into Heron's own evidence - or shows it does not happen on the release Heron runs, which is worth the same. Each fragment named carries a note on its card saying so. Written 2026-09-23 on a machine with no Revit: **nothing below has run.** Every row needs Revit on the owner's PC and a **detached copy** of a model ([Golden Rule 18](../14-golden-rules.md)).

**Five fragments went back from `PROVEN` to `DRAFT`** because their code changed - `find-dead-ends`, `find-system-islands`, `color-by-parameter`, `override-graphics-in-view` and `dimension-grids-and-levels`. Each keeps its old proof as the record of what was run and needs a new one on a named model ([D-30](../DECISIONS.md)); AJ6, AJ7 and AJ8 say what to arrange. `connect-air-terminals`, `auto-size-pipe` and `find-clashes` changed too, and were `DRAFT` already. The defects behind them are FRAGMENT-ISSUES rows 5b-173 to 5b-181.

| # | Check | Expected |
|---|---|---|
| **AJ1** | `connect-air-terminals`: three supply diffusers on one branch - one whose connector sits a few hundred millimetres **below** the duct, one whose connector already **touches** the duct's underside, and one under a **return** duct. Measure each diffuser's height in a section first, then run the fragment on all three with `maxDistanceMm` large enough to reach | The low one tapped - and if Revit lifts it, as another tool saw (625 mm), its id in `moved` with the z change, matching the section measured before and after. The touching one tapped and **not** in `moved`. The one under the return duct refused, and not in `moved`. **A FAIL that matters**: an unmoved terminal listed at 0.0 mm - record the size, because it goes to the owner with the tolerance question rather than a number being chosen. Then prove the fragment (D-30) on that arrangement |
| **AJ2** | `auto-size-pipe` on a **connected** pipe run with unions and elbows, sized one or two steps up; then again on a run whose fittings already carry the new sizes. Then `set-mep-size` and `auto-size-mep` once each on a connected run, looking at every joint | The open-connector and fitting counts before and after, and `openedByResize` naming each joint Revit left open - every one checked by eye in plan and 3D. The second run names nothing. For `set-mep-size` and `auto-size-mep`: if a fitting stayed at the old size, the trap is reproduced here, and those two need the same count - a code change that owes each a re-proof |
| **AJ3** | A duct main with two takeoffs and a terminal on each branch, one fresh copy per fragment: `split-mep-run` between the takeoffs; `trim-extend-elements` and `offset-elements` on the main; `set-mep-slope` on a pipe with a tee in its side. After each, count the takeoffs and trace from each terminal back to the main. After the split, edit one half and put it back | Every takeoff still there and every branch still reaching the main - or the trap reproduced, named by fragment and release. **A terminal reading "connected" is not the evidence; the trace to the main is.** After the split and the edit, still **two** ducts: if they merged into one, the plain rejoin in `split-mep-run` is the cause and a union fitting is the recorded fix - a code change owing a re-proof |
| **AJ4** | A ceiling-mounted terminal family whose **Default Elevation** is not zero. Place three through `place-family-instances` at points on the level, and one by hand | The hand-placed one at its Default Elevation. The three placed through Heron read back: if they sit at the level, the trap is reproduced here, and the height has to come in the points, asked for (D-33) - the skills that place terminals and sprinklers carry it as a precondition |
| **AJ5** | One room whose **Upper Limit stops at the ceiling**, and one diffuser whose insertion point is in the void **above** it. Run `count-by-spatial-container` (room), `filter-elements-in-room` (that room) and `check-room-mep-completeness` (a rule wanting one Air Terminal). Then raise the room's Upper Limit to the level above and run all three again | First pass, recorded side by side - what the code predicts: counted IN by the first, outside by the other two. Second pass: all three say inside. **Then each card is settled with that evidence, one at a time** - `check-room-mep-completeness`'s purpose sentence or its code, whichever the owner wants a room to mean. FRAGMENT-ISSUES 5b-175 |
| **AJ6** | `find-clashes` with a batch holding a view or a group (Revit will not test those) and one real clash; then with an empty set to check against. `find-dead-ends` and `find-system-islands` on a **Revit 2024** model where a pipe run ends at a water heater or pump in **Plumbing Equipment**; then both on the arrangement their old proofs used | The refused element in `notChecked`, not in `clear`, and clashing + clear + notChecked equal to what was handed in; the empty set counts nothing clear. The run at plumbing equipment `served`, and its network not sourceless. The old arrangement answers as the recorded proofs did. **A re-proof for `find-dead-ends` and `find-system-islands`** on a named model (D-30) |
| **AJ7** | Insulated and lined ducts, in a plan and a 3D view: `color-by-parameter` on "System Name"; `override-graphics-in-view` making them red; then the insulation **alone** given a grey dashed look through `override-graphics-in-view`. Then `set-category-solid-fill` on Ducts, Pipes, Air Terminals, Sprinklers, Cable Tray and Conduit, read back with `report-category-overrides` | Insulation and lining in their duct's colour and `wrapsFollowed` counting them; the duct still readable inside its jacket in plan - if the jacket's fill hides it, record it for the owner. The grey look on the insulation alone leaves the ducts red. For the category wash, what the read-back says landed on each category - which ones dropped the surface fill - recorded. **Re-proofs for `color-by-parameter` and `override-graphics-in-view`** (D-30) |
| **AJ8** | A section with three levels on a model whose level type's **Elevation Base is the Survey Point**, the survey point set well away from the project origin vertically - read one level's two elevations with `report-level-elevations` first, and they must differ. `dimension-grids-and-levels` on the three; then the same on an ordinary model | One string beside the levels, with the right floor-to-floor values, on both models. **The re-proof of `dimension-grids-and-levels`** (D-30), with the survey-based model as the case that tells the old code from the new |
| **AJ9** | The lower-priority four: `create-opening` on a **wall**, then on a floor; a room tag parked by `arrange-tags-to-view-edges` and one stacked by `stack-tags`; a family re-categorised after `check-category-mismatch` finds it, with a project parameter bound only to the old category; `select-touching` timed on a large model | The wall: an opening, or Revit's refusal recorded - then the wall needs its own API call. The room tags: whether Revit flags them outside their rooms, and whether a leader cures it. The category change: whether the parameter's value survives. `select-touching`: the time, recorded - if it is slow, the quick filters go first |

---
