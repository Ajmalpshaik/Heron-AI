# Needs checking — Group AJ

> One group of [the register](../NEEDS-CHECKING.md), in its own file since 2026-09-23 so that it can be read
> alone. **The register's rules, and every group's place in it, are on that page.** A new row
> for this group goes in this file. [`tools/needs-checking-register.py`](../../tools/needs-checking-register.py)
> reads it back into the register for every tool that reads the register, so a row here is seen
> exactly as it was seen there. Written by
> [`tools/split-needs-checking.py`](../../tools/split-needs-checking.py).

## Group AJ - Revit traps seen elsewhere, written into the fragments they affect (earlier-brain plan, package C4)

Nine traps another tool met on real models, mostly on Revit 2020, that Heron's fragments can meet too. **Every one is OBSERVED ELSEWHERE AND NOT YET PROVEN HERE.** A row below is the check that turns it into Heron's own evidence - or shows it does not happen on the release Heron runs, which is worth the same. Each fragment named carries a note on its card saying so. Written 2026-09-23 on a machine with no Revit: **nothing below has run.** Every row needs Revit on the owner's PC and a **detached copy** of a model ([Golden Rule 18](../14-golden-rules.md)).

**Five fragments went back from `PROVEN` to `DRAFT`** because their code changed - `find-dead-ends`, `find-system-islands`, `color-by-parameter`, `override-graphics-in-view` and `dimension-grids-and-levels`. Each keeps its old proof as the record of what was run and needs a new one on a named model ([D-30](../DECISIONS.md)); AJ6, AJ7 and AJ8 say what to arrange. `connect-air-terminals`, `auto-size-pipe` and `find-clashes` changed too, and were `DRAFT` already. The defects behind them are FRAGMENT-ISSUES rows 5b-173 to 5b-181.

| # | Check | Expected |
|---|---|---|
| **AJ1** | `connect-air-terminals`: three supply diffusers on one branch - one whose connector sits a few hundred millimetres **below** the duct, one whose connector already **touches** the duct's underside, and one under a **return** duct. Measure each diffuser's height in a section first, then run the fragment on all three with `maxDistanceMm` large enough to reach | The low one tapped - and if Revit lifts it, as another tool saw (625 mm), its id in `moved` with the z change, matching the section measured before and after. The touching one tapped and **not** in `moved`. The one under the return duct refused, and not in `moved`. **A FAIL that matters**: an unmoved terminal listed at 0.0 mm - record the size, because it goes to the owner with the tolerance question rather than a number being chosen. Then prove the fragment (D-30) on that arrangement - **RUN 2026-09-23 on the owner's PC - FAIL: the trap is reproduced on Revit 2024.** Model `Heron-C3-test`, Revit 2024 (session 3544). A supply duct and a return duct, 400 x 300, centreline 3000 mm; three `M_Supply Diffuser: 600 x 600 Face 300 x 300 Connection` under them - the low one and the one under the return duct at 2300, the touching one raised to 2748 so that its neck met the supply duct's underside (the owner's section). **Spot elevations by hand, before:** 2300, 2748, 2300. `fragment connect-air-terminals --write --apply --set ducts=Ducts --set maxDistanceMm=1000`: `connected` 3, `refused` 0, `notMeasured` 0, `moved` *[925837: moved 550.0 mm (x 0.0, y 0.0, z +550.0), 925838: moved 101.6 mm (x 0.0, y 0.0, z +101.6), 925839: moved 550.0 mm (x 0.0, y 0.0, z +550.0)]*, and *"925839 tapped into 925835"* - 925835 is the RETURN duct. **By hand, after:** 2850, 2850, 2850, each diffuser drawn inside its duct (the owner's screenshot); read back the same; no duct fitting anywhere in the model - Revit fixed each terminal to the duct's face and made no tap. So the low one tapped and in `moved`, matching the section, as this row says; the touching one tapped and **in** `moved`, by 101.6 mm, its own height; the one under the return duct tapped, **not refused**, and moved 550 mm. No terminal was listed at 0.0 mm. No dialog. One Ctrl+Z put all three back at 2300, 2748, 2300. FRAGMENT-ISSUES 5b-187. **The tolerance question** was put to the owner once, with these numbers, and is not answered yet. **The proof** was drafted on this arrangement and **accepted by the owner**: `connect-air-terminals` carries his proof, fingerprint `9455f0c5551fe947`, and is still DRAFT - promoting it is his step |
| **AJ2** | `auto-size-pipe` on a **connected** pipe run with unions and elbows, sized one or two steps up; then again on a run whose fittings already carry the new sizes. Then `set-mep-size` and `auto-size-mep` once each on a connected run, looking at every joint | The open-connector and fitting counts before and after, and `openedByResize` naming each joint Revit left open - every one checked by eye in plan and 3D. The second run names nothing. For `set-mep-size` and `auto-size-mep`: if a fitting stayed at the old size, the trap is reproduced here, and those two need the same count - a code change that owes each a re-proof - **NOT RUN 2026-09-23 - the owner: "plubing we will do later".** `auto-size-pipe` sizes from the flow a pipe already carries, so the arrangement needs a model whose pipe systems carry flow - a copy of Revit 2024's own *Snowdon Towers Sample Plumbing* was proposed for it |
| **AJ3** | A duct main with two takeoffs and a terminal on each branch, one fresh copy per fragment: `split-mep-run` between the takeoffs; `trim-extend-elements` and `offset-elements` on the main; `set-mep-slope` on a pipe with a tee in its side. After each, count the takeoffs and trace from each terminal back to the main. After the split, edit one half and put it back | Every takeoff still there and every branch still reaching the main - or the trap reproduced, named by fragment and release. **A terminal reading "connected" is not the evidence; the trace to the main is.** After the split and the edit, still **two** ducts: if they merged into one, the plain rejoin in `split-mep-run` is the cause and a union fitting is the recorded fix - a code change owing a re-proof - **NOT RUN 2026-09-23 - not reached in the session** |
| **AJ4** | A ceiling-mounted terminal family whose **Default Elevation** is not zero. Place three through `place-family-instances` at points on the level, and one by hand | The hand-placed one at its Default Elevation. The three placed through Heron read back: if they sit at the level, the trap is reproduced here, and the height has to come in the points, asked for (D-33) - the skills that place terminals and sprinklers carry it as a precondition - **RUN 2026-09-23 on the owner's PC - NEEDS REVIEW: the hand-placed control did not do what this row assumes.** Model `Heron-C3-test`, Revit 2024. `M_Supply Diffuser` read a Default Elevation of 0, so the owner set `M_Return Diffuser`'s to 2700 by hand in Type Properties - read back afterwards as 2700 (8.86 ft) on every air terminal in the model, the supply family's too, which read 0 before: not explained. His own placement, by hand in the Level 1 plan, landed at 0 (Elevation from Level 0), and a second attempt showed *Elevation from Level 0* in Properties before the click. Three through `PLACE_FAMILY_INSTANCES` with points at z 0 landed at the level as well (926460, 926461, 926462). So Heron placed exactly as a hand placement did, and the difference this row is about cannot be seen on this model. A height given in the points is kept - AJ1's 2300 and 2500 read back as asked. Whether a hand placement should follow the Default Elevation is the owner's call |
| **AJ5** | One room whose **Upper Limit stops at the ceiling**, and one diffuser whose insertion point is in the void **above** it. Run `count-by-spatial-container` (room), `filter-elements-in-room` (that room) and `check-room-mep-completeness` (a rule wanting one Air Terminal). Then raise the room's Upper Limit to the level above and run all three again | First pass, recorded side by side - what the code predicts: counted IN by the first, outside by the other two. Second pass: all three say inside. **Then each card is settled with that evidence, one at a time** - `check-room-mep-completeness`'s purpose sentence or its code, whichever the owner wants a room to mean. FRAGMENT-ISSUES 5b-175 - **RUN 2026-09-23 on the owner's PC - NEEDS REVIEW: the prediction held, and the choice is the owner's.** Model `Heron-C3-test`, Revit 2024: room `AJ5 Room` inside four walls, Upper Limit Level 1, Limit Offset 2700, and diffuser 926469 with its insertion point at 3000 mm, inside the room on plan. **First pass, side by side:** `count-by-spatial-container` (room) - IN, *"AJ5 Room 1: 1"*; `filter-elements-in-room` - OUTSIDE, *outside [926469]*; `check-room-mep-completeness` (one Air Terminal wanted) - OUTSIDE, *"short of: Air Terminals (0 of 1)"*. **Second pass**, the owner raising the Upper Limit to Level 2 (the Limit Offset still read 2700, so the top was at 6700): all three inside. The room's name as Revit gives it is `AJ5 Room 1`, and `room=AJ5 Room` was refused by name. **The owner was asked once which answer a ceiling-void device should get; not answered yet.** FRAGMENT-ISSUES 5b-175 |
| **AJ6** | `find-clashes` with a batch holding a view or a group (Revit will not test those) and one real clash; then with an empty set to check against. `find-dead-ends` and `find-system-islands` on a **Revit 2024** model where a pipe run ends at a water heater or pump in **Plumbing Equipment**; then both on the arrangement their old proofs used | The refused element in `notChecked`, not in `clear`, and clashing + clear + notChecked equal to what was handed in; the empty set counts nothing clear. The run at plumbing equipment `served`, and its network not sourceless. The old arrangement answers as the recorded proofs did. **A re-proof for `find-dead-ends` and `find-system-islands`** on a named model (D-30) - **RUN IN PART 2026-09-23 on the owner's PC.** **`find-clashes` - PASS.** Model `Heron-C3-test`, Revit 2024: a duct pushed through a wall, the two AJ1 ducts and model group 926493 as the batch, the six walls as `against=selected` - handed 4, clashing 1, clear 2, notChecked 1 [926493], *"Revit's intersection test refused..."*, so 1 + 2 + 1 = 4 and the group is not clear. The empty set never reaches the fragment: with nothing selected the binder refuses first - *""selected" means what is selected in Revit, and nothing is selected."* - so nothing is counted clear because nothing runs, and the fragment's own empty-set branch cannot be reached by a caller. Proof drafted, not signed (`286f312d2314fd8c`). **`find-dead-ends` and `find-system-islands` - NOT RUN, plumbing deferred by the owner**, so their re-proofs are still owed |
| **AJ7** | Insulated and lined ducts, in a plan and a 3D view: `color-by-parameter` on "System Name"; `override-graphics-in-view` making them red; then the insulation **alone** given a grey dashed look through `override-graphics-in-view`. Then `set-category-solid-fill` on Ducts, Pipes, Air Terminals, Sprinklers, Cable Tray and Conduit, read back with `report-category-overrides` | Insulation and lining in their duct's colour and `wrapsFollowed` counting them; the duct still readable inside its jacket in plan - if the jacket's fill hides it, record it for the owner. The grey look on the insulation alone leaves the ducts red. For the category wash, what the read-back says landed on each category - which ones dropped the surface fill - recorded. **Re-proofs for `color-by-parameter` and `override-graphics-in-view`** (D-30) - **RUN 2026-09-23 on the owner's PC - PASS for what C4 changed, with two things recorded.** Model `Heron-C3-test`, Revit 2024, views `1 - Mech` and `{3D}` (neither has a template). Insulation 50 mm on both ducts through `SET_MEP_INSULATION`; lining on the supply duct by hand - Heron has no lining capability. `color-by-parameter` on System Name, kept: coloured 2, **wrapsFollowed 3**, and read back in both views every wrap in its own duct's colour (supply RGB 76,217,217, return RGB 217,76,76). **For the owner: with that solid fill the duct cannot be seen inside its jacket in plan** - his words, *"if we need to visible difrend color or transparancey need to ad"*. `override-graphics-in-view` red: overridden 2, wrapsFollowed 3, every duct and wrap RGB 255,0,0. Grey on the insulation alone: overridden 2, **wrapsFollowed 0**, the ducts still red - one way only, and *"now its okkey"*, the duct readable in its jacket. **Dashed could not be asked for:** the add-in's override parser leaves line and fill patterns out on purpose. **The category wash:** `set-category-solid-fill` on Ducts, Pipes, Air Terminals, Sprinklers, Cable Trays and Conduits answered overridden 6, refused 0, notCuttable 6 - and `report-category-overrides` found **none** of them carrying a fill, by the command line and again through `revit_change` on Air Terminals alone; the reader was shown to work on blue the owner set by hand. Every category dropped the fill: FRAGMENT-ISSUES 5b-188. **Re-proofs drafted, not signed:** `color-by-parameter` (`a1e4d6b80960d11d`) and `override-graphics-in-view` (`b07412335fdc5b6e`) |
| **AJ8** | A section with three levels on a model whose level type's **Elevation Base is the Survey Point**, the survey point set well away from the project origin vertically - read one level's two elevations with `report-level-elevations` first, and they must differ. `dimension-grids-and-levels` on the three; then the same on an ordinary model | One string beside the levels, with the right floor-to-floor values, on both models. **The re-proof of `dimension-grids-and-levels`** (D-30), with the survey-based model as the case that tells the old code from the new - **NOT RUN 2026-09-23 - not reached in the session.** The re-proof of `dimension-grids-and-levels` is still owed |
| **AJ9** | The lower-priority four: `create-opening` on a **wall**, then on a floor; a room tag parked by `arrange-tags-to-view-edges` and one stacked by `stack-tags`; a family re-categorised after `check-category-mismatch` finds it, with a project parameter bound only to the old category; `select-touching` timed on a large model | The wall: an opening, or Revit's refusal recorded - then the wall needs its own API call. The room tags: whether Revit flags them outside their rooms, and whether a leader cures it. The category change: whether the parameter's value survives. `select-touching`: the time, recorded - if it is slow, the quick filters go first - **NOT RUN 2026-09-23 - not reached in the session** |

**Run in part on the owner's PC, 2026-09-23.** Revit 2024 and one test project for every row that ran - `D:\Ajmal\Heron-Proving\Heron-C3-test.rvt`, a new project from the owner's template - with each arrangement built through `revit_change` by proven capabilities, and a few steps by his hand where Heron has none (the locked dimension, the lining, a room tag, one Default Elevation). No row is struck: each owes something - a signature, a decision of the owner's, or the plumbing half deferred to another day. The job file for the proofs is [`tools/jobs/c4-heron-c3-test-2026-09-23.yaml`](../../tools/jobs/c4-heron-c3-test-2026-09-23.yaml). [05-pc-proving](../work-notes/plans/earlier-brain/05-pc-proving.md) section 3 also asked for the owner's three most-used jobs that were still DRAFT: `connect-air-terminals` **accepted by him**; `find-clashes` and `center-room-tags` **drafted, not signed** - the second on one room tag he placed off-centre by hand, centred 1 with the three ducts as the negative (notRoomTags 3, centred 0), fingerprint `3fa828aaf1086daf`.

---
