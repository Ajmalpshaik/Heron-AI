# Fragment issues — section 5b, rows 101 to 125

> Rows 101 to 125 of section 5b of [the register](../FRAGMENT-ISSUES.md), in their own file since 2026-09-23 so that
> they can be read alone. **The section's own rules are on that page, under its heading.** A new
> row goes at the end of the section's last file, whatever its number, and the next run of
> [`tools/split-register.py`](../../tools/split-register.py) moves it to the file its number
> belongs to. [`tools/register-text.py`](../../tools/register-text.py) reads every row back into
> the register for every tool that reads it, so a row here is seen exactly as it was seen there.

| # | Defect | State |
|---|---|---|
| **101** | THE ONE LOOKUP WHOSE ANSWER CANNOT DEPEND ON A SCORE IS EXECUTED BY NO SUITE, AND ITS ONLY GUARD… — [full row](../fragment-issues-archive/reading-defects-5b-101-125.md#row-5b-101) | **FIXED 2026-09-21 - §5b holds the contract the caller actually needs.** |
| **102** | THE CITATION CHECKER TAKES THE FIRST CAPITALISED WORDS IN THE SENTENCE AS THE DOCUMENT, SO A… — [full row](../fragment-issues-archive/reading-defects-5b-101-125.md#row-5b-102) | **FIXED 2026-09-21.** |
| **103** | **THE READING LEDGER SIGNS EVERY CLOUD SESSION'S WORK WITH THE OWNER'S NAME, BECAUSE IT READS THE BRIDGE LEASE AS AN AUTHOR.** Found 2026-09-21 while marking `brain/heron_research.py` for [row 5b-102](../FRAGMENT-ISSUES.md#) - **the mark came back *by ajmal-pc*, and no person had read that file.** `tools/review-ledger.py`'s `who()` takes `HERON_CLIENT_ID` first. [`docs/38 §Environment variables`](../38-the-cloud-environment.md) sets that variable to **`ajmal-pc`** in the cloud environment, and the reason it gives is entirely about the bridge: *"No Revit is reachable from a cloud session, so it can never compete for a lease - it is set so that a command copied out of a session prompt is correct wherever it is run."* Both halves are right on their own. **One variable is doing two jobs** - a lease id, which this register's own [ONE PERSON, ONE `HERON_CLIENT_ID`](../FRAGMENT-ISSUES.md#) note says is deliberately SHARED across a person's chats and doors, and an author id, which is worthless the moment it is shared. **`who()`'s own docstring is the standard it fails**: *"never a hardcoded default that quietly attributes work"* - it is not a hardcoded default, and it quietly attributes the work anyway, to somebody who was not there. **Measured:** `docs/REVIEW-LEDGER.tsv` carries **24 rows signed `ajmal-pc`**, five of them dated 2026-09-21 - and one row signed **`claude-linux`**, which is the same variable being set by hand, so at least one earlier session hit this and worked around it without writing it down. This session marked its own read by overriding the variable and says so here rather than letting the row read as the owner's | **OPEN — needs a sitting, and the question is one sentence long: when an agent session reads a file, whose read is it?** Two answers and they lead different places. **His**, because it is his repository and his instruction - then nothing changes in the code and `who()` gains a comment saying the id is a lease and the signature is a person, deliberately. **The session's**, because the ledger exists so the next reader knows what has actually been read by whom - then `who()` needs a source that is not the lease, which is one line, plus one more variable in docs/38's box that only he can set. **NOT GUESSED AT, and the reason is this register's own rule**: a conflict between a stated policy and running code is left open rather than closed whichever way makes the task easier. **NOTHING IN `tools/review-ledger.py` WAS EDITED.** What was done is one row: the 24 existing signatures are left exactly as they are, because rewriting a record of who read what is the one repair that cannot be checked afterwards |
| **104** | FOUR COMMAND LINES TAKE A FLAG'S VALUE WITHOUT ASKING WHETHER ONE WAS GIVEN - THREE CRASH WITH A… — [full row](../fragment-issues-archive/reading-defects-5b-101-125.md#row-5b-104) | **FIXED 2026-09-22, in the change after the one that found it.** |
| **105** | **THE COMPATIBILITY MATRIX REPORTS ZERO PROVEN ON EVERY RELEASE WHILE 328 RECORDED PROOFS SIT IN THE LIBRARY, BECAUSE A PROOF ONLY COUNTS IF A COMPILER AGREED FIRST.** Found 2026-09-21 reading `brain/heron_matrix.py` - 338 lines, 7 public functions, 13 suites, the next thinnest live-path module after [row 5b-102](../FRAGMENT-ISSUES.md#). **MEASURED, on this checkout:** `python brain/heron_matrix.py` prints **0 PROVEN and 0 COMPILES for all eight releases**, and `build_matrix()` over the real library counts **328 fragments whose proof names exactly one release** - 315 on Revit 2024, 13 on 2020. The line is `if proven_on == rel and state == COMPILES: state = PROVEN`, and `COMPILES` needs `build/compile-results.json`, which `.gitignore` excludes twice - so **on any fresh checkout PROVEN is unreachable.** **THE FILE STATES THE RULE THAT WOULD HAVE CAUGHT IT AND NOTHING USES IT**: `STRENGTH = [NOT_CLAIMED, UNKNOWN, CLAIMED, COMPILES, PROVEN]`, with the comment *"A cell only ever moves up this list"* - **referenced nowhere in the repository.** A prerequisite chain is not an ordering, and the ordering is the half that was written down. `UNKNOWN` is the same shape one step smaller: `build_matrix` never assigns it, so the documented four-state model has a state its own builder cannot produce - it survives only because `brain/heron_runs.py` uses `MTX.UNKNOWN` as a value. **AND A PROOF IS THE STRONGER EVIDENCE**: a D-30 proof is a recorded run against a **named real model on that release**, which cannot happen unless it compiled there - so the gate makes the weaker evidence a precondition for the stronger. `docs/28` asks this agent for *"the status coming from TESTS, NEVER ASSUMPTION"*, and the report's own closing line says *"the distance between the last two columns is what this matrix exists to show"* - both columns read zero. **AND IT IS NOT ONLY A PRINTED TABLE - IT IS AN MCP TOOL**, confirmed 2026-09-22 reading `mcp/server/heron_brain.compatibility()`, which is registered as `heron_compatibility` in `heron_tools.TOOLS` and served by `heron_mcp_server`. It hands the caller `"proven": tally[MX.PROVEN]` straight out of `build_matrix()`, so a modeller who asks which releases Heron is proven on is told **none, on all eight**. **WHY THE SUITE PASSES:** `tests/test_matrix.py` §5 checks the promotion, and every one of its PROVEN checks runs **after injecting a fake `evidence` dict**. §3 checks the no-evidence path with a library holding **no PROVEN fragment**. The two halves are each tested and the case a person actually runs - a proof, no compile evidence - is between them | **OPEN — measured and deliberately NOT fixed here.** Two things to settle and only the first is a judgement. (1) **Is the gate intended?** Nothing in the file says it is; no sentence anywhere argues that a proof needs a compiler's agreement, and `STRENGTH` says the opposite. If it is not intended the fix is that line, plus a check in §5 that a PROVEN fragment with **no** evidence still reads PROVEN on the release its proof names. (2) **`STRENGTH` and `UNKNOWN` are dead here either way** - a constant stating a rule nothing enforces is the shape [rows 5b-96, 5b-97 and 5b-100](../FRAGMENT-ISSUES.md#) are all about. **NOTHING WAS EDITED.** |
| **106** | THE RULE THE SKILL VALIDATOR EXISTS TO ENFORCE - A SKILL NAMES A CAPABILITY, NEVER A FRAGMENT - IS… — [full row](../fragment-issues-archive/reading-defects-5b-101-125.md#row-5b-106) | **FIXED 2026-09-22.** |
| **107** | **`nearest()` SAYS IT REFUSES A VECTOR FROM A DIFFERENT BACKEND AND IT ONLY CHECKS THE LENGTH.** Found 2026-09-22 reading `brain/heron_embed.py`, 579 lines. The comment inside the loop reads *"A vector from a different backend or dimension. Not comparable, and quietly comparing it would produce a confident wrong number"* - and the test beside it is `len(got) != len(want)`. **The row's `backend` column is never read**; the query is select `id, embedding` filtered on `kind` alone. **MEASURED, not read**: a row stamped `model:model2vec:minishlab/potion-base-8M` carrying 256 floats is scored against a **lexical** query vector and comes back in the result. `DIMS` is 256 and a trained model at 256 is not hypothetical - `HERON_EMBED_MODEL` lets a person name any model. **THE FILE ALREADY NAMES THIS EXACT FAILURE, TWICE**: `_WHICH_MODEL`'s comment - *"at the same dimension it computes meaningless cross-model dot products ... the semantic route quietly stops working and nothing says so"* - and `encoder()`'s, which exists because resolving the model per ROW inside one indexing pass stored two encoders' work under one name. **Both fixes landed on the WRITE side. `nearest()` is the read side and it still calls `vector()`, which resolves the model per call.** So the query can be embedded by one encoder and the rows by another, and the only thing between them is a length comparison | **OPEN — measured and NOT fixed, and the window is stated rather than talked up.** **`mcp/server/heron_brain._Open` runs `EMBED.index(store)` on every open**, and `index()` re-embeds any row whose stamp differs from the encoder answering now - so on the live path the store is normally normalised before anything is compared. What is left is the gap between that pass and the `nearest()` call in the same request: `brain.warm()` imports the trained encoder on a background thread at server start, so a warm-up finishing **between the index pass and the query** gives a MODEL query vector over a LEXICAL store, at equal dimensions, with nothing to catch it. **The comment is wrong either way and that half needs no judgement.** The behaviour half does: filtering `nearest()` to the current stamp would make that request return **nothing** rather than noise, and a silent nothing is D-52's plausible zero - so what a mismatch should PRODUCE is a decision, not a repair. **NOTHING WAS EDITED.** |
| **108** | **THE ONE LOOPHOLE `heron_scope` EXISTS TO CLOSE IS OPEN THROUGH A PUBLIC ATTRIBUTE THAT FIVE BRAIN MODULES USE AS THE HOUSE PATTERN.** Found 2026-09-22 reading `brain/heron_scope.py`, 462 lines. The module's headline is *"A cross-scope query must be impossible to WRITE, not merely absent"*, and it names two things that make it so: `open_scope` takes one scope, and **ATTACH is refused** - *"SQLite's ATTACH DATABASE is the one mechanism that could reach a second file through a connection that legitimately holds one, so every statement is checked for it and refused by name. **Without this, rule 1 is a convention with a loophole.**"* **The check lives in `Store.execute()`. `Store.db` is the raw `sqlite3.Connection` and it is public.** MEASURED end to end on a temporary knowledge folder: `store.execute("ATTACH ...")` is refused by name, and `store.db.execute("ATTACH ...")` **attaches the company store to the global store's connection and reads a value straight out of it**. **AND `store.db` IS NOT AN OBSCURE CORNER** - `store.db.execute` and `store.db.executescript` are the normal way DDL is run here: **8 call sites across 5 modules** - `heron_search` (3), `heron_embed` (2), `heron_capability`, `heron_ingest`, `heron_graph`. So rule 2 is itself a convention with a loophole, and it is Golden Rule 5 - which `docs/10 §2` calls a **contractual** matter rather than a technical one, because Project A and Project B routinely belong to different clients under NDA | **OPEN — measured and NOT fixed, and the severity is stated honestly rather than talked up.** **This is a hole in a guarantee, not a live leak**: nothing in the repository writes an `ATTACH` anywhere, through either route, and all 8 `store.db` uses are ordinary `CREATE TABLE` / `ALTER TABLE` DDL. What is false is the *impossible to write* claim, which is the claim the module is built around. **Two mechanisms, and choosing is a judgement rather than a repair.** (1) `sqlite3.Connection.set_authorizer` denying `SQLITE_ATTACH` closes **every** route at the engine - `execute`, `executescript`, `db.execute` alike - in a few lines and with no call site changed; it is the database refusing rather than a regular expression, and it costs a callback per statement. (2) Wrapping the connection so `.execute` and `.executescript` cannot be reached unguarded, which keeps the named refusal this module is careful about but touches how every DDL site is written. **Probably both** - the authorizer as the wall and the regex for the sentence a person reads. **NOTHING WAS EDITED**, and a change to a boundary this one deserves its own commit and its own test rather than a corner of somebody else's |
| **109** | THE FIFTH COPY OF THE PLAUSIBLE ZERO, IN THE ONE LOOKUP THAT EXISTS TO STOP IT - AND THE GATE BUILT… — [full row](../fragment-issues-archive/reading-defects-5b-101-125.md#row-5b-109) | **FIXED 2026-09-22, both of them.** |
| **110** | **THE GATE BUILT FOR THE PLAUSIBLE ZERO ASKS ABOUT ONE SPELLING OF IT.** Raised 2026-09-22 out of [row 5b-109](../FRAGMENT-ISSUES.md#), and it is a question rather than a fault. `tools/check-narrow-errors.py` is one of the twelve CI runs and its own docstring says why it exists: this shape *"keeps arriving one file at a time"*, naming four rounds in four files. **What it asks is that every `except sqlite3.OperationalError` narrows on the message before it swallows.** A broader handler - `except Exception` - around the same `store.execute` swallows strictly more and the gate does not look at it. Row 5b-109's two were exactly that, and they sat through four review rounds, the tool being written, and a grep the tool's own history records as having been run *"for the shape"* | **OPEN — and it is measured rather than guessed, which is the point of raising it separately.** After 5b-109's two fixes, an AST walk over `brain/`, `mcp/` and `tools/` finds **three** `try` blocks whose body runs `.execute` or `.executescript` and whose handler is `Exception`: `brain/heron_company.py` line 328, `brain/heron_iso.py` line 207, `tools/check-skill-routing.py` line 203. **Three is small enough that widening the gate is cheap, and small enough that none of them may be wrong** - a handler around a whole block that happens to contain a query is not the same thing as one wrapped around the query. So the work is to READ those three first and then decide, rather than to widen a gate and inherit three findings nobody has looked at. **AND THE GATE WORKS, WHICH 5b-109's FIX FOUND THE HARD WAY**: the narrowed handler was flagged anyway, because the seventeen-line comment explaining it pushed the `not in str(exc)` past `WINDOW`. That is the tool being right - it reads TEXT on purpose, *"the rule is about what a person maintaining this file will see beside the handler"* - so the explanation moved above the `try` and the handler stayed three lines. **One stale sentence found while reading it and not fixed**: the comment on `WINDOW` says *"Five is room for a comment and the two lines that do the work"* and the constant is **12**. **NOTHING WAS EDITED**, and `check-narrow-errors.py` is unchanged. **THE THREE HAVE NOW BEEN READ, 2026-09-22, and they are not one thing** - which is the answer to the question this row asked. `brain/heron_iso.py` line 207 **swallowed and produced a false sentence**; that is [row 5b-114](../FRAGMENT-ISSUES.md#), measured and fixed. `brain/heron_company.py` line 328 has the same broad handler on the same query and **does not swallow** - the `None` goes to an `unreadable` list the answer names, so a fault is reported as a fault. `tools/check-skill-routing.py` line 203 is **already narrowed inside** - `except sqlite3.OperationalError` with the *no such table* test and a `raise` - and its outer `except Exception` **records** `counts_error` with the fault's type and message rather than returning a zero. **So the score is one defect in three, and the row stays OPEN for the reason the reading exposed**: widening the gate to every `except Exception` around a `.execute` would flag the two that are RIGHT. What separates them is not the handler, it is the four lines after it - whether the failure is named or becomes a count. This tool reads TEXT on purpose, and *does this handler swallow?* is not a question a text scan has been shown to answer. **That is the remaining question, and it is a tooling decision rather than a defect.** **A SECOND WIDENING CANDIDATE, measured 2026-09-22 and added here rather than answered - see row 5b-128.** The pattern expects the exception name straight after `except`, so a handler written as a TUPLE is invisible to it: `except (AttributeError, sqlite3.OperationalError):` is never checked. There is exactly one in the repository, `heron_embed._try_vec_extension`, and it is **correct** - both types there mean the sqlite extension is unavailable, it answers a boolean rather than a store's contents, and the fallback gives the same answers. So widening the pattern today would flag right code, which is the same shape as the question above and has the same answer waiting on it. **Still yours** |
| **111** | **THE WARNING THAT NAMES THE PARAMETERS A MODELLER MUST NOT ASK BY COUNTS ROWS AND LISTS NAMES, AND STOPS AT FIVE WITHOUT SAYING SO.** Found 2026-09-22 finishing the read of `mcp/server/heron_mcp_server.py`, the last 266 lines of helpers in a 3,516-line file. `_parameter_coverage()` renders the `revit_parameters` MCP tool's answer and closes with the sentence that matters most in it: which parameter names answer to TWO different parameters on one element, so asking by them would hit whichever Revit returned first. **The count came from `len(clashes)`, which is a list of ROWS, and the list from `sorted(set(clashes))`, which is the DISTINCT names.** The add-in writes ONE ROW PER NAME PER `where` and says so in the `reads` note it sends beside every answer - *“the same name can appear twice, once as each, and they are two different parameters”* - and `RevitParameters.Count` keys them `i:` plus name and `t:` plus name, so a name ambiguous in BOTH buckets is two rows and one name. **MEASURED, by lifting the function out of the file and calling it rather than by reading it**: a reply flagging `Comments` on its instance row and on its type row prints **`2 parameter name(s) ... — Comments.`** The second name is not missing from the list. It never existed, and a modeller who counts goes looking for it. **AND THE SECOND HALF IS IN THE SAME SENTENCE**: the list is capped `[:5]` with no marker, so seven flagged names print five and the sentence says nothing about the other two - measured, `7 parameter name(s) ... — Comments, Height, Level, Mark, Phase.` **THE RULE AGAINST THAT IS STATED EIGHTEEN LINES ABOVE IT, IN THIS SAME FUNCTION**, on the `notListed` block: *“WHICH KIND WENT ... saying which was cut is what lets a reader tell a long answer from a misleading one”* and *“a truncated answer that does not say what it dropped is the one a reader trusts by mistake”*. **NOT AN R-82 BREACH AND NOT CLAIMED AS ONE** - [row 5b-100](../FRAGMENT-ISSUES.md#) recorded that distinction the first time it came up, where a report's 80-character excerpt looked like one and was not: R-82 is about the scanner's window, not the report's margin. The authority here is the function's own rule, eighteen lines up. **AND NOTHING ANYWHERE EXECUTED IT**: before this change no file in `tests/` named `_parameter_coverage`, `sameNameOnOneElement`, or any words of the sentence | **FIXED 2026-09-22, in the change that finished reading the file.** `clashes` is the sorted SET of names, so the number stated and the names listed are one collection read twice and cannot come apart; and a list that stops names how many it left out - *“and 2 more”*. **Shown to FAIL, and every check FAILS rather than raises**: the new `tests/test_parameter_clash.py`, **3 red** against the module as found - the count, the reader's arithmetic, and the silent cut. **The five checks that nothing legitimate moved were green BEFORE the fix**, which is what made it safe to make: two genuinely different ambiguous names still read two and are both named, five names carry no *more* because nothing was cut, and a model with no clash still gets no sentence at all. **IT LIFTS THE FUNCTION OUT OF THE FILE RATHER THAN IMPORTING THE MODULE**, which is `tests/test_values_crossing.py`'s technique and its recorded reason: importing `heron_mcp_server` needs the MCP SDK that `gates.yml` leaves out on purpose, so an importing suite exits 3, lands in *could not run*, and never runs where it matters - which for a regression guard is the same as not existing. **WHY IT SURVIVED**: the clash sentence is the one line in this renderer a reader cannot check against anything else on the screen. Every other figure in the table adds up out loud - `withAValue` plus `noValue` plus `blank` is `onElements`, and the answer says so - while this one is a count beside a list, checkable only by counting the list, which is exactly what nobody does |
| **112** | THREE BRAIN COMMAND LINES ANSWER A FLAG THEY DO NOT HAVE - TWO BY SEARCHING FOR IT, ONE BY DROPPING… — [full row](../fragment-issues-archive/reading-defects-5b-101-125.md#row-5b-112) | **FIXED 2026-09-22, all three, in the house words.** |
| **113** | **A SUB-CLAUSE LETTER TURNS A CITATION INTO AN INVENTED FACT, AND THE SAME CLAUSE SPELLED OUT GETS THE RIGHT ANSWER.** Found 2026-09-22 reading `brain/heron_ground.py`, 1,115 lines, 9 suites - the thinnest-held live-path module left part-read. A bracketed span is a CITATION or it is part of the CLAIM, and `_is_marker` decides: citation-shaped by `_CITE_SHAPED`, and not a measurement by `_MEASURED`. **`_CITE_SHAPED`'s own comment names `7a` as a locator it accepts** - *“Section 3, para-7, clause 4.1a”* and *“4, 4.1, 9.1.1, 7a”* - and `_MEASURED` then reads it as a measurement, because its letter arm is `[a-z]+` and `a` is also **amperes**. **MEASURED END TO END on one sentence and one packet holding clause 9.1.1**, verdict and added facts, citing a clause the packet does NOT carry: `[9.1.2]` gives **unresolved**, added `25mm`; **`[7a]` gives `uncited`, added `25mm` AND `7a`**; **`[4.1a]` gives `uncited`, added `25mm`, `4.1a` AND `4`**; and `[clause 4.1a]` - the same clause with one word in front - gives **unresolved**, added `25mm`. **BOTH HALVES ARE WRONG AND THE FILE SAYS WHY.** `check()` separates those two verdicts deliberately - *“TWO DIFFERENT WRONGS, AND THEY NEED DIFFERENT ANSWERS”*, R-65 for a sentence that cites nothing and R-22 for a citation a human cannot follow - and a sub-clause citation gets the other one, so the report tells a modeller who DID cite that they cited nothing. And the citation's own text is listed as a fact the source does not carry, which this module's docstring calls the failure that gets it switched off: *“every false flag it produces is a true sentence called a lie”*. **WHEN THE PACKET DOES CARRY THAT LOCATOR IT IS FINE** - `known` resolves it and the verdict is `grounded` - so this bites exactly where the check matters most: a clause the packet does not hold | **OPEN - measured and deliberately NOT fixed, because the obvious fix re-opens a door two review rounds closed.** `_MEASURED` exists as a second guard for a reason `_is_marker` states: bracketing a value turned the fabrication check off for that sentence, so `[50mm]` must stay a claim. **Narrowing its letters to Heron's own `_UNITS` does not help, and that was checked rather than assumed: `a` is amperes and `7a` still loses.** So `[7a]` is genuinely ambiguous in isolation, and the module already has a stated policy for exactly that - `_is_marker`'s note on `[99]`, *“the honest edge”*, says a bare count is both a plausible locator and a checkable fact, `known` decides it, and **where nothing resolves it, this still reads it as a MARKER**. **THE LETTERED CASE DOES THE OPPOSITE OF THE POLICY WRITTEN BESIDE IT**, and that is the conflict to settle: one sentence, whose answer decides the fix. Two candidates and each costs something. Read an unresolved citation-shaped span as a MARKER, which is what `[99]` already does and what the sub-clause needs - then `[50mm]` with no other citation reports `unresolved` rather than `uncited`, which is the wrong one of the two wrongs for an abuse case. Or keep the measurement veto and accept that a sub-clause letter is not citable in brackets, which every standard in Qatar MEP work writes. **NOTHING WAS EDITED**, per this register's own rule: a policy conflict is left open rather than closed whichever way makes the task easier |
| **114** | A LOCKED STORE MAKES THE ISO AGENT SAY HERON HAS NO COPY OF A STANDARD IT HAS INDEXED - IN THE… — [full row](../fragment-issues-archive/reading-defects-5b-101-125.md#row-5b-114) | **FIXED 2026-09-22.** |
| **115** | A FIFTH COMMAND LINE ANSWERS A TYPO WITH A TRACEBACK, AND THE SCAN THAT FOUND THE OTHER FOUR COULD… — [full row](../fragment-issues-archive/reading-defects-5b-101-125.md#row-5b-115) | **FIXED 2026-09-22, both halves, in the words the other eight use.** |
| **116** | SAFE MODE SWEEPS A FLAG TO A STATE THE FLAG AGENT REFUSES TO WRITE, AND THE COMPONENT IT GATES IS… — [full row](../fragment-issues-archive/reading-defects-5b-101-125.md#row-5b-116) | **FIXED 2026-09-22.** |
| **117** | THE CONFIGURATION AGENT REFUSES A PRACTICE'S OWN SETTINGS AS CREDENTIALS, AND TELLS THEM SO IN A… — [full row](../fragment-issues-archive/reading-defects-5b-101-125.md#row-5b-117) | **FIXED 2026-09-22.** |
| **118** | ONE UNDERSCORE TURNS OFF THE GOLDEN RULE 19 GUARD ON AN MCP SERVER, AND THE ANSWER THEN SAYS THE… — [full row](../fragment-issues-archive/reading-defects-5b-101-125.md#row-5b-118) | **FIXED 2026-09-22.** |
| **119** | THE ONE COMPARISON THAT STOPS AN INSTALL DESTROYING A YEAR OF PROJECT MEMORY IS RAW STRING EQUALITY… — [full row](../fragment-issues-archive/reading-defects-5b-101-125.md#row-5b-119) | **FIXED 2026-09-22.** |
| **120** | THE RAG PLAN COUNTS ONE INDEX TWICE AND CALLS AN EXISTING ONE MISSING, AND THE STEP BEFORE IT… — [full row](../fragment-issues-archive/reading-defects-5b-101-125.md#row-5b-120) | **FIXED 2026-09-22.** |
| **121** | A PACKAGE LISTED AS BOTH REQUIRED AND OPTIONAL IS DECIDED BY THE ORDER OF THE LIST, AND THIS AGENT… — [full row](../fragment-issues-archive/reading-defects-5b-101-125.md#row-5b-121) | **FIXED 2026-09-22.** |
| **122** | A PERSON'S NAME IS PASTED STRAIGHT INTO YAML, AND A COLON IN IT DESTROYS THE PROOF IT WAS SIGNING. — [full row](../fragment-issues-archive/reading-defects-5b-101-125.md#row-5b-122) | **FIXED 2026-09-22.** |
| **123** | THE TOOL THAT EXISTS SO NOBODY TYPES A NUMBER HAD A TYPED NUMBER IN IT, AND THE FIGURE IT OWNS HAS… — [full row](../fragment-issues-archive/reading-defects-5b-101-125.md#row-5b-123) | FIXED 2026-09-22, and one wrong number in a governing document corrected with it… |
| **124** | A GENERATOR KEPT ITS OWN PLACEHOLDER AS IF A PERSON HAD WRITTEN IT, SO A DECISION THAT GAINS A… — [full row](../fragment-issues-archive/reading-defects-5b-101-125.md#row-5b-124) | **FIXED 2026-09-22.** |
| **125** | **D-72 IS THE ONE DECISION OF NINETY-SEVEN WHOSE ROW IN THE INDEX OF DECISIONS SAYS ITS STATUS WAS NEVER STATED.** Found 2026-09-22 beside row 5b-124. Seven decisions carry no `**Status:**` line of their own - D-72, and D-75 through D-80. **Six of the seven have a status cell somebody filled in by hand**; D-72 has the generator's placeholder, `• status not stated`, and after 5b-124 no run will ever change it, because D-72 still states nothing. Its own body is not ambiguous - it is dated **2026-09-14**, it carries a `### Decision` heading, it supersedes a paragraph of D-67 by name, and the four types it released are in the library and cited in HANDOVER as governing shipped behaviour | **OPEN - measured and deliberately NOT fixed, because writing it is not this agent's to write.** A status cell is a claim about what the OWNER decided, and D-30's whole shape is that the machine gathers evidence and a person signs. The evidence here is strong and it is all above; the act is his. **Two lines settle it, and either one is enough**: add `**Status:** Accepted - **Date:** 2026-09-14` to D-72's own block, which is the shape the other ninety carry and which `generate-decision-summary.py` picks up on its next run, or type the cell in the table by hand the way D-75 to D-80's were, which the generator now keeps. **Nothing is broken while this is open** - the table is honest about what the file says, which is exactly what row 5b-124 made it |
