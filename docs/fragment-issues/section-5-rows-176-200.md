# Fragment issues — section 5, rows 176 to 200

> Rows 176 to 200 of section 5 of [the register](../FRAGMENT-ISSUES.md), in their own file since 2026-09-23 so that
> they can be read alone. **The section's own rules are on that page, under its heading.** A new
> row goes at the end of the section's last file, whatever its number, and the next run of
> [`tools/split-register.py`](../../tools/split-register.py) moves it to the file its number
> belongs to. [`tools/register-text.py`](../../tools/register-text.py) reads every row back into
> the register for every tool that reads it, so a row here is seen exactly as it was seen there.

| # | Defect | State |
|---|---|---|
| **176** | **FOUR OF THE OWNER'S OWN QUESTIONS THAT ASK FOR NO CHANGE ARE ANSWERED BY SOMETHING THAT CHANGES THE MODEL - AND ONE OF THEM IS "PING THE REVIT MODEL".** Found 2026-09-23 by the first run of [`tools/score-routing.py`](../../tools/score-routing.py): his 79 questions from real work, asked through `heron_brain.lookup` (the function `heron_lookup` calls), 396 fragments, `lexical` backend, no Revit filter - row 1 of the owner's-questions table in [`brain/retrieval-history.md`](../../brain/retrieval-history.md). Each with the answer he confirmed in [`tests/data/owner-questions.yaml`](../../tests/data/owner-questions.yaml): *"Ping the Revit model."* -> `RELOAD_LINKS` (MODIFY), his answer the `revit_health` tool; *"What is the biggest duct size?"* -> `AUTO_SIZE_MEP` (MODIFY), his answer `GROUP_AND_COUNT`; *"Check all the ducts and make a visual report by width."* -> `EXPORT_VIEWS_TO_FBX` (**PUBLISH** - it writes a file), his answer `GROUP_AND_COUNT`; *"Select any one air terminal of this family so I can check it."* -> `PLACE_FAMILY_INSTANCES` (MODIFY), his answer `SELECT_BY_FAMILY`. **All four came by RANKING, not by a declared phrase** - no fragment claims these sentences, so identity cannot fire and ranking decides, which is [row 116](../FRAGMENT-ISSUES.md#)'s shape met on real questions. **All six of his GAP questions also landed on a MODIFY** - jobs nothing in Heron does, answered by a stand-in that writes (*"Make the ducting for the corridor."* -> `RENUMBER_SEQUENTIAL`). **THE FLOOR HELD, THE RANKING DID NOT.** Measured by asking the four again: a READ sat in every one of the four shortlists (`READ_MODEL_FILE_INFO` behind the ping, `CHECK_SLEEVE_SIZE` behind the duct size, `REPORT_DUCT_WEIGHT` behind the report, `SELECT_GROUP_MEMBERS` behind the select), so `heron_lookup`'s *"CHECK THIS IS A CHANGE YOU MEANT TO MAKE"* warning ([row 113](../FRAGMENT-ISSUES.md#)) would print on all four. **What was NOT checked:** the trained `model` backend, which this container cannot fetch - the owner's PC is the comparable run; and whether a host shown that warning actually stops. | **OPEN - recorded, not fixed, and the obvious repair is ruled out.** Rows 113 and 116 closed the crossings of a list a session wrote by DECLARING each sentence on the READ that should own it. **Here that is the forbidden move:** a declared owner's question is answered by identity, so the score would stop measuring the search and measure the declaration - `check-routing.py` wearing his words ([row 113](../FRAGMENT-ISSUES.md#), D-34, the scorer's own docstring). The repair belongs to the retrieval layer (the trained backend, `A7`; the re-ranker, `A10`) or to a READ that does not exist yet. `python tools/score-routing.py` re-measures it, and names these four under *"asked for no change to the model, and handed one"*. |
