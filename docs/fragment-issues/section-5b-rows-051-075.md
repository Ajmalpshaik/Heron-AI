# Fragment issues — section 5b, rows 51 to 75

> Rows 51 to 75 of section 5b of [the register](../FRAGMENT-ISSUES.md), in their own file since 2026-09-23 so that
> they can be read alone. **The section's own rules are on that page, under its heading.** A new
> row goes at the end of the section's last file, whatever its number, and the next run of
> [`tools/split-register.py`](../../tools/split-register.py) moves it to the file its number
> belongs to. [`tools/register-text.py`](../../tools/register-text.py) reads every row back into
> the register for every tool that reads it, so a row here is seen exactly as it was seen there.

| # | Defect | State |
|---|---|---|
| **51** | A SECOND DOOR ONTO THE SAME BRIDGE, WITHOUT THE ONE SETTING THE FIRST DOOR EXISTS TO CARRY. — [full row](../fragment-issues-archive/reading-defects-5b-051-075.md#row-5b-51) | **FIXED 2026-09-21** |
| **52** | **A DECISION WHOSE OWN TEXT SAYS "THE RENAME IS THE DELIVERABLE, NOT A TIDY-UP" SHIPPED WITHOUT THE RENAME, AND THE WORD IT WITHDREW IS IN THE FILE A USER READS BEFORE TRUSTING HERON NEAR A LIVE MODEL.** Found 2026-09-21 reading `DISCLAIMER.md`. [D-84](../DECISIONS.md), accepted **2026-09-20**, answers Q-56 with *"keep what exists, build no subprocess, and take the word out"* - because `heron_sandbox` runs a new agent as ordinary Python in the supervising process, so *"an agent that cooperates is held. One that does not can call `open()`, import the bridge, reach into globals or delete files, and nothing is in its way. That is most of the value and none of the guarantee, and the word sandbox implies otherwise."* It then says what to do: ***"Sandbox" is replaced with "watched, not contained" in the module and in the `HERON-AHR-SBX-016` register row***, and **"shipping the behaviour without the rename would deliver the opposite of what was decided."** **NONE OF IT WAS DONE.** The phrase *watched, not contained* appears nowhere; `brain/heron_sandbox.py` still opens *"The Agent Sandbox - where a new agent runs before it is allowed near real work"*; the register row still reads *"Runs a newly built agent **in isolation**"*, which is the exact promise the decision says the code does not keep; and `DISCLAIMER.md` tells a modeller *"Generated code is **tested in a sandbox** and reviewed before it is allowed near a live model."* **AND THE DECISION'S OWN CONSEQUENCE LINE IS THE FALSE ONE**: *"what it does not catch is a hostile or badly broken agent, **and there is now nowhere in the repository that claims otherwise**."* **Q-56 IS ALSO STILL CITED AS OPEN ACROSS THIRTEEN FILES** - seven `brain/` modules, three agent contracts and three suites - *"Q-56 is open and waiting on the owner"* - while `README.md` and `HANDOVER.md` had been reporting **57 answered, 0 open**. **AND THREE SUITES HOLD IT IN POSITION**: `tests/test_builder.py` asserts the source cites Q-56 *"which is recorded as the open question it is"*, and `test_shadow.py` and `test_healing.py` do the same | **FIXED 2026-09-21, AS THE DECISION WROTE IT.** `watched, not contained` in the module docstring and in the `HERON-AHR-SBX-016` register row; `DISCLAIMER.md` now describes what the first run actually does - every door shut, every knock written down, the run marked so it can never count as evidence - and then says plainly that it holds a cooperating agent and does not contain a hostile one, naming D-84. `SECURITY.md`'s *"Sandbox escape"* row keeps its example and loses the word. **THE ELEVEN Q-56 CITATIONS ARE DELIBERATELY NOT TOUCHED HERE, AND THIS IS THE RECORD OF THAT**: `heron_builder`, `heron_healing`, `heron_mentor`, `heron_packages`, `heron_sandbox`, `heron_shadow`, `heron_tooling` three agent contracts and the suites `test_builder`, `test_shadow` and `test_healing` all still read *"Q-56 is open"*. Each is load-bearing - it is why that agent refuses to run anything - so none can be deleted, only reworded, and D-84 named **two** places rather than thirteen. Widening a reviewable change is how it stops being reviewable, so they are recorded here as owed and taken in the next change rather than this one. **NOTHING ABOUT THE BEHAVIOUR CHANGED, WHICH IS D-84's POINT** - build no subprocess, and stop the name promising what the code does not do |
| **53** | THE DISCLAIMER TOLD A MODELLER UNDER AN NDA THAT HERON HAS A LOCAL-ONLY MODE. IT HAS NEVER HAD ONE. — [full row](../fragment-issues-archive/reading-defects-5b-051-075.md#row-5b-53) | **FIXED 2026-09-21.** |
| **54** | **THE EIGHTH, NINTH AND TENTH COPIES OF THE NEVER-RUN SENTENCE, AND [ROW 26](../FRAGMENT-ISSUES.md#) SAID IN WRITING WHERE THEY WOULD BE.** Found 2026-09-21 reading `README.md`. Its status table says of Step 6, the write path: **"⚠️ Built and compiled. Never run."** and *"It has still **never loaded into Revit and has never moved anything**."* `docs/README.md`'s status banner says Step 6 and the whole of Phase 2 *"are built, compile on all eight releases, and **have never loaded into Revit**"*. `docs/27-build-order.md`'s Phase 1 gate says *"Step 6 is written, compiles on all eight Revit releases, and has **never loaded into Revit**."* **ALL THREE ARE FALSE, AND HAVE BEEN FOR A FORTNIGHT.** `NEEDS-CHECKING.md`'s **B8 is struck through**: *"PASSED 2026-09-07 ... Revit 2024, `Project1 work_ajmal.al`, **3 ducts moved up 200 mm** - the first time Heron has ever changed a Revit model"*, with the amber banner measured across 918 captured frames. The add-in is deployed on 2020, 2024 and 2027, and `README.md`'s own next paragraph says so. **ROW 26 HUNTED THIS SENTENCE AND FOUND FOUR COPIES; [ROW 37](../FRAGMENT-ISSUES.md#) FOUND TWO MORE; [ROW 44](../FRAGMENT-ISSUES.md#) FOUND THE SEVENTH AND CALLED ITSELF THE LAST.** Every one of those hunts grepped **the wording it had just fixed** - *"never been compiled"*, *"never been proven"*, *"never been run against a real model"*. These three say *"never loaded into Revit"* and *"never moved anything"*, and no grep for the first phrasing can see them. **ROW 26 CLOSES WITH THE PREDICTION**: *"WHAT WAS NOT CHECKED: whether the same claim appears in the numbered documents under `docs/` **in a form the grep missed**."* One of the three is a numbered document under `docs/`. Nobody went back | **FIXED 2026-09-21 IN ALL THREE, AND THE CLASS IS CLOSED BY A GATE RATHER THAN A FOURTH HUNT.** Each now says what is true and what is still owed: the write path has loaded, compiled inside Revit's own process and moved three ducts on 2026-09-07, `write.enabled` is still `false`, and **D3 - somebody putting a tape measure on those ducts - is not struck through**. **`check-docs.py` now derives the answer from `NEEDS-CHECKING.md` itself**: while `B8` is recorded as a dated PASS, no markdown file may carry a live claim built from *"never loaded into"*, *"never moved"* or *"never been run against"* and the write path. Quotations and dated records are skipped by the rules the file already has, and **`docs/DECISIONS.md` is exempted by name, for a stated reason**: [D-19](../DECISIONS.md)'s Context records the conditions the decision was taken under - *"written on a machine with no Revit, no Windows and no .NET SDK"* - and rewriting that would be rewriting history rather than correcting a claim. Proved by making it fail: the gate reports all three before the fix and nothing after, and putting any one sentence back turns it red. **AND THIS ROW SET THE GATE OFF WHILE DESCRIBING IT**, which is the same trap `.claude/skills/heron-guard` solves by building the Revit namespace out of parts: a file that explains a forbidden string contains it. The phrases are quoted here rather than written plainly, and the check reads quote parity **from the start of the table CELL** rather than of the block - a register section is one block of several hundred rows, so parity from its start is the parity of everything above, and it called six citations claims on the first run. **ROWS 52 AND 53 WERE ANNOUNCED HERE BEFORE THEY WERE WRITTEN**, and they are above: the `DISCLAIMER.md` pair - a local-only mode that has never existed, and the word [D-84](../DECISIONS.md) withdrew. Announcing a number before using it is [row 149](../FRAGMENT-ISSUES.md#)'s rule, and the half nobody follows is the announcement |
| **55** | A GATE THAT MATCHES ONE WORDING LEAVES EVERY SYNONYM OPEN, AND FOUR FRAGMENT COUNTS ROTTED IN THREE… — [full row](../fragment-issues-archive/reading-defects-5b-051-075.md#row-5b-55) | **FIXED 2026-09-21.** |
| **56** | **A QUESTION ANSWERED ON 2026-09-20 WAS STILL CALLED OPEN IN THIRTEEN FILES, AND THREE SUITES HELD IT THERE.** Found 2026-09-21, recorded in [row 52](../FRAGMENT-ISSUES.md#) as owed and taken here. `Q-56` - *what actually contains a new agent while it runs* - was answered by [D-84](../DECISIONS.md): **nothing more than today, build no separate process, and take the word sandbox out**. `README.md` and `HANDOVER.md` had been reporting **57 answered, 0 open** from the moment it closed. Meanwhile seven `brain/` modules, three agent contracts and three suites went on saying *"Q-56 is open and waiting on the owner"*, *"Q-56 is still open about what contains a process Heron starts"*, *"for the reason Q-56 is open"*. **AND THE THREE SUITES WERE NOT BYSTANDERS.** `tests/test_builder.py` asserted `"Q-56" in source` and described it as *"the open question it is"*; `test_shadow.py` required `Q-56` in the answer's `unjudged`; `test_healing.py` required it in `why`. **A test asserting the stale wording is what keeps it stale** - correct the module and the suite goes red, so the cheapest move is to leave the sentence alone. That is [row 26](../FRAGMENT-ISSUES.md#)'s fifth copy in a new place: *"a test keeping an expired sentence alive: it would have gone red on any honest correction, which is exactly what it did"* | **FIXED 2026-09-21, REWORDED AND NOT DELETED.** Every citation is load-bearing - it is the reason that agent returns a command rather than running it - so removing it would take the reasoning with it. Each now names **D-84** and what it settled, so the reason survives and the status is true. The three suites assert the same thing about the decision instead of the question, with a comment saying why the assertion moved. **TWO OF THEM WENT RED ON THE FIRST ATTEMPT, FOR A REASON WORTH COPYING**: `test_builder` and `test_healing` each check that their module's source contains no `subprocess`, which is a guard against the module ever spawning one - and the new prose quoted D-84's *"build no subprocess"* verbatim, so the docstring broke a rule about the code. The wording is *build no separate process* now. **The heading `IT RUNS NOTHING. Q-56` stays**, and so does `test_builder`'s *"Q-56 says it is not"*: a pointer to where the reasoning lives is not a claim that the question is open |
| **57** | THE FILE THAT OWNS THE EXIT-CODE CONTRACT SAID TWO SUITES BREAK IT, AND NEITHER DOES. — [full row](../fragment-issues-archive/reading-defects-5b-051-075.md#row-5b-57) | **FIXED 2026-09-21.** |
| **58** | THE REPOSITORY'S OWN GAP REPORT CHECKED THREE OF THE FOUR GATES THAT MUST PASS, AND THE MISSING ONE… — [full row](../fragment-issues-archive/reading-defects-5b-051-075.md#row-5b-58) | **FIXED 2026-09-21.** |
| **59** | **WHICH REVIT RELEASES HERON SUPPORTS IS DECLARED IN FIVE PLACES, AND FOUR OF THEM WERE COMPARED TO NOTHING.** Found 2026-09-21 reading `tools/check-package.py`, whose own `releases()` docstring records the duplication and defers it: *"it is already stated twice in this folder ... recorded rather than fixed here, because merging those two is a change to two working gates and belongs in its own review."* **THAT UNDERCOUNT IS THE FINDING.** Two things were wrong with it. `check-compile.py` does **not** declare one - its `ALL_VERSIONS` **IS** `heron_dotnet.RELEASES`, by identity, and `tests/test_dotnet.py` asserts exactly that. And the count was scoped to `tools/`, where the declarations are not. Measured: **`brain/heron_dotnet.RELEASES` is the one home; `Directory.Build.props` and `check-compile.py` are held to it by `test_dotnet.py`; and `tools/check-api-surface.py`, `brain/heron_fragment.py`, `brain/heron_packages.py` and `mcp/server/heron_register.py` each type the same eight years with nothing comparing them.** All five agree today. **THIS IS [GOLDEN RULE 4](../14-golden-rules.md)'s LIST** - *never break a working Revit version* - and [docs/16](../16-version-support-strategy.md) is the strategy resting on it, so the day a release is added or dropped, two of these move together and four do not. **AND ONE OF THE FOUR IS PINNED BY A TEST THAT AGREES ONLY WITH ITSELF**: `tests/test_register.py` asserts `REG.REVIT_VERSIONS == ("2020", ... "2027")` - the same literal typed a second time - which is the shape `tests/test_unit_test.py` names in its own docstring as what *"let a withdrawn agent keep its contract"* | **HELD TOGETHER 2026-09-21, NOT MERGED.** The deferral was right: five working modules is not a change to make in passing. `tests/test_supported_releases.py` **finds** every module-level list of Revit years under `brain/`, `mcp/`, `platform/`, `revit/` and `tools/` and requires each to be `RELEASES` - so a sixth copy is caught the day it arrives rather than the day somebody notices. `tests/` is excluded on purpose, because a suite may legitimately use three releases as a fixture and `tests/test_regression.py` does; a SET is not matched either, because `NEEDS_WINDOWS_DESKTOP` is a genuine subset with its own meaning. A module-level subset that is ever legitimate goes in a named excuse table with its reason, the way `check-reachable.py` records one rather than widening a pattern until it catches nothing - **empty today, which is the honest state**. Proved by making it fail twice: adding a 2028 to one copy, which is named with both values; and narrowing the sweep so it finds nothing, because **a sweep that compares nothing passes perfectly and means the opposite**. `check-package.py`'s paragraph now says five and why, and keeps the deferral |
| **60** | THE EVIDENCE TOOL'S OWN SUMMARY MERGED THE FOUR STATES, THREE LINES BELOW ITS GATE THAT DOES NOT… — [full row](../fragment-issues-archive/reading-defects-5b-051-075.md#row-5b-60) | **FIXED 2026-09-21.** |
| **61** | **THE ONE CLAIM WRITTEN FOR A BIM MODELLER DECIDING WHETHER TO TRUST HERON NEAR THEIR MODEL WAS TRUE WHEN IT WAS WRITTEN, AND IS NOW THE OPPOSITE OF TRUE.** Found 2026-09-21 reading `docs/PROJECT-MAP.md` §B. Its **BIM modeller or coordinator** route says *"read and write are different things, and they are proven to different depths. Reading is proven far more widely - **106 `READ` fragments carry a proof against 63 `MODIFY`**"*, and `README.md`'s modeller row says the same sentence. **At 106 against 63 it was correct.** Derived today: **145 `READ` and 166 `MODIFY` are `PROVEN`** - so more MODIFY fragments carry a proof than READ ones, and proportionally the gap is seven points (89% against 82%), not *far more widely*. **NOBODY CHANGED THE CLAIM; THE LIBRARY MOVED PAST IT**, which is the harder half of a stale number: a sentence that was checked when it was written and never again. **AND THE WORDING PUT IT OUTSIDE THE ONE GATE THAT WOULD HAVE NOTICED.** `check-docs.py` matches *N RISK fragments are PROVEN* and its trailing *against N RISK* - a pattern added after `README.md` said 55 MODIFY PROVEN on one line and 63 on another while the fragments said 77. This says **carry a proof**, three words outside it, so the gate that exists for exactly this claim could not see this copy of it. Third wording, after [rows 50](../FRAGMENT-ISSUES.md#) and [55](../FRAGMENT-ISSUES.md#). **THE SAME PARAGRAPH ALSO SAID *185 of 360* ARE `DRAFT`** - it is 68 of 395 - **directly above its own line *"Derive both rather than believing any of those numbers, including these"*.** Fifth time in this sitting a file has warned about its own stale figures immediately above stale figures | **FIXED 2026-09-21, AND THE SENTENCE IS GONE RATHER THAN RE-AIMED.** What was being reached for is true and does not move: **what keeps a modeller safe is the switch, not the library** - `write.enabled` defaults to `false` and stays there until the write path has been through the register, and `D3` is still owed on the three ducts it moved on 2026-09-07. Both files say that instead, with no typed count in either and the old sentence kept with its date, because *it was true when written* is the part worth keeping. **`check-docs.py` can see the third wording now**, read sentence by sentence like the other two and measured before it was added: the pattern fires on that one line in the whole repository and on nothing correct. Proved by putting the claim back |
| **62** | THE TWO DOCUMENTS A BIM MANAGER IS SENT TO BOTH UNDERSTATE WHAT HAS BEEN CONFIRMED ABOUT REVIT… — [full row](../fragment-issues-archive/reading-defects-5b-051-075.md#row-5b-62) | **FIXED 2026-09-21, WITH THE TWO LISTS SAID SEPARATELY.** |
| **63** | THE SWEEP'S OWN BOOKKEEPING TOOL REFUSED TWICE IN ONE SITTING AND EXITED 0 BOTH TIMES, SO NEITHER… — [full row](../fragment-issues-archive/reading-defects-5b-051-075.md#row-5b-63) | **FIXED 2026-09-21.** |
| **64** | THE FILE EVERY AGENT READS FIRST SAYS `.claude/skills/` IS "the ONLY skills tree", AND HERON'S OWN… — [full row](../fragment-issues-archive/reading-defects-5b-051-075.md#row-5b-64) | **FIXED 2026-09-21, BY SAYING WHICH KIND OF SKILL.** |
| **65** | **[D-84](../DECISIONS.md) SAYS *"there is now nowhere in the repository that claims otherwise"*, AND THE WORD IT WITHDREW IS STILL IN THE CONSTITUTION, IN A GOLDEN RULE, AND IN THE README's DECIDED TABLE.** Found 2026-09-21 reading `HERON_CONSTITUTION.md`. [Row 5b-52](../FRAGMENT-ISSUES.md#) took the word out of `brain/heron_sandbox.py`, the register row, `DISCLAIMER.md` and `SECURITY.md` - the four places D-84 named or implied. **THREE MORE STAND, AND THEY ARE THE THREE THAT BIND.** `HERON_CONSTITUTION.md` **Article 11**: *"Untested code never touches a live model. Generated or newly imported code runs in a **sandbox** or against a detached copy first."* `docs/14-golden-rules.md` **Rule 18**: *"New code runs in a **sandbox** or against a detached copy."* `README.md`'s **Decided** table: *"Generated code - Hybrid: **scripting sandbox** while testing."* **THE CONSTITUTION IS THE WORST OF THE THREE** because its own Enforcement section says the Articles *"are assembled into an agent's instructions from this file"* - so this is not a document a person might read, it is the text injected into a running agent, which is told a containment exists that D-84 recorded as not built. **NOT FIXED HERE, DELIBERATELY, AND THE REPOSITORY'S OWN RULE SAYS SO.** The Constitution's **Amendment** section reads *"Articles are never weakened silently, and **never by an agent**"*, and Golden Rule 18 is the owner's on the same footing. `AGENTS.md` names the answer for exactly this case: *record both, name the governing decision, name the observed evidence, and name who resolves it - leave a policy conflict open rather than closing it whichever way makes your task easier*. **CHECKED AND DISCARDED IN THE SAME SWEEP**: the other 30-odd uses of the word are the agent's NAME (`HERON-AHR-SBX-016`), a state value (`first_run: SANDBOX`) and a field (`sandboxed`) - D-84 withdrew a promise of containment, not an identifier - and `brain/agents/HERON-AHR-SBX-016.yaml` is already honest, saying in its own contract that it *"cannot interrupt arbitrary code and says so"* | **RECORDED, NOT APPLIED - IT IS THE OWNER'S CALL AND IS NOW ONE LINE TO SAY YES TO.** Written up as **[F23](../PROPOSALS.md)**, which `tools/owner-queue.py` counts, with the exact replacement wording drafted for all three so nothing has to be composed at the sit-down. **What is NOT proposed**: no Article's requirement changes, because the requirement is met - the first run really does shut the Revit door and the production scopes and record every knock. Only the word that names the mechanism overstates it, which is the whole of what D-84 decided |
| **66** | THE SIZE OF THE FRAGMENT LIBRARY WAS TYPED AS A PRESENT-TENSE FACT IN ELEVEN LIVE PLACES, WRONG BY… — [full row](../fragment-issues-archive/reading-defects-5b-051-075.md#row-5b-66) | **FIXED 2026-09-21, AND HELD BY A NEW SUITE.** |
| **67** | THE SENTENCE SPLITTER IN THREE GATES DOES NOT BREAK ON . — [full row](../fragment-issues-archive/reading-defects-5b-051-075.md#row-5b-67) | **FIXED 2026-09-21 IN BOTH FILES, WITH THE SAME REGEX.** |
| **68** | THE BUILD FILE EVERY C# TARGET INHERITS SAYS ONE REVIT VERSION IS PROVEN. EIGHT COMPILE ON EVERY… — [full row](../fragment-issues-archive/reading-defects-5b-051-075.md#row-5b-68) | **FIXED 2026-09-21.** |
| **69** | **A COMMENT INSIDE A `PROVEN` FRAGMENT'S IMPLEMENTATION IS NOT A FREE EDIT, AND I LEARNED IT BY HAVING CI REJECT MY OWN FIX.** Found 2026-09-21 the expensive way. [Row 66](../FRAGMENT-ISSUES.md#) corrected a stale *349 fragments* in a **comment** in `brain/fragments/report-schedule-definition/impl/any/fragment.cs`. The four gates were green, the suites were green, it was pushed - and CI failed: *"the store STILL does not match after a rebuild - 1 on disk the store does not hold (FRG-VIEW-056)"*. `brain/heron_fragment.py` says why in one sentence: ***"status is PROVEN but the proof does not match this implementation - no fingerprint, or one taken against different bytes."*** **THIS IS [D-30](../DECISIONS.md) WORKING, NOT A FAULT.** A proof's staleness fingerprint is taken over the **whole implementation file**, because there is no honest way to tell a comment from code by bytes - and a proof is evidence about the code it was taken against. **THE CONSEQUENCE IS THE PART WORTH WRITING DOWN**: inside a PROVEN fragment's `impl/`, **the correction and the re-proof are one act**, and the re-proof needs a real Revit and a named model. So a stale sentence in there simply cannot be fixed on this machine - not *should not*, cannot. **AND THE THREE WRONG WAYS OUT WERE ALL AVAILABLE**: demote the fragment to DRAFT (throws away a real proof to tidy a comment), re-take the fingerprint without the model (a proof about nothing), or delete the check | **REVERTED, AND THE SUITE NOW REPORTS IT AS THE THIRD STATE RATHER THAN THE WRONG ONE.** The comment is back exactly as it was and the fragment is `PROVEN` again. `tests/test_library_total.py` works out for each finding whether the file is inside a `PROVEN` fragment's `impl/` and prints **`wait ... sealed by <fragment>'s proof, and correcting it needs a Revit`**, counted separately and failing nothing - *a stale count is cheaper than a broken proof*. **The fragment is found, not listed**, so a second one is named the day it appears. One waits today |
| **70** | THE JOB CALLED "THE GATES THAT MUST PASS" RUNS NINE COMMANDS. FOUR DOCUMENTS SAY FOUR, AND ONE OF… — [full row](../fragment-issues-archive/reading-defects-5b-051-075.md#row-5b-70) | **FIXED 2026-09-21 IN ALL FOUR PLACES, BY NAMING THE OTHER FIVE AND WHAT EACH CATCHES.** |
| **71** | CI'S OWN COMMENT SAYS TWO CHECKERS "say so rather than failing" WHEN THERE IS NOWHERE TO KEEP A… — [full row](../fragment-issues-archive/reading-defects-5b-051-075.md#row-5b-71) | **FIXED 2026-09-21 IN BOTH TOOLS, AND BOTH PATHS RUN.** |
| **72** | **THE FUNCTION WHOSE DOCSTRING SAYS IT ANSWERS *ALIVE*, *DEAD* OR *CANNOT TELL* HAD A FOURTH OUTCOME: NEVER.** Found 2026-09-21 sweeping `brain/` and `mcp/` for unbounded calls. `mcp/client/heron_bridge_client.py`'s `process_is_running()` shells out to `tasklist` with `subprocess.check_output` and **no timeout**, under a docstring that reads ***"Returns None when it cannot tell - and 'cannot tell' must never be treated as 'dead'."*** A hang is not None. It is never. **AND IT IS THREE CALLS, NOT ONE**: `bridge_process_is_running()` asks once per image in `BRIDGE_IMAGES` - `Revit.exe`, the test host, `dotnet.exe` - so a stalled `tasklist` is three unbounded waits per process id, and the session picker asks per discovered bridge. **WHAT MAKES IT WORTH A ROW IS THE COMPANY IT KEEPS.** Every pipe operation in that same file is bounded on purpose: `_connect` runs a deadline loop and returns None when it expires, `_read_line` runs the read on a **daemon thread** and joins with a timeout, `RESPONSE_TIMEOUT_S` comes from `heron_config.response_timeout()` and four call sites pass `fragment_timeout()`. The one subprocess call in the file is the one that was not - and it is on the path a modeller waits on. `tools/check-gaps.py` already carries the lesson one layer out, `SUITE_TIMEOUT = 300`, with its own comment: *"a test that hangs is work somebody can do today"* | **FIXED 2026-09-21 WITH ONE KEYWORD, BECAUSE THE EXISTING HANDLER ALREADY DID THE RIGHT THING WITH IT.** `subprocess.TimeoutExpired` is an `Exception`, and the call was already wrapped in `except Exception: return None` - so `timeout=TASKLIST_TIMEOUT_S` turns a hang into exactly the answer the docstring promises. **Five seconds and not one**: `tasklist` on a loaded machine is slow rather than instant, and a deadline that fired on a healthy box would call a live Revit unknown and prune nothing. **Not a config key**, deliberately: eight settings are declared in `HeronConfig.Defaults` and read back by a second reader, and [row 5b-29](../FRAGMENT-ISSUES.md#) is what a ninth costs when the two drift. **MEASURED FIRST, AND THE FIRST COUNT WAS WRONG**: a line-scoped grep said 55 of 56 `subprocess` calls carry no timeout; an AST scan says **40 of 56** - the grep missed every call whose `timeout=` sits on the next line, `change-evidence.py` among them. The other 39 are developer tools and suites, where a hang is visible at a terminal and CI has a job timeout. Reported as **one place**, not as a sweep |
| **73** | **THE TOOL THAT CAPTURES EVIDENCE CANNOT RUN TWO OF THE GATES THE TOOL THAT READS IT DEMANDS, SO EVERY C# CHANGE COMES BACK `REVISE`.** Found 2026-09-21 running the gate on PR #228. `check-change.py`'s `BY_SIGNAL` owes `["check-compile", "check-api-surface"]` for a **revit-version** signal - which any change touching a `.cs` file or `Directory.Build.props` raises - and `change-evidence.py`'s `RUNNABLE` table held eight gates, neither of them among them. **MEASURED**: `REVISE - gate(s) this change owes were not run: check-package, check-compile, check-api-surface`. **THE ESCAPE HATCH IS THE WRONG ONE.** `--stated` exists for a gate that ran **somewhere else**, and marks it *as a claim rather than a measurement* - so the only route through was to file a real local measurement as a claim, which is the one use that flag exists not to have. **AND BOTH RUN HERE**: `check-api-surface` answers in seconds with its assemblies cached in `tools/api-surface/.assemblies` - 362 referenced Revit types and members, exit 0 - and `check-compile` builds all eight releases in about four minutes. **TWO TOOLS DESIGNED AS A PAIR, AND ONE COULD NOT PRODUCE WHAT THE OTHER REQUIRED** | **FIXED 2026-09-21 - two lines in `RUNNABLE`, and nothing gets slower by default.** `--gate` is opt-in, so the change is not that these now run, it is that it becomes **possible** to record what the other tool requires. The comment above the table says which signal asks for them and why `check-compile` was presumably left out - it is slow - and names `check-gaps`, slower still and in that table all along. Proved on the same range: `check-change` over `887d9df..b086543` went from **REVISE** to **PASS** with all seven owed gates present |
| **74** | AN MSBUILD TOOLCHAIN ERROR IS NOT A COMPILE ERROR, AND THE GATE'S CLOSING SENTENCE TOLD ME IT WAS.… — [full row](../fragment-issues-archive/reading-defects-5b-051-075.md#row-5b-74) | **FIXED 2026-09-21, AND NOT BY TURNING A FAILURE INTO A SKIP.** |
| **75** | **THE LIFECYCLE LADDER IS DECLARED SEVEN TIMES AND THE CODE IS SPLIT DOWN THE MIDDLE - FOUR DECLARATIONS FOLLOW THE CONFIRMED SPECIFICATION AND TWO HAVE ADOPTED AN UNCONFIRMED PROPOSAL.** Found 2026-09-21 reading `brain/heron_capability.py`. **THE TWO LADDERS.** [00 §18](../00-master-specification.md) is the confirmed fragment lifecycle - `DISCOVERED DRAFT TESTING VALIDATED PROVEN PRODUCTION DEPRECATED ARCHIVED`, no `SHADOW`. [24](../24-trust-model.md) Axis 1 proposes the same eight **with `SHADOW`** between `VALIDATED` and `PROVEN`, and its own header says it ***"proposes a resolution and needs confirmation - see [Q-34](../OPEN-QUESTIONS.md)"***. [D-14](../DECISIONS.md) stays **Proposed**: the owner answered ***"yes, but show me on screen first"*** **twice, eleven days apart**, `R1b` in [NEEDS-CHECKING.md](../NEEDS-CHECKING.md) is the confirmation, and **only a screen closes it**. **WHERE THE SPLIT RUNS, MEASURED**: `heron_fragment.STATUSES`, `heron_capability.TRUST`, `heron_retrieve.OFFERABLE` and `heron_retrieve.QUALITY` follow the **CONFIRMED** ladder; `tools/check-metadata.py`'s `STATUSES` and `heron_deployment.LADDER` follow the **PROPOSED** one - and `heron_deployment` gates it properly, *"SHADOW needs a shadow plan"*. **SO A FILE CAN BE GIVEN `Heron-Status: SHADOW`, PASS THE GATE, BE PROMOTED THROUGH IT, AND BREAK THREE MODULES**: `can_promote` answers *"'SHADOW' is not a lifecycle state"*, `STATUSES.index` raises **ValueError** for a fragment already there, `TRUST.get(status, 0)` ranks it **0** - the value of `DEPRECATED`, below `DRAFT` and `DISCOVERED` - and retrieval never offers it at all. Nothing carries `SHADOW` today (327 `PROVEN`, 78 `DRAFT`), so it is latent on either side | **NOTHING WAS MOVED, AND THE REASON IS THAT I MOVED IT THE WRONG WAY FIRST AND HAD TO WITHDRAW IT.** I read docs/24 as policy, added `SHADOW` to all four CONFIRMED declarations, wrote a suite requiring every copy to match docs/24, and opened a pull request. **It was wrong in its direction.** docs/24's own header, [docs/README.md](../README.md)'s *"two items need confirmation before building"*, D-14's status and Q-34 all say the same thing, and **any one of them was enough** - I had read the table and not the sentence above it. `AGENTS.md` names both halves of what that breaks: *a rule is never edited to match code*, and **leave a policy conflict open rather than closing it whichever way makes your task easier**. Reverted, in full. **WHAT SHIPS INSTEAD IS A SUITE THAT DECIDES NOTHING.** `tests/test_lifecycle_ladder.py` reads **both** ladders out of the documents that carry them - by heading, not by line number - reports which side each of the seven declarations is on, prints the split in one place, and **fails only on a declaration matching NEITHER**. It also makes each subset name what it leaves out and why, because a subset with no declared exclusions cannot be told from an accident. **The conflict is now visible, cannot drift further unnoticed, and is still the owner's to close - on a screen, as he asked, twice** |
