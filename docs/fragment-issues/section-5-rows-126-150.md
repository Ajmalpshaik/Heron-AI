# Fragment issues — section 5, rows 126 to 150

> Rows 126 to 150 of section 5 of [the register](../FRAGMENT-ISSUES.md), in their own file since 2026-09-23 so that
> they can be read alone. **The section's own rules are on that page, under its heading.** A new
> row goes at the end of the section's last file, whatever its number, and the next run of
> [`tools/split-register.py`](../../tools/split-register.py) moves it to the file its number
> belongs to. [`tools/register-text.py`](../../tools/register-text.py) reads every row back into
> the register for every tool that reads it, so a row here is seen exactly as it was seen there.

| # | Defect | State |
|---|---|---|
| **126** | I FILED A DEFECT THAT WAS MY OWN MISUSE OF THE TOOL, AND ROW 71's HAZARD IS THE REASON IT LOOKED… — [full row](../fragment-issues-archive/proving-defects-126-150.md#row-126) | NOT A DEFECT - WITHDRAWN 2026-09-19, the same day it was written, and it had already… |
| **127** | **`heron_lookup` READS A CAPABILITY'S STATUS FROM THE INDEX AND ITS TOTALS FROM DISK, SO ONE REPLY CAN SAY `PROVEN` AND `83 ARE NOT` ABOUT THE SAME FRAGMENT.** Measured 2026-09-19, twice, minutes apart. `select-types` (FRG-SEL-027) had been set to DRAFT that morning ([row 123](../FRAGMENT-ISSUES.md#)'s demotions). `heron_lookup` answered **`FRG-SEL-027 is PROVEN, so it may run without asking`** - and closed the same reply with **`312 of 395 capabilities are PROVEN... The other 83 are not, and a fragment that has not been proved is a claim`**. The 312/83 is derived live and is correct for this working tree; the per-capability line is not. **THE DANGEROUS HALF IS THE CLAUSE, NOT THE WORD.** *so it may run without asking* is a PERMISSION sentence, and it was granted on a stale row - exactly what a status is for. **THE STORE IS THE SAME ONE [ROW 116](../FRAGMENT-ISSUES.md#) FOUND WAS NOT DETERMINISTIC** - `%APPDATA%\Heron\knowledge\global.db`, one file for every worktree, rebuilt by `index()` wholesale - so this is that row's hazard in a second place, and this time it is carrying an authorisation rather than a ranking | **FIXED 2026-09-19 BY THE FIRST OF THE TWO ROUTES THIS CELL NAMES** - *derive the status the same way the totals are derived*. `may_run_unasked(fid, index_status)` in `brain/heron_search.py` asks BOTH sources and grants only when they agree; `ask()` and `heron_retrieve.find()` both call it, so the one seam a host uses and the one the search layer uses cannot diverge. **WHY NOT `FRAG.load_all()`, WHICH IS ALREADY THE AUTHORITY: measured at 1.95 s**, because it parses every contract, and a lookup cannot pay that. A SHALLOW read of the two top-level lines needed costs **9 ms** for all 395, a stat walk to know whether even that is needed costs **3 ms**, and the map is cached against that signature - **1.4 ms warm**. Same answer as the authority, at a thousandth of the price. **TWO RULES WERE WRITTEN AND REMOVED BEFORE THE RIGHT ONE, AND THEY ARE THE USEFUL PART.** (1) *A status that cannot be checked is not a permission* - fails closed, and **wrong**: it broke `tests/test_search.py`, which indexes a synthetic `FRG-QA-900` with `folder='x'` and no file anywhere. **Absence is a LEGITIMATE state** - a store can be built from another root - and it is not evidence of staleness. (2) *Report any disagreement* - also wrong, because the index can be the STRICTER of the two, and there a correct sentence was being replaced by a louder one that caught nothing. **The rule that survives fires in ONE direction only: where the index would have GRANTED and the file disagrees**, which is this row's hazard exactly and nothing else. **Twelve checks in `tests/test_search.py`**, including one that walks every combination of id and index status and asserts it never grants where the index alone would not have. **WHAT IS NOT FIXED:** the store still goes stale - [row 116](../FRAGMENT-ISSUES.md#) and [row 136](../FRAGMENT-ISSUES.md#) are that - and this only stops a stale row carrying an AUTHORISATION. The totals line remains the live fingerprint of which tree is being read, which this cell rightly calls worth more than the row. **WHAT THIS CELL SAID WHILE IT WAS OPEN, kept in full because the check it names outlives the defect:** found 2026-09-19 while checking which checkout Heron reads. **AND THE CHECK THAT FOUND IT IS WORTH MORE THAN THE ROW**: the totals line, because it is derived live, is a reliable fingerprint of WHICH TREE the running server is reading - 312/83 named this worktree against main's 316/79, which is how the proofs above were known to be testing the new code and not the old. **The cheap fix is to derive the status the same way the totals are derived**, or to stop stating it in a sentence that grants permission. Not attempted here: the lookup path is not this session's, and [row 116](../FRAGMENT-ISSUES.md#) is the standing warning that this store is written by more than one session |
| **128** | **A COLUMN FAMILY ASKED TO GO IN AS A BEAM WENT IN ANYWAY, AND THE REFUSAL THAT WAS SUPPOSED TO BE THE NEGATIVE CAME TOO EARLY TO BE ONE.** Measured 2026-09-19 on `Project1 work_ajmal.al` (3,529 elements), Revit 2024, session 20472, through `tools/batch-prove.py`. `place-structural-family` was given the model's ONLY structural column type, `305x305x97UC`, at `0,0,0` on `Level 1`. The positive asked for `structuralType=Column` and placed one. **The negative changed exactly one value - `structuralType=Beam` - and placed one as well: `placed 1 item(s) [926179]`.** A universal column section is not a beam, and a request naming the wrong structural type is the clearest thing this fragment could decline. **WHICH OF TWO CAUSES IT IS WAS NOT ESTABLISHED, AND SAYING SO IS THE POINT:** either the fragment never passes `structuralType` to Revit, or Revit accepts `Beam` for a column symbol and places it regardless. One is ours and one is not, and nothing in this run separates them. **THE FIRST NEGATIVE ATTEMPTED WAS A DIFFERENT SHAPE AND FAILED FOR A DIFFERENT REASON, WHICH IS WORTH KEEPING.** `points: ""` - the degenerate-input pattern `tools/jobs/creators-project1.yaml` settled for creators, where a wall runs from a point to itself and grids get no spacings - came back `bad_request_value: No points were given`, **before the fragment ran**, so the second leg never happened at all and the runner reported `NO NEGATIVE`. **The guard is good behaviour and the wrong tool for D-30's second leg:** a fragment that refuses an input has not demonstrated that it finds nothing, it has demonstrated that it never looked. A creator whose only failure mode is an up-front refusal has no observable empty case, and that is a real gap in how creators get proved rather than a fault in this one. | **OPEN.** Found 2026-09-19. The fragment is NOT signed and stays DRAFT. The next step is one cheap experiment that separates the two causes - place the same symbol by hand in Revit as a beam, or read whether the fragment's own code passes the value through - and it needs no batch. The second half, how to arrange a negative for a creator that guards its inputs, is a question for `.claude/skills/fragment-proving/SKILL.md` rather than for this fragment |
| **129** | **THE SAME CHAIN RAN UNDER `prove` AND WAS REFUSED UNDER `validate`, SO A HAND-RUN SUCCESS DOES NOT PREDICT A BATCH ONE.** Measured 2026-09-19 on `test projject` (3,594 elements), Revit 2024, session 36908. `find-nearest-elements` and `check-minimum-clearance` each declare TWO element sets - `elements` and `targets`. Driven by hand, both ran and did real work: `prove select-by-category-name set-selection find-nearest-elements` returned **`nearest 8`, `targetBoxes 8`, `tied 1`** on 8 ducts, and its own binding note reads **`elements from set-selection (2); targets from set-selection as 'elements' (2)`** - the second set was filled from the first. The IDENTICAL chain through `tools/batch-prove.py`, which calls `validate`, was refused before running: **`needs_unbound: Cannot run: 'elements (IList<Element>)', 'targets (IList<Element>, filled from 'elements')' were never supplied. There is a selection, but this fragment needs 2 separate sets of elements and one selection cannot say which is which.`** **BOTH BEHAVIOURS ARE DEFENSIBLE AND THE PAIR IS NOT.** The refusal's reasoning is sound and is quoted in its own message - *"Running anyway would report 0 results, which reads as 'there was nothing to find' rather than 'nobody was asked'"* - and that is exactly the failure D-30 exists to catch. Filling the second set from the first is also defensible for a reader whose two sets are meant to be the same. **WHAT IS NOT DEFENSIBLE IS THAT THEY DISAGREE**, because the proving path is the strict one and the exploring path is the one a person uses to decide what to put in a job file. A fragment can therefore be measured working, written into a job file on that evidence, and refused by the batch - which is what happened here, in that order. **WHICH SIDE IS WRONG WAS NOT ESTABLISHED** and is not guessed at here; the two messages above are the whole of what was observed. | **OPEN.** Found 2026-09-19 while proving fragments on the one free Revit session. Neither fragment is signed and both stay DRAFT. This is the `IList<Element>` second-set shape that [PR #190](https://github.com/Ajmalpshaik/Heron-AI/pull/190) proposed a rule for - *"never a list you point at - always the SECOND SET, all three of them, named by category the way the first set is found"* - so a resolver for it would settle the job-file half. It would NOT settle the disagreement, which is about two code paths and not about the input |
| **130** | **`heron_lookup` TOLD A MODELLER WITH TWO REVITS OPEN THAT NO REVIT WAS CONNECTED, AND SKIPPED THE VERSION FILTER WHILE SAYING SO.** Measured 2026-09-19 with sessions 20472 and 36908 both live. `heron_lookup("how many ducts are there")` answered correctly on the routing - `FILTER_ELEMENTS_BY_CATEGORY`, matched by **identity**, provided by a PROVEN fragment - and then printed: **"No Revit is connected, so the version filter did not run."** Two were. `revit_use_session 36908` in the very next call returned *"Now working with Revit 2024 (session 36908) - test projject, 3,594 elements"*, and the identical lookup then printed **"Filtered to Revit 2024 (chosen)."** **THE CAUSE IS A FALL-THROUGH, AND THE FUNCTION'S OWN DOCSTRING DESCRIBES ONLY ONE OF ITS TWO EXITS.** `_revit_version()` in `heron_mcp_server.py` returns the bound session's release when one is chosen, ASSUMES the release when exactly one Revit is live - and naming that assumption is careful, deliberate work - and then `return None, None`. That last line is reached when **two or more are connected and none has been chosen**, which is a completely different fact from nothing being connected. The docstring says *"Returns (None, None) when nothing is connected"*, which is true of one path and not of the other. **THE WORDING IS THE SMALLER HALF.** The version filter genuinely did not run, so on that call no fragment was checked against the release it would run on - and the configuration where that check matters MOST is precisely two Revits open, because they can be different releases. A 2025-only fragment could be offered to somebody working in 2020, with a line above it saying the filter was skipped for a reason that is not true. **IT IS THE SAME FAMILY AS [ROW 117](../FRAGMENT-ISSUES.md#) AND [PR #188](https://github.com/Ajmalpshaik/Heron-AI/pull/188)**: a message describing one state while the system is in another, and both of those also surfaced only with two Revits open. | **FIXED 2026-09-20, AND IT WAS THE TWO SENTENCES THIS CELL ASKED FOR.** `heron_session.version_for_filter(live, chosen_pid, chosen_explicitly)` decides the release and says HOW, and **`how` is never `None`** - an answer with no release still names why, because *nothing is connected* and *two are and you have not picked* send a reader to different actions (D-52). **Six answers where the old body had four, and one of those four covered three different situations** - `(None, None)` was returned alike for *nothing is connected*, for *several connected and none chosen*, and for [row 159](../FRAGMENT-ISSUES.md#)'s *the Revit you chose has closed*. The new one that matters here is **`N Revits are connected and none has been chosen`** - with the count in it, because a reader told *2 are connected* knows there is something to pick between where *none is connected* sends them to start Revit. Both call sites print that sentence instead of the one that was wrong, and the several-connected case adds **what to do**: *Pick one with `revit_use_session <pid>` and ask again - with more than one release open, the filter is the thing that matters most.* **THE RULE IS PURE AND LIVES IN `mcp/server/heron_session.py`; ONLY THE PLUMBING STAYED IN `_revit_version`** - gather, ask, close. That module imports no MCP SDK, so `tests/test_session_binding.py` exercises every outcome with no Revit and **CI runs it**, where a test beside the caller could never run at all: CI cannot import `heron_mcp_server`, which is why `test_mcp_serves.py` is on the known-failure list. **THE UNREADABLE CASE IS NAMED TOO** - `binding.sessions()` raising now answers *the connected Revits could not be read*, with the exception type, rather than the same silence: a reader told *nothing is connected* when the truth is *I could not look* goes and starts a Revit that is already running. **AND THE PLUMBING WAS VERIFIED END TO END ON 2026-09-20, WHICH THIS ROW HAD SAID WAS OUT OF REACH.** The change shipped with a caveat that only the PURE rule could be exercised here, because `heron_mcp_server` needs the MCP SDK. **That was the same false excuse the four "cannot run on Linux" suites were** - `pip install --user mcp` followed by the `cryptography` and `cffi` upgrade, both written in [`heron-ship`](../../.claude/skills/heron-ship/SKILL.md), and the module imports. `_revit_version()` on a machine with no Revit running answers **`(None, 'nothing is connected')`** - the real `binding.sessions()`, not a fake. **What is still NOT verified is the case the row is about**: *several connected, none chosen* needs two Revits, and no test host can supply one - `tests/Heron.Bridge.TestHost` pretends to be Revit only BELOW the API, answering ping and framing and no operations at all. That half stays reasoned from six outcomes the pure rule returns, and the distinction between the two is the point of saying which is which. **AND READING THE SAME EIGHT LINES FOUND A SECOND DEFECT THIS ROW DOES NOT NAME**, recorded apart as [row 159](../FRAGMENT-ISSUES.md#): with the CHOSEN session closed and one other live, the old body fell through and returned the survivor's release as *the only Revit connected* - the wall sliding onto a release nobody picked, against this file's own table. **WHAT THIS ROW SAID WHILE IT WAS OPEN, kept because the next step it named is the one that was taken:** Found 2026-09-19 while running skills end to end the way a modeller would. The repair is two sentences rather than one - `_revit_version` should distinguish *nothing connected* from *several connected, none chosen*, and the caller should say which. Python only, so no Revit restart; the Claude app must be restarted for `mcp/` to reload |
| **131** | **THE KNOWLEDGE STORE IS SHARED BY EVERY SESSION AND IS REBUILT FROM WHICHEVER WORKTREE INDEXED LAST, SO ONE SESSION'S FRAGMENT EDIT SILENTLY DISAPPEARS FROM ANOTHER'S.** Measured 2026-09-19. Two utterances were added to `count-elements/fragment.yaml` to stop *"how many sprinklers on level 2"* resolving to `PLACE_FAMILY_INSTANCES`. After rebuilding, both resolved by identity to `FRG-ELE-002`, confirmed in the `identities` table and live through `heron_lookup`. Minutes later, **with nothing run in this session but `grep`**, the phrase was `ABSENT` again. **THE TWO PATHS ARE NOT IN THE SAME PLACE.** The store is `%APPDATA%\Heron\knowledge\global.db` - **one file, outside every worktree, shared by all sessions**. The fragments are loaded by `heron_search.index()` from `FRAG.load_all()`, which reads **the worktree of whoever is running**. `index()` opens with `DELETE FROM identities` and rebuilds the lot. So any session that re-indexes rewrites the shared table from ITS OWN copy of the fragment files - and two other worktrees were live at the time, neither carrying the edit (`grep -c` returned 0 in both). **SO HERON'S ROUTING DEPENDS ON WHICH SESSION INDEXED LAST**, which is a race, and the losing side sees a fix it made and verified quietly revert. **THE DECISIVE OBSERVATION IS THE ROUTE FIELD, NOT THE COUNTS**, contributed by the session sweeping the same store: `brain.lookup` reports which door answered, and two runs that read `identities 2575` at the top - both phrases present - still answered *"how many sprinklers on level 2"* by route **`hybrid`**, never `identity`. A declared phrase resolves by `short_circuit` BEFORE any ranking, so ranking having run at all proves the identity row was gone at the moment that lookup executed, minutes after the count was read in the same process. **That also excludes the `recall` cache**, which was measured at **0 rows** in every sweep and cannot serve a stale answer holding nothing. **AND NEITHER SESSION WAS INDEXING**: runs 2 and 3 ran no indexer at all. The store does not need a checker to be rewritten - only a rebuild triggered from any tree, by anyone. **AND THAT SCOPE WAS STILL TOO NARROW - IT DOES NOT TAKE A REBUILD EITHER.** `heron_brain._Open.__enter__` (`mcp/server/heron_brain.py`) calls `SEARCH.index(self.store)` **UNCONDITIONALLY on every open** - the emptiness test above it guards `SCOPE.rebuild()`, not this - and `index()` opens with `DELETE FROM identities`. **So the ORDINARY READ PATH does it: asking Heron a question from a checkout replaces what Heron knows with that checkout's opinion.** Established by the session sweeping the skills and then read here directly: identities 2580 with all six declared phrases present, six lookups from a tree declaring none of them, then 2573 and all six ABSENT, every one `route=hybrid`. Five worktrees on this PC means Heron knows whatever the folder that asked last happened to declare. **THE DOCSTRING REASSURES ABOUT THE WRONG THING AND BOTH OF US TOOK THE SAME COMFORT FROM IT** - *"indexing is content-hashed on both routes, so re-indexing files that have not changed costs nothing"* is true, and it is about COST; the DELETE is neither hashed nor conditional, because hashing decides what to re-embed and never what to remove. **IT IS ALSO [ROW 116](../FRAGMENT-ISSUES.md#)'s MISSING CAUSE** - that row ruled out the ranker, the cache, a swallowed lock and a half-read rebuild, and closed on *"which write made the identity miss"*. There need be no write: every reader was rebuilding the table the whole time. **I FIRST FILED THIS AGAINST `tools/check-routing.py`** because the phrase vanished each time I ran it - four consistent observations, and consistency is exactly what a shared resource under another writer produces. That tool is innocent: it re-indexes like everything else, and the wipe happened again when it had not been run at all. **IT IS [ROW 126](../FRAGMENT-ISSUES.md#)'s LESSON ARRIVING FROM A DIFFERENT DIRECTION ON THE SAME DAY** - a defect row written in confident prose from four real measurements of the wrong thing - and the standing note that a worktree's numbers are not the project's is the same warning about a different shared file. | **OPEN, and the first version of this row was WRONG.** Found 2026-09-19 while verifying a routing fix. The fragment FILES are the source of truth and are correct; what is volatile is one shared cache. Nothing here is fixed. Worth settling: whether a scope should be per-worktree rather than global, or whether `index()` should refuse to rebuild from a tree that is not the one the store was built for - it knows both paths and compares neither. Until then, a fragment edit is not live until the LAST session to index has the edit, and `check-risk-crossings.py` printing the store hash before and after is the only thing in the repository that would have shown this **HALF BUILT 2026-09-19, AND THE HALF BUILT IS THE ONE THAT SETTLES NOTHING.** This cell said `index()` *"knows both paths and compares neither"*, and that was the literal state: `index_state` held one row, `('search', digest)`, and nothing anywhere recorded WHICH TREE the store was rebuilt from. So the losing session had no sentence to read - the edit was simply absent, with nothing to say who had overwritten it. `index()` now writes `('search_root', <abspath of FRAG.FRAGMENTS_DIR>)` beside the digest, `heron_search.indexed_from(store)` reads it back, and `check-skill-routing._index_fingerprint()` - the block `check-risk-crossings.py`, `prove-skill.py` and `check-skill-routing.py` all print before any count - carries `built_from`, `running_in` and `other_tree`, saying **loudly** when the store was built by a tree that is not the one running. **IT RECORDS AND DOES NOT REFUSE.** Both questions this cell leaves open - a per-worktree scope, or `index()` declining to rebuild from a foreign tree - are the owner's, and a library function that started refusing would settle them by accident. Naming is the half that cannot break a caller, which is [row 71](../FRAGMENT-ISSUES.md#)'s rule one layer down. **AND AN ABSENT ANSWER IS NOT AGREEMENT**: a store written before this says `NOT RECORDED`, never *the same tree* (D-52). Pinned in `tests/test_search.py` - no row before indexing, the tree named after it, and a second rebuild from the same tree still accepted, so the test would fail if this ever grew into a gate. **AND CI CAUGHT THE FIRST VERSION OF BOTH READERS**: `except sqlite3.OperationalError:` bare, in `indexed_from` and in `_index_fingerprint`, which `tools/check-narrow-errors.py` refuses in one line - *a plausible zero is worse than a crash, because nobody goes looking*. Narrowed to `no such table` with everything else re-raised, and **both directions are now pinned**: a store with no such table answers None, a store reporting *database disk image is malformed* raises rather than reading as *no tree*. That gate is not in `AGENTS.md`'s five and was not being run here; it is now **AND A TRAP IN THE PROVENANCE CHECK ITSELF, FOUND 2026-09-20 BY FALLING INTO IT.** `heron_scope.rebuild()` rewrites the `fragments` table and **does not write `search_root`** - only `heron_search.index()` does, at the line this cell describes. So a session that calls `rebuild()` and then reads `indexed_from(store)` to check whose library it is holding gets the PREVIOUS indexer's tree and concludes it has been clobbered when it has not. That happened here: the check read False immediately after a clean 395-fragment rebuild, then True once a lookup had run, because the lookup indexed. **The answers were sound the whole time and the proof is the ROUTE** - all five came back `identity` on a capability whose identity rows exist only in this tree, and an identity hit short-circuits before ranking, so the rows had to be present. That is [row 133](../FRAGMENT-ISSUES.md#)'s argument run backwards. **The reliable order is `rebuild()` then `SEARCH.index()` then read `indexed_from` either side of the measurement**, which is what the record for those five did |
| **132** | **A SKILL DECLARES THE WORDS AJMAL SAYS AND THE CAPABILITIES IT NEEDS, AND NOTHING HAD EVER CHECKED THAT THE FIRST REACH THE SECOND. SIXTEEN OF FORTY-TWO DO.** Measured 2026-09-19, Revit 2020, by `tools/check-skill-routing.py` built for this. **Ten skills, 42 utterances: 16 reached a capability the skill declares, 20 reached one it never asked for, 5 were answered by something that CHANGES THE MODEL ([row 133](../FRAGMENT-ISSUES.md#)), and 1 escalated above the skill's own risk. None unresolved.** Per skill, reaching declared needs: `mep-grayout` 4/5, `sprinkler-layout` 3/5, `count-elements` 2/4, `terminal-layout` 2/4, `trace-system` 2/4, `check-connectivity` 1/4, `select-elements` 1/4, `size-breakdown` 1/4, **`find-blank-parameters` 0/4 and `space-airflow` 0/4**. **WHY NO EXISTING GATE COULD SEE THIS.** `check-routing.py` asks each FRAGMENT its own declared utterances, and its own output says that section is *"empty BY CONSTRUCTION"* because a declared sentence resolves by IDENTITY before ranking runs - *"THE REAL RISK SURFACE IS PARAPHRASE, which no fragment declares and this corpus therefore does not contain"*. A skill's utterances ARE that paraphrase corpus, and **37 of the 42 are declared by no fragment at all**, so identity cannot fire and ranking decides - which is exactly the hole [rows 113](../FRAGMENT-ISSUES.md#) and [116](../FRAGMENT-ISSUES.md#) name from the fragment end. **SKILLS ARE NOT INDEXED, AND THAT IS DELIBERATE, NOT A DEFECT** - `brain/heron_index.py` carries `NO_INDEX = {"skill": "neither heron_search nor heron_embed knows what a skill is... loaded by name, not searched for"}`, and `heron_brain.py` imports the skill module four times and binds it to `_SK` every time, unused. **SO THERE ARE TWO DOORS FOR ONE SENTENCE.** A host reaches a skill through `heron_brain.catalogue()`, which hands it every skill's `utterances` and `needs` and marks `ready` - and `ready` is `not missing`, computed from whether a capability has ANY provider. **All 15 distinct needs are provided (measured, 15 of 15), so all ten skills report ready**, while only 16 of their own 42 sentences reach a capability they declare. The same words put to the other door, `heron_lookup`, return a fragment - and five times that fragment writes. **`ready` is a statement about the registry and reads as a statement about the skill.** | **OPEN - a FINDING, and explicitly NOT a proof of anything. Found 2026-09-19.** No skill is marked PROVEN and none may be on this evidence: understanding is half a proof and the other half is a model (D-30), which this session never opened. **What the tool deliberately does not decide:** a miss is not automatically a defect - a skill composes several capabilities and `lookup` returns ONE, so the single best answer sitting outside the plan can mean the plan is short a capability, or that the sentence names a COMPOSITION no fragment can win, which is the third reading `check-routing.py`'s own closing text offers. `find-blank-parameters` is the clearest of those: its *"find the blank parameters"* resolves to `DESCRIBE_BLANK_PARAMETERS` (ANALYZE), which [row 116](../FRAGMENT-ISSUES.md#) already named as *"the right owner it had previously hidden"* - the routing is right and the skill's `needs` do not list it. **`space-airflow` fails in the opposite direction and is safe:** declared MODIFY, all four of its sentences reach READs, including *"set the supply air flow"* to `REPORT_SPACE_AIRFLOW` - a skill that will report where it promised to write. **The tool prints the index fingerprint [row 116](../FRAGMENT-ISSUES.md#) asked for** and its store-independent half FIRST, because that half cannot wobble: which sentences a fragment also declares, which none does, and which two skills claim. Exit 0 whatever it finds, the rule both siblings set **THE OWNER FOUND A CROSSING THIS TOOL SCORES AS CORRECT, AND IT IS THE TOOL'S LIMIT RATHER THAN AN OVERSIGHT.** 2026-09-19, on being shown the five: *"if i say how meny sriklers is there that time if you place the sproklers that menas that is dangeeres"*. **`how many sprinklers` resolves to `PLACE_FAMILY_INSTANCES` in all three runs and is scored a correct REACH**, because the skill claiming it is `sprinkler-layout`, declared MODIFY, which declares that capability. The discriminator compares the resolved risk against **the skill's declared risk**, so a question sitting inside a write skill cannot be seen by it at all - **a declaration cannot make a question safe**. Five such sentences exist: this one, *"pendent or upright here"*, *"how many diffusers for this room"*, *"how many at 3 m coverage and where"* and *"how much air does this room need"*. **THE RULE WAS NOT WIDENED TO CATCH THEM**, because the only test that separates *"how many sprinklers"* from *"lay out the heads"* is what the words MEAN, and `check-risk-crossings.py`'s docstring rules that out in terms this tool inherits - *a tool that tried to parse intent would be a second, worse retriever*. **The repair is to the LIBRARY, not the checker**: `sprinkler-layout` no longer claims the bare count and claims *"how many sprinklers do I need"* and *"how many sprinklers for this room"* instead, and the count belongs to `count-elements`. That is not [row 113](../FRAGMENT-ISSUES.md#)'s forbidden weakening - nothing is chasing a rank, the sentence was miscategorised, and the library ends with MORE declared words rather than fewer |
| **133** | **ASKING WHETHER THE DUCTWORK IS STILL CONNECTED RESOLVES TO CONNECTING IT, AND ASKING WHETHER ANYTHING CAME APART RESOLVES TO TAKING IT APART.** Five of the ten skills' own 42 sentences reach a capability that CHANGES THE MODEL while the skill claiming them does not ([row 132](../FRAGMENT-ISSUES.md#) has the method and the totals). Measured 2026-09-19, Revit 2020, through `heron_brain.lookup` - the seam a host uses - against index `fragments 395, identities 2573, vectors 397`. Verbatim, with what each resolved to and how it won: **(1)** *"is the ductwork still connected"* - `check-connectivity`, ANALYZE - resolved to **`CONNECT_AIR_TERMINALS`** (MODIFY), words #1 and nearness #3, both routes agreeing, over a runner-up at words #3 and nearness #4. **(2)** *"did anything come apart"* - `check-connectivity`, ANALYZE - resolved to **`UNGROUP_ELEMENTS`** (MODIFY), words #2 and nearness #6, over words #12 and nearness #14. **(3)** *"how many sprinklers on level 2"* - `count-elements`, READ - resolved to **`PLACE_FAMILY_INSTANCES`** (MODIFY), words #6 and nearness #1, over words #7 and nearness #2. **(4)** *"what sizes are the ducts"* - `size-breakdown`, READ - resolved to **`AUTO_SIZE_MEP`** (MODIFY), **words #1 AND nearness #1**, over words #6 and nearness #8. **(5)** *"duct schedule by size"* - `size-breakdown`, READ - resolved to **`SET_MEP_SIZE`** (MODIFY), words #3 and nearness #3, over words #1 and nearness #7. **NUMBER 4 IS THE ONE THAT DECIDES THE FIX, AND IT CLOSES A QUESTION [ROW 113](../FRAGMENT-ISSUES.md#) LEFT OPEN.** That row found the keyword route wrong on one sentence and the meaning route wrong on another, and concluded *"a rule preferring either route fixes one and causes the other"*. Here **both routes independently rank the write FIRST** - `AUTO_SIZE_MEP` is words #1 and nearness #1 - so no fusion rule, no route preference, no re-ranker and no quality nudge can reach it by construction. **[Row 116](../FRAGMENT-ISSUES.md#)'s "THE FIX IS DECLARING, NOT DEMOTING" is the only repair that touches this case**, and this is the strongest evidence for it yet. **NUMBER 3 IS [ROW 113](../FRAGMENT-ISSUES.md#)'s OWN SHAPE, REPEATED WITH A DIFFERENT NOUN** - that row recorded *"how many pipes are there"* reaching `CAP_OPEN_PIPE_ENDS`; a count question now places sprinklers. **And the same capability is the CORRECT answer for a different skill**: `sprinkler-layout` is declared MODIFY and its *"how many sprinklers"* reaches `PLACE_FAMILY_INSTANCES` legitimately. **One capability, right for one skill and dangerous for another, and the library has exactly one answer per sentence** - so the two count-shaped sentences cannot both be served until somebody decides which owns them. **THE MARGIN CANNOT BE GIVEN AS A NUMBER HERE, AND THAT IS ITS OWN SMALL DEFECT.** `heron_retrieve.find` puts `score`, `words_score`, `nearness_score` and `rerank_score` on every candidate; `heron_brain.lookup` copies five keys across - capability, provider, status, risk, why - and drops all four. So at the seam a host actually calls, a landslide and a photo-finish are indistinguishable, and **[row 109](../FRAGMENT-ISSUES.md#)'s defining measurement, *"2.4 ranks clear of the runner-up"*, is not reproducible through `lookup` at all** - it was taken by hand. The ranks above are what `why()` reports, which is what row 109 quotes when it explains itself | **OPEN - five FINDINGS, no fix attempted, and none of these was re-ranked to make it go away. Found 2026-09-19.** **Nothing here was changed**: [row 113](../FRAGMENT-ISSUES.md#)'s rule holds that weakening an utterance to buy back a rank is never the answer, and a skill's utterances are the words Ajmal actually says. **Numbers 1 and 2 are the dangerous pair and they share a cause**: `check-connectivity` asks whether something is broken, and the library's nearest answers are the tools that JOIN and SPLIT. A modeller asking *"did anything come apart"* whose model is then ungrouped has been given the exact opposite of a diagnosis. **Which of the five is repaired by declaring is not obvious per sentence and was not guessed at:** `CHECK_FIXTURE_CONNECTIVITY`, `TRACE_CONNECTIVITY`, `COUNT_ELEMENTS`, `GROUP_AND_COUNT` and `REPORT_DUCT_WEIGHT` are each plausible owners and the choice is the owner's, exactly as [row 116](../FRAGMENT-ISSUES.md#) left its remaining seven. **A count from this sweep is a SAMPLE** - [row 116](../FRAGMENT-ISSUES.md#) measured 7, 9 and 8 from one tool minutes apart - so these five are recorded BY NAME and the total is not the finding. **The sibling sweep was run the same day and returned the stable eight** [row 116](../FRAGMENT-ISSUES.md#) names, two days after it named them, with `how many pipes are there` NOT among them - so that row's declared fix was holding on this run **A FIX FOR NUMBER 3 IS IN FLIGHT FROM ANOTHER SESSION AND THIS ROW MEASURED IT NOT SURVIVING.** S1 added *"how many sprinklers on level 2"* and *"give me the number of fire dampers"* to `count-elements`'s `utterances` to claim the sentence for `COUNT_ELEMENTS` - which is [row 116](../FRAGMENT-ISSUES.md#)'s *declaring, not demoting*, and is the right repair. **The index fingerprints across this session's three runs of `check-skill-routing.py` read `identities` 2573, 2575, 2575** - the +2 is S1's two phrases landing - and **both runs that STARTED at 2575 still answered *"how many sprinklers on level 2"* with `PLACE_FAMILY_INSTANCES`**. Checked afterwards, the table is back to **2573 with both phrases ABSENT**. **THE ROUTE IS WHAT SETTLES IT, NOT THE COUNT**: those answers came back `hybrid`, never `identity`. A declared phrase resolves by `short_circuit` BEFORE ranking, so ranking having run at all proves the identity row was gone by the time that lookup executed - minutes after the fingerprint was read at the top of the same process. **AND IT IS NOT A STALE CACHE**, the other candidate: the same fingerprint prints `utterances 0` in every run, and `utterances` IS the `recall()` cache [row 116](../FRAGMENT-ISSUES.md#) already measured as empty, so it cannot have served an older answer. `check-skill-routing.py` calls no indexer of its own - no `SEARCH.index`, no `EMBED.index`, no `rebuild` - so the write that removed them came from outside this process while it was running. **A SECOND EXPLANATION THAT MUST BE RULED OUT BEFORE THE TOOL IS BLAMED:** `%APPDATA%\Heron\knowledge\global.db` is ONE file for every worktree, this session's tree does NOT carry S1's edit, and a reindex triggered from a tree without it rebuilds `identities` from THAT tree's fragments - so the phrases would go, with no tool having a defect. That is [row 116](../FRAGMENT-ISSUES.md#)'s one-store-many-trees hazard, and it is testable **RE-MEASURED 2026-09-19 ON A LINUX CONTAINER, TWICE, AND ALL FIVE ARE GONE - BUT TWO NEW ONES STAND IN THEIR PLACE.** Two independent sweeps of `tools/check-skill-routing.py` and `tools/prove-skill.py`, against DIFFERENT index fingerprints (`identities` 2573 then 2580, the first a store this container built from nothing), agree on every one. Numbers 1, 2, 4 and 5 now resolve by **identity** - `is the ductwork still connected` to `TRACE_CONNECTIVITY`, `did anything come apart` to `SELECT_BY_CONNECTION_STATUS`, `what sizes are the ducts` and `duct schedule by size` to `GROUP_AND_COUNT` - and number 3, `how many sprinklers on level 2`, to `COUNT_ELEMENTS`, also by identity. **[Row 116](../FRAGMENT-ISSUES.md#)'s *declaring, not demoting* held on all five**, and S1's two phrases that this row measured NOT SURVIVING are surviving now. **THE TWO NEW ONES ARE IN THE SAME SKILL AND ARE THE SAME HAZARD**, both `check-connectivity`, ANALYZE, both `hybrid`: *"check the connections"* resolves to **`MIRROR_ELEMENTS`** (MODIFY), words #2 + nearness #15 over a runner-up at words #10 + nearness #2; *"check the system is complete"* resolves to **`AUTO_SIZE_MEP`** (MODIFY), words #4 + nearness #5 over words #8 + nearness #1. Both were MISSES on the run this row was written from and are crossings now, which is the movement [row 116](../FRAGMENT-ISSUES.md#) predicts and no count can show. **WHAT THIS DOES NOT ESTABLISH:** these two sweeps ran against a store built in a container from one worktree, where row 133's ran against the shared `%APPDATA%` file on a machine with several - so this is a cleaner reading, not a reading of the same thing. Neither was re-ranked and nothing was edited to produce it **RE-MEASURED 2026-09-20 ON THE OWNER'S PC, AND ALL FOUR NAMED SENTENCES NOW ANSWER WITH A READ.** Through `mcp/server/heron_brain.lookup` - the same seam - against the shared `%APPDATA%` store, `built_from` this worktree, `fragments 395` / `utterances 2,209`: *"check the connections"* -> **`TRACE_CONNECTIVITY`** (READ), *"check the system is complete"* -> **`READ_MEP_SYSTEM`** (READ), *"is the ductwork still connected"* -> **`TRACE_CONNECTIVITY`** (READ), *"did anything come apart"* -> **`SELECT_BY_CONNECTION_STATUS`** (READ). `MIRROR_ELEMENTS` and `AUTO_SIZE_MEP` do not come back for either of the two this cell added. **THIS IS A THIRD MACHINE AGREEING, NOT A FIX** - nothing was edited to produce it and no session claims the repair. **AND THE ROUTE IS THE PART THAT MATTERS, WHICH IS WHY IT IS RECORDED PER SENTENCE:** the two original ones come back `identity` and are therefore settled - a declared phrasing short-circuits before any ranking runs, so no re-rank can move them. **The two this cell added come back `hybrid`, which is a ranked guess and is exactly what drifted onto a writer twice already.** They are safe today and nothing guarantees they are safe tomorrow. **THE ONE-LINE REPAIR THAT WOULD SETTLE THEM, so the next reader does not have to derive it:** declare *"check the connections"* on `TRACE_CONNECTIVITY` and *"check the system is complete"* on `READ_MEP_SYSTEM` - pinning the answer the ranking ALREADY gives, so behaviour today is unchanged and the drift route is closed. That is [row 116](../FRAGMENT-ISSUES.md#)'s *declaring, not demoting* and it is not [row 113](../FRAGMENT-ISSUES.md#)'s forbidden move, because no rank is being chased and nothing is weakened. **NOT DONE, because this cell reserves the choice of owner to him** and pinning a sentence is that choice; it is now a yes or no rather than a piece of research **HE SAID YES, AND BOTH ARE PINNED 2026-09-20.** The authorisation was narrow and is recorded as given: *pin a sentence onto the READ the ranking already picks, where it closes a route that could drift onto a write*. `check the connections` is declared on `trace-connectivity` and `check the system is complete` on `read-mep-system`, each with the measurement in a comment beside it, in the style the 2026-09-19 addition on the same file already set. **VERIFIED AFTER A REBUILD FROM THIS TREE: all four sentences in this row now resolve by `identity`** - `check the connections` -> `TRACE_CONNECTIVITY`, `check the system is complete` -> `READ_MEP_SYSTEM`, and the two that already did are unmoved. **THE ANSWER DID NOT CHANGE AND THAT IS THE POINT** - the same fragment answers as before; what changed is the ROUTE, and a declared phrasing short-circuits before ranking, so no re-rank, no re-index and no new fragment can move it. `brain/heron_fragment.py` reads all 395 well-formed after the edit, and neither fragment's proof is touched because the fingerprint walks `impl/` only |
| **134** | **[ROW 116](../FRAGMENT-ISSUES.md#) CLOSED WITH "FIVE AMBIGUOUS PHRASES ALSO REMAIN, ALL READ-VS-READ SO NONE IS DANGEROUS". IT IS SIX, AND TWO OF THEM ARE WRITE-VS-WRITE.** Measured 2026-09-19 two ways that agree: from the store, `SELECT phrase FROM identities WHERE fragment_id='__ambiguous__'` returns **6** of 2,573; read independently from `brain/fragments/*/fragment.yaml` on disk, the same **6** of 2,196 distinct phrases. They are *"show me the drawing list"* and *"what sheets do we have"* (`FIND_SHEETS` against `LIST_SHEETS`, both READ), *"what parameters does this duct have"* (`READ_ELEMENT_PARAMETERS` against `REPORT_PARAMETER_INVENTORY`, both READ), *"check the tags before i issue this"* (`FIND_OVERLAPPING_TAGS` against `REPORT_TAGS_AND_TARGETS`, both READ) - **and the two that row 116's sentence does not cover**: *"duplicate with detailing"*, claimed by `DUPLICATE_VIEW` (FRG-VIEW-012) and `DUPLICATE_VIEWS` (FRG-VIEW-029), **both MODIFY**; and *"change the insulation thickness"*, claimed by `SET_COMPOUND_LAYER_WIDTH` (FRG-ELE-070) and `SET_MEP_INSULATION` (FRG-MEP-007), **both MODIFY**. **THE STALE PART IS THE REASONING, NOT JUST THE COUNT.** Row 116's clause is *all read-vs-read SO none is dangerous* - the safety follows from the risk levels, and for these two the premise is simply false. **The hazard is a different shape and is not the one this thread has been chasing**: not a question answered by a write, but a write aimed at the wrong thing. `identities` stores `__ambiguous__` as a deliberate MISS, so *"change the insulation thickness"* falls through to ranking, and the two candidates change different objects - **a wall's compound layer width, or a pipe's insulation**. A modeller who says it about a pipe and gets the wall fragment has had something else in the model edited | **FIXED 2026-09-20 - BOTH WRITE-VS-WRITE PHRASES ARE GONE AND THE FOUR THAT REMAIN ARE READ AGAINST READ.** The account of the fix is at the end of this cell; `tools/open-defects.py` reads the first word of a state, so it leads. **THE ORIGINAL ACCOUNT FOLLOWS, because the reasoning is what settled which fragment owns the sentence, and a row that keeps only its verdict teaches nobody why.** **OPEN, and recorded rather than fixed - the fragments are not this session's to touch. Found 2026-09-19 while checking row 116's numbers rather than trusting them.** **THIS IS NOT A CLAIM THAT ROW 116 WAS WRONG WHEN WRITTEN.** It measured on 2026-09-17 and the library has moved since - [row 120](../FRAGMENT-ISSUES.md#) and [row 123](../FRAGMENT-ISSUES.md#) both record fragment edits in between - so the honest reading is prose outliving what it counted, which is [row 113](../FRAGMENT-ISSUES.md#)'s own closing failure repeating one row later in the same thread. **What it costs:** row 116 is the LIVE row for this whole thread and a reader takes *"none is dangerous"* as the reason to leave all of them alone. Four of the six still deserve that; two now need a judgement about which fragment owns the sentence, and both are writes, so the judgement cannot be deferred by calling it harmless. **The count is honest as a count and useless as a reassurance** - the discriminator that matters is the pair of risk levels, which is why they are named here per phrase rather than totalled **THE TWO WRITES HAVE NOW BEEN JUDGED, AND THEY ARE NOT THE SAME SIZE.** Read from disk 2026-09-19 - all six are still live, and the library has grown to **2,203** distinct phrases with the ambiguous count unchanged at 6, so nothing has been repaired here since. **"change the insulation thickness" BELONGS TO `SET_MEP_INSULATION`, and the fragments' own other words settle it rather than taste.** `set-compound-layer-width`'s four utterances are *"make this layer thicker"*, *"set the wall layer width"*, *"modify the thickness of this build up"* - and this one, **the only one of the four that does not name its own subject**. `set-mep-insulation`'s are *"insulate these"*, *"insulate the pipework"*, *"put 50 mm insulation on these"*; the sentence sits among them without straining. **AND THE HAZARD IS ASYMMETRIC, WHICH IS WHAT MAKES IT A FIX RATHER THAN A PREFERENCE:** `set-compound-layer-width` edits a wall, floor, roof or ceiling **TYPE**, so every element of that type in the project changes; `set-mep-insulation` edits the elements handed in. A modeller who means a pipe and gets the type editor has changed the whole project's walls - the reverse touches only what is selected. **The repair is one line:** `change the insulation thickness` leaves `set-compound-layer-width`'s `utterances:` and is replaced by one that names its subject, *"change the insulation layer in this wall type"*. That is [row 116](../FRAGMENT-ISSUES.md#)'s *declaring, not demoting* - the library ends with the same number of words and each has one owner - and it is not [row 113](../FRAGMENT-ISSUES.md#)'s forbidden move, because nothing is chasing a rank and the sentence was miscategorised, the same reasoning [row 132](../FRAGMENT-ISSUES.md#) applied to *"how many sprinklers"*. **"duplicate with detailing" IS A DIFFERENT AND MUCH SMALLER PROBLEM.** Both claimants are right: the phrase carries no NUMBER, and the discriminator between `DUPLICATE_VIEW` and `DUPLICATE_VIEWS` is how many views are handed in. `duplicate-views` also declares *"copy the view with detailing"*, **singular**, which is a second and softer collision in the plural fragment. The repair is to put the number in the words - *"duplicate this view with detailing"* against *"duplicate these views with detailing"*. **And nothing is destroyed either way**: both outcomes ADD views and one Ctrl+Z removes them, where the insulation pair can rewrite a type. Row 134's sentence *"both are writes, so the judgement cannot be deferred by calling it harmless"* is right and incomplete - **the two are not equally serious, and which to do first is the part an owner needs**. **NEITHER WAS EDITED AND THAT IS AN OWNERSHIP BOUNDARY, NOT A JUDGEMENT:** all four files are under `brain/fragments/`, which this session was told explicitly not to touch. The exact one-line edits are written out above so that whoever owns those files does not have to re-derive them **FIXED 2026-09-20, AND THE MEASUREMENT WAS TAKEN BOTH SIDES OF THE EDIT.** The ownership boundary above was that session's instruction, not a rule about these files; this one had no such instruction and applied the four one-line repairs as written. `set-compound-layer-width` gives up *"change the insulation thickness"* for *"change the insulation layer in this wall type"*, leaving `SET_MEP_INSULATION` the only owner of the sentence a modeller says about a pipe; `duplicate-view` takes *"duplicate this view with detailing"* and `duplicate-views` *"duplicate these views with detailing"*, which is this cell's *put the number in the words*; and `duplicate-views`'s singular *"copy the view with detailing"* became *"copy these views with detailing"* - the softer collision named above, repaired by the same rule rather than left to re-create the one beside it. **READ FROM DISK BEFORE AND AFTER: 6 phrases claimed twice, then 4.** The four that remain are `FIND_SHEETS` / `LIST_SHEETS` twice, `READ_ELEMENT_PARAMETERS` / `REPORT_PARAMETER_INVENTORY` and `FIND_OVERLAPPING_TAGS` / `REPORT_TAGS_AND_TARGETS` - **all READ against READ**, so [row 116](../FRAGMENT-ISSUES.md#)'s *none is dangerous* is true now by repair rather than by luck. The distinct-phrase total reads 2,151 by this reader against 2,203 above, because it lowercases and stops at the first non-list line; **the figure under test was the 6 and it reproduced exactly**, naming the same six pairs. **NO PROOF WAS STALED, AND THAT WAS CHECKED BEFORE ANY FILE WAS OPENED:** `Fragment.proof_files()` walks `impl/` only, so the fingerprint a proof is measured against cannot see `utterances:` - all three fragments stay PROVEN with their signatures untouched, and no Revit time is owed for this. `test_authoring`, `test_declared_questions`, `test_fragment_store`, `test_revit_gate` and `test_generate` pass, and `python brain/heron_fragment.py` reads all 395 well-formed. **IT IS LIVE ON THIS MACHINE ALREADY, AND [ROW 131](../FRAGMENT-ISSUES.md#) IS WHY THAT IS NOT THE SAME AS DONE.** The shared store at `%APPDATA%\Heron\knowledge` reports `built_from` = this worktree, so a gate run after the edit reindexed it, and the store was read back directly to check rather than assumed: `SELECT fragment_id FROM identities WHERE phrase='change the insulation thickness'` now returns **`FRG-MEP-007` alone** where it returned `__ambiguous__`, and *"duplicate with detailing"* is **ABSENT** with *"duplicate this view with detailing"* on `FRG-VIEW-012`. **IT REVERTS THE MOMENT ANY TREE WITHOUT THIS EDIT INDEXES NEXT** - one store serves every checkout on the machine, and the ID SET is unchanged by an utterance edit, so the drift check that guards a rebuild cannot see this coming. It is durable when it reaches `main` |
| **135** | **A FRAGMENT WHOSE PURPOSE SAYS "TWO SETS" CAN NEVER BE HANDED TWO DIFFERENT ONES, AND THE GUARD THAT REFUSES IT CALLS ITS ONLY REACHABLE SHAPE MEANINGLESS.** Measured 2026-09-19 on `test projject`, session 36908. `check-minimum-clearance` opens *"Measures the real gap between **two sets** of elements and reports every pair closer than the clearance required"*. Its contract declares `targets` with **`binds: elements`** and no `source:`, so the second set is filled from the first and there is **no route by which a caller can supply a different one** - `FromRequest` resolves `source: request`, and this is not that. `find-nearest-elements` is identical in shape. **SO THE HALF THAT WORKS IS THE SELF-COMPARISON, AND IT IS NOT A DEGRADED CASE - IT IS THE JOB A MODELLER ACTUALLY RUNS.** *"Are any of these ducts too close to each other?"* Measured: 8 ducts in one view, **56 pairs checked, 10 too close**, and with the threshold dropped to 0.001 mm it still returned **4** - because four of those ducts are joined end to end and their gap really is zero. A correct, useful answer, from the shape the guard rejects. **`generate-jobs.py` RULE (d) CALLS IT `two needs are both filled by 'elements'` AND REFUSES THE JOB; `validate` REFUSES IT AGAIN AT RUN TIME** - *"this fragment needs 2 separate sets of elements and one selection cannot say which is which"* - while the exploring path honours `binds:` and runs it. `RevitFragment.Binds()` exists precisely so a need can be filled from a provide of another name, added 2026-09-15. **SO THE CONTRACT ASKS FOR THIS AND TWO TOOLS OUT OF THREE REFUSE IT.** | **OPEN.** Found 2026-09-19. **The repair is NOT to flip `targets` to `source: request`**, which was measured as sufficient to unblock both and was declined: it would delete the self-comparison default, force every caller to name a second category for the commonest question, and change what the fragment MEANS rather than how it is plumbed. **My first proposed repair - *a guard should honour an explicit `binds:`* - was WRONG and is kept here because the reason is the useful part.** Rule (d) fires on exactly FOUR fragments in the library and **all four declare `binds:` explicitly**, so exempting on that key deletes the rule rather than narrowing it. Worse, the four split two and two: this pair measures a set against itself and that IS the job, while `switch-join-order` and `unjoin-geometry` declare `first` and `second` as the two SIDES OF A PAIR - filling both from one set pairs every element with itself, and `unjoin-geometry`'s own purpose warns that *"a join is a pair... handing in a set and unjoining everything from everything is a different and much more destructive operation"*. **Rule (d) is RIGHT about those two.** So `binds: elements` is carrying two meanings at once - *the same set, deliberately* and *one side of a pair that happens to come from the same chain* - and nothing in a contract tells them apart. The discriminator has to be a new declaration the second need carries, not the bind. Separately and more quietly: the purpose sentence promises two sets, so either a `source: request` route for `targets` should exist ALONGSIDE the bind, or the purpose should stop promising it. [Row 129](../FRAGMENT-ISSUES.md#) is the same two fragments from the other end, where the two code paths disagreed |
| **136** | ASKING HERON A QUESTION REBUILDS THE SHARED INDEX FROM THE ASKER'S OWN FOLDER AND DELETES WHAT… — [full row](../fragment-issues-archive/proving-defects-126-150.md#row-136) | FIXED 2026-09-19 IN `heron_search.index()`, AND THE FIX IS DELIBERATELY NARROWER THAN THE… |
| **137** | **A QUESTION INSIDE A MODIFY SKILL REACHES A MODIFY THAT HAS NOTHING TO DO WITH IT, AND THE CHECKER CANNOT SEE IT BY CONSTRUCTION.** Found 2026-09-19 by READING `check-skill-routing.py`'s output rather than by the tool flagging anything. `sprinkler-layout` declares *"pendent or upright here"* - a question about which head to use - and it resolves to **`CREATE_ANGULAR_DIMENSION`, a MODIFY**, by route `hybrid`. **THE RANKING FOUND NOTHING AND SAID SO**: it won on words **#17** and nearness **#17**, both last, which is the ranker reporting that no fragment in the library answers this sentence. **IT IS INVISIBLE TO THE TOOL FOR THE REASON [ROW 132](../FRAGMENT-ISSUES.md#) ALREADY WROTE DOWN** - a crossing is decided by comparing the sentence's reach against the SKILL's declared risk, and this skill is declared MODIFY, so a question sitting inside it can never register as a question answered by a write. **THIS IS THE SAME BLIND SPOT THE OWNER FOUND BY HAND** on the same day, when *"how many sprinklers"* sat in this same file and the checker scored it safe. That one was repaired by giving the sentence to `count-elements`; **this one has no owner to give it to**, because Heron has no capability that chooses a sprinkler orientation. **AND A SECOND THING WAS MEASURED WHILE CHECKING HOW BAD THIS IS, WHICH IS THE HALF WORTH KEEPING.** How dangerous a crossing actually is had never been established anywhere in this file, and it was traced end to end rather than assumed: **`autorun` IS ADVISORY AND NOTHING IN HERON ENFORCES IT.** `autorun=True` is set on the **identity** route only, for `PROVEN` or `PRODUCTION` (`heron_search.py` `RUNNABLE_UNASKED`); `hybrid`, `keywords`, `cache` and `nothing` all leave it False. It is then **reported in the lookup reply and read by nobody** - one hit across `mcp/`, `brain/` and `tools/`, the line that writes it. So every crossing in [rows 113](../FRAGMENT-ISSUES.md#), [116](../FRAGMENT-ISSUES.md#), [132](../FRAGMENT-ISSUES.md#) and [133](../FRAGMENT-ISSUES.md#) - all of them route `hybrid` - was Heron OFFERING a wrong write, never running one unasked. **THE REAL GATE IS THE RIBBON PADLOCK, AND IT IS WHOLE.** Verified by reading, not by the docstring that claims it: `run_fragment_write` is declared `HeronRisk.Modify` at `platform/Heron.Core/HeronOperationRegistry.cs:193`; `RevitOperations` calls `HeronPermissions.Explain(risk)` and returns `write_disabled` **before routing**, so `RevitFragment.cs` contains no permission check at all and correctly should not (Golden Rule 19); `Allows()` reads `write.enabled` **fresh from the config on every call**; and the default is OFF. **So a mis-routed MODIFY cannot change the model while the padlock is closed, and closed is where it starts** - which is the sentence this register has never been able to say | **OPEN as a FINDING, and no repair was attempted on the utterance.** Two halves, and they close differently. **THE SENTENCE**: the honest repair is a capability that answers *"pendent or upright"* - it depends on the ceiling, the obstruction and the hazard class - and **building one is a new feature in a project that is in completion mode**, so it is recorded rather than started. The alternatives were weighed and both rejected in writing: declaring the phrase on the nearest READ would be [row 113](../FRAGMENT-ISSUES.md#)'s forbidden move, buying a rank with words that do not mean it; withdrawing the utterance would make the tool report a clean sweep while Ajmal still says the sentence, which is the measurement getting tidier and the product not. **THE EXPOSURE IS BOUNDED AND THAT IS WHY IT CAN WAIT**: with the padlock closed - the default - the worst outcome is Heron proposing a dimension to somebody who asked about sprinklers. **THE SECOND HALF NEEDS NO REPAIR AND IS THE REASON TO READ THIS ROW.** The gate chain above is CORRECT as built and was verified end to end. What it corrects is this file's own tone: a crossing is a WRONG SUGGESTION, and it becomes a wrong CHANGE only if the owner has opened the padlock. **ONE STALE SENTENCE FOUND WHILE VERIFYING IT** - `HeronPermissions.Explain` still tells the user *"the write path has never been run against a real model"*, which [row 123](../FRAGMENT-ISSUES.md#) and every MODIFY proof since have made untrue. It is in the text a user READS when a write is refused, so it is worth one line of somebody's time. **THAT ONE LINE IS DONE, 2026-09-19 - the SENTENCE, not the default.** `Explain` now gives the reason that will not expire: *"This is the default and it stays the default: changing somebody's model is their decision to make, not Heron's."* **The switch is untouched** - `write.enabled` is still `false` and still read fresh on every call, which `AGENTS.md` requires and this row is not a licence to widen. How many MODIFY fragments are PROVEN is derived from the library and never typed into the comment beside it, and a single one is enough to make the old sentence false, because a proof under D-30 IS a recorded run against a named real model. **AND THE NEIGHBOURING BANNER WAS CHECKED AND DELIBERATELY LEFT ALONE**, which is the part worth writing down: `revit/Heron.Revit.Addin/RevitWrite.cs` opens `==== NEVER RUN ====`, and it is **still true**. `RevitOperations` routes `run_fragment_write` to `RevitFragment.Run(app, request, true)` and reaches `RevitWrite.Run` only from its `default:` case, so **the two write paths are separate** and every MODIFY proof in this register is evidence about `RevitFragment` and none about `RevitWrite`. A reader who took the proofs as licence to clear that banner would delete a true warning. **THE ROW'S OWN MEASUREMENT HAS MOVED, AND THE FINDING HAS NOT.** Re-asked 2026-09-19 through `heron_brain.lookup` against a store this session had rebuilt from this worktree, *"pendent or upright here"* now lands on **`CREATE_DUCT` (MODIFY, `hybrid`)**, not `CREATE_ANGULAR_DIMENSION`, and the winner's ranks read *words #6 + nearness #1* rather than *#17 and #17*. That is [row 131](../FRAGMENT-ISSUES.md#) in one line - **the capability name in the claim above is a SAMPLE, not a constant** - and the finding is untouched by it: a question inside a MODIFY skill still reaches a MODIFY that has nothing to do with sprinklers, and the checker still cannot see it by construction |
| **138** | `create-roof` COULD NEVER MAKE A ROOF: AN `out` ARRAY DECLARED `null` MADE `NewFootPrintRoof` THROW… — [full row](../fragment-issues-archive/proving-defects-126-150.md#row-138) | **FIXED AND PROVED, 2026-09-19 - BUT THE FIRST PROMOTION WAS WRONG AND A REVIEW CAUGHT IT.** |
| **139** | `generate-jobs.py` OFFERED A FRAGMENT IT COULD NEVER RUN, BECAUSE IT ASKED WHETHER THE TYPE… — [full row](../fragment-issues-archive/proving-defects-126-150.md#row-139) | **FIXED 2026-09-19, AND THE RULE IS READ OUT OF THE C# RATHER THAN TYPED** |
| **140** | **A SKILL DECLARES A RISK AND A LIST OF CAPABILITIES, AND NOTHING HAS EVER COMPARED THE TWO. FOUR DECLARATIONS IN THREE SKILLS NAME A CAPABILITY THAT OUTRANKS THE SKILL'S OWN RISK.** Measured 2026-09-19 by `tools/prove-skill.py`, read from `brain/skills/*.yaml` and `brain/fragments/*/fragment.yaml` on disk, so this one does not move between runs the way a ranked measurement does. Verbatim: `check-connectivity` is **ANALYZE** and declares `REPORT_FINDINGS`, whose only provider `report-findings` is **SUGGEST** - one rung above. `trace-system` is **ANALYZE** and declares the same one. `find-blank-parameters` is **READ** and declares `DESCRIBE_BLANK_PARAMETERS` (`describe-blank-parameters`, **ANALYZE**, one rung above) **and** `REPORT_FINDINGS` (**SUGGEST**, two rungs above). The other six skills are clean, so the rule is a comparison rather than a dislike of writes. **THE REGISTER ALREADY ASKED FOR THIS CHECK AND IT WAS NEVER BUILT.** `docs/28-agent-registry.md` line 188 gives `HERON-SKL-VAL-004`, the Skill Validation Agent, the job *"Metadata complete, fragments exist and are compatible, **risk level correct**, no duplicate skill"*. `brain/heron_skill.validate` checks that `risk:` is one of the seven words and nothing else - `grep -n risk brain/heron_skill.py` returns **four** lines: one naming it in `REQUIRED`, and three that are the whole of that single test. Nothing anywhere compares a skill's declared risk against what it goes on to declare it needs; `check-skill-routing.py` is the only other reader of that field and it uses it as the discriminator for a crossing. **WHY IT IS NOT COSMETIC.** The skill's declared risk is what a permission decision about the skill would be taken at, and it is what `check-skill-routing.py` measures crossings against - `ASKS_A_QUESTION = ("READ", "ANALYZE")`. A skill whose declaration understates its own plan moves that line without anyone deciding to | **OPEN - a FINDING, and no skill was edited. Found 2026-09-19.** **THE OBVIOUS FIX IS THE ONE THING THAT MUST NOT BE DONE.** Raising `find-blank-parameters` from READ to SUGGEST, or `check-connectivity` from ANALYZE to SUGGEST, makes the comparison pass and takes both skills **out of `ASKS_A_QUESTION`** - so their crossings stop being reported at all. [Row 133](../FRAGMENT-ISSUES.md#) names two live crossings in `check-connectivity`, and that edit would hide both. That is [row 113](../FRAGMENT-ISSUES.md#)'s forbidden move arriving at the skill layer: a declaration weakened to buy back a number. **Which way each of the four should be repaired is the owner's**, and it is a question about what the skill IS - whether a QA sweep that ends in a sentence a modeller can act on is a READ that reports or a SUGGEST that proposes. `tools/prove-skill.py` reports it per skill and blocks on it; it does not choose. **What this row said it could not establish, MEASURED 2026-09-19 BY READING, and it needed no add-in after all.** The sentence was: *"there is no evidence that a skill approved at READ has ever actually executed a SUGGEST - `HeronPermissions` gates the OPERATION by name (Golden Rule 19) and the skill's risk may never reach a gate at all. That is the next thing to measure and it needs the add-in"*. **It does not.** Whether a skill's risk can reach a gate is a question about which code reads that field, and every reader is in this repository. Swept: in `brain/heron_skill.py` the word `risk` appears **four times** - once naming it in `REQUIRED`, and three that are the whole of a single check that the value is a permission level. Every other reader across `tools/` and `tests/` is a REPORT. `mcp/server/heron_brain.py`'s two `risk` keys are the **capability's** (`CAP.resolve`), and `heron_runtime.Capability.risk` is `_worst_risk(rows)`, derived from the FRAGMENTS serving it. And the C# side never sees one at all: `grep -rn -i skill --include=*.cs revit/ platform/` returns **six hits, every one a comment**. **SO A SKILL'S DECLARED RISK REACHES NO GATE, ANYWHERE** - the operation's own risk does, by name, which is Golden Rule 19 working exactly as written and the same chain [row 137](../FRAGMENT-ISSUES.md#) traced end to end for crossings. **THIS DOES NOT MAKE THE FOUR MISMATCHES HARMLESS AND IT IS NOT A REASON TO CLOSE THIS ROW.** A card declaring READ over a chain that reaches SUGGEST is a card that lies about itself, which is what the row is about; what changes is the EXPOSURE, which is a wrong sentence on a page rather than a permission granted by accident. **And it makes the forbidden fix worse, not better**: raising the declared risk buys nothing at the gate - the gate was never reading it - and costs the crossings, which is the entire loss for no gain at all |
| **141** | **NOTHING IN THIS REPOSITORY CAN RUN A SKILL'S PLAN, AND THE RUNNER THAT COMES CLOSEST REFUSES 32 OF THE 37 STEPS BEFORE IT SENDS ANYTHING.** Measured 2026-09-19 by `tools/prove-skill.py`, and confirmed by running one of its emitted files: `python tools/batch-prove.py <skill>-model-half.yaml --dry-run` on `size-breakdown` answered **`ALREADY  heron-status is PROVEN`** for all three steps. Ten skills compose into **37 steps**; **32 are fragments at PROVEN**, and `batch-prove` refuses those by design - its own docstring says *"There is deliberately no flag to override that"*. **SEVEN OF THE TEN SKILLS HAVE NOT ONE STEP IT WILL RUN**: `count-elements` 2 of 2, `mep-grayout` 5 of 5, `select-elements` 3 of 3, `size-breakdown` 3 of 3, `space-airflow` 4 of 4, `sprinkler-layout` 5 of 5, `terminal-layout` 5 of 5. **AND THE REFUSAL IS CORRECT** - each capability's own model half is already evidenced and signed. What is not evidenced is the COMPOSITION, and that is what a skill IS. **THREE RUNNERS EXIST AND EACH IS MISSING A DIFFERENT THIRD OF THE JOB.** `batch-prove.py` judges D-30 and can write, but proves ONE fragment with a setup chain and refuses a proved one. `prove` in `mcp/client/heron_bridge_client.py` **does run a chain** - `cmd_prove` sends `chain: "reset"` on the first fragment only and the rest continue it, one lease, one process, which is exactly a skill's plan - but it calls **`run_fragment_read` and nothing else**, so the four MODIFY skills cannot be run as a chain at all, and it judges nothing: no negative case, no draft, no signature. `validate` is one fragment at a time and is what `batch-prove` drives. **Chain, write, judgement - no one of the three has all three** | **OPEN - ROOT CAUSE for why no skill has ever been proved, and nothing was changed. Found 2026-09-19.** This is the structural reason the ten skills have sat DRAFT since Step 14 while 328 of 395 fragments reached PROVEN: **the proving apparatus is fragment-shaped**, exactly as [row 128](../FRAGMENT-ISSUES.md#)'s neighbour `tools/prove-agent.py` found the same thing for add-in agents - *"an add-in operation has no route at all"*. A skill has no route either, and the shape of the gap is different: an agent needed a place to record a proof, a skill needs a RUNNER. **THE FIX IS NOT TO WEAKEN `batch-prove`'s ALREADY GUARD.** Re-proving a signed fragment spends the one input only the owner can give, twice - `.claude/skills/fragment-proving/SKILL.md` records a batch that was *15 of 16 already PROVEN* and produced nothing. The composition is a DIFFERENT claim from its parts and wants its own record, the way `brain/agent-proofs/` is a different file from `brain/fragments/*/fragment.yaml`. **What was built instead:** `tools/prove-skill.py` measures everything provable without a Revit and emits the model half as a job file per skill under `tools/jobs/skills/`, each naming its own ALREADY steps and carrying the `prove` command line for the chain. **It never prints PROVEN** and `tests/test_skill_proving.py` pins that: a skill is proved when a model has answered, and the machine never signs |
| **142** | **`trace-system` DECLARES A CAPABILITY ITS OWN PLAN CANNOT FEED, AND THE CAPABILITY IS THE ONE ITS PURPOSE PROMISES.** Measured 2026-09-19 by `tools/prove-skill.py` from disk. The skill's purpose is *"Walks a real system from one element outward and **reports** what it actually reaches"*, and it declares `FILTER_ELEMENTS_BY_ID`, `TRACE_CONNECTIVITY` and `REPORT_FINDINGS`. `report-findings` needs **`findings` as `IList<string>`** from another fragment. `filter-elements-by-id` provides `elements` and `missing`; `trace-connectivity` provides `reached`, `openEnds` and `joinedByGeometry`. **Neither provides `findings`, and the setup chain does not either** - so the reporting step can never run, whatever the library holds elsewhere. `check-connectivity` declares the same capability and is fine, because `select-by-connection-status` provides `findings` - which is what makes this a defect in the PLAN rather than in `report-findings`. **IT IS THE SAME SHAPE THE SKILL FILE ALREADY WARNS ABOUT ONE SKILL ALONG.** `check-connectivity.yaml` carries a comment added 2026-09-19 reading *"THE PURPOSE ABOVE PROMISES 'reports where it is not [joined]' AND NOTHING HERE COULD DO IT"*. Nobody checked whether any other skill had it; this is the check, and one other did | **OPEN - recorded, and NOT guessed at. Found 2026-09-19.** **The repair needs a judgement and there is more than one plausible answer**, which is why none was taken: either the skill declares a fourth capability that turns a trace into findings - `CHECK_FIXTURE_CONNECTIVITY` (READ, PROVEN) provides `findings` and [row 133](../FRAGMENT-ISSUES.md#) already named it a plausible owner for connectivity sentences - or `REPORT_FINDINGS` is the wrong capability here and the report is the `reached` list itself, in which case the `needs` line goes and the purpose stays true. The first adds a fixture check to a skill about tracing one system; the second changes what the skill promises. **Both are the owner's, and neither is dangerous** - the skill is ANALYZE and nothing here writes. **How it was found, and the general form:** `prove-skill.py` orders each skill's providers greedily over what the setup chain leaves, and reports what will not place rather than dropping it. Nine of the ten skills ordered completely; this is the one that did not. A capability a skill declares but cannot reach is invisible to `heron_brain.catalogue()`, whose `ready` is `not missing` and asks only whether ANY provider exists - [row 132](../FRAGMENT-ISSUES.md#)'s *"a statement about the registry that reads as a statement about the skill"*, met again from a different direction |
| **143** | **THE REGISTER'S SORT OF THE TWENTY-THREE IS ALREADY OUT OF DATE BY FOUR AND BY SEVEN, AND THE READING IT RESTS ON CANNOT BE REDONE ON ANY CLONE.** Measured 2026-09-19 on a Linux container at `main`. `docs/NEEDS-CHECKING.md` **Group I** opens *"71 fragments are DRAFT. 44 cannot be arranged at all (`generate-jobs.py`, measured over all 71 rather than only the never-tried ones)"*. Today `heron_fragment.load_all` counts **395 fragments, 328 PROVEN, 67 DRAFT**, and `python tools/generate-jobs.py` reports **27 emitted, 40 unarrangeable of 67**. **THE SEVEN ARE NOT A MYSTERY - THE COMMIT SAYS SO IN ITS OWN TITLE.** Group I's text arrived in [PR #189](https://github.com/Ajmalpshaik/Heron-AI/pull/189) (`de34462`); [PR #196](https://github.com/Ajmalpshaik/Heron-AI/pull/196) (`cbd0227`) landed after it and is titled *"Five of the six proposed rules are built, and **seven fragments can be arranged**"*. `git merge-base --is-ancestor` confirms the order. So a reader of Group I today is told 44 by a paragraph a later commit in the same branch had already moved, and told 71 where the library says 67. **AND THIS ROW'S OWN FIRST NUMBER WAS WRONG IN THE SAME WAY, CORRECTED 2026-09-19:** it said `generate-jobs.py` reports 37, which was read out of [PROPOSALS](../PROPOSALS.md) rather than run. That 37 is **correct where it lives** and is measured over a different population - *DRAFT with no run record*, on a machine that HAD run records - while this container has none, so the tool measures all 67 and answers **40**. Re-measured at `3dd7159`, the commit the 37 was written at: **40 there too**. Nothing had moved; a number was taken from prose and attributed to a command that says something else, which is `AGENTS.md`'s *never type a number a command can derive* failing inside the row written to enforce it. **It also makes this row's point harder rather than softer**: the count depends on a folder that is gitignored, so two honest readers on two machines get two true answers. **THIS IS [ROW 116](../FRAGMENT-ISSUES.md#)'s LESSON AND [ROW 134](../FRAGMENT-ISSUES.md#)'s, ONE FILE ALONG**: a prose total is a cache with no invalidation, and this one went stale inside a single day. **AND THE SORT ITSELF CANNOT BE RE-DERIVED ANYWHERE BUT THE PROVING MACHINE.** Group I says it was read *"from the run records in `brain/proof-drafts/runs/`"*. That folder is **gitignored**, so on a fresh clone it does not exist at all - `generate-jobs.py` measured 67 candidates here precisely because it could find no run record for any of them, where Group I found 23. The classification in Group I is therefore a **one-machine fact** recorded in a shared file, and nothing in CI, on another clone, or in this container can check a single row of it | **OPEN as a finding about the register, and NOTHING IN GROUP I WAS EDITED. Found 2026-09-19 while being asked to redo its reading.** **THE READING WAS ASKED FOR AS THIRTEEN FRAGMENTS WHOSE POSITIVE WAS REFUSED BEFORE IT RAN, IN A "GROUP V", AND THAT IS NOT WHAT IS THERE.** `grep -nE '^## Group' docs/NEEDS-CHECKING.md` returns A, K, J, R, B, C, D, E, F, H, L, M, N, P, S, G, T, I and U - **there is no Group V**. The thirteen are **Group I2**, and the three categories the request offered - *a value nobody supplied*, *a shape the add-in cannot receive*, *a contract asking for the wrong thing* - **fit none of them**: I2's own answer is a FOURTH thing, *"the model has not got it"*, content rather than input, and it names what is missing per fragment (an imported CAD, rooms with doors between them, an area scheme with areas, electrical circuits, a section mark on a sheet). The offered categories do fit the OTHER two sub-groups: **I1's six** are arrangement (*a value nobody supplied*) and **I3's four** are shape and contract. **So the work was already done, four days' worth of it is already stale, and the split matters more than the total** - Group I says so itself: *"Saying 23 without the split would send somebody to re-run thirteen fragments that cannot pass and to blame them when they do not"*. **What this does NOT establish:** whether any individual row of I1, I2 or I3 is still true. Each rests on a run record this container cannot see, and re-checking one means the proving machine - which is the same reason the numbers above could drift for a day without anyone noticing |
| **144** | **THE QUESTION LIST GREW BY TWENTY-TWO AND FOUR OF THE TWENTY-TWO FOUND A CROSSING NOBODY HAD SEEN. TWO OF THE SEVEN THAT WERE OWED HAVE RESOLVED THEMSELVES, AND THREE THE HANDOVER NEVER NAMED ARE LIVE.** Measured 2026-09-19, Revit 2020, `tools/check-risk-crossings.py` at **78 questions** against index `fragments 395, identities 2580, vectors 395`. **12 crossings and 3 view changes.** `docs/HANDOVER.md`'s closing table owes *"seven crossings - duct size, pipe diameter, an element's workset, what is missing its Mark, views on a sheet, insulation thickness, selecting pipes in a view"*. **FIVE ARE STILL LIVE**, verbatim with what each beat: *"what size is this duct"* to `AUTO_SIZE_MEP` over `REPORT_DUCT_WEIGHT`; *"what workset is this on"* to `SET_VIEW_WORKSET_VISIBILITY` over `SELECT_BY_WORKSET`; *"what views are on this sheet"* to `PLACE_VIEWS_ON_SHEET` over `FIND_UNPLACED_VIEWS`; *"what is the insulation thickness"* to `SET_MEP_INSULATION` over `CHECK_INSULATION_CLEARANCE`; *"select all the pipes in this view"* to `CAP_OPEN_PIPE_ENDS` over `FILTER_ELEMENTS_BY_TYPE`. **TWO ARE GONE** - *"what is the diameter of this pipe"* and *"what is missing its mark"* did not come back as crossings on this run. **AND THREE THE TABLE NEVER NAMED ARE CROSSINGS TODAY**, all three from the ORIGINAL 56 rather than the additions: *"how high is this off the floor"* to `MOVE_TO_RAY_HIT` over `MEASURE_CEILING_HEIGHT` - **a question about height answered by moving the thing**; *"find the fire dampers"* to `PLACE_ACCESSORY_ON_RUN` over `SELECT_BY_FAMILY` - **asking where they are answered by putting one in**; and *"hide everything except the walls"* to `HIDE_ELEMENTS`, which is [row 145](../FRAGMENT-ISSUES.md#) and not a simple crossing. **THE FOUR THE GROWTH FOUND**, each a subject the list had none of: *"what is the scale of this view"* to `SET_VIEW_SCALE` over `REPORT_VIEW_TEMPLATE_CONTROL`; *"what is the airflow in this duct"* to `AUTO_SIZE_MEP` over `REPORT_CONNECTOR_LOADS`; *"what is the flow rate in this pipe"* to `AUTO_SIZE_PIPE` over `MEASURE_MEP_VELOCITY`; *"what levels are in this model"* to `CREATE_LEVELS` over `LIST_LEVELS` - **asking what the model HAS answered by making more**. Three of those four are the same shape: **a question about a QUANTITY answered by the tool that SETS it** | **OPEN - twelve findings, nothing re-ranked and no utterance weakened. Found 2026-09-19.** **THE ADDITIONS WERE CHOSEN BEFORE ANY RESULT WAS SEEN**, and the criterion is written into the file above them: a QUESTION rather than an imperative, a SUBJECT the list did not touch, in Ajmal's own words. The subjects were rooms and spaces, tags, model warnings, airflow and flow rate, the ceiling void, grids, what changed between issues, and connection asked as a sweep. **Choosing them by which ones failed is the drift the list's own docstring exists to prevent**, and 18 of the 22 found nothing, which is what an honestly chosen sample looks like. **`docs/HANDOVER.md`'s CLOSING TABLE IS NOW STALE IN TWO ROWS** and was not edited - it is another session's handover and a merge conflict there has cost this repository three times. Its *"seven crossings"* is five, plus three it never named; its *"five ambiguous phrases - all read-vs-read, so none is dangerous"* was already corrected to six with two write-vs-write by [row 134](../FRAGMENT-ISSUES.md#), which this session then judged. **WHAT THIS DOES NOT ESTABLISH:** the two that resolved are NOT recorded as fixed. A count from this sweep is a sample - [row 116](../FRAGMENT-ISSUES.md#) measured 7, 9 and 8 from one tool minutes apart - and a sentence absent from one run's list is not a sentence that has been repaired. They are named here so a later run can tell the difference |
| **145** | **`check-risk-crossings.py` NAMES A SENTENCE IN ITS OWN DOCSTRING AS CORRECT AND ITS OWN RULE REPORTS THAT SENTENCE AS A CROSSING - AND THE RULE IS RIGHT.** Measured 2026-09-19. The docstring's *WHAT IT DOES NOT DECIDE* section reads: *"An imperative is not a question. 'isolate all the pipes' resolving to ISOLATE_ELEMENTS is CORRECT even though isolating is not a READ, **and so is 'hide everything except the walls' landing on HIDE_ELEMENTS**. Those are reported separately and are not failures"*. On this run *"isolate all the pipes"* and *"show me just the ducts"* are indeed reported separately, under the view-change list - and **"hide everything except the walls" is in the CROSSINGS list**, having beaten `REPORT_COMPOUND_STRUCTURE`. **THE TWO SIBLING CAPABILITIES CARRY DIFFERENT RISKS AND THAT IS NOT A MISTAKE.** `isolate-elements` is **EXECUTE**; `hide-elements` is **MODIFY**, and its own purpose gives the reason in full: *"Temporary unless somebody asked for permanent, and the two are genuinely different things... A permanent hide is **saved into the view**: it survives closing the model, it appears on the printed sheet, and the next person to open that view has no idea why something is missing... a capability's risk is the worst thing it can do."* `isolate-elements` has no permanent mode - Revit's Temporary Isolate only - so the pair is a real difference correctly declared, not an inconsistency. **SO IT IS THE DOCSTRING THAT IS WRONG**, and it is wrong in the direction that matters: it tells a reader to disregard the one imperative in the list whose capability can write something into a view that then prints | **FIXED 2026-09-19 - THE SENTENCE WAS REPAIRED AND THE RULE WAS NOT TOUCHED**, which is what this cell asked for when it was written. The docstring's *WHAT IT DOES NOT DECIDE* section now puts the line at **EXECUTE** rather than at *it is an imperative*: *"isolate all the pipes"* reaching `ISOLATE_ELEMENTS` is correct and reported separately; an imperative reaching a **MODIFY** is still worth reading, and widening the discriminator to excuse it would delete the check. The pair's reason is named where a reader meets it: `isolate-elements` is Revit's Temporary Isolate, gone when the view is reset, and `hide-elements` is saved into the view, survives closing the model and appears on the printed sheet - **a capability's risk is the worst thing it can do**, not the thing the modeller probably meant. **VERIFIED BEFORE IT WAS WRITTEN, NOT QUOTED FROM THIS ROW**: `brain/fragments/isolate-elements/fragment.yaml` line 14 reads `risk: EXECUTE` and `hide-elements` line 14 reads `risk: MODIFY`, and the second gives that reason in its own purpose. **WHY IT WAS SAFE TO DO NOW AND WAS NOT THEN:** this cell held the repair back because *"a second session rewriting it in the same hour as [row 144](../FRAGMENT-ISSUES.md#)'s growth is how two edits to one file collide"* - [row 144](../FRAGMENT-ISSUES.md#)'s growth from 56 questions to 78 has since landed, the file loads with all 78, and the two edits are in different sections. **NOTHING IN THE RULE OR THE QUESTION LIST CHANGED**, so no measurement in [row 144](../FRAGMENT-ISSUES.md#) is affected. **One neighbouring fact, recorded and not chased:** `isolate-elements` at EXECUTE threw *"Attempt to modify the model outside of transaction"* on a real run - it is in this file's own §3 table as DID NOT RUN, *"it is `risk: EXECUTE` and still needs the write path"* - so the EXECUTE half of the pair has a separate, older question hanging over it that this row does not answer |
| **146** | **THE CROSSINGS ARE TWO DIFFERENT PROBLEMS WEARING ONE NAME: SEVEN HAVE AN OWNER SITTING IN THE LIBRARY THAT NEVER DECLARED THE SENTENCE, AND FOUR HAVE NO READ TO GIVE THEM TO AT ALL.** Judged 2026-09-19 over [row 144](../FRAGMENT-ISSUES.md#)'s eleven (its twelfth, *"hide everything except the walls"*, is [row 145](../FRAGMENT-ISSUES.md#)). Every owner below was read out of the candidate's own `purpose:` rather than chosen by name. **SEVEN HAVE AN OWNER, AND [ROW 116](../FRAGMENT-ISSUES.md#)'s *declaring, not demoting* is the whole repair - one line added to one `utterances:` block, after which identity fires before ranking ever runs:** *"what views are on this sheet"* -> **`LIST_SHEETS`**, whose purpose already says it reports each sheet's number, name *"and how many views are on it"*; *"select all the pipes in this view"* -> **`SELECT_VISIBLE_IN_VIEW`**, *"narrows the model to what a named view actually SHOWS, optionally bounded to a list of categories"*; *"how high is this off the floor"* -> **`READ_ELEMENT_LEVEL`**, *"which level each element belongs to, and its **offset from that level in millimetres**"*; *"find the fire dampers"* -> **`SELECT_BY_FAMILY`**; *"what levels are in this model"* -> **`LIST_LEVELS`**; and both flow questions - *"what is the airflow in this duct"* and *"what is the flow rate in this pipe"* -> **`REPORT_CONNECTOR_LOADS`**, *"reads what each element's connectors are CARRYING - assigned flow, demand, K-factor..."*. **ON TWO OF THOSE SEVEN THE RUNNER-UP WAS NOT THE RIGHT READ EITHER**, which is worth more than the crossing: *"what is the flow rate in this pipe"* beat `MEASURE_MEP_VELOCITY`, and velocity in metres per second is **not** a flow rate - so a rule that simply preferred the highest-ranked READ would have answered the wrong question politely. `SELECT_BY_FAMILY` and `LIST_LEVELS` were the runners-up and ARE right; `LIST_SHEETS`, `SELECT_VISIBLE_IN_VIEW`, `READ_ELEMENT_LEVEL` and `REPORT_CONNECTOR_LOADS` were not in second place at all. **FOUR CANNOT BE REPAIRED BY DECLARING, BECAUSE NOTHING READS THE THING.** Every capability carrying the word was listed and each list is short. *"what size is this duct"* - the library has `AUTO_SIZE_MEP`, `AUTO_SIZE_PIPE` and `SET_MEP_SIZE` (all MODIFY), plus `SELECT_BY_CONNECTOR_SIZE`, `CHECK_SLEEVE_SIZE` and `REPORT_FAMILY_SIZE_TABLE`, and **not one reports an MEP element's own size**. *"what workset is this on"* - `LIST_WORKSETS` reports the MODEL's worksets and `SELECT_BY_WORKSET` selects by one; **nothing says which workset an element is on**. *"what is the insulation thickness"* - `SELECT_BY_INSULATION` selects by it and `CHECK_INSULATION_CLEARANCE` measures the gap around it; **nothing reports the thickness**. And the starkest: *"what is the scale of this view"* - **`SET_VIEW_SCALE` is the only capability in the library with SCALE in its name, and it writes**. A question about a drawing's scale has exactly one place to land and that place changes it | **OPEN - eleven judgements, and NOTHING WAS EDITED. Found 2026-09-19.** **THE SPLIT IS THE FINDING, NOT THE LIST.** `docs/HANDOVER.md` files all of these as *"crossings, each needs an owner chosen per sentence"*, and for four of them there is no owner to choose - they are docs/18's capability gaps wearing a routing defect's clothes, and no amount of declaring, re-ranking or nudging reaches them. **A gap and a mis-route need different people:** one is a fragment somebody has to write, the other is a line somebody has to add. Counting them together is how the seven have stayed owed. **WHY THE SEVEN WERE NOT FIXED HERE, and it is an ownership boundary rather than a judgement:** the edit is one line in a `brain/fragments/*/fragment.yaml` `utterances:` block, and this session was told explicitly not to touch `brain/fragments/**`. The sentence and its owner are written out above so whoever owns those files does not re-derive them. **WHAT THIS DOES NOT ESTABLISH:** that declaring will actually move any of the seven. [Row 133](../FRAGMENT-ISSUES.md#)'s number 4 is the standing warning - `AUTO_SIZE_MEP` won *"what sizes are the ducts"* on **both** routes at once, words #1 and nearness #1, where nothing but identity could reach it. Declaring is the repair that should work and it has to be measured afterwards, on this tool, with the index fingerprint read first. **AND THE ENUMERATION ABOVE IS BY CAPABILITY NAME, WHICH IS NARROWER THAN IT READS** - noted 2026-09-19 with [row 158](../FRAGMENT-ISSUES.md#). *"Not one of the capabilities with SIZE in its name reports an MEP element's own size"* is true and was checked again; *"there is no READ to give the sentence to"* is a bigger claim and does not follow from it. `READ_ELEMENT_PARAMETERS` (FRG-PAR-001, READ, PROVEN) reads **one named parameter off every element** and declares *"get the size parameter"* in its own `utterances:` block - it has SIZE nowhere in its name and is a candidate owner for three of these four. **The fourth is not:** a view's scale is not an element parameter read off a selection, and `SET_VIEW_SCALE` remains the only capability in the library with SCALE in its name. **So this row's *four capability gaps* is really one certain gap and three sentences that may only need declaring** - which is the cheaper repair, and whoever owns `brain/fragments/` should try `READ_ELEMENT_PARAMETERS` before writing anything new. Not settled here: insulation thickness lives on a SEPARATE element wrapping the host, as `SELECT_BY_INSULATION`'s purpose warns, so that one may still be a gap |
| **147** | NOTHING COULD ROLL BACK THE ADD-IN, WHILE HERON'S OWN UPDATE GATE REFUSED TO SHIP ANY RELEASE THAT… — [full row](../fragment-issues-archive/proving-defects-126-150.md#row-147) | FIXED AND PROVED 2026-09-19, ON ALL THREE RELEASES INSTALLED ON THE OWNER'S PC, AND… |
| **148** | **A SKILL'S PLAN DOES NOT STOP WHEN A STEP FAILS, AND THE STEP AFTER IT ANSWERS FROM WHATEVER WAS LEFT SELECTED IN REVIT.** Found 2026-09-19, running the `count-elements` plan end to end on `test projject` (session 36216) - the first time any skill's composition has been run against a model, using the job file [row 141](../FRAGMENT-ISSUES.md#)'s session generated. **MEASURED, BOTH WAYS.** Handed values the FIRST step could not bind, `filter-elements-by-category` refused with `needs_request_values` - and `count-elements` **RAN ANYWAY AND RETURNED `count 1`, `countedNothing false`**, a confident number a modeller would believe. The 1 was a floor left selected by an unrelated probe minutes earlier. `prove` printed *"1 ran, 1 failed"* underneath it. **THIS IS THE SKILL'S OWN STATED HAZARD ARRIVING BY A ROUTE IT DID NOT DESCRIBE** - `count-elements` says *"a count that silently filtered to nothing looks exactly like a model with nothing in it"*, and this is worse: the filter did not run at all and the count answered from a stale selection. **THE BINDING NOTE IS HONEST AND IS THE ONLY THING THAT SAYS SO** - it read `<- elements from the selection (1)` rather than `from filter-elements-by-category`, which is exactly the line `fragment-proving` rule 5 says to judge a run on. So the machinery does not lie; it simply does not stop, and a reader who trusts the number without reading the note has no way to tell. **THE SAME PLAN WITH VALUES THAT BIND WORKS PERFECTLY**, which is the other half and the reason this is a finding about composition rather than about either fragment: `category=Ducts, levelId=Level 1` gave `filter-elements-by-category` **8 matching**, `count-elements` reported **`count 8`** and its note read **`<- elements from filter-elements-by-category (8)`**, and `prove` printed *"2 ran, 0 failed"*. The hand-over is real | **OPEN, and it belongs to whoever owns [row 141](../FRAGMENT-ISSUES.md#)** - the skill runner that does not exist yet. Recorded here rather than fixed because `prove` is a diagnostic that judges nothing, and the decision - does a failed step STOP the plan, or does the next step refuse a chain it did not receive - is a design question about skills, not a fragment defect. **NOTHING ABOUT EITHER FRAGMENT IS WRONG.** Both are PROVEN, both behaved exactly as declared, and `count-elements` counting the current selection when nothing was handed to it is its documented behaviour. **THE CHEAPEST REPAIR IS PROBABLY THE CHAIN EXPECTATION THAT ALREADY EXISTS** - `expect_from` ([docs/36](../36-remembering-between-steps.md)) makes a consumer NAME the producer it expects and refuses when something else left the values, which is this exact failure one layer down and already built and proved. A skill runner that emitted `expect_from` between its steps would get this for free. **AND A CORRECTION TO THIS ROW, MADE BEFORE ANYBODY ACTED ON IT.** It first said the generated job files carry the wrong values for their first step - that `count-elements`'s `set:` held `categoryName` and `inViewOnly` where `filter-elements-by-category` needs `category` and `levelId`. **THAT WAS WRONG AND THE GENERATOR IS RIGHT.** The file sets `category` and `levelId` PER JOB, exactly as that step declares; `categoryName` and `inViewOnly` sit in `defaults.set` because they belong to the SETUP CHAIN, `select-by-category-name` then `set-selection`, which is also correct. **The failing first step above was this session running `prove` BY HAND with the setup chain's values instead of the step's own** - my mistake, not the generator's, and the finding about the plan not stopping stands on its own either way. Written down because a peer sent chasing a defect that is not there costs more than the finding is worth |
| **149** | **WAS 142, THEN 143, THEN 147, AND IS 149 - THE REGISTER'S OWN HAZARD, FOUR TIMES IN ONE DAY.** The skills session took 142 (#199) and then 143 to 146 (#201), each reaching `main` first, and each time this row moved. **THE RULE THAT WOULD HAVE STOPPED IT IS THE ONE ALREADY WRITTEN DOWN** - read the last row on `main` and ANNOUNCE the number you are taking - and the half nobody follows is the ANNOUNCE. 140 and 141 were handed over in writing and were the only two that did not collide. The rule that would have avoided it is the one already written down: read the last row on `main` and ANNOUNCE the number you are taking. 140 and 141 were handed over in writing and 142 was not. **A SETUP CHAIN AND THE FRAGMENT UNDER TEST CANNOT BE GIVEN DIFFERENT VALUES FOR A NAME THEY SHARE, AND THE COLLISION IS SILENT.** Found 2026-09-19 arranging `select-touching`. It needs `target` (one element) and `categories` (what to look for); the obvious chain is `select-in-region`, whose own selector need is ALSO called `categories`. **There is nowhere to say they differ.** `tools/batch-prove.py:227` merges `defaults.set` with the job's `set` into ONE flat dict and `:441` emits one `--set` per key; `mcp/client/heron_bridge_client.py:1515` then does `chosen_setup = values` and hands **the same dict to every setup step AND to the fragment**. So `categories: Floors` for the chain and `categories: Walls` for the question collapse to one, and the dry run's argv said so plainly: **a single `--set categories=Walls`**. **CAUGHT BY READING THE ARGV, NOT BY A FAILURE.** In this case it would have failed loudly afterwards - the region would have selected 28 walls and `target` binds exactly one - **but that is luck, not a guard.** Where the fragment's need is a LIST, the chain silently selects the wrong population, the job RUNS, and it reports a number about something nobody asked for. That is the confident-wrong result this register exists for. **MEASURED ACROSS THE WHOLE LIBRARY RATHER THAN LEFT AS ONE STORY: 88 fragment/chain pairs share at least one caller-supplied need name, 16 of them on a DRAFT fragment.** `categories` is the common one (`select-in-region`, `select-by-categories`, `select-by-parameter-value` all take it), then `parameterName`, then `inViewOnly` and `levelId`. **IT HAS NOT BITTEN BEFORE BECAUSE OF HOW THE PROOFS WERE ARRANGED, not because it is rare**: a fragment needing `categories` was proved with `select-by-category-name`, which takes `categoryName` and does not collide. Nobody chose that on purpose | **FIXED 2026-09-19 - AND THE FIRST VERSION OF THE FIX HAD A P1 IN IT, FOUND BY REVIEW ON PR #198.** **`--negative-setup-set` COULD NOT WORK AT ALL**, which is the sharpest kind of defect this file can carry: a proof that RAN, PASSED, and was about something else. `arrange()` inferred the phase from `using`, and a negative differing ONLY in its arrangement leaves `negative_values` empty - so `using` arrived `None`, `arrange` read that as the positive phase, and the negative leg was recorded against the POSITIVE setup. The phase is passed in explicitly now and never inferred. `cmd_validate` also gated the whole negative leg on `negative_in or negative_values`, so such a proof fell through to the interactive prompt and waited at a keyboard nobody was at. **AND `batch-prove` HELD THE SAME GAP ONE LAYER UP, WHICH ONLY RUNNING IT FOUND** - its own check demanded `negative-set:` or `negative-in:` and refused the job before the client was ever called, naming the two keys it knew. The client's half had already been repaired and the batch runner still said no. Reading would not have caught that one. **PROVED ON A MODEL, BOTH LEGS, AND THE RECORD NAMES THEM APART**: `test projject`, session 36216, the two legs differing ONLY in `negative-setup-set` - positive bound `target` to **'600 x 600mm Grid' (id 926468)**, the one ceiling; negative bound it to **'Generic 300mm' (id 926428)**, the one floor; and the negative's `arranged` line reads *"run arranged with categories=Floors..."*. Before the repair the negative would have silently re-used the ceiling. **ONE CASE IS REFUSED RATHER THAN FIXED, AND IT IS WRITTEN DOWN AS SUCH.** A setup step at MODIFY or above travels WITH the fragment in the request's `setup` array, which carries a name, a source and needs - no values - and `RunSetupSteps` binds every step from the run's top-level `supplied`. So `setup-set` could be accepted, sent, and quietly not applied, leaving the original silent collision alive for WRITE arrangements. Carrying per-step values needs the ADD-IN to read them, which is C# and costs a rebuild and a Revit restart. Until then the combination is REFUSED by name, so it is a wrong answer that cannot be produced rather than one produced silently. **AND THE STORED DIGEST NOW CARRIES AN `INDEX_FORMAT` VERSION** - without it an upgrade that changes how `heron_search.index()` derives anything would still match the digest the PREVIOUS version wrote, and since `_Open` never forces, the old tables would be served until some unrelated fragment happened to change. **THE ORIGINAL ACCOUNT FOLLOWS.** **FIXED 2026-09-19, AND THE FIX IS ADDITIVE SO NOT ONE EXISTING PROOF CHANGES.** `setup-set:` and `negative-setup-set:` in a job file become `--setup-set` / `--negative-setup-set` on `validate`, and `arrange()` uses them for the setup steps ONLY when they are present - **absent, every step gets exactly what it got before**, which is why no job file written before today needs touching. Two files: `tools/batch-prove.py` (job keys plus argv) and `mcp/client/heron_bridge_client.py` (parser, signature, `arrange`). **THE PARSER NOW ROUTES BY A LOOKUP TABLE RATHER THAN BY SPELLING, AND THAT WAS FORCED BY THIS CHANGE**: the old rules were `token.endswith("set")` and `token.startswith("--negative")`, and `--negative-setup-set` satisfies BOTH, so two string rules that had agreed for a year stopped agreeing. There is a check for exactly that. **PROVED ON A MODEL, NOT JUST IN A TEST** - `test projject`, session 27244: the chain was given `categories: Ceilings` while the fragment was asked about `categories: Ducts`, and the run bound `target` to **'600 x 600mm Grid' (id 926468)** - the one ceiling - while the question stayed on ducts. Before this, that job could not be written down at all. Five checks in `tests/test_caller_values.py`, including that absent leaves both lists empty. **THE ORIGINAL STATE LINE FOLLOWS, because the workaround it describes is still what any job file written before today is doing.** **OPEN, and the workaround used today is written down because it is not a fix.** `select-touching` was re-arranged onto `select-by-category-name` purely because `categoryName` does not collide with `categories` - and that chain is VIEW-SCOPED, which promptly cost a second run: `categoryName: Ceilings` in a FloorPlan returned nothing, because ceilings do not draw in a floor plan, and the refusal read *"NOTHING is selected"*. **Trading a silent collision for a view-scoping trap is not a repair.** **THE FIX IS ADDITIVE AND CARRIES NO RISK TO ANY EXISTING PROOF** - built the same day, see the head of this cell. **WHAT WAS ESTABLISHED ABOUT THE FRAGMENT ITSELF, since it ran twice**: `select-touching` means OVERLAP and says so in its own words - *"Nothing OVERLAPS it. That is not the same as nothing being related"* - so a floor and the walls it sits against correctly return 0. It bound its target properly (`'Generic 300mm' (id 926428)`), which is the half that was in doubt. It needs two things that genuinely interpenetrate, and the ducts passing through walls in this model are exactly that - it is the SINGLE target that is hard to arrange, not the question. **ONE MORE THING WORTH KNOWING**: `batch-prove` deletes the run record before each run ([row 87](../FRAGMENT-ISSUES.md#)'s fix, so a missing file means nothing ran), so a probe re-run DESTROYS the previous record. Copy it first if the evidence matters |
| **150** | **THE SKILL CATALOGUE SHOWED THE CHAIN AND NOT THE WORDS, SO A SKILL WITH A PROVEN CHAIN AND ONE SENTENCE IN FOUR REACHING READ EXACTLY LIKE A SKILL THAT WORKS.** Measured 2026-09-19. `tools/generate-skill-catalog.py` computes an EFFECTIVE status - the lowest rung among the fragments serving a skill's capabilities - and its own docstring gives the reason: *"A card can say anything; the chain underneath is the fact."* It is a good rule and it is **half** the fact. Run against the library it reports **10 skills: 7 PROVEN all the way down, 3 weaker**. Run beside `tools/prove-skill.py`'s recorded routing, **exactly ONE of the ten has every utterance reaching a capability it declares**, and **6 of the 7 with a PROVEN chain have at least one that does not**. The starkest is `space-airflow`: chain **PROVEN** throughout, **1 of 4** words reaching - it is declared MODIFY and *"set the supply air flow"* lands on `REPORT_SPACE_AIRFLOW`, a skill that will report where it promised to write. And `check-connectivity`, which the page showed only as chain DRAFT, answers **two** ordinary questions with a write ([row 133](../FRAGMENT-ISSUES.md#) as re-measured). **THIS IS [ROW 132](../FRAGMENT-ISSUES.md#)'s SHAPE ONE LAYER ALONG.** That row named `heron_brain.catalogue()`'s `ready` - computed from whether a capability has ANY provider - as *"a statement about the registry that reads as a statement about the skill"*. The catalogue's chain is a statement about the FRAGMENTS that reads as a statement about the SKILL, and on a page of cards `count-elements` and `space-airflow` were the same card | **FIXED 2026-09-19, in the half this session owns, and the finding is kept because the shape returns.** Each card now carries its WORDS beside its chain - how many of its own utterances reach a capability it declares, every sentence marked with where it actually landed, and a loud tag when one reaches something that changes the model. The subtitle carries both halves under the labels **CHAIN:** and **WORDS:** so neither reads as the whole, and a filter selects *a question answered by a write*. **IT IS READ, NEVER RE-MEASURED**: one `heron_brain.lookup` per utterance is about twenty-five minutes for forty-three sentences, which is not a page render - so it loads the newest `tools/jobs/skills/routing-*.json` that `prove-skill.py --routing-to` wrote, and prints the date and the index fingerprint it was taken against, because [row 116](../FRAGMENT-ISSUES.md#)'s rule is that two measurements are comparable only when that block matches. **AND A MISSING RECORDING SAYS `NOT MEASURED` RATHER THAN SHOWING NOTHING** - D-52's rule, and the distinction is the whole point: an absent measurement is not a clean one. `tests/test_skill_catalog.py` pins all four failure paths (no folder, empty folder, unreadable file, and the counting itself) and states the case in its own output: **6 skills have a PROVEN chain AND a word that does not reach, invisible on the chain alone**. **NEITHER HALF IS A PROOF AND THE FOOTER NOW SAYS SO:** a chain of PROVEN fragments is not a proven skill, and words that reach are understanding rather than evidence - the other half is a model (D-30), and no skill has met one **DEFERRED BY THE OWNER 2026-09-22**, at the close of the session that found it: not now, later, and he said later is better. It stays OPEN because the defect is still in the file - **open here means the defect exists, not that somebody is part-way through it.** Nothing is blocked and nothing was left half-changed; the tool is exactly as it was found. Pick it up when he asks. |
