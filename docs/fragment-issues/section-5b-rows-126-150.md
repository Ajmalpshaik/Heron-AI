# Fragment issues — section 5b, rows 126 to 150

> Rows 126 to 150 of section 5b of [the register](../FRAGMENT-ISSUES.md), in their own file since 2026-09-23 so that
> they can be read alone. **The section's own rules are on that page, under its heading.** A new
> row goes at the end of the section's last file, whatever its number, and the next run of
> [`tools/split-register.py`](../../tools/split-register.py) moves it to the file its number
> belongs to. [`tools/register-text.py`](../../tools/register-text.py) reads every row back into
> the register for every tool that reads it, so a row here is seen exactly as it was seen there.

| # | Defect | State |
|---|---|---|
| **126** | **ONE HOP WAS BEING READ AS REACH, AND THE NUMBER ROW 5b-83 TURNS ON IS 17 RATHER THAN 53.** Found 2026-09-22 reading `tools/module-reach.py` - 177 lines, **no suite**, the fifth of the six tools in `tools/` that no suite names. It exists to re-measure `mcp/README.md`'s own sentence: modules *imported by nothing but their own tests, and therefore UNREACHABLE FROM ANY CONVERSATION*. It sorts every module in `brain/` into four buckets by **who imports it**, and the first is labelled *reached by mcp/ or another brain module*. **That label is true as written, and it is not the question the docstring asks.** A module imported by a neighbour lands there whether or not the neighbour is itself reached by anything. **MEASURED 2026-09-22 on this repository, by following the imports rather than counting one hop**: of 145 modules the first bucket holds **53**, and only **17** are reachable from `mcp/` at all. **Thirty-six of the 53 are reached only by neighbours that nothing reaches** - closed loops inside `brain/`. So the figure a reader takes as *reached* overstates it by more than a factor of three, and the count of modules no conversation can arrive at is **128** rather than the 90 in bucket three | **FIXED 2026-09-22 by WIDENING WHAT IT REPORTS, not by changing a bucket.** A new `reached_from()` walks the import graph, and the report prints *reachable from mcp/ by following imports* **beside** the four buckets, with a line naming how many of the first bucket are closed loops. `--list` names them too. **Neither number stands in for the other**: the buckets say who imports a module, the walk says whether a conversation can arrive at it, and both are worth having. Nothing about the buckets moved, and it still exits 0 whatever it finds - it is a report, and **row 5b-83 is still the one that asks whether any of this is wrong, still OPEN, and still the owner's**. `tests/test_module_reach.py` is the first suite this tool has ever had: **one red** against the module as found, with every bucket check green before and after, which is what makes this an addition rather than a correction. Both halves shown to have teeth - removing the walk reds the line that asks for it, and making the walk stop after one hop reds the count. **THREE THINGS CHECKED AND FOUND RIGHT rather than assumed**: no file anywhere imports a brain module by package path, which the head-of-the-dotted-name reading would miss; `platform/` and `revit/` hold no Python at all, so leaving them out of the searched roots costs nothing; and the one Python file under `.claude` imports no brain module, so a module reached only by a hook - which would land in the first bucket under a label naming mcp and brain - does not exist today. That last one is a shape, not a defect, and it is not a separate row |
| **127** | A MEASUREMENT TOOL RAN THE WHOLE MEASUREMENT AT A SETTING NOBODY ASKED FOR, AND PRINTED THAT… — [full row](../fragment-issues-archive/reading-defects-5b-126-150.md#row-5b-127) | **FIXED 2026-09-22.** |
| **128** | **A GATE A COMMENT COULD SATISFY.** Found 2026-09-22 reading `tools/check-narrow-errors.py` - 130 lines, **no suite**, and **one of the ten gates a pull request has to pass**. It looks for D-52's commonest shape: an `except sqlite3.OperationalError` that swallows a locked, malformed or out-of-date store and reports it as an empty one. Reviews found that same line in four files on four consecutive rounds, which is why it is a command rather than a habit. It reads TEXT rather than an AST on purpose, and the reason is good - the rule is about what a maintainer sees beside the handler - **but the narrowing test searched the handler's RAW text for the word `raise`**. **MEASURED, on modules written for the purpose**: a handler that swallows and carries the comment `# deliberately do not raise here` **passed the gate**; so did one carrying `log("nothing to raise")`; so did one carrying `note = "we could raise"`. A handler that says in words it will not re-raise, and then does not, is the clearest possible case of the thing this gate exists to catch, and it was the one shape it could not see. **ALSO FOUND, and smaller**: the comment above `WINDOW` said **five** while the value said **twelve** - a typed number gone stale, in the file whose whole argument is that a shape is something a command should look for | **FIXED 2026-09-22.** Comment text and string contents are taken out of each handler line before the narrowing search, by a `_code()` that tracks quotes so a `#` inside a string does not cut the line short and skips an escaped character so a quote inside a string does not end it early. The word has to be code now. A string spanning lines is not tracked across them and the docstring says so - the worst that costs is the behaviour this already had. The `WINDOW` comment says what the value says. **Nothing else moved, and the gate is still green on the repository** - every `sqlite3.OperationalError` handler in `brain/`, `mcp/` and `tools/` narrows, so the fix flags no correct code. `tests/test_narrow_errors.py` is the first suite this gate has ever had: **3 red** against the module as found, all three in the section about comments, with the checks either side green before and after - the bare swallow still reported, both narrowing forms still accepted, a `raise` in the NEXT handler still not rescuing this one, and a `ValueError` handler still left alone. **ONE THING MEASURED AND DELIBERATELY NOT FIXED**, recorded in the docstring and added to row 5b-110 rather than settled here: a handler written as a TUPLE is invisible to the pattern, and the one tuple handler in this repository - `heron_embed._try_vec_extension` - is CORRECT, so widening today would flag right code |
| **129** | A GERMAN WORD COUNTED AS A LICENCE, AND A LIST NOBODY COULD FINISH READING. — [full row](../fragment-issues-archive/reading-defects-5b-126-150.md#row-5b-129) | **FIXED 2026-09-22.** |
| **130** | THE GATE THAT GUARDS THE OWNER'S SCARCEST INPUT DROPPED A FRAGMENT IT COULD NOT READ AND SAID… — [full row](../fragment-issues-archive/reading-defects-5b-126-150.md#row-5b-130) | **FIXED 2026-09-22.** |
| **131** | **A MACHINE'S NAME IN `by:` IS COUNTED AS A SIGNATURE BY THE GATE THAT COUNTS SIGNATURES.** Found 2026-09-22 beside row 5b-130. `check-signatures.py` reads `proof['by']` and counts anything non-empty. **MEASURED**: a proof signed `Claude Opus 5, at Ajmal PS's PC` is counted in *Signatures in the library* and listed under STALE like any other. **D-30 says the machine gathers evidence and a PERSON signs**, and `resign-machine-proofs.py` exists because sixteen fragments carried that exact string - in its own words, *it is not a signature, it is the thing the rule exists to forbid, written into the field meant to prevent it*. So the gate that guards the owner's signature counts the forbidden thing as the thing it forbids. **NOT A LIVE FAILURE**: measured, zero fragments in the library are machine-signed today, and `resign-machine-proofs --list` says so. But **nothing in CI runs that tool**, so nothing would notice the first one | **OPEN - measured and deliberately NOT fixed, because the question is WHERE the answer lives rather than what it is.** Two tools need to answer *is this a person's name?* and neither should hold a second copy of the other's word list - the duplicated-constant failure this repository keeps catching. Three ways, and it is a tooling decision: (1) a `MACHINE_NAMED` constant and helper in **`brain/heron_fragment.py`**, which already owns `can_promote`, `proof_problems` and `proof_is_stale`, with both tools asking it - but `resign-machine-proofs` imports no brain module today and is deliberately runnable on `fragment.yaml` text alone; (2) **`check-signatures` imports the constant from `resign-machine-proofs`** by path, which is a CI gate reaching sideways into a hand-run tool; (3) **a CI step that runs `resign-machine-proofs --list`**, leaving both files alone and putting the answer where it already is. **The measurement is the deliverable here** - `tests/test_check_signatures.py` section 5 prints it rather than asserting it, on purpose, because asserting today's behaviour would lock in the thing this row is open about |
| **132** | THE THREE FORMAT PATTERNS GUARDING THE ONE FILE NOTHING COMPILES ALL END IN `$`, AND IN PYTHON `$`… — [full row](../fragment-issues-archive/reading-defects-5b-126-150.md#row-5b-132) | **FIXED 2026-09-22.** |
| **133** | **NO COMPILER ON THIS MACHINE CAME OUT AS A TRACEBACK, AT THE SAME EXIT CODE AS A LIBRARY THAT DOES NOT COMPILE.** Found 2026-09-22 reading `tools/check-fragments-compile.py` - 400 lines, **no suite**, the sixth of the seven CI gates that had never been opened. It compiles every fragment's C# against every Revit release it claims, with no Revit and no Windows, and until it existed **nothing compiled the fragments at all** - the library's whole point was the one part no compiler had ever read. It shells out to `dotnet build`. **MEASURED on this container, which has no .NET SDK**: `FileNotFoundError: [Errno 2] No such file or directory: 'dotnet'`, and **exit 1**. A traceback is none of AGENTS.md's four states, and **exit 1 is the code for a fragment that does not compile** - so a reader, or a script, cannot tell a broken library from a machine with no compiler on it. **THE WORKFLOW ALREADY KNEW AND WORKED AROUND IT RATHER THAN FIXING IT**: the step sits in a different CI job, and the comment beside it says *it shells out to `dotnet build` with no guard - the gates runner has no SDK and it would fail there for the wrong reason*. That is this defect, written into `gates.yml` instead of into the tool. And the file does the right thing one function below, for the smaller dependency: `load_fragments()` says *This needs PyYAML: pip install --user pyyaml* | **FIXED 2026-09-22.** A `no_compiler()` looks for the SDK and its absence prints **NOT RUN** in those words, names what is missing, and returns **3** - the code `tests/README.md` gives a suite that could not run for want of an optional dependency, and which it states plainly is *not a pass*. **It is asked LAST, on purpose**: an empty library and a release nobody claims are argument questions, answerable without an SDK, and a machine with no compiler should still get those answers - the first version of the guard ran first and turned the undeclared-release case from 2 into 3, which the suite caught. **Nothing that reads this tool is affected**: measured, `gates.yml` runs it with a plain `run:` in a job that installs .NET 10, so the guard never fires there and a non-zero still fails the step; and `brain/heron_matrix.py` reads `build/compile-results.json`, whose absence it already handles. `tests/test_fragments_compile.py` is the first suite this gate has ever had and **it never compiles anything** - a compile needs the SDK and minutes, and CI already does it. **3 red** against the module as found, with everything else green before and after: what it reads out of a `fragment.yaml`, the harness parameters generated from the contract, the contract assignment that turns a broken promise into a compiler error, `GC.KeepAlive` so it cannot be optimised away, the ABSOLUTE `#line` path, the record it leaves for HERON-FRG-MTX-009, and both refusals. **Shown to have teeth three ways**: the guard removed (3 red), the typed contract assignment replaced with `var` (1 red), and the `#line` path made repo-relative again (1 red). **AND A GATE CAUGHT THIS SESSION IN THE ACT**: the suite's first draft asserted `"using Autodesk` plus the vendor namespace `in text`, and `check-structure.py` failed the build - *references Autodesk.Revit outside revit/, the adapter boundary is broken*. It greps on purpose and says why: *a vendor namespace named in a COMMENT is still a boundary being discussed in the wrong file*. The check reads `USINGS` from the tool that owns it now and asserts the harness declares every name on it and no others, which is tighter than naming one and types no vendor namespace at all |
| **134** | **THE FRAGMENT COMPILE HARNESS CARRIES A VERBATIM SECOND COPY OF `Directory.Build.props`'s COMPILATION SYMBOLS, AND ITS OWN COMMENT SAYS SO.** Found 2026-09-22 beside row 5b-133. The generated `CSPROJ` in `tools/check-fragments-compile.py` repeats the five `DefineConstants` lines from `Directory.Build.props` word for word, with a note attached: *If Directory.Build.props gains a symbol, add it here too, or fragments and the add-in stop being compiled the same way.* **MEASURED: the two lists are identical today** - same five lines, same conditions - so nothing is wrong, and this is a hazard rather than a defect. **IT LOOKS PROVABLY REDUNDANT, and the evidence is in the file itself**: the generated project is created under `build/fragment-check/` INSIDE the repository, deliberately, *so Directory.Build.props applies*; and the same CSPROJ sets `<TargetFramework>$(HeronTfm)</TargetFramework>`, a property only that props file defines. So the props file demonstrably reaches the generated project, which means it already supplies the symbols and the copy appends them a second time | **OPEN - measured and deliberately NOT fixed, because it cannot be verified from here.** This container has no .NET SDK, so removing the block would be a blind change to the harness that decides whether **396 fragments** compile across eight releases - and a fix nobody could see fail is not a fix this repository accepts. **It is one line of work at a machine with the SDK**: delete the five `DefineConstants` lines from `CSPROJ`, run `python tools/check-fragments-compile.py`, and confirm it still reports every fragment compiling on every release it claims. If it does, the note about keeping two lists in step goes with them. If it does not, the copy is load-bearing for a reason nobody has written down, and THAT is worth knowing and belongs in the comment instead. **Either answer closes this row**; guessing between them from a machine with no compiler does not |
| **135** | THE FILE THAT FORBIDS TWO NUMBERS FOR ONE MEASUREMENT PRINTED THE OTHER ONE. — [full row](../fragment-issues-archive/reading-defects-5b-126-150.md#row-5b-135) | **FIXED 2026-09-22.** |
| **136** | **FOUR TOOLS DECLARE THEMSELVES `Heron-Layer: brain`, AND THE STANDARD NEVER SAYS WHAT THE FIELD MEANS.** Found 2026-09-22 beside row 5b-135. **MEASURED across the repository**: of everything in `tools/`, four files say `brain` - `check-routing.py`, `check-intrusion.py`, `check-skill-routing.py` and `check-risk-crossings.py` - and every other tool says `tool`. `docs/29-metadata-standard.md` lists the six permitted values and **gives no definition of any of them**, and `check-metadata.py` only asks that the value is one of the six. **THE FOLDER IS NOT THE ANSWER EITHER**, measured: every file in `mcp/` says `bridge`, three files in `revit/` say `bridge` too, and three in `tests/` say `revit`. So the field plainly means something other than the folder name - and `tools/check-products.py` is the one place that enforces a relationship between the two, refusing the product manifest if it does not say `platform` because *it lives in platform/*. Four files answer one way, forty answer another, and nothing in the repository can say which is right | **OPEN - measured and deliberately NOT fixed, because the answer is a DEFINITION and that is the owner's to write.** Two readings are defensible and the repository contains evidence for both. (1) **The layer a file BELONGS to**, which makes `brain` right for four tools that measure retrieval and exist to answer questions about the brain - and makes `bridge` right for `mcp/`. (2) **The layer a file IS**, which makes `tool` right for anything in `tools/` whatever it reasons about - and is what `check-products.py` enforces for the one file it checks. **NOTHING IS BROKEN TODAY**: the field is validated only for membership, and `check-structure.py` does its layering from the FOLDER rather than from this header, so no rule turns on the four. What is missing is a sentence in `docs/29-metadata-standard.md` saying which reading governs - and once it exists, either four files change or forty do, which is exactly why guessing it from here would be the wrong kind of tidy |
| **137** | **THE PAGE THAT EXISTS TO REFUSE A PLAUSIBLE ZERO PRINTS ONE OF ITS OWN, ON THE DAY ITS TOP ROW REACHES ZERO.** Found 2026-09-22 reading `tools/balance-of-work.py` - 417 lines, and one of only **two** tools in `tools/` that **no suite names at all**, measured against every file in `tests/`. It is the page a person opens to decide what to do next, and its own helper states the rule it runs on: *A figure a tool did not give back is not a zero.* `statuses()` tallies only the statuses it **finds**, and rows 1 and 2 read that tally with a bare `.get("DRAFT")`. **MEASURED on a library written for the purpose, every fragment `PROVEN`:** row 1 - *"Fragments that have never met a model - the largest single body of work left"* - printed **`not derived` of `3`**, and the console the same. The day the library is finished is the day the page stops being able to say so, and this page tells its reader in its own footer that *not derived* does **not** mean zero. **THIS IS THE THIRD TIME THIS EXACT COLLAPSE HAS HAPPENED IN THIS ONE FILE**, and the first two are commented beside the rows they hit: row 4, where an empty list of unsigned agent proofs was falsy on a board where every draft had in fact been signed; and row 9, where a clean signature board read as an unchecked one. **Both were found on the day their count first reached zero.** Also found: `live_work_notes()` had no missing-file guard, while `register_rows()` and `proposals_open()` beside it both have one - a deleted or renamed `docs/work-notes/README.md` answered `[]`, which the page printed as *"lists no live note"*. **NOT A LIVE FAILURE TODAY** and that is part of the finding: 68 fragments and 10 skills are DRAFT, the index is present, and every figure on the page reads the same before the fix and after it. **TRACED AND NOT RAISED**: the `TOTAL ids, all sections` read cannot walk onto the following line the way `q_ids` once did, because `open-defects.py` prints `(none)` rather than an empty tail. **RECORDED, NOT FIXED**: `--write` is the only flag and an unknown one is ignored in silence - the shape of rows 5b-112, 5b-127, 5b-129 and 5b-132 - but here the page's own generated stamp is what tells a reader it was not rewritten. | **FIXED 2026-09-22.** A `counted(tally, key)` gives the derived zero when something was read and `None` when nothing was, and rows 1 and 2 in both the page and the console go through it; `live_work_notes()` answers `None` for an index it could not read, and the page says *not derived* rather than *none*. `tests/test_balance_of_work.py` is the first suite this tool has ever had: **5 red** against the module as found. **AND THE FIRST TEETH PROOF FOUND THE SUITE TOO LOOSE RATHER THAN THE TOOL SOUND** - the break that makes an unread library report zero left every check green, because each row prints *count of total* and asserting *"not derived" is somewhere on the line* was satisfied by the total half. The suite asserts the count cell on its own now, and the teeth are proved four ways: the derived zero dropped (**4 red**), the *nothing was read* guard removed (**3 red**), the missing-index guard removed (**1 red**), and the page listing itself as outstanding work (**1 red**). |
| **138** | **THE INSTALLER'S OWN FINISH MESSAGE SENT EVERY NEW USER TO A RIBBON TAB THAT DOES NOT EXIST.** Found 2026-09-22 reading `tools/setup.ps1` - 209 lines, and the **last** tool in `tools/` that no suite names at all. It is the first thing anybody runs, and the last thing it prints is what to do next: [`tools/setup.ps1:183`](../../tools/setup.ps1) said ***Ribbon -> Heron AI -> Heron***. **[D-85](../DECISIONS.md) renamed that tab on 2026-09-20** - tab `Heron AI` to `Heron`, panel `Bridge` to `AI Bridge` - and its own consequences section says what a miss costs: *"Eight printed strings moved with it ... **A tab renamed without them is a tool that tells the user to press a button that no longer exists.**"* It listed *"the deploy script"*; **`setup.ps1` was the ninth and it was missed.** **MEASURED FIVE WAYS, ALL AGREEING**: [`HeronApplication.cs:53-54`](../../revit/Heron.Revit.Addin/HeronApplication.cs) declares `TabName = "Heron"` and `PanelName = "AI Bridge"`, under a comment saying *"anything that prints it - the MCP server, the health report, the deploy script - has to agree with them"*; [`platform/heron-products.json`](../../platform/heron-products.json) gives the bridge `"tab": "Heron"`, and D-93 makes that file the one true list; [`tests/test_ribbon_tab_sharing.py:136`](../../tests/test_ribbon_tab_sharing.py) asserts the same; row **Z1** in [NEEDS-CHECKING](../NEEDS-CHECKING.md) is the struck-through observation on a **real Revit** - *"a tab reading Heron, holding a panel reading AI Bridge"*; and [`tools/deploy-addin.ps1:721`](../../tools/deploy-addin.ps1) - **the script `setup.ps1` itself calls** - prints it correctly one step earlier in the same install. Nine files in the repository print that path; **eight were right and this was the one that was not.** **TRACED AND NOT RAISED**: [`docs/07-installation-and-update.md:326`](../07-installation-and-update.md) also says tab **Heron AI**, and it is **right to** - it is a recorded journal entry carrying a note beside it that says the labels changed on 2026-09-20, that a proof is not edited after the fact ([Golden Rule 4](../14-golden-rules.md)), and what a journal written today would say instead. | **FIXED 2026-09-22.** One line: the script now prints the tab, the panel and the button in the order a user meets them. `tests/test_setup_script.py` is the first suite this tool has ever had, and it **cannot run the script** - there is no PowerShell on this container and the script builds C# for a Revit that is not here, which is stated in its own docstring rather than papered over. It asks only the half that is answerable without Windows: whether what the installer SAYS agrees with what the repository DOES. **THE RIBBON PATH IS DERIVED FROM THE ADD-IN'S OWN CONSTANTS AND NEVER TYPED IN THE TEST**, so the next rename fails here until the script follows; the supported release list is read from `brain/heron_dotnet.RELEASES` for the same reason (AJ Tools' L3). **2 red** against the file as found. Teeth proved five ways: the line exactly as found (**2 red**), the add-in renaming its tab with the script left behind (**1 red**), a path the user is told to type stopping existing (**1 red**), a list of years coming back into the code (**1 red**), and a year shown to a user leaving the supported list (**1 red**). **TWO OF THE SUITE'S OWN CHECKS WERE WRONG FIRST AND WERE REWRITTEN RATHER THAN RELAXED**: a negative lookahead that backtracked past the space and so failed on a correct line, and a year count that counted years in comments and examples. **RECORDED, NOT FIXED**: nothing checks that the nine printers agree **with each other** - this suite holds one of the nine. That check belongs with `test_ribbon_tab_sharing.py`, which owns the tab question, and widening it here would be widening someone else's suite. |
| **139** | A SIGNATURE THE PROOF TOOL COULD NOT WRITE WAS RECORDED AS SIGNED, AND THE EVIDENCE WAS DELETED… — [full row](../fragment-issues-archive/reading-defects-5b-126-150.md#row-5b-139) | **FIXED 2026-09-22.** |
| **140** | A SKILL WHOSE DECLARED RISK IS NOT A RISK IS EXEMPT FROM EVERY RISK CHECK, AND THE REPORT NAMES IT… — [full row](../fragment-issues-archive/reading-defects-5b-126-150.md#row-5b-140) | **FIXED 2026-09-22.** |
| **141** | A QUARTER OF THE SWEEP'S QUESTIONS CANNOT PRODUCE THE FINDING IT EXISTS FOR, AND THE HEADLINE… — [full row](../fragment-issues-archive/reading-defects-5b-126-150.md#row-5b-141) | **FIXED 2026-09-22.** |
| **142** | **A DIGEST THAT WILL NOT PARSE WAS READ AS AN EMPTY ONE, PUTTING BACK THE DEFECT THE COMMENT ABOVE IT SAYS WAS FIXED.** Found 2026-09-22 reading `tools/api-changes.py` - 208 lines, and **the last tool in `tools/` that nothing runs**: no suite directly, none through another tool, and not CI. It writes `tools/api-surface/changes.json`, the committed evidence `HERON-REVIT-ACI-034` reads to say which members a Revit release stopped shipping. Its own comment records a real failure already fixed once: *"A TARGETED RUN REFRESHES, IT DOES NOT REPLACE ... until 2026-09-15 it wrote a digest holding that one transition over the committed one holding all seven. HERON-REVIT-ACI-034 then had no transition into 2027 - and read the absence as '2027 removed nothing', reporting every fragment clear."* The fix reads the digest and keeps what it did not recompute - and answered a digest that **does not parse** with `except ValueError: existing = {}`. **MEASURED on the seven-transition shape truncated mid-file**, `api-changes.py 2025 2026` wrote a digest holding **one** transition, **exit 0**, and said nothing. Six went out silently; the same failure through a different door, and D-52 is the rule - a file that cannot be read is not an empty one. A digest that parses but is a JSON **list** was worse still: `AttributeError: 'list' object has no attribute 'get'`, a traceback. **ALSO FIXED, and it is row 5b-133's shape**: `surface_of` raises `NO_ASSEMBLIES`, `NO_READER` and `DUMP_FAILED` with good messages and **nothing catches them**. Measured on this container - no cached assemblies, no .NET SDK - `api-changes.py 2025 2026` gave a **traceback and exit 1**, the code a real failure uses, while *"two releases are needed"* correctly exits 2. So the file already separated an argument problem and had no third state for a machine that cannot answer. **ALSO FIXED, wording**: the comment above `releases()` said *"tools/check-api-surface.py owns which releases are supported ... Bound rather than retyped so one list stays one list."* It is bound to `heron_fragment.REVIT_VERSIONS`, and **measured, `check-api-surface.py:63` types its own `ALL_VERSIONS`** - so there are three lists, not one. All three agree today; what was wrong was a sentence telling the next reader a binding exists where it does not. | **FIXED 2026-09-22.** A digest that will not parse, or that parses to anything but a mapping, is **refused** - the run writes nothing, names the file and why, and returns 1; **measured, the corrupt file is left byte for byte as it was found**. A `cannot_answer(years)` prints `NOT RUN - ...` and returns **3**, the code `tests/README.md` gives a suite that could not run and states plainly is not a pass. It is asked **after** the argument question, so *"two releases are needed"* keeps its exit 2, and it works out what still needs dumping **first** - a release whose surface is already on disk needs neither the assemblies nor the SDK. The `releases()` comment now names what it actually binds to. `tests/test_api_digest.py` is the first suite anything has pointed at this tool - `tests/test_api_changes.py` is the suite for the **agent** that READS the digest, a different subject - and it **never dumps a surface or compares two releases**, which would need the network, 264 MB of reference assemblies and a .NET SDK. **5 red** against the module exactly as found. **THE SUITE CRASHED TWICE ON ITS OWN CASES BEFORE IT WAS RIGHT**, and both were the suite's fault: it parsed the corrupt digest the refusal is supposed to leave alone, and it let the tool's `AttributeError` out instead of naming it. A raise is caught and reported as the result now, so an error reads as a failure rather than killing the run (`heron-ship` section 2a). Teeth proved six ways: the module exactly as found (**5 red**), the corrupt-digest refusal alone (**4**), the NOT RUN guard alone (**3**), the guard firing even where the surface is already dumped (**5**), the merge no longer keeping what it did not recompute (**3**), and `members()` no longer trimming (**1**). **AND A GATE CAUGHT THIS SESSION IN THE ACT, for the second time**: the suite's surface fixture typed the real Revit vendor namespace as a member name and `check-structure.py` failed the build - *"a vendor namespace named in a COMMENT is still a boundary being discussed in the wrong file"*. What is under test is trimming and de-duplication and the text of the member has nothing to do with either, so the fixture names a neutral namespace. **The gate was right; the test was wrong** - the same verdict as row 5b-133. **Needs no network**: `tools/api-surface/changes.json` was never written to, checked with `git status`. |
| **143** | **THE RESTORE CARRIED ON AFTER A SAFETY COPY IT COULD NOT TAKE, AND THE TOOL SAID IT HAD TAKEN ONE.** Found 2026-09-22 reading `tools/heron-backup.py` - 454 lines, the tool that moves a person's own data. **FOUR findings, all measured, all in `restore` and `drill`.** **ONE.** `restore()`'s docstring makes one guarantee: *"IT TAKES A SAFETY COPY FIRST, ALWAYS ... the thing it destroys is the only copy of what was on the machine a second ago. If the backup turns out to be the wrong one, the state it replaced has to still exist."* `safety = take(...)` and **nothing checks the answer**. `take()` returns `None` when a backup of that label already exists, and the label is a timestamp **to the second** - so two restores inside one second gave the second one *"A backup called X already exists. Nothing was written"* and **it overwrote anyway**. A backup root that cannot be written raised `NotADirectoryError` straight out of `restore`. **TWO.** The safety copy was taken from the **data root**, not from what was about to be overwritten. Measured restoring into another folder: it copied the data root - a folder that was never at risk - destroyed the other one, and printed *"The state it replaced was copied to ... first"*, **which was false**. Not reachable from the command line, where only `drill` passes `into` and its landing is always fresh; a reassurance that can be false is worth fixing before it is reachable. **THREE.** A restore is a **merge**, and nothing said so. Measured: a file the backup never held was left in place while the tool printed *"Restored 2 file(s)"* and nothing else. The behaviour is **right** - deleting a person's files is the dangerous direction, and an append-only audit log wants the extra entries - but somebody restoring **because the folder is wrong** is left believing it now matches the backup. **FOUR.** `drill` on a machine where Heron has written nothing exited **1**, the code a broken restore path uses. `docs/21` section 8 asks for *a periodic automated drill*, so that reading goes to a schedule, not to a person. | **FIXED 2026-09-22.** `take()` takes a `source`, defaulting to the data root; the safety copy is of `target`, its label carries microseconds, and **a safety copy that returns `None` or raises stops the restore** with the reason and nothing changed - refusing loses nothing, since the backup is still there. The manifest note and the restore output both say a restore **removes nothing**, in those words. `drill` answers `NOT RUN` and **3** when there is nothing to drill - the same remedy as rows 5b-133 and 5b-142, and the third time this session that a tool had no third state. **`tests/test_backup.py` ALREADY OWNED THIS TOOL and is a good suite** - the byte-for-byte round trip, the excluded derived index with its reason, the unanticipated file kept anyway, verify by content, both refusals, the safety copy and the drill. It covered none of these four, so it was **extended rather than duplicated**: a second backup suite would be two suites for one tool. **5 red** against the module as found. **AND CASE 9 FOUND FINDING ONE BY ACCIDENT**: it took no safety copy at all, because the case before it had already taken one in the same second. The case that was meant to measure where the copy comes from measured that there was none - which is why a suite is proved by breaking the thing it guards. Teeth proved five ways: the module as found (**5 red**), the refusal on a failed safety copy alone (**2**), the safety copy's source alone (**1**), the NOT RUN drill alone (**2**), and the derived index no longer excluded (**4**). **Run end to end on a fixture**: backup, a stray file, restore - the stray survives and is named; `drill` exits **3** with nothing and **0** with data. Nothing under a real `%APPDATA%` was touched. |
| **144** | THE SCAFFOLDER THAT EXISTS TO STOP HEADER MISTAKES WROTE `Heron-Step: banana` INTO TWO FILES AND… — [full row](../fragment-issues-archive/reading-defects-5b-126-150.md#row-5b-144) | **FIXED 2026-09-22.** |
| **145** | **THREE AGENTS ARE COUNTED AS BUILT ON THE STRENGTH OF A TEST SUITE'S HEADER, AND THE TWO TOOLS THAT ANSWER "IS IT BUILT" DISAGREE.** Found 2026-09-22 reading `tools/generate-contract-reference.py`. **The tool itself is sound and this is what it correctly REPORTS** - on every run, in its own words, and nothing records it. **THE DISAGREEMENT, MEASURED.** `tools/generate-contract-reference.py` separates them in terms: *"A SUITE IS NOT THE AGENT. tests/ files carry the same `Heron-Agent` header - which is right, it is how a suite says what it proves - ... It is just not read for what the agent does."* `tools/agent-count.py`, which **owns the count**, lists `"tests"` in its `SOURCE_ROOTS` and makes no distinction: any file claiming an agent makes it BUILT. **THE THREE**: `HERON-DEV-INT-012` claimed only by `tests/test_bridge_roundtrip.py`; `HERON-RAG-DUP-012` and `HERON-RAG-RIX-011` claimed only by `tests/test_maintenance.py`. Confirmed by calling `agent-count.built()` directly - all three come back **built**, with a suite as the only claimant. **WHAT IT MOVES.** `agent-count` reports `0 outstanding`, `docs/28-agent-registry.md` publishes **Totals: 250 agents**, and `tools/balance-of-work.py` row 3 reads **0 of 250 agents left to build**. If a suite-only claim is not a build, that zero is wrong by three. **ONE OF THE THREE IS ALREADY THE OWNER'S AND IS NOT NEW**: `DEV-INT-012` is named in [`docs/DECISIONS.md`](../DECISIONS.md)'s F27 table - *"tests/test_bridge_roundtrip.py and tests/Heron.Bridge.TestHost fit the row exactly"* - and that decision closes two of four and says **"the other two are unchanged and are the owner's"**. **THE OTHER TWO ARE NOT RECORDED AS A DEFECT ANYWHERE**, and the plan notes say what the code does not: `docs/work-notes/plans/rag/02-implementation.md` gives `RAG-RIX-011` as *"Re-index on change. The hashing exists; nothing fires it"* and `RAG-DUP-012` as duplicates at write time, both marked for Stage 6. **THIS IS ROW 5b-95's SHAPE A SECOND TIME**: that row exists because the same page *"ALREADY REPORTS THESE ON EVERY RUN and nothing records them, so they are re-discovered and never closed"*. This is the other half of the same page's output, and it had the same fate. **RAISED, NOT FIXED, AND IT IS A DEFINITION**: does a claim by a suite alone count as built? Changing `agent-count`'s `SOURCE_ROOTS` would move a figure in `docs/28`, in `agent-count`'s own reconciliation and in the balance page, and guessing which way is the wrong kind of tidy. **The owner writes the sentence; the two tools then follow it.** | **OPEN - the owner's.** Neither tool was changed. `tools/generate-contract-reference.py` is marked **clean** in the review ledger: it is right, and it is the only thing in the repository that says this out loud. |
| **146** | TWO GENERATORS TYPE A COUNT THEY DERIVE CORRECTLY ONE LINE LOWER, AND BOTH TYPED COUNTS ARE STALE. — [full row](../fragment-issues-archive/reading-defects-5b-126-150.md#row-5b-146) | **FIXED 2026-09-22.** |
| **147** | THE API PAGE'S ONE LIVE FINDING WAS FALSE, BECAUSE A PARAMETER NAMED IN CAPITALS READ AS… — [full row](../fragment-issues-archive/reading-defects-5b-126-150.md#row-5b-147) | **FIXED 2026-09-22.** |
| **148** | ELEVEN OF THE FOURTEEN QUESTIONS REPORT `0`, AND THAT ZERO MEANS THREE DIFFERENT THINGS. — [full row](../fragment-issues-archive/reading-defects-5b-126-150.md#row-5b-148) | **FIXED 2026-09-22.** |
| **149** | THE DISPATCH RULE STORED THE DICT KEY, AND THE LOOKUP ASKED BY FUNCTION NAME. IT NEVER ONCE… — [full row](../fragment-issues-archive/reading-defects-5b-126-150.md#row-5b-149) | **FIXED 2026-09-22.** |
| **150** | **EVERY WAY IT CANNOT ANSWER EXITS 1, THE SAME CODE A MISSING REVIT MEMBER USES.** Found 2026-09-22 reading `tools/check-api-surface.py` - 223 lines, never read before, and the tool that answers *does Revit 2020 actually ship every API member Heron calls*. Its answer matters: `Document.CreationGUID` compiled clean on 2024, does not exist on 2020, and had survived several readings. **MEASURED on this container, which has no `dotnet` on PATH:** `python tools/check-api-surface.py 2020` raises `FileNotFoundError` straight out of `subprocess.run` and exits **1** - a traceback, which is none of AGENTS.md's four states. **The same door is open twice more**, read rather than run: no network gives `if not dirs` and *"FAIL no reference assemblies - is the network reachable?"* at exit 1, and a machine with no built reader gives exit 1 as well. **So a reader or CI cannot tell *Heron calls an API Revit 2020 does not have* from *this machine has no .NET*** - and the first is a cross-version defect while the second is nothing at all. This is the fourth tool this session with no third state, after rows 5b-133, 5b-142 and 5b-143, and their remedy is settled: a `cannot_answer()` asked after the argument check and before any build, printing NOT RUN and exiting **3**. **WHAT IS NOT WRONG, TRACED AND NOT RAISED:** `ALL_VERSIONS` is typed here rather than read, which looks like the third release list beside `heron_dotnet.RELEASES` and `heron_fragment.REVIT_VERSIONS` - but `tests/test_supported_releases.py` finds all five declarations and pins each against the one home, naming `tools/check-api-surface.py:63 ALL_VERSIONS` by line, and **PROPOSALS.md F2 already holds the merge question for the owner**. It is a guarded duplication with a recorded reason, not a defect, and binding it here would have pre-empted his decision. `--no-build` already behaves well: a clean FAIL at exit 1 that names the exact build command. | **OPEN - recorded, not repaired.** The sweep's own rule at the head of this section: a file found wrong is marked `issue` in the ledger and written up here, and the file is left alone. The remedy is named above and is three other rows old, so it needs no design - it needs a sitting with the suite. **There is no suite that owns this tool**: `tests/test_supported_releases.py` pins its release list and `tests/test_deploy_script.py` checks the path it names, but nothing exercises `main()`, which is why the missing third state was never caught. **Needs no Revit**, and the fix can be proved on this container precisely because `dotnet` is absent here - the failing case is the default one. |
