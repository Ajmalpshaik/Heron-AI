# Fragment issues — section 5, rows 151 to 175

> Rows 151 to 175 of section 5 of [the register](../FRAGMENT-ISSUES.md), in their own file since 2026-09-23 so that
> they can be read alone. **The section's own rules are on that page, under its heading.** A new
> row goes at the end of the section's last file, whatever its number, and the next run of
> [`tools/split-register.py`](../../tools/split-register.py) moves it to the file its number
> belongs to. [`tools/register-text.py`](../../tools/register-text.py) reads every row back into
> the register for every tool that reads it, so a row here is seen exactly as it was seen there.

| # | Defect | State |
|---|---|---|
| **151** | **D-53 TRACKING HAS BEEN JUDGED FOR A FRAGMENT SINCE THE DECISION WAS WRITTEN, AND NOTHING HAS EVER PRODUCED ONE.** Measured 2026-09-19. `brain/heron_validate.py` reads `record["tracking"]`, refuses fewer than three rows in its own words - *"the tracking set has only %d row(s). D-53 asks for the answer to follow the input across SEVERAL different inputs; two cannot show that"* - and refuses rows that all came back the same, *"which is what tracking exists to rule out"*. **`grep -n tracking mcp/client/heron_bridge_client.py` returns NOTHING.** No command writes those rows, so a `record["tracking"]` could only ever be typed by hand. **The judging half was built and the producing half was not**, and the gap stayed quiet because the fragments that need it are exactly the fragments nobody could prove. `tools/prove-agent.py vary` is the working example one layer up and has done this for an ADD-IN AGENT since 2026-09-19; the fragment side had no equivalent. **[docs/NEEDS-CHECKING.md Group W](../NEEDS-CHECKING.md) found the same gap from the other end**, reading the skills' own job files, and called it *"THE HIGHEST-LEVERAGE THING LEFT IN THE PROVING MACHINERY"*: **three fragments - `trace-connectivity`, `report-findings` and `describe-blank-parameters` - stand between three skills and three more proofs, and all three are shapes `batch-prove`'s rule cannot judge** | **OPEN - HALF BUILT 2026-09-19, and the state cell says OPEN on purpose.** `tools/open-defects.py` counts a row by whether its state STARTS with the word, and the runs are genuinely still owed - a cell reading only *HALF BUILT* would drop this out of the sweep while the unfinished half sat there, which is the shape [row 118](../FRAGMENT-ISSUES.md#) is about. **Which half is built is the whole point.** `tools/prove-tracking.py` does every check that can be made **before a model is opened**, and `--dry-run` answers completely: a fragment already at PROVEN, a need that is not `source: request`, a shape D-54 cannot receive, fewer than three **DISTINCT** values, another caller value not held still, and a `--set` the fragment does not declare (which is [row 71](../FRAGMENT-ISSUES.md#)'s rule applied one step earlier). **THE JUDGEMENT IS IMPORTED, NOT RE-DECIDED** - `contract_of`, `looks_empty` and `_as_count` come from `heron_validate`, the module that reads the record back, because two copies of *what counts as an answer* is how a runner and a drafter start disagreeing about the same fragment on the same day. **WHAT A FRAGMENT HAS THAT AN AGENT HAS NOT DECIDES THE DESIGN**: `prove-agent.py vary` collects every number in the reply because an agent has no contract saying which is the answer; a fragment's `provides` does, so this tracks the DECLARED result and refuses to guess when more than one is declared. **ALL THREE OF GROUP W's FRAGMENTS ARE NOW ARRANGEABLE, PROVED IN `tests/test_prove_tracking.py`** - `trace-connectivity` varying `tolerance` with `start=selected` held still, `report-findings` varying `whatWasChecked` with `checkedCount` held, `describe-blank-parameters` varying `parameterName`. **AND GROUP W IS TOO PESSIMISTIC ABOUT ONE OF THEM**: it reads `describe-blank-parameters` as having *"nothing for the rule to look at"* because it declares no explicit `role:`, and `heron_fragment.provide_role` treats an absent `role:` as **`result`** - the stricter read - so `findings` is trackable. **THE RUNS ARE BUILT TOO, AND EXACTLY ONE FUNCTION IS UNEXERCISED.** `live_runner` resolves the source and contract, opens one bridge and sends **`cmd_prove`'s own call** - same operation, same three arguments, same `chain: "reset"` on the first and not after, same timeout - because that call has run against real models for weeks and a second opinion about how to talk to the executor is the last thing this file should contribute. With no Revit it refuses cleanly: *"NOT RUN - No Revit is connected. Press Heron on the ribbon to connect first."* **A run that cannot pass `judge()` WRITES NOTHING** - a record that looks like a proof and is not is worse than having none - and one that passes is written where `heron_validate` reads it, which then asks the same two questions again before a person signs. **`live_runner` is the one function no test here can reach**, and the tool's own docstring names it rather than leaving a reader to find out. **It never signs and never promotes**, and tracking is not an easier route past D-30: it is the substitute only where no arrangement makes the answer empty, which is D-53's whole condition **THE RUN LOOP IS BUILT TOO, AND IT IS PROVED ON A FAKE RUNNER.** `run_rows()` takes the runner as an ARGUMENT, so the loop, the refusal handling and the row building are all exercised on a machine with no Revit - `tests/test_prove_tracking.py` passes a fake that returns known replies. **The only line left unproved is the bridge call itself**, which is the same call `cmd_prove` already makes. It also proves the rule `tools/prove-agent.py vary` states in one sentence and this inherits: **a refused value is not a tracking row** - it shows the fragment never looked, not that it looked and found nothing - so refusals are reported, do not count toward the three, and a set left short by them says so rather than passing. **AND A WRITE IS NOT TRACKED DOWN THE READ PATH.** `write_path_refusal()` refuses anything at or above the write threshold, and the threshold is **read from `HeronOperationRegistry.cs`** through `generate-jobs.write_threshold()` rather than typed - Golden Rule 19, where risk is looked up by name and never supplied by a caller. Two reasons, both already in this register: a write varied three ways changes the model three times, and D-53 has no equivalent of the `TransactionGroup` `validate` holds; and sending a write down the read path is [defect row 6](../FRAGMENT-ISSUES.md#), which cost half a morning by arriving silently. A risk that is not a level HeronRisk names is refused rather than assumed safe. Verified: `set-view-scale` (MODIFY) is refused with that reason, `report-findings` (SUGGEST) is sent **DEFERRED BY THE OWNER 2026-09-22**, at the close of the session that found it: not now, later, and he said later is better. It stays OPEN because the defect is still in the file - **open here means the defect exists, not that somebody is part-way through it.** Nothing is blocked and nothing was left half-changed; the tool is exactly as it was found. Pick it up when he asks. |
| **152** | BOTH TOOLS THAT HANDLE A ROUTING RECORDING PRINTED THE STALENESS FINGERPRINT AND COMPARED NOTHING… — [full row](../fragment-issues-archive/proving-defects-151-175.md#row-152) | **FIXED 2026-09-19, and the comparison is made against the PHRASES rather than the store.** |
| **153** | **A ROW FIXED BY APPENDING TO ITS STATE CELL GOES ON TELLING THE SWEEP IT IS OPEN, AND NOTHING SEES IT.** Found 2026-09-19 while reading `tools/open-defects.py`'s own output. [Row 127](../FRAGMENT-ISSUES.md#) was fixed the same day and its state cell was written by putting **FIXED 2026-09-19 BY THE FIRST OF THE TWO ROUTES THIS CELL NAMES** *underneath* the sentence that said OPEN rather than in place of it. `open-defects.py` reads the FIRST word of the cell - the register's own convention, and [row 151](../FRAGMENT-ISSUES.md#)'s cell states it explicitly as the reason a half-built row must still say OPEN - so the row counted as open while its own cell said it was fixed. **The count said 36 and the truth was 35, and the list said `127` beside a paragraph explaining how it was repaired.** It is the shape [row 118](../FRAGMENT-ISSUES.md#) is about, one layer up: not a wrong number, a number and a sentence in the same document disagreeing with nobody to notice. Rows 71, 112 and 15 - all closed the same day, by the same session - open with their state word and were never wrong; **this one was the odd one out, which is exactly why no reader caught it** | **FIXED 2026-09-19, in two parts, and the second is the one worth having.** (1) Row 127's cell now OPENS with `FIXED`, and everything it said while open is kept in full underneath, labelled - the paragraph about the totals line being a live fingerprint of WHICH TREE the server reads is worth more than the defect, and deleting it to tidy the cell would have been the real loss. Nothing was dropped. (2) `open-defects.py` now ASKS about a state that begins with *open* and carries a **dated** `FIXED` or `CLOSED` claim further down the same cell. **THE TEST IS NARROW ON PURPOSE AND WAS MEASURED BEFORE IT WAS KEPT**: a dated claim in capitals, inside one sentence (`[^.]{0,40}?`). Eleven open rows mention something else being fixed or closed - *"7 of 14 fixed"*, *"two of the four it names are CLOSED"*, *"it was fixed in `RENAME_PHASE`"* - and **every one of them is genuinely open**; a detector that flagged them would be eleven false questions a reader learns to skip, which is how a report stops being read. Run against every row in the register: **zero false hits, and it fires on row 127's cell as it actually stood**. **IT ASKS AND NEVER RE-COUNTS** - which sentence is the state is a reader's judgement, and a tool that guessed would start closing rows; the open count is identical with and without a contradicting row, which `tests/test_open_defects.py` pins. **The suite reports the real register and does NOT fail on it**, because a finding is a question - the rule every report in `tools/` is written to - and it reconstructs row 127's old cell in its own source rather than reading it, since the register has been repaired and a suite that read it would stop testing anything the moment it was. **WHAT IS NOT FIXED:** a row whose state says OPEN when a LATER row closed it, which the tool has always said in its own output it cannot see - the closure is written in a different row, in prose, and four of those were found by hand on 2026-09-16. Reading still beats grepping here |
| **154** | TWO SWEEPS OVER THE SAME STORE EACH KEPT THEIR OWN COPY OF THE FINGERPRINT BLOCK, AND FOUR PLACES… — [full row](../fragment-issues-archive/proving-defects-151-175.md#row-154) | **FIXED 2026-09-19, in the direction the repository already runs.** |
| **155** | **A TEST SUITE BREAKS 55 REAL `fragment.yaml` FILES FOR TWELVE SECONDS OF EVERY `check-gaps` RUN, AND A `finally` IS NOT A PROMISE A KILLED PROCESS KEEPS.** Reported by the session validating the Windows install (PR #200), which watched `git status` go from 6 modified files to 61 and back while `tools/check-gaps.py` was running. **REPRODUCED EXACTLY**, 2026-09-19: a poller sampling `git status --porcelain -- brain/fragments` caught the window and listed the same 55 cards, `select-with-warnings`, `select-without-level` and `ungroup-elements` among them. **THE WRITER WAS FOUND BY A SWEEP, NOT BY A GREP**, and that is the part worth keeping: a static search for anything writing `brain/fragments` finds nothing, because the write is one `io.open(path, "w")` inside a closure over paths that are derived rather than spelled. So every suite under `tests/` was re-run under a `sys.addaudithook` logging every write beneath `brain/fragments/` - no total typed here, because this register has been wrong about that one twice. **EXACTLY ONE SUITE WRITES THE REAL LIBRARY: `tests/test_graph.py`, in `break_providers()`.** It has to show the deriver catching a broken contract, so it renamed `- name: elements` in the `provides:` block of every one of the 55 fragments that provide it, asked the graph, and wrote the originals back. **Measured: 67s for the suite, the real library wrong for 11.9s of it - 18% of the run** - sitting inside a sweep that takes about eight minutes. **TWO THINGS WERE WRONG AND ONLY ONE OF THEM WAS ABOUT EXCEPTIONS.** The `finally` was already there and already correct, so every path that RETURNS puts the files back. But `check-gaps.py` runs each suite through `subprocess.run` with `SUITE_TIMEOUT = 300`, and a timeout **kills** the child; so does Ctrl+Break, and so does the machine. `finally` runs through none of them, and what survives is 55 fragments whose contract says `somethingElse`, with nothing on disk saying why. **AND NOTHING HAS TO CRASH AT ALL**: several sessions share this checkout and its worktrees, and one that runs `git add -A` inside those 11.9 seconds commits 55 `fragment.yaml` changes nobody made and is told nothing. That is [ROW 131](../FRAGMENT-ISSUES.md#) one layer out - a shared thing left as whichever process got there last wrote it - except that here the shared thing is the repository itself. **THE HOUSE PATTERN WAS ALREADY IN THE SAME FOLDER**: the same sweep shows `test_docs_guard` and `test_metadata_guard` each copying the whole repository into the system temp folder and breaking THAT, 1,185 files apiece. One file did not follow it. **THE FINGERPRINT WAS NOT INVOLVED**, which was the first guess and is worth recording as excluded: `heron_fragment.fingerprint()` only reads, `proof_is_stale()` only compares, and the one thing that rewrites a `fingerprint:` line in a real card is `heron_validate.restamp(apply_changes=True)`, reachable only from `python brain/heron_validate.py restamp --apply` - no test and no checker calls it, and the sweep logged not one write from it. **ONE OTHER SUITE TOUCHES A REAL CARD AND IS LEFT ALONE**: `test_embed.py` calls `os.utime` on `set-selection/fragment.yaml` to prove indexing is content-hashed rather than timed. mtime only, no content, git sees nothing, and it is the whole point of that test. | **Fixed 2026-09-20 - `tests/test_graph.py` and `brain/heron_graph.py`.** The break now happens in a throwaway copy: `stand_in_library()` writes all 395 `fragment.yaml` into a root under the system temp folder in 2.0s, and nothing in the suite opens a file under `brain/` for writing. It could not have been done without the second change - `orphans()` was the one deriver with no way to be pointed at a library, so breaking `brain/fragments` itself was the only way to ask it anything. It now takes `fragments=`, the way `composes_into()` and `composes_from()` already did, and both existing callers are untouched. **Copying only `fragment.yaml` and not the `impl/` trees is a bet on what `load()` reads, so the suite checks it rather than assuming**: the copy must hold the same 395 fragments with the same 0 problems, and `brain/fragments` must answer `composes_into` identically, before anything is broken. **Re-measured: 58,336 reads of a watched card across a 63s run, never once broken**, against 11.9s before. **Worth settling, and NOT fixed:** nothing in this repository would have caught it. No gate runs the suites and then asks whether the tree changed, which is the check that turns this whole class - a test that edits the repository it is testing - from something a person happens to notice into something that fails **Written as 142, which `trace-system` had already taken in #199, and renumbered on merge - [ROW 149](../FRAGMENT-ISSUES.md#) counted four of these collisions in one day and this is the fifth. The register's own hazard is not incidental to this row: it is the same shared thing, written by whoever got there last** |
| **156** | **A RULE THE REPOSITORY HAS ENFORCED SINCE IT WAS WRITTEN, AGAINST A SHAPE NO FRAGMENT COULD EVER ARRIVE IN.** Found 2026-09-20 by reading [D-53](../DECISIONS.md)'s two halves against each other. `heron_validate` has judged a fragment TRACKING set all along: `draft_from_record` reads `record["tracking"]`, writes the negative text itself, and `_evidence_refusal` turns down fewer than three rows in those words - *"D-53 asks for the answer to follow the input across SEVERAL different inputs; two cannot show that"* - and turns down a set whose rows all came back the same. **NOTHING HAS EVER WRITTEN ONE.** The only producer of `tracking` anywhere is `tools/prove-agent.py`, and it writes it for an AGENT. So the judging half was complete, the running half did not exist, and the rule stood over an empty room. **WHAT IT COSTS IS NAMED IN [NEEDS-CHECKING Group W](../NEEDS-CHECKING.md)**: `trace-connectivity`, `report-findings` and `describe-blank-parameters` are unprovable as arranged and between them block THREE skills - and all three are this shape, a fragment that cannot come back empty. `trace-connectivity` seeds `reached` with its own `start`; `report-findings` answers with prose and its negative is CORRECT; `describe-blank-parameters` is the only fragment of 395 declaring no `role: result` at all | **BUILT 2026-09-20 AND DEMONSTRATED ON A MODEL, AND IT IS AN ADDITION RATHER THAN A MECHANISM** - the mechanism was already there. `validate --vary NAME=a,b,c --vary-field FIELD` runs the fragment once per value on ONE model, records what the named field came back as each time, and writes `tracking` into the run record that `heron_validate` already knows how to judge. **MEASURED END TO END on `test projject`, Revit 2024, session 36216**, against `filter-elements-by-category` varying `category`: |
| **157** | THE RETRIEVER HAS BEEN SAYING "NO ROUTE PREFERRED THIS" IN PLAIN WORDS AT THE SEAM EVERY SWEEP… — [full row](../fragment-issues-archive/proving-defects-151-175.md#row-157) | **FIXED 2026-09-19, by READING what was already there rather than deriving anything.** |
| **158** | **TEN QUESTIONS ARE DECLARED, IN WRITING, ON FRAGMENTS THAT CHANGE THE MODEL - AND NEITHER ROUTING SWEEP CAN SEE ONE OF THEM.** Found 2026-09-19 while checking whether [row 146](../FRAGMENT-ISSUES.md#)'s four crossings had a READ to give them to. **THE CASE THAT STARTED IT:** *"what scale is this view"* resolves to `SET_VIEW_SCALE`, a **MODIFY**, by route **`identity`** - and `brain/fragments/set-view-scale/fragment.yaml` **line 127** declares that exact phrase in its own `utterances:` block. It is not a ranking accident and **no re-ranking repairs it**: identity short-circuits before ranking runs. **IT FALLS BETWEEN THE TWO SWEEPS BY CONSTRUCTION.** `check-routing.py` asks each fragment's own utterances back to the search and separates *a sentence a READ claims, answered by something that WRITES* - and it is blind here for **TWO INDEPENDENT REASONS, both read off its source rather than inferred**. Its first test keeps a row where `rung(risk[winner]) > rung(risk[claimer])`; a fragment declaring its OWN sentence and winning has winner == claimer, so the comparison is false and there is no crossing by construction. Its second opens `if risk_of(store, fid) != "READ": continue`, so a sentence claimed by a **MODIFY** is skipped on the first line - and two lines later it says `if served == fid: continue` with the belief written beside it: **`# the host answers correctly`**. That comment is the whole defect in four words. It is right about every case the tool was built for and wrong about this one, and nothing in the file is at fault - the question was never asked of it. `check-risk-crossings.py` asks sentences **nobody declares**, precisely because the first can only test what is declared - so a declared one is not in its list. **Checked rather than assumed: of its 78 questions, ZERO are among these ten.** And the near-miss is the whole story. Its closest sentence to *"what scale is this view"* is **"what is the scale of this view"** - five words shared, one phrasing apart - and [row 146](../FRAGMENT-ISSUES.md#) reports that one crossing to `SET_VIEW_SCALE` and calls it a **capability gap**, which it also is. **So two sweeps found two halves of one defect from opposite sides, a week apart, and neither could see the other half.** The hand-written phrasing crosses because nothing READ answers it; the declared phrasing crosses because the WRITE claims it in its own `utterances:` block. The first needs a new capability. The second needs one line deleted from a card. A reader given only row 146 would have built the fragment and left the declaration in place, and the sentence Ajmal actually says would have gone on landing on a write by identity, which no new READ can outrank. **It is [row 137](../FRAGMENT-ISSUES.md#)'s blind spot one layer down**, and like that one it needs no search at all to find: it is a property of the card. **ALL TEN, SWEPT OVER THE LIBRARY**: `SET_VIEW_SCALE` *"what scale is this view"*; `SET_VIEW_RANGE` *"what is the view range"*; `SET_PRINT_SETTINGS` *"what paper sizes can i print"*; `SET_DATUM_EXTENT_TYPE` *"is this grid 2d or 3d here"*; `ASSIGN_LOCATION_DATA` *"which room is each device in"*; `EXTRACT_CAD_CURVES` *"what layers are in this cad file"* and *"...in this dwg"*; `DIMENSION_FAMILY_INSTANCES` *"how far apart are these on the drawing"*; `DIMENSION_MEP_RUNS` *"how far apart are these pipes on the drawing"*; and `REPLACE_MATERIAL` *"why will this material not purge"*. **TWO ARE WORTH A SECOND LOOK RATHER THAN A REPAIR**, which is why this reports and does not judge: the DIMENSION pair say *on the drawing*, which is arguably an instruction to annotate; and `REPLACE_MATERIAL`'s is a CONSIDERED DECISION with its reasoning in a comment above it - it was moved there in 2026-09-06 because it had been resolving to `FIND_UNUSED_MATERIALS`, *"which lists materials with NO user, and therefore cannot say what is still holding on to THIS one"*. That comment is right about the routing and the sentence is still a question answered by a write; **the repair it actually names is a READ that says what still uses a material**, which nothing provides | **OPEN - TEN FINDINGS, THE SWEEP THAT FINDS THEM IS BUILT, AND NOTHING IN `brain/fragments/` WAS EDITED. Found 2026-09-19.** The state says OPEN on purpose and `tools/open-defects.py` counts a row by its first word: the ten sentences are still declared on writes today, and a cell opening *the sweep is built* would drop this out of the sweep while the unrepaired half sat there - which is [row 118](../FRAGMENT-ISSUES.md#)'s shape and [row 153](../FRAGMENT-ISSUES.md#)'s. **WHAT IS BUILT:** `tools/check-declared-questions.py` reads the FILES through `heron_fragment.load_all()` - **never the store**, so it holds in CI where there is none and two runs disagree only if somebody edited a fragment ([rows 116](../FRAGMENT-ISSUES.md#) and [136](../FRAGMENT-ISSUES.md#)). The write line is read from `HeronOperationRegistry.cs` through `generate-jobs.write_threshold()` - Golden Rule 19, imported and not copied, because that function REFUSES to guess when the registry is rearranged - and a risk `HeronRisk` does not name is **reported rather than assumed safe** (D-52). It is now in `gates.yml`'s *reports* job beside the other three, so it runs on every pull request instead of when somebody remembers. **AN IMPERATIVE IS NOT A QUESTION, AND THE PATTERN WAS CORRECTED BEFORE IT LANDED**: the first version included `do` and `did` as auxiliaries and filed *"do the grayout"* as a defect on the fragment that correctly owns it. Both cases are pinned in `tests/test_declared_questions.py`, and a sentence that asks and **then says what to do** - *"which elbow this type inserts, change it"* - is listed separately and never counted, which is the separation `check-risk-crossings.py` already makes for imperatives. **EVERY SENTENCE IN THAT SUITE IS A REAL DECLARED UTTERANCE**, not an invented one: an invented sentence proves only that the pattern matches itself. **IT NEVER SUGGESTS DELETING AN UTTERANCE TO TIDY THE REPORT** - [row 113](../FRAGMENT-ISSUES.md#)'s forbidden move is weakening a declaration to buy a number, and deleting a sentence a modeller really says so a sweep comes back clean is the mirror of it. **NOTHING IN `brain/fragments/` WAS TOUCHED**: the ten are a finding for whoever owns those files, and the repair for each is one line - declare the sentence on the READ that should own it - **except where no READ exists**, which is [row 146](../FRAGMENT-ISSUES.md#)'s capability gap and a larger thing. **AND ROW 146's ENUMERATION IS RIGHT WHILE THE CONCLUSION DRAWN FROM IT IS TOO STRONG** - measured the same way, and the distinction matters because the repair is different. That row lists the capabilities with SIZE, WORKSET, INSULATION or SCALE **in their names** and says truly that not one of them reports the value. [HANDOVER](../HANDOVER.md) then carries it as *"four have no READ to give the sentence to at all"*, and **that does not follow from an enumeration by name**: `READ_ELEMENT_PARAMETERS` (FRG-PAR-001, READ, PROVEN) reads *one named parameter off every element* and **already declares the utterance `get the size parameter`** - a READ that claims size-reading in its own words and has SIZE nowhere in its name. **WHAT IS MEASURED AND WHAT IS NOT:** all four sentences still land on a MODIFY today, asked through `heron_brain.lookup` - `AUTO_SIZE_MEP`, `SET_VIEW_WORKSET_VISIBILITY`, `SET_MEP_INSULATION` and `SET_VIEW_SCALE`. Whether `READ_ELEMENT_PARAMETERS` can actually answer each of them is a **Revit question this container cannot settle** - insulation is a separate element wrapping its host, which `SELECT_BY_INSULATION`'s own purpose warns about, so reading the duct's parameters would not give the thickness. **View scale is the one that is certainly a capability gap**: `SET_VIEW_SCALE` is the only capability in the library with SCALE in its name, and it writes. **AND TWO NEIGHBOURING SWEEPS WERE RUN AND BOTH CAME BACK CLEAN, WHICH BOUNDS THIS ROW RATHER THAN WIDENING IT.** (1) A capability NAMED like a read - `CHECK_`, `REPORT_`, `LIST_`, `FIND_`, `READ_`, `MEASURE_`, `DESCRIBE_`, `AUDIT_`, `COMPARE_`, `COUNT_`, `DIAGNOSE_`, `PROBE_` - declared at a risk that WRITES: **zero of 395**. (2) A capability named like a write - `SET_`, `CREATE_`, `DELETE_`, `MOVE_`, `PLACE_`, `REPLACE_`, `ASSIGN_` and seventeen more - declared at READ or ANALYZE: **one**, `EXTRACT_DATES_FROM_TEXT`, and it is **correct** - it pulls dates out of text notes, extracting information rather than elements. **So the library's NAMES and RISKS agree everywhere**, and these ten are not a pattern of careless declaration: they are ten specific sentences, each with an owner and each repairable in one line. Neither sweep is kept as a tool - a check that has never found anything is a check nobody reads, and the result is recorded here instead. **AND THE SAME RULE, ONE LAYER UP, FINDS FIVE MORE - WHICH IS EXACTLY WHERE [ROW 137](../FRAGMENT-ISSUES.md#) SAID THE BLIND SPOT WAS.** A skill declares a risk and a list of utterances just as a fragment does, so the sweep reads both through a four-line adapter rather than a second copy of the rule. Five skills above the write line claim a question: `space-airflow` *"how much air does this room need"*; `sprinkler-layout` *"how many sprinklers do I need"* and *"how many sprinklers for this room"*; `terminal-layout` *"how many diffusers for this room"* and *"how many at 3 m coverage and where"*. **ROW 137 BELIEVED TWO OF THESE WERE ALREADY REPAIRED.** It records that the owner found *"how many sprinklers"* in `sprinkler-layout` by hand and that *"that one was repaired by giving the sentence to `count-elements`"* - and `count-elements` does now declare *"how many sprinklers on level 2"*, a THIRD phrasing, while the two above stayed where they were. **The repair moved one phrasing and left two, and nothing could see it**, for the reason row 137 gives: a crossing compares a sentence's reach against the SKILL's own declared risk, so a question inside a MODIFY skill never registers. This sweep asks nothing about reach. **SOME OF THE FIVE ARE PROBABLY RIGHT AND THE TOOL SAYS SO IN ITS OWN OUTPUT**: *"how many sprinklers do I need"* may genuinely belong to a layout skill, because answering it IS the layout - unlike *"how many sprinklers on level 2"*, which is a count. That is a judgement for the owner, and **this session owns `brain/skills/*.yaml` and edited none of them**: deleting a sentence Ajmal really says to make a list shorter is [row 113](../FRAGMENT-ISSUES.md#)'s forbidden move held up to a mirror **THE RULE EXISTS NOW: [Q-57](../OPEN-QUESTIONS.md) IS ANSWERED, 2026-09-21, AS [D-86](../DECISIONS.md).** A write may declare a question **only when the question cannot be answered without performing the write**. He chose it over a blanket ban and over leaving it unruled. **THIS ROW'S OWN JUDGEMENT IS NOW THE TEST, NOT A GUESS**: *"how many sprinklers do I need"* stays on the layout skill, because answering it IS the layout; *"what scale is this view"* leaves `SET_VIEW_SCALE`, because a scale can be read without touching it. **AND THE ORDER MATTERS, WHICH IS WHY NOTHING IS EDITED HERE YET:** a sentence that leaves a write needs somewhere to go first, and the starkest case is the worst one - [row 146](../FRAGMENT-ISSUES.md#) measured a view's scale as the library's ONE CERTAIN capability gap, so removing that sentence before something can answer it drops it into ranking, where it may find a different writer. **Build the READ, then move the sentence.** The fifteen still need judging one at a time; what has changed is that there is now a test to judge them against, and `tools/check-declared-questions.py` is a report a reader can close |
| **159** | THE VERSION WALL SLID ONTO A RELEASE THE USER NEVER CHOSE, WHEN THE ONE THEY DID CHOOSE CLOSED. — [full row](../fragment-issues-archive/proving-defects-151-175.md#row-159) | FIXED 2026-09-20 in the same change as row 130, and the two are reported apart because… |
| **160** | A TEST THAT PASSES ON WINDOWS, IN A CLEAN CLONE, WITHOUT `%APPDATA%` AND WITH A STRIPPED… — [full row](../fragment-issues-archive/proving-defects-151-175.md#row-160) | **FIXED 2026-09-21, AND THE ANSWER IS ONE SENTENCE.** |
| **161** | THE COMPILE GATE SAID "EVERY PROJECT" WHILE NAMING FIVE OF SIX, AND THE SIXTH'S ABSENCE WAS A… — [full row](../fragment-issues-archive/proving-defects-151-175.md#row-161) | **FIXED 2026-09-20 - and the fix is the check, not the list.** |
| **162** | **ONE SUITE OF THE 204 IS NEVER RUN BY THE SWEEP AND NEVER MENTIONED BY IT, SO "EVERY TEST PASSES" MEANS 203.** Found 2026-09-20 while counting suites after [row 161](../FRAGMENT-ISSUES.md#), by comparing `ls tests/test_*.py` against what `check-gaps.py` actually printed: **204 on disk, 203 reported.** The missing one is `tests/test_bridge_roundtrip.py`, skipped by an explicit `if name == ...: continue` in `tools/check-gaps.py`. **THIS IS NOT ROW 160's DEFECT AND SAYING SO IS THE POINT** - the reason is written **in the code, beside the skip**, which is exactly where row 161 says an exclusion belongs: *"Needs the compiled test host. Its absence is a build step, not a gap in the code."* **AND THE REASON IS CORRECT.** Read at the source: with no host binary the suite prints `FAIL ... not found` and **`return 1`**, so sweeping it in on a machine that has not run `dotnet build` would report a FAIL for a build step nobody took. **THE DEFECT IS THAT IT CANNOT SAY THE OTHER THING.** Heron has a fourth state for precisely this - **exit 3, "could not run for want of an optional dependency"** - and `check-gaps.py` already reads it (`SKIPPED = 3`) and prints `wait ... skipped, see its own output for why`. `test_bridge_roundtrip.py` has **no exit-3 path at all**: it returns 0 or 1 and nothing else. Because it cannot say *"I could not run"*, the sweep has to silence it by name, and a silenced suite prints **nothing** - not `ok`, not `wait`, not `FAIL`. A reader counting the output gets 203 and has no way to learn there is a 204th. **WHAT WAS MEASURED:** run directly here it **PASSES, exit 0** (*"Step 1 PASSED - the bridge works, without Revit"*), because the host is built in this container; the sweep in the same session reported 203 suites, 0 not passing, and its verdict line reads *"every test passes"*. **THE REPAIR IS THE ONE THIS SESSION ALREADY WROTE NEXT DOOR.** `tests/test_binding_note.py` builds a test host too, and returns **3** with *"This is exit 3, which is NOT a pass: nothing was checked"* when `dotnet --list-runtimes` fails. Give `test_bridge_roundtrip.py` the same path and `check-gaps.py` can delete its special case: the suite says for itself that it could not run, and the sweep names it as `wait` instead of omitting it. **Silent becomes named, which is the whole of it.** **WHAT IS NOT ESTABLISHED:** whether the suite ever regressed silently while it was outside the sweep - it passes today, which is a measurement of today. The missing-host path was read from the source and **not run**: the binary was not removed to watch it return 1. And nothing here says the 203 are wrong - they passed, 0 did not. | **FIXED 2026-09-21, exactly as the repair above describes it.** `tests/test_bridge_roundtrip.py` now returns **3** with *"COULD NOT RUN ... This is exit 3, which is NOT a pass: nothing was checked"* when the host binary is absent, and `tools/check-gaps.py`'s `if name == ...: continue` is deleted, so the sweep runs and names all of them. **BOTH PATHS WERE RUN RATHER THAN READ**: with the host built it passes, exit 0; with the .dll moved out of the way it prints the build line and exits 3; moved back, exit 0 again. `ls tests/test_*.py \| wc -l` and the count the sweep prints now agree. CI is untouched - `gates.yml` treats any non-zero as a failure, so the suite stays on its known-failure list until that runner builds a host |
| **163** | A TOOL CRASHED INSTEAD OF REPORTING A MISSING FOLDER, ON THE OWNER'S PC ONLY, AND CI CAN NEVER SEE… — [full row](../fragment-issues-archive/proving-defects-151-175.md#row-163) | **FIXED 2026-09-20.** |
| **164** | THE FIX FOR ROW 5b-23 STOPPED THE BRIDGE STARTING ANYWHERE BUT WINDOWS, AND THE SUITE THAT PROVES… — [full row](../fragment-issues-archive/proving-defects-151-175.md#row-164) | **FIXED 2026-09-21, in the sitting that found it.** |
| **165** | A LITERAL PIPE IN A ROW'S TEXT MAKES THE REGISTER'S OWN SWEEP READ A FRAGMENT OF A SENTENCE AS THE… — [full row](../fragment-issues-archive/proving-defects-151-175.md#row-165) | **FIXED 2026-09-21, in the sitting that found it.** |
| **166** | THE SUITE SWEEP THROWS AWAY THE ONE THING A FAILING SUITE SAYS, AND THAT COST SIX RED CI RUNS, A… — [full row](../fragment-issues-archive/proving-defects-151-175.md#row-166) | **FIXED 2026-09-21, in the sitting that found it.** |
| **167** | `main` HAS BEEN RED SINCE #221, AND #223 WAS MERGED ON TOP OF IT. — [full row](../fragment-issues-archive/proving-defects-151-175.md#row-167) | **FIXED 2026-09-21 for the drift; the two proofs are the owner's, on a PC with Revit.** |
| **168** | THE REGISTER'S OWN SWEEP READ A DATED DENIAL AS A DATED FIX, SO THE ONE QUESTION IT ASKS WAS THE… — [full row](../fragment-issues-archive/proving-defects-151-175.md#row-168) | **FIXED 2026-09-21, AND MEASURED RATHER THAN IMAGINED.** |
| **169** | **TWO AGENTS A DECISION DELIBERATELY DID NOT BUILD WERE COUNTED AS WORK LEFT, SO THE PAGE THAT SAYS WHAT REMAINS COULD NEVER REACH ZERO.** Found 2026-09-21 while reading the balance of work for something Linux can do. `tools/balance-of-work.py` printed **`agents left to build  2 of 250`**, and the two are `HERON-DOC-REL-005` (release notes — there are no releases) and `HERON-DOC-CHG-008` (a change log — 683 files carry `Heron-Since: 0.1.0`, so there is one version to write between). **[D-77](../DECISIONS.md) settled both in writing on 2026-09-16** and the registry row says so. `tools/agent-count.py` had no state for it: its own docstring opens *"Three states, not two. An agent is BUILT, HOST or LEFT"* and then argues, correctly, that **collapsing HOST into LEFT produces a to-do list with four items on it that will never be done** — which is exactly what it was doing to these two, one paragraph below the argument against it. **THE COST IS NOT THE NUMBER, IT IS THE READING**: the balance page exists so a person can see what remains, and a row that cannot reach zero teaches them to skip it | **FIXED 2026-09-21. A FOURTH STATE, DERIVED.** `agent-count.py` reads `DEFERRED` **and a decision id in the same sentence** off the registry's own "Does" cell — the only copy of the fact — and counts it apart: a new `DEFER` column, excluded from `LEFT` and from `T1 LEFT`, and a block at the end naming each one beside the decision, the way the four host-provided agents are already named. `Register reconciles:` now reads **250 agents, 239 built, 9 host-provided, 2 deferred, 0 left**, and the balance page carries both figures: `agents left to build 0 of 250` with `deferred by a decision 2` under it, *not left, and not done*. **THE HALF THAT KEEPS THIS HONEST IS THE DECISION ID**: `DEFERRED until we have time` does not match and stays LEFT, loudly, because a promise with nobody's name on it is still work — otherwise this becomes a way of emptying the list rather than of reading it. Pinned in `tests/test_agent_count.py` section 6, which asserts both directions and that no deferred agent is also claimed by a file. **AND THE DEFERRALS ARE NOW VISIBLE, WHICH IS THE POINT OF PRINTING THEM** — both are waiting on things that will arrive without anybody re-reading the file, so the row says that when what it waits for exists, the decision is what has to change first |
| **170** | 882 CHECKS COMPILED EVERY DAY AND RAN ON NO DAY. — [full row](../fragment-issues-archive/proving-defects-151-175.md#row-170) | **FIXED 2026-09-21 - `tests/test_stack_guard.py`, the missing runner.** |
| **171** | `revit_systems` COUNTED A SYSTEM'S TERMINALS AND CALLED THEM THE SYSTEM, SO EVERY DUCT, PIPE AND… — [full row](../fragment-issues-archive/proving-defects-151-175.md#row-171) | **FIXED 2026-09-22, and proved the same day** |
| **172** | THE SAME TOOL COUNTED A DUCT'S LINK TO ITS OWN SYSTEM AS A PHYSICAL JOIN, SO EVERY OPEN END ON A… — [full row](../fragment-issues-archive/proving-defects-151-175.md#row-172) | **FIXED 2026-09-22** |
| **173** | **`REPORT_CATEGORY_OVERRIDES` COULD NOT SHOW THE FILL IT EXISTS TO REPORT. EVERY LINE IT WROTE WENT OUT AS A LIST ENTRY, AND A REPLY CUTS A LIST ENTRY AT SIXTY CHARACTERS.** Observed 2026-09-22 on `Project1`, Revit 2024, session 37184, view `1 - Mech`, straight after `SET_CATEGORY_SOLID_FILL` on Walls with `projection-line-colour=0,255,0; cut-line-colour=0,255,0; surface-colour=255,0,0; cut-colour=255,0,0`, which returned `overridden 1, solidPattern <Solid fill>`. Reproduced that evening on the same model: `findings 2 item(s) [1 of 7 category(ies) present in '1 - Mech' carry an override..., Walls: line RGB(0,255,0), cut line RGB(0,255,0), surface RGB...]`. **TWO CUTS, BOTH IN `RevitFragment.Describe`**: a list shows its first three entries (`RevitFragment.cs:4307`) and `ShortName` cuts each at sixty characters (`:4332`) - and `Walls: line RGB(0,255,0), cut line RGB(0,255,0), surface RGB` is exactly sixty. The line built at `fragment.cs:87` carried the surface colour and the surface pattern's name (`:74-77`), and neither arrived. **AND THE CUT HALF OF THE FILL WAS NEVER READ**: no `CutForegroundPatternId` and no `CutForegroundPatternColor`, so the cut pattern `SET_CATEGORY_SOLID_FILL` stamps on every cuttable category, and the `cut-colour` that tints it, could not appear however long the line was - and a category overridden only there read as not overridden. So the read half of `SET_CATEGORY_GRAPHICS` (FRG-VIEW-006) and `SET_CATEGORY_SOLID_FILL` (FRG-VIEW-107) could confirm two line colours and nothing of the fill. **MEASURED BOTH WAYS ON ONE ARRANGEMENT**, `Project4`, `FloorPlan: Level 1`, each run rolled back: a solid fill stamped by `set-category-solid-fill`, and the same four colours with NO pattern set by `set-category-graphics`, read back **identically** - both `Walls: line RGB(0,255,0), cut line RGB(0,255,0), surface RGB...`. It is the repair [row 120](../FRAGMENT-ISSUES.md#) names for a refusal: when the whole text is the answer, declare a string, because a string is never cut. | **FIXED 2026-09-22 and re-proved the same day.** A `string` output, `overrideList`, joins every overridden category's line in full - `role: result`, empty when nothing is overridden. `findings` is unchanged, because every proof record quotes it as prose. The cut pattern and its colour are read beside the surface pair, as `cut pattern <name>` and `cut RGB(...)`. **The same arrangement now returns** `overrideList  Walls: line RGB(0,255,0), cut line RGB(0,255,0), surface RGB(255,0,0), surface pattern <Solid fill>, cut RGB(255,0,0), cut pattern <Solid fill>`. **Printed by the MCP tool with no Python change**: `revit_change`'s formatter prints every `provides` key, and run through the tool itself `overrideList` appears (on a clean view, empty). **Proof**: `Project4` (3,276 elements), Revit 2024, session 37184 - positive the fill on Walls in `FloorPlan: Level 1`; negative the same fill on Ducts, which that view does not show (`0 of 6`, `overrideList` empty). Both legs were rolled back, because that view is the owner's working plan, and the read-back afterwards found 0 category overrides and the model unchanged. Fingerprint `140301fac35c76c9`, signed Ajmal PS on his instruction. Compiles on all eight releases (`check-fragments-compile`). **Second route owed**: the V/G dialog has nothing to show after a rollback, so the by-hand read needs a view where a fill can be kept |
| **174** | **`SET_CATEGORY_SOLID_FILL`'S SIGNED PROOF SAYS THE READ-BACK CANNOT TELL A STAMPED PATTERN FROM NONE. THE CODE AND A RUN SAY IT COULD - THE REPLY'S SIXTY-CHARACTER CUT HID IT.** Two sources disagree, and both are recorded rather than one rewritten (AGENTS.md). **The proof** - `brain/fragments/set-category-solid-fill/fragment.yaml`, `positive_case`, signed Ajmal PS 2026-09-22: *"The read-back is NOT the evidence here and must not be used as it - REPORT_CATEGORY_OVERRIDES reports the same four colours whether or not a pattern was ever stamped, which is the exact failure this fragment exists to catch. Both halves of the cluster were run on this model an hour apart and read back identically; only the screen told them apart"*. **The code** - `brain/fragments/report-category-overrides/impl/any/fragment.cs:72-78`, unchanged from its first commit `241e6db` (2026-09-03) until [row 173](../FRAGMENT-ISSUES.md#): `if (settings.SurfaceForegroundPatternId != ElementId.InvalidElementId)` adds `"surface pattern " + pattern.Name` to the line. **The evidence**: (1) the two read-backs WERE identical, and still are on the old code - [row 173](../FRAGMENT-ISSUES.md#) measured a stamped fill and the same colours with no pattern arriving as the same sixty characters, cut before the pattern clause; (2) that clause fires only on a real pattern - a lone surface colour on Walls, rolled back on `Project1` view `2 - Mech`, read back whole as `Walls: surface RGB(255,0,0)`, no pattern named; (3) with the cut removed, the stamped walls read `... surface pattern <Solid fill>, cut RGB(255,0,0), cut pattern <Solid fill>`. **So**: *"read back identically"* was a true observation; *"reports the same ... whether or not a pattern was ever stamped"* put the cause in the wrong place - the report computed the difference and `Describe` cut it off; and *"the same four colours"* was never so - it read three, and the cut pattern's colour not at all until row 173. **The half that was right**: nothing read back could have confirmed the CUT pattern, because it was never read. **Governing decision**: [D-30](../DECISIONS.md) - a proof is evidence for a later reader to judge, so the signed text stands as the record of that day and this row is the correction beside it. | **OPEN - the owner's call.** The proof is signed in Ajmal PS's name, so whether its text is annotated, re-taken with the read-back as evidence (possible since row 173), or left standing is his decision. **Nothing in that fragment was changed.** Its fingerprint covers `impl/` only, so the proof is not STALE whichever way this goes |
| **175** | **`SET_CATEGORY_GRAPHICS` ANSWERED `overridden 1` ON DUCTS WHILE REVIT KEPT ONE SETTING OF THE FOUR IT WAS GIVEN, AND NOTHING IN THE REPLY SAID SO.** Found 2026-09-22 arranging [row 173](../FRAGMENT-ISSUES.md#)'s proof on `Project1` (3,846 elements), Revit 2024, session 37184. `revit_change SET_CATEGORY_GRAPHICS` with `view=1 - Mech`, `categories=Ducts`, `overrides=projection-line-colour=0,255,0; cut-line-colour=0,255,0; surface-colour=255,0,0; cut-colour=255,0,0` returned `overridden 1`, `refused 0 item(s)` and *"was KEPT"*. The read-back: `Ducts: line RGB(0,255,0)` - the projection line colour and nothing else. **ONE SETTING AT A TIME, ROLLED BACK** (`validate --write --setup set-category-graphics`): `surface-colour=255,0,0` alone on Ducts left Ducts with no override at all (`1 of 7 category(ies)`, Walls only); the same value alone on Walls in `2 - Mech` read back `Walls: surface RGB(255,0,0)`, and `cut-line-colour=0,255,0` alone on Walls read back `Walls: cut line RGB(0,255,0)`. So the CATEGORY decides what Revit keeps, and it keeps it silently. **Wider than what `SET_CATEGORY_SOLID_FILL`'s own purpose already says** (not every category can be CUT): a SURFACE colour was dropped too. **Not established**: whether Revit discards those settings or keeps them where the getter cannot see them - the V/G dialog for Ducts is the second route and was not opened. The fragment's count is true - it called `SetCategoryOverrides` once - and says nothing about what landed. | **OPEN - recorded, not fixed.** The repair belongs in the write: read `GetCategoryOverrides` back after setting and name, per category, what did not land. That changes a PROVEN fragment and is its own change. The kept Ducts override went when the owner closed `Project1` unsaved, so nothing was left behind |
