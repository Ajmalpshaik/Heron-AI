# Fragment issues — section 5b, rows 151 to 175

> Rows 151 to 175 of section 5b of [the register](../FRAGMENT-ISSUES.md), in their own file since 2026-09-23 so that
> they can be read alone. **The section's own rules are on that page, under its heading.** A new
> row goes at the end of the section's last file, whatever its number, and the next run of
> [`tools/split-register.py`](../../tools/split-register.py) moves it to the file its number
> belongs to. [`tools/register-text.py`](../../tools/register-text.py) reads every row back into
> the register for every tool that reads it, so a row here is seen exactly as it was seen there.

| # | Defect | State |
|---|---|---|
| **151** | **THE REPORT PRINTS AN ABSOLUTE CONTAINER PATH IN ONE HALF AND A REPO-RELATIVE ONE IN THE OTHER.** Found 2026-09-22 reading `tools/check-declared-questions.py` - 277 lines, never read before, run by CI under *The reports - a finding is a question*. **Measured:** all ten fragment rows print `/home/user/Heron-AI/brain/fragments/.../fragment.yaml`, naming a container that will not exist when the owner reads the CI summary, while the skill section two blocks below prints `brain/skills` relative. `tools/check-revit-gate.py` reads the same `frag.folder` and prints `os.path.basename` of it, so this is the only tool in the tree publishing the absolute one. **AND A SECOND FINDING THAT DOES NOT FIRE TODAY, SAID SO PLAINLY:** the *ASKS AND THEN SAYS WHAT TO DO* header counts `len(told) + len(s_told)`, but the very next line tests `if not told` and prints `none`, and the line after prints `len(told)` alone. Measured today `told` is 1 and `s_told` is 0, so the two agree by coincidence. With any skill utterance that asks and then instructs, the header would print a number above the word `none`, and `--all` could never print the skill instructions its own help promises. A shape, not a behaviour - recorded because it is three lines from the one that does fire, not because a scan found it. | **OPEN - recorded, not repaired**, same rule as row 150. The repair is `os.path.relpath(folder, ROOT)` with separators normalised to `/`, and the count block reading both lists. **WHAT WAS TRACED AND IS SOUND, so nobody re-reads it:** it **does** catch the case it was written for - `SET_VIEW_SCALE` with *what scale is this view* is among the ten it reports, which is the 2026-09-19 finding that caused it to exist. All three of its quotations from `tools/check-routing.py` check out against that file today: the rung comparison at line 307, `risk_of(store, fid) != "READ"` at 347, and the comment `the host answers correctly` at 353. `tools/generate-jobs.py` has no import-time side effects beyond constants, so importing it for `write_threshold()` costs nothing. 396 fragments and 10 skills, both derived and both right; the write line is ordinal 4, read from the registry by name. `_AsCard` reading a skill through the same two fields is the right call and not a second opinion. **`tests/test_declared_questions.py` exercises `questions_on_writes` directly and never `main()`'s output**, which is exactly where both findings live - so the extension goes there rather than into a second suite. **Needs no Revit.** |
| **152** | **THE CROSS-DRIVE `os.path.relpath` CRASH KEPT COMING BACK BECAUSE ITS ONE SAFE ANSWER COULD NOT BE IMPORTED WITHOUT PyYAML - AND TWO MORE CALLS COULD RAISE IT, BOTH AFTER THEIR WORK WAS DONE.** Audited 2026-09-22 - the sweep [row 163](../FRAGMENT-ISSUES.md#) and [row 5b-17](../FRAGMENT-ISSUES.md#) both ended by leaving *"UNAUDITED"*. Every `os.path.relpath` call in `tools/`, `brain/` and `mcp/`, read with an AST: **58 real calls**, the rest of `grep`'s hits being comments, and **every one traced to where its path comes from** rather than sorted by the shape of the line. **THE RULE THAT SORTED THEM:** a call can raise only when its `path` can come from outside the checkout - a command-line folder, a redirected output, a temp directory. A path joined onto `ROOT`, or walked from under it, is on `ROOT`'s drive by construction, because the call is lexical and never touches the disk; so is a path paired with the folder it was walked from. **53 are safe by that rule and were left alone**, including every candidate both rows listed: `check-fragments-compile.py` (sources under `FRAGMENTS`, the record at `ROOT/build/`), `new-agent.py` (every destination a `ROOT` join), `prove-agent.py` (`source_of()` walks `ROOT/revit/`), `check-gaps.py`, `check-narrow-errors.py` and `check-reachable.py` (walks under `ROOT`) and `check-skill-routing.py` (`SKILLS_DIR`). `api-changes.py`, the one real hit on those lists, was fixed earlier the same day by `7edd753` (#286). **AND TWO THAT NEITHER LIST NAMED, because both take their path from the COMMAND LINE and row 163's scan looked for a function parameter:** `tools/prove-skill.py:920` (`--jobs DIR`) and `tools/build-release-assets.py:449` (`--out`, kept absolute at `:398`). **BOTH RAISE AFTER THE WORK.** Measured on the owner's PC, `python tools/prove-skill.py --plan-only --jobs <a folder on C:>` exited **1** with `ValueError: path is on mount 'C:', start on mount 'D:'` having written **1 job file of 10**, and left the folder half-written. The release builder raises on the line that names the folder, after every asset is packed and `checksums.txt` - the completion mark - is written: a finished release, reported as a failed one. `.github/workflows/release.yml` runs it as `--out dist`, relative, so only a person typing an absolute `--out` on another drive meets it. **WHY THE COPIES KEPT COMING:** `heron_fragment.repo_relative()` was the one answer, and `heron_fragment` cannot be imported without PyYAML. `release.yml` installs none before it runs the release builder; `check-licence.py` is the tool run on a bare machine, and said so in the docstring of the copy it kept; `api-changes.py` answers NOT RUN without importing `heron_fragment`, and grew a copy of its own under #286. An answer half its callers cannot import is not shared - each of them writes its own | **FIXED 2026-09-22.** The rule moved to [`brain/heron_relpath.py`](../../brain/heron_relpath.py), which imports nothing but `os`: `relpath(path, start)` is `os.path.relpath`, or the absolute path when there is no relative form. `heron_fragment.repo_relative()`, `api-changes.py`'s `short()` and `check-licence.py`'s `repo_relative()` now ask it - **the two copies are gone**, and each still passes its own `ROOT`, read at the call, so a suite that moves `ROOT` is still obeyed - and the two reachable calls use it. [`tests/test_relpath.py`](../../tests/test_relpath.py) **FORCES THE CONDITION** by standing in for `os.path.relpath` with one that raises as Windows does across a scratch folder, so it proves something on CI's single mount: run against the UNFIXED tools on a one-drive copy it **FAILED 11 checks** - `prove-skill` wrote 1 job of 10, and the release builder raised with `checksums.txt` already on disk - and it passes with the fix. On the owner's PC its real two-drive half runs as well. It also imports `heron_relpath`, and runs the release builder and `check-licence.py`, **with PyYAML blocked**, so the next attempt to route either of them through `heron_fragment` fails a suite instead of a release. **MEASURED ON THE OWNER'S PC**, repository on `D:` and `TEMP` on `C:`: `python tests/test_api_digest.py` exit **0**; `prove-skill.py --plan-only --jobs <C:>` exit **0**, all 10 jobs written; and the full sweep before and after on the same base, with `tools/change-evidence.py` - all seven gates it runs PASS on both sides, 244 suites before and 245 after (this one the difference), the same two red on both sides and `test_bridge_roundtrip.py` unable to run on both. The two red were `test_check_dependencies.py` and `test_decision_summary.py`, which #288 had already fixed on main, and both pass on the rebased tree. Its ruling was **NO CHANGE MEASURED**, and that is right - no suite before this one could see two drives, which is how the defect lived. `check-signatures`, `check-narrow-errors` and `check-products` exit 0 with byte-identical output before and after, and all ten CI gates exit 0 on the rebased tree. **NOT DONE, ON PURPOSE:** the 53 safe calls stay raw - converting calls that cannot raise would have widened a reviewable change and changed no behaviour. That leaves the recurrence itself unguarded: nothing stops the next NEW raw call on a path from outside the checkout, which is how every instance so far arrived. A gate refusing `os.path.relpath` outside `heron_relpath` would stop it, and it is a change to CI's gate list and to `tests/test_tag.py`, which pins that list - its own piece of work, not this one |
| **153** | **`open-defects.py` COUNTS A ROW BY THE FIRST WORD OF ITS STATE, AND TWO ROWS SAY IN PLAIN WORDS THAT THEY ARE STILL OPEN WHILE IT COUNTS THEM CLOSED.** [`tools/open-defects.py`](../../tools/open-defects.py) decides open or closed by whether the state cell *begins* with the word *open* - the register's own convention, honoured literally, and its docstring says so. **Row 106** of section 5 breaks it from the other side: its state opens *"MEASURED 2026-09-16, AND THE MEASUREMENT REFINES THE ROW RATHER THAN CONFIRMING IT. STILL OPEN."* **Row 150** opens *"FIXED 2026-09-19, in the half this session owns"* and says further down *"It stays OPEN because the defect is still in the file"*. Both drop out of every open count and every open list `open-defects.py` prints, while saying in words that they are open. Its docstring names the mirror image - a row that still says OPEN after a later row closed it - and it already asks about a row that begins OPEN and carries a dated FIXED. **This third shape, a closing word or a measurement first and the open part after it, is named nowhere.** **Found 2026-09-22 by [`tools/archive-fragment-issues.py`](../../tools/archive-fragment-issues.py)**, whose first version read only the opening of a state: it would have MOVED row 150 into the archive, together with 58 other rows carrying words like STILL OPEN, NOT FIXED or NOT YET RUN IN REVIT further down. It now reads the whole state and holds every one of them back, so neither row was moved or changed. **WHAT WAS NOT CHECKED:** which of those other rows are open in substance. Some point at a DIFFERENT row that is correctly counted open - row 79 at row 75, row 5b-126 at row 5b-83 - and some use the words in passing (*"while Revit is open"*); only 106 and 150 were read closely enough to say. `open-defects.py` itself was not read word by word, so no ledger mark was made. **TWO REPAIRS, AND CHOOSING IS NOT A SWEEP'S JOB:** reword the two states so each begins with OPEN, or teach `open-defects.py` to *ask* about this shape the way it asks about a row that argues with itself | OPEN - recorded, not repaired, by the rule at the head of this section: a defect found while doing something else is written up, and the rows and the tool are left as they were |
| **154** | **`READ_GRAPHIC_OVERRIDES` HAS [ROW 173](../FRAGMENT-ISSUES.md#)'S SHAPE, AND ONE STEP WORSE: IT READS NO PATTERN AT ALL.** Found 2026-09-22 by reading `brain/fragments/read-graphic-overrides/impl/any/fragment.cs` (FRG-VIEW-049, PROVEN 2026-09-10, fingerprint `d61edb1615a78aa0`) while fixing its category-level twin. Its per-element lines (`:84`) go out only as `findings` entries, so a reply cuts them to three elements of sixty characters each. And it reads no pattern: `:56-79` read the line colour, cut line colour, surface colour, transparency, halftone and line weight, and never `SurfaceForegroundPatternId`, `CutForegroundPatternId` or `CutForegroundPatternColor` - so an element overridden only by a fill pattern, or only by a cut colour, is reported *"NO per-element override in this view"* (`:89`), the sentence that sends a person to look somewhere else. **What is unaffected**: the `overrides` object it hands on (`:52`) is Revit's own, patterns included, so matching through `OVERRIDE_GRAPHICS_IN_VIEW` still copies them; only the report is blind. | **OPEN - recorded, not repaired.** The repair is row 173's: read the four pattern fields, and join the lines into one string. It stales a PROVEN proof, so it needs its own re-proof on a model with a per-element pattern override. The ledger mark waits for this row to exist |
| **155** | **`revit_change` RAN THE PUBLISH AND ADMIN FRAGMENTS THE COMMAND LINE REFUSES - SYNC WITH CENTRAL, CREATE WORKSET, SAVE, UPGRADE FAMILY FILES - WHENEVER CHANGES WAS ON.** Found 2026-09-22 while comparing Heron's MCP tools against the owner's earlier library, one tool at a time. [Row 9](../FRAGMENT-ISSUES.md#) closed this on the command line: `risk_refusal` in `mcp/client/heron_bridge_client.py` refuses to SEND a fragment declared above Modify, because HeronPermissions puts Publish and Admin out of reach in Phase 0 and Phase 1 - and the add-in cannot tell, since `run_fragment_write` is declared Modify and row 9 records why the fragment's own risk is not sent over for the add-in to judge. [Row 54](../FRAGMENT-ISSUES.md#) found the same gap in `validate` and closed it there. **`revit_change` in `mcp/server/heron_mcp_server.py` was written on 2026-09-15, a week after row 9, and never called it**, so the tool a chat actually uses was the one path with no risk gate - [row 71](../FRAGMENT-ISSUES.md#)'s shape exactly, where `undeclared_values` guarded the command line and not this tool. **FIFTEEN FRAGMENTS WERE REACHABLE**, derived rather than listed: every `fragment.yaml` whose `risk:` is not in `RUNNABLE_RISKS`. They include `SYNC_WITH_CENTRAL`, which reaches everybody on the project, and `UPGRADE_FAMILY_FILES`, which rewrites family files on disk. **WHAT THE AUDIT LOG SAYS HAS HAPPENED, READ ON THE OWNER'S PC:** twelve of the fifteen appear as `run_fragment_write`, 49 rows, **all between 2026-09-08 and 2026-09-12 - before `revit_change` existed** - so every one came through the command line, and mostly in the pairs a proving run leaves. `SYNC_WITH_CENTRAL`, `SAVE_DOCUMENT` and `CREATE_FAMILY_DOCUMENT` have never run at all. **So the hole was open for a week and nothing went through it.** **NO OTHER MCP TOOL SENDS A FRAGMENT**: `run_fragment_write` is requested once in `mcp/server/`, by `revit_change`, and `heron_resolve` - the one other tool that takes a capability - only prints what to type. That was read, not exercised | **FIXED 2026-09-22.** `revit_change` now calls `risk_refusal` FIRST - before the fragment's code is read and before a session is bound - and returns its sentence, which already ends *"Nothing was sent to Revit."* **Called, not reimplemented**: the rule keeps its one home in the client. The tool's description now tells the host that a publishing or administering capability is refused, and the server's header no longer says the ribbon switch is the only thing standing in the way. `tests/test_change_risk_refusal.py` puts every fragment in the library through the helper - all fifteen above Modify refused, none of the other 381 - and checks that `revit_change` calls it before binding and before reading code. **Run against the code as it stood, it failed on exactly those four wiring checks; it passes now.** **WHAT IS NOT DONE, ON PURPOSE:** there is no chat equivalent of the command line's `--allow-publish`. A chat that must export or sync waits for a decision that makes Publish reachable, which is the owner's. **WHAT IS NOT PROVEN:** the refusal has not been seen through a live chat. It returns before any Revit call, so nothing about a model can differ - but a chat only sees it after the MCP server restarts, which means restarting the Claude app, not pressing the ribbon button |
| **156** | **THREE READERS OF NEEDS-CHECKING.md MATCH A ONE-LETTER ID, SO THE 38 OPEN ROWS OF GROUPS AA AND AB REACH NONE OF THEM - AND THE CHECK THAT SHOULD HAVE NOTICED USES THE SAME PATTERN.** [`tools/owner-queue.py`](../../tools/owner-queue.py), [`tools/check-gaps.py`](../../tools/check-gaps.py) and [`tools/balance-of-work.py`](../../tools/balance-of-work.py) each find a row as a bold ID of ONE capital letter and digits - the pattern `[A-Z]` followed by digits. Groups AA and AB, the installer checks added 2026-09-20 to 2026-09-21, number their rows `AA1`, `AB3`: two letters. **Measured 2026-09-23:** 38 open rows in NEEDS-CHECKING.md have a two-letter ID, and `owner-queue.py` - the list the owner is told to run for "what is waiting on me" - printed **0** of them. The rows ask for exactly the owner's hands: *"Deploy **both** proofs with the AI Bridge add-in also installed. Start Revit. **Screenshot.**"* (AA1). **The guard did not fire because it shares the blind spot**: `owner-queue.py` counts ID-shaped rows independently to prove its own pattern "saw what is there" - with the same one-letter pattern, so both counts agreed on the wrong number. NEEDS-CHECKING.md records this failure shape against itself three times - *"a count that quietly omits a whole group is worse than no count"*. **Found 2026-09-23 while splitting NEEDS-CHECKING.md**, not by any check. **WHAT WAS NOT CHECKED:** whether any other reader of that file (`heron_healing` only names it) or of another register has the same pattern. | **FIXED 2026-09-23, on the owner's instruction.** All three take one letter or more now - `[A-Z]+` - the self-check's pattern included, and `check-gaps.py` takes a row's group from ALL its letters, so a row `RA1` is not filed like Group R's. `tests/test_needs_checking_ids.py` runs the three on a register it builds, with one- and two-letter rows open and struck and a results table: **against the code as it stood it failed on five checks** - owner-queue reported `A1, R1` of five open rows while its guard said ok - **and it passes now.** `tests/test_owner_queue.py` derives its own list with the wider pattern, groups by every letter, and proves the guard fires when the pattern is narrowed back to one letter. **THE 38 ABOVE WAS THE PATTERN'S COUNT, NOT THE OWNER'S, and the rows could not simply be made visible:** the queue would have been wrong the other way. Four register repairs came first. (1) The results tables of Groups AA and AB named each row's ID in bold, so every result read as a second copy of its row - they are plain text now, as Group N's already were. (2) Fourteen rows of AA and AB PASSED on 2026-09-21, and AA9 was ruled no longer open by [D-96](../DECISIONS.md), but none was struck - they are struck now, each with its verdict first. (3) The `heron-install` rows AE1 to AE6 and AF1 to AF7 wrote their IDs in backticks, a shape no reader matches, so they could never have reached the queue - they take the bold ID now. (4) The Model Auditor's rows, placed as Group AC by the register split, collided with Stage 5's AC1 to AC5 - they are Group AG now. `python tools/owner-queue.py` lists 161 rows from NEEDS-CHECKING where it listed 129: every open row of the two-letter groups, AA to AG, and none of the fifteen closed ones. **The row's own open question is answered:** no other Python file in the repository matches a one-letter bold ID - `check-docs.py` reads `B8` by name and `archive-needs-checking.py` already took `[A-Z]+` |
| **157** | **TWO DECISION NUMBERS WERE USED TWICE, AND THE FIRST DECISION UNDER EACH LEFT THE LOG.** DECISIONS.md is append-only - a reversal gets a NEW entry - but commit `2248cb3` (2026-08-31) wrote *"Heron tracks the MCP SDK across major versions"* as **D-45**, over *"The library is built out first, and proved in one pass later"* (2026-08-30), and commit `75c22a9` (2026-09-06) wrote *"The Emergency Stop button is removed"* as **D-46**, over *"A context fragment is consumed by the host, not by another fragment"* (2026-08-31). Neither first text was anywhere in DECISIONS.md; both survive in `git show 6582810:docs/DECISIONS.md`. Documents still cite the first meanings - `docs/31` lines 52, 341, 354 and 359, HANDOVER's manual, two fragments' `purpose`, three code comments - and read today they point at the wrong rule. **Found 2026-09-22** by a reader in the AJ AI Brain hooks audit, then checked by that session and again here. `check-docs.py` checks that no decision number is DEFINED twice at once; nothing checks that a number's heading kept its words. | **FIXED 2026-09-23, on the owner's choice** (*"Restore as D-97, D-98"*). The two first decisions are back, word for word from `6582810`, as **D-97** and **D-98**, each with a `Numbering` line saying which number it held until which commit; the current D-45 and D-46 carry a `Numbering` line pointing the other way, so a citation written before those dates can still be followed. Only references that meant the first decisions were repointed, each read in its context; dated session notes in `handover-archive/` were left as written, because they are history - and so was the `purpose` of two PROVEN fragments, `find-sheets` and `find-views`, because changing a proven fragment's file owes a real-Revit proof under D-30 that a citation does not justify. The Numbering line answers all of them. **The check it called worth its own row is [row 5b-170](../FRAGMENT-ISSUES.md#), 2026-09-23 - and building it found that the same merge lost D-47, D-48 and D-49 as well, [row 5b-171](../FRAGMENT-ISSUES.md#).** |
| **158** | **THE CHAT'S MOVE PREVIEW NAMES TWO OF THE FOUR REASONS AN ELEMENT IS SKIPPED.** Found 2026-09-23 while adding the other two (earlier-brain plan, package C3). `RevitWrite.Partition` now skips an element that is pinned, owned by someone else, **changed in central since this copy last reloaded**, or **deleted in central**, and sends the reasons as `skipReasons` on both `preview_move` and `move_elements`. [`mcp/server/heron_mcp_server.py`](../../mcp/server/heron_mcp_server.py) `revit_preview_move` and `revit_apply_move` print their own line - *"N would be skipped - pinned, or owned by another user."* - from `willSkip` and `skipped` alone, so an element skipped for a central reason is reported under the wrong ones. **Not fixed here:** `mcp/server/` is package C2's, which was being built in parallel. The repair is small: print `skipReasons` when the reply carries it, and keep today's sentence as the fallback for an add-in that has not been redeployed. NEEDS-CHECKING **E23** reads the reply rather than the chat until then. | **OPEN - recorded, not repaired**, by the rule at the head of this section and the plan's file boundaries. For whoever next changes those two tools, after C2 has merged |
| **159** | **A FRAGMENT WRITE'S AUDIT LINE DOES NOT SAY WHAT REVIT RAISED.** Found 2026-09-23 (package C3). The move path's own audit record has carried `warnings` since it was written. From 2026-09-23 a fragment write's REPLY carries `warnings` and `warningsDismissed` too - but a fragment run's audit line is written by [`RevitDispatcher.cs`](../../revit/Heron.Revit.Addin/RevitDispatcher.cs) from the request and the reply's `error` and `document`, so the trail that exists to answer *"what happened to my model"* records that a fragment ran and not that Revit warned about what it did. The chat hears it; the trail does not. **Not fixed here:** the dispatcher is outside C3's files. The repair is one field: read `warnings` off the response beside `error`, for the two fragment operations. | **OPEN - recorded, not repaired** |
| **160** | **THE BUILD-MATRIX BASELINE HAS NOT MOVED WITH THE BUILD.** `python brain/heron_buildmatrix.py` reports `MOVED (repository) projects` against [`brain/regression-matrix.json`](../../brain/regression-matrix.json) and exits 0. On 2026-09-23 the baseline listed 9 projects and `heron_dotnet.PROJECTS` 15 - derive both with `python3 -c "import json,sys; sys.path.insert(0,'brain'); import heron_dotnet as N; print(len(json.load(open('brain/regression-matrix.json'))['projects']), len(N.PROJECTS))"`. The three installer projects, `Heron.Installer.TestHost` and `Heron.Kernel.TestHost` were added without `--update`, and package C3's `Heron.FailureNote.TestHost` is a sixth. The module's own docstring says moving the baseline is deliberate and never automatic - so the drift is REPORTED on every run and acted on by nobody. **Not fixed here:** five of the six moves are not C3's to vouch for, and `--update` records all six at once. | **OPEN - recorded, not repaired** |
| **161** | **ARTICLE 7'S "EXPLICIT CONFIRMATION FOR THAT SPECIFIC OPERATION" HAS NO CODE BEHIND IT ON `revit_change`.** Found 2026-09-23 while recording [D-99](../DECISIONS.md#d-99--a-change-asked-for-in-a-chat-is-kept-at-once-with-no-preview-and-article-9-says-so). Article 7 asks for explicit confirmation before deleting elements, purging, bulk parameter writes, family reloads, workset changes and anything touching a linked model, and the Constitution's Enforcement table placed it at *"Revit add-in permission gate + transaction wrapper"*. That gate reads one risk per OPERATION - `run_fragment_write` is `MODIFY` in [`HeronOperationRegistry.cs`](../../platform/Heron.Core/HeronOperationRegistry.cs) - plus the Changes switch, and never the fragment. So with Changes ON, `revit_change` runs `DELETE_ELEMENTS`, `UNLOAD_LINKS`, `RELOAD_LINKS`, `RENAME_WORKSET` and `SET_ELEMENT_WORKSET` - every one `risk: MODIFY` - and nothing asks: `grep -n "TaskDialog\|onfirm"` over `RevitFragment.cs`, `RevitOperations.cs`, `RevitDispatcher.cs` and `HeronPermissions.cs` finds nothing, and the server sends straight through. **Not introduced by D-99** - the path has run this way since 2026-09-15; D-99 is what wrote it down. **What carries Article 7 now is text:** every chat's instructions include it (`host.chat`), which is guidance a model can be argued out of - the Constitution's own warning about a rule stated and not enforced. **What was NOT checked:** which of the library's `MODIFY` fragments are destructive in Article 7's sense. No field on a fragment says so, and that missing field is the likely shape of any repair - a declared `destructive:` the server reads before sending, or a question the add-in asks before it opens the transaction group. | **OPEN - needs the owner**: should a destructive fragment through `revit_change` stop and ask, as Article 7 says, or be recorded as a second exception? The Enforcement table now says the switch is all that stands there. |
| **162** | **THE CLOUD PAGE SAYS A CLOUD SESSION LISTS HERON'S TOOLS, AND THE SERVER REFUSES TO START THERE.** Found 2026-09-23 in a cloud session (package C2). [`docs/38-the-cloud-environment.md`](../38-the-cloud-environment.md) §4: *"In a cloud session the `revit_*` MCP tools are still listed ... every call reports no session. That is correct, and it looks like a fault the first time it is seen."* The server's `__main__` in [`mcp/server/heron_mcp_server.py`](../../mcp/server/heron_mcp_server.py) exits **2** when `os.name != "nt"` - *"Heron's bridge uses Windows named pipes. Revit is Windows-only."* - before a single tool is served. **Measured** in that session: `python mcp/server/heron_mcp_server.py` printed that sentence and exited 2, and the host reported the `heron` server as `CONNECTION_CLOSED`, listing **no** Heron tool at all. `tests/test_mcp_stdio.py` skips off Windows for the same reason, so nothing on the Linux side contradicts the page. The earlier-brain plan's cloud prompts ([04 §0](../work-notes/plans/earlier-brain/04-cloud-prompts.md), point 1) repeat it. **Not introduced by C2, and not repaired:** the exit is deliberate, and whether a cloud session should serve the brain tools without the Revit ones is a design question, not a typo. **What was NOT checked:** whether any earlier version of the server started off Windows. | **OPEN - recorded, not repaired.** The smallest honest repair is the page: a cloud session does not start the server, and the server's own suites run in-process instead (`test_mcp_serves`, `test_served_claims`). |
| **163** | **TWO ROWS OF THIS TABLE CAN CARRY THE SAME NUMBER, AND NO GATE OR SUITE THAT WAS RUN NOTICES.** Found 2026-09-23 merging `main` into [#307](https://github.com/Ajmalpshaik/Heron-AI/pull/307) (package C2) after [#306](https://github.com/Ajmalpshaik/Heron-AI/pull/306) (package C3) merged: two sessions working in parallel had each appended rows **158** and **159** to this section, both taking the same next free number. Git stopped at the conflict, so they were renumbered by hand - C2's became 161 and 162. **Measured:** the other resolution - both sides kept, numbers untouched - left two rows 158 and two rows 159 here, and all ten CI gates, `tools/open-defects.py`, `test_archive_fragment_issues`, `test_owner_queue` and `test_naming` exited 0 on it. A citation such as `row 5b-158` would then name two rows. NEEDS-CHECKING has the check this table lacks: `test_owner_queue` fails when an ID is reported twice, and caught the same kind of clash between [#304](https://github.com/Ajmalpshaik/Heron-AI/pull/304) and #307 that day (C2's Group AE, now AH). **What was NOT checked:** the full suite run on that tree, and the other numbered tables in this file. | **OPEN - recorded, not fixed.** The repair is a check that fails when a numbered table repeats a number, as `test_owner_queue` does for NEEDS-CHECKING. Until then, every package that writes a row while another is open can meet this again. |
| **164** | **THE BOUNDARY GUARD RAN ONLY IN A SESSION THAT HAD LOADED ITS SKILL.** `heron-guard`'s hook was declared in the skill's own frontmatter - the shape [Q-49](../OPEN-QUESTIONS.md) chose on 2026-09-09 - and a hook declared there is registered only when the skill is invoked (Claude Code's own skills documentation). So every session that never loaded `heron-guard` could write the Revit vendor namespace outside `revit/` with nothing refusing it until somebody ran `check-structure.py`. **Proven by hand on 2026-09-22** (the earlier-brain plan's finding H2): the script refused a forbidden edit piped into it, and the same edit made through the editor in a normal session went straight through. **WHAT WAS NOT CHECKED:** whether a real Windows session refuses it now - NEEDS-CHECKING **AI2**. No other skill declares a hook in its frontmatter (`grep -l '^hooks:' .claude/skills/*/SKILL.md` finds none). | **FIXED 2026-09-23** in [PR #308](https://github.com/Ajmalpshaik/Heron-AI/pull/308). Wired from `.claude/settings.json`, which every session reads; the frontmatter declares nothing, because the host runs a skill's copy of a hook separately from the settings' copy and both would run it twice. `tests/test_heron_guard.py` section 7 checks both halves and runs the exact command the settings give; it failed six checks on the old wiring. Seen live in the cloud session that built it: the guard refused a Write into `brain/` with the skill never loaded. |
| **165** | **ON WINDOWS THE GUARD REFUSED ANY EDIT CARRYING ARABIC, BY CRASHING ON IT.** The host writes a hook's payload as UTF-8. Windows decodes a piped stdin in the ANSI code page, and cp1252 has no character for five byte values UTF-8 uses constantly - Arabic among them - so `sys.stdin.read()` raised on such a payload, and the guard's trap 2 turns a crash into a refusal. With the guard now running in every session, every edit carrying an Arabic letter such as the one encoded `D9 81` would have been refused, for a reason that has nothing to do with the boundary. Found 2026-09-23 while building earlier-brain package C1, and reproduced on Linux with `PYTHONIOENCODING=cp1252` and the payload sent as raw UTF-8 - the first version of the test sent it through `json.dumps`, which escapes every non-ASCII character and hid the case. **WHAT WAS NOT CHECKED:** a real Windows console - NEEDS-CHECKING **AI3**. | **FIXED 2026-09-23** in [PR #308](https://github.com/Ajmalpshaik/Heron-AI/pull/308). The guard and both `heron-session` hooks read the payload as bytes and decode it as UTF-8 (`read_payload`). `tests/test_heron_guard.py` section 4a and the Arabic check in `tests/test_heron_session.py` each failed on the old code and pass now. |
| **166** | **DRIFT NOTHING WAS CHECKING: TOOLS NAMED NOWHERE ON THEIR PAGE, CONTROL CHARACTERS IN THE DOCS, AND A TYPED MCP TOOL TOTAL.** The earlier-brain plan's finding H8, verified 2026-09-22 and re-measured 2026-09-23: eight files in `tools/` named nowhere in `tools/README.md`, three of them not Python, so the page's own `*.py` one-liner could not have seen them; a backspace and a form feed in archived row 5b-21, where the row meant to write their escapes, and a C1 control character in `docs/07`, where `Addins\2020` had once been read as an octal escape; `docs/33` and `docs/34` saying *"Heron has 14 MCP tools"* while `heron_tools.TOOLS` held more than twice that; and the count row `check-docs.py` kept for tools, which looked for the phrase *"N tools mentions"* and matched no sentence in the repository. **WHAT WAS NOT CHECKED:** any other wording of an MCP tool total - the check reads one form, *N MCP tools*, deliberately. | **FIXED 2026-09-23** in [PR #308](https://github.com/Ajmalpshaik/Heron-AI/pull/308). `check-docs.py` section 10 names every file in `tools/` and every skill folder, section 11 reads every tracked file for control characters, double-encoded text and conflict markers, and section 7 derives the MCP total - each seen to fail on these before they were corrected, and each planted in a copy by `tests/test_docs_guard.py`. The nine rows the plan listed are in `tools/README.md`, the three characters are written back as the text they stood for, and the two documents type no MCP total. |
| **167** | **THE CLOUD SETUP SCRIPT INSTALLED NOTHING ON THIS IMAGE, SO THE `heron` MCP SERVER COULD NOT START.** Read in the setup logs of a fresh cloud session on 2026-09-22: `/tmp/heron-apt.log` - `apt-get update` failed with `403 Forbidden` on two third-party PPAs already configured in the image, and because [`tools/cloud-setup.sh`](../../tools/cloud-setup.sh) chains it with `&&`, `dotnet-sdk-10.0` and `python-is-python3` were never installed; `/tmp/heron-pip.log` - both pip attempts failed with *"Cannot uninstall PyYAML 6.0.1, RECORD file not found ... installed by debian"*, because `pip install --upgrade` tries to replace Debian's PyYAML, so `mcp`, `model2vec`, `sqlite-vec` and `pypdf` were all missing; `/tmp/heron-restore.log` - `dotnet` not found. The `heron` server in `.mcp.json` failed to connect, the MCP and .NET suites exit 3, and the script still exits 0. **WHAT WAS NOT CHECKED:** whether the server serves under MCP SDK 2.x, which an unpinned `mcp` installs now - `tests/test_mcp_serves.py` was seen passing only with `mcp<2` installed by hand. | **FIXED 2026-09-23** in [PR #316](https://github.com/Ajmalpshaik/Heron-AI/pull/316). The update's exit code no longer gates the install; pip tries a plain install, then `--ignore-installed`, which installs beside Debian's copies and never removes one; and the script's last lines name whatever does not import, with the log that says why - it must still exit 0. [`tests/test_cloud_setup.py`](../../tests/test_cloud_setup.py) runs the real script against stand-ins that fail the way that image's logs recorded: 9 of its checks failed on the old script, and five planted faults each turned it red. **Measured here:** `pip install --upgrade pyyaml` still fails with the logged words and changes nothing, and `--ignore-installed` would install PyYAML 6.0.3 beside Debian's 6.0.1. **THE NOT CHECKED, NOW CHECKED:** both MCP suites pass on SDK **2.2.0** in a clean environment, so `mcp` stays unpinned ([D-45](../DECISIONS.md)); the *could not run* seen before was the mixed install, not the SDK. **AND THE HEADLINE WAS HALF WRONG:** the script was never why the `heron` server did not connect - the server exits off Windows by design, [row 5b-162](../FRAGMENT-ISSUES.md#), still open. **NOT YET RUN BY A REAL ENVIRONMENT:** the script is pasted into the environment's settings, not read from the checkout - NEEDS-CHECKING **Group AK**. |
| **168** | **`heron_bridge_client` RESOLVES HERON'S LOG AND BRIDGE FOLDERS TO RELATIVE PATHS ON LINUX.** [`mcp/client/heron_bridge_client.py`](../../mcp/client/heron_bridge_client.py) builds `DISCOVERY_DIR` and `LOG_DIR` from `os.environ.get("LOCALAPPDATA", "")`, and on Linux that variable is unset - measured 2026-09-23, they come back as `Heron/bridges` and `Heron/logs`, relative to whatever folder the process happens to run in. It is the Python seam for `HeronPaths.Logs`, so anything that asks it on Linux is told a path inside the working folder. Found while choosing where the development hooks keep their diary: `.claude/skills/heron-session/bin/hook_log.py` refuses a relative answer and falls back to the knowledge folder. **WHAT WAS NOT CHECKED:** whether any Linux test or the bridge round-trip depends on the relative form, or what the C# side resolves on Linux. | **FIXED 2026-09-23** in [PR #316](https://github.com/Ajmalpshaik/Heron-AI/pull/316). `local_app_data()` resolves the folder the way .NET resolves `LocalApplicationData`: `%LOCALAPPDATA%` on Windows, exactly as before; elsewhere an absolute `$XDG_DATA_HOME`, else `~/.local/share`. **THE NOT CHECKED, NOW CHECKED:** no Linux test used the relative form - `test_bridge_roundtrip` reads its pipe name from the host, not from the discovery folder - and .NET 10, asked directly, answers `~/.local/share` there, ignoring a relative `XDG_DATA_HOME` and `LOCALAPPDATA` alike. It was already on disk: the bridge test host had been leaving its discovery files in `~/.local/share/Heron/bridges`, the folder the Python side could not see. [`tests/test_bridge_paths.py`](../../tests/test_bridge_paths.py) failed 6 checks on the old code, and asks .NET again wherever the .NET 10 SDK is. **The hooks' diary moves with it:** on Linux it is Heron's log folder now, not the knowledge folder, and the three hook suites steer it with `XDG_DATA_HOME` as well as `LOCALAPPDATA` - without that they wrote into the real home folder, which is how it was found. |
| **169** | **THREE PAGES STILL SAY HERON HAS NO HOOKS, OR DO NOT SAY WHERE THEY ARE WIRED.** [`docs/README.md`](../README.md)'s row for Q-49 says *"Heron has no hooks of any kind — no `.claude/settings.json`"* and marks the question open, though it was answered and built on 2026-09-09; [`docs/33`](../33-external-repository-research.md) says the same in the present tense in its sections on ECC and gstack, written that day; and [`docs/PROJECT-MAP.md`](../PROJECT-MAP.md)'s row for `.claude/` does not mention `settings.json`, which since 2026-09-23 wires three hooks every session runs. **WHAT WAS NOT CHECKED:** any other page describing the development hooks - `grep -rn 'settings.json' docs` is the start. | **FIXED 2026-09-23** in [PR #316](https://github.com/Ajmalpshaik/Heron-AI/pull/316). The Q-49 row in docs/README.md is closed and says where the hooks are wired; docs/33 keeps both passages as written and gains a dated correction under each; docs/PROJECT-MAP.md's `.claude/` row names `settings.json` and what it wires. **THE NOT CHECKED, NOW CHECKED:** `grep -rn 'settings.json' docs` and a search for *no hooks* found no other page saying Heron has none - OPEN-QUESTIONS Q-49 keeps its question as it was asked, with the answers under it. |
| **170** | **NOTHING CHECKED THAT A DECISION HEADING KEEPS THE TITLE IT WAS FIRST WRITTEN WITH, SO A NUMBER COULD CHANGE MEANING WITH EVERY GATE GREEN.** The check [row 5b-157](../FRAGMENT-ISSUES.md#) closed by calling *"worth its own row"*. [`tools/check-docs.py`](../../tools/check-docs.py) fails a decision number that is DEFINED twice at once, and nothing else looked: D-45 from 2026-09-02 and D-46 from 2026-09-06, until #303 restored them, each named a different decision from the one first written under it, and every gate was green. | **FIXED 2026-09-23, on the owner's instruction.** [`tools/check-decision-titles.py`](../../tools/check-decision-titles.py) reads every decision heading in DECISIONS.md and that file's git history, and fails on a number whose heading no longer carries its first title, a title under a number it was not first written under, a number or a title that has left the log, and a `decisions/D-NN.md` record that no longer opens with its heading. **IT READS HISTORY, NOT A LIST KEPT IN THE TREE, BECAUSE OF HOW THE FIRST ONES WERE LOST** - by a merge that kept one side's log whole ([row 5b-171](../FRAGMENT-ISSUES.md#)). A list in the tree goes through such a merge the way the log does. And it walks `--full-history`, because git's default walk drops the side a merge threw away, which is where the evidence is. **The one excuse lives in the decision:** a changed title passes only when the decision's **Numbering** line QUOTES the first title, a moved one only when that line NAMES the first number. D-45, D-46, D-97 and D-98 pass on exactly that, and each fails the moment its line is taken away. Titles that changed before the check existed are named one by one in its `KNOWN` table - D-00 to D-04, D-26 and D-32, retitled the day they were written, and D-47 to D-49, [row 5b-171](../FRAGMENT-ISSUES.md#) - and an entry no longer needed fails the run, so the table only shrinks. **It exits 2 in a shallow clone** rather than pass on history it cannot see: the gates job fetches the whole history for it (`fetch-depth: 0`) and passes `--published HEAD^1`, so a title a branch changed before it was ever merged is a draft, and a branch that renumbers its own decision after main took the number is not failed for being right. Wired into the gates job, with `tests/test_tag.py`'s pinned list, AGENTS.md, the `heron-ship` skill, `tools/README.md`, `check-gaps.py`'s comment and docs/38 saying so. [`tests/test_check_decision_titles.py`](../../tests/test_check_decision_titles.py) builds its own repositories, so it runs on CI's shallow checkout: a changed title fails, today's log passes over a history carrying every retitle the real one does, and the one-sided merge that lost D-45 to D-49 fails whichever side wrote the number first. **Seen to fail:** nine deliberate breaks of the tool, one per rule, each turned the suite red at the check aimed at it and none crashed it. On main's whole history, measured in a full clone, the tool passes. **NOT DONE:** it reads headings, not the words under them - a decision rewritten under an unchanged title is not something a title can show |
| **171** | **THREE MORE DECISION NUMBERS WERE USED TWICE - D-47, D-48 AND D-49 - AND THEIR FIRST DECISIONS ARE STILL OUT OF THE LOG. ONE MERGE TOOK FIVE, AND [ROW 5b-157](../FRAGMENT-ISSUES.md#) RESTORED TWO.** Found 2026-09-23 while building [`tools/check-decision-titles.py`](../../tools/check-decision-titles.py) ([row 5b-170](../FRAGMENT-ISSUES.md#)), by reading the whole history of DECISIONS.md rather than the two numbers row 5b-157 named. **ONE BRANCH** wrote D-45 on 2026-08-30 (`85619eb`) and D-46 to D-49 on 2026-08-31 (`6582810`, `290edb5`, `5ff8ac0`, `469fd2e`). **MAIN** wrote a different D-45 on 2026-08-31 (`2248cb3`) without having seen them. **The merge of 2026-09-02, `1f884ae`** - *"take the merged session's version wherever both built the same thing"* - came out with exactly main's decision headings, ending at D-45, and the branch's five were gone. On 2026-09-06 three of the freed numbers went to new decisions: D-47 and D-48 by `ec17e79`, D-49 by `2087022`. The first D-47 was *"Risk is part of routing, not only part of permission"*, the first D-48 *"Several fragments may jointly feed one, and the orphan check asks per need"*, the first D-49 *"A need may bind to a provided name that is not its own"*. None of the three is anywhere in the tree today; all three are in `git show c4eea6e:docs/DECISIONS.md`. **TWO SOURCES DISAGREE, AND BOTH ARE RECORDED:** row 5b-157 says `2248cb3` wrote its D-45 *"over"* the first, and `75c22a9` its D-46 likewise, and the Numbering lines on D-45, D-46, D-97 and D-98 name those two commits. The history says `2248cb3` was on a branch that never held the first D-45, and the first D-46 had left the log in `1f884ae` four days before `75c22a9` reused its number. So the lines name the commit that REUSED each number, not the merge that LOST the decision. The dates the lines give for reading an old citation still hold, and nothing was edited. **WHAT WAS NOT CHECKED:** which documents cite D-47, D-48 or D-49 meaning the first decisions. #303 read each reference to D-45 and D-46 in its context and repointed the eleven that meant the first decisions; that reading was not done here | **FIXED 2026-09-23, on the owner's instruction** (*"fix row 5b-171"*), with the repair row 5b-157 took for D-45 and D-46. The three first decisions are back as [D-101, D-102 and D-103](../DECISIONS.md), word for word from commit `5c84302` - the branch tip the merge `1f884ae` dropped them from - each with a Numbering line naming its first number, and D-47, D-48 and D-49 each carry one quoting the title they were first written with. The check's `KNOWN` table lost its three entries, which it would otherwise have failed as no longer needed. **Two changes to the words, both named on the Numbering lines:** D-102 cites the first D-46 and D-49 as D-98 and D-103, and D-103 cites the first D-48 as D-102. Wrapped metadata lines were also joined, which renders the same, because `split-decisions.py` keeps a metadata line in the index only while it starts with `**Key:**`. **EVERY CITATION WAS READ IN ITS CONTEXT** - the half this row had left unread. Five meant the first D-47 and now say D-101: `brain/retrieval-history.md`, `tools/check-intrusion.py`, `tools/README.md` and two in `docs/HANDOVER.md`, on routing risk and ladder-crossings, each first written 2026-08-31 on the branch that held it. One meant the first D-48 and now says D-102: the docstring of `need_binds()` in `brain/heron_fragment.py`, whose own subject is the first D-49. None meant the first D-49. Left as written: three PROVEN fragments whose comments say a sentence naming two capabilities *"belongs to a SKILL (D-47)"* - written on 2026-09-06 before `ec17e79` reused the number, which D-47's Numbering line covers - and the dated session notes, which are history. Section 9 of [`tests/test_check_decision_titles.py`](../../tests/test_check_decision_titles.py) now carries all ten renumbered decisions and fails each the moment its line is taken away. Reading the citations also found [rows 5b-184 and 5b-185](../FRAGMENT-ISSUES.md#). |
| **172** | **HEALING NAMES THE DECISION LOG AND THE NEEDS-CHECKING REGISTER BY THEIR OLD SINGLE FILES, SO ITS REFUSAL OF ONE DECISION'S FILE OR ONE GROUP'S FILE NO LONGER SAYS WHAT IT IS PROTECTING.** Found 2026-09-23 while splitting NEEDS-CHECKING.md one file per group, by searching the code for every list that names the register by path. [`brain/heron_healing.py`](../../brain/heron_healing.py) `SOURCES_OF_TRUTH` exists *"so a refusal can say what kind of thing it is protecting, rather than only that it will not touch it"*, and it matches `docs/DECISIONS.md` and `docs/NEEDS-CHECKING.md` as parts of the path. Since #303 each decision's full record is `docs/decisions/D-NN.md`, and since this split each group is `docs/needs-checking/group-x.md`; neither path contains either name, so `repair()` on one answers with the generic *"not in the derived register, and this agent does not guess"*. Measured 2026-09-23 by calling `repair()` on `docs/decisions/D-30.md` and `docs/needs-checking/group-j.md`. **The refusal itself is unchanged**: anything not in `DERIVED` is refused, which is the fail-closed half, so nothing can be regenerated over either file - only the reason is lost. | **FIXED 2026-09-23, on the owner's instruction.** `SOURCES_OF_TRUTH` names `docs/decisions/` and `docs/needs-checking/`, each in the words of the single file it came from, and `docs/needs-checking-archive/group-` as well: that folder holds the register's done checks, moved out of NEEDS-CHECKING.md by #303 with their words unchanged - the same gap, found while fixing this one. Only the archive's group files are named, because its README is the index its own tool rewrites. `repair()` on each now says what it protects and ends *"Nothing regenerates it."* Section 3 of [`tests/test_healing.py`](../../tests/test_healing.py) asks all three for that sentence and for a word their path cannot supply - the generic refusal repeats the path, so a check for *decision* alone passes on `docs/decisions/` unfixed - and was run red against the unfixed module first. |
| **173** | **`CONNECT_AIR_TERMINALS` SAID IT MOVES NOTHING, AND THE REVIT CALL IT MAKES WAS SEEN ELSEWHERE LIFTING A TERMINAL 625 MM.** Found 2026-09-23 by the earlier-brain plan (package C4, row 1) and checked in the files first: `brain/fragments/connect-air-terminals/impl/any/fragment.cs` ended its header with *"NOTHING IS MOVED. The terminal has to be in the right place already."* and the card's purpose said *"It connects and moves nothing."* Nothing in the fragment moves a terminal - but `MechanicalUtils.ConnectAirTerminalOnDuct` was observed by another tool, on real models and mostly on Revit 2020, lifting one 625 mm to meet its duct. **Not yet reproduced here.** A tap that drags a diffuser out of its ceiling still returns `connected`, so the count could never show it. **Not checked**: whether Revit 2024 does it at all, and whether a terminal that did not move reads back at exactly the same point. | **FIXED 2026-09-23 in code, NOT PROVEN.** Every terminal handed in has its point and its facing read before any call and again after one regeneration; every change goes to a new `moved` (id, distance, x, y and z in mm, degrees turned) and an unreadable one to `notMeasured`, never counted as unmoved. **No distance is called acceptable**: the only comparison is Revit's own `XYZ.IsAlmostEqualTo`, and how far a terminal may move is the owner's question, put in the pull request rather than answered with an invented number. Card and cases corrected, with a negative that must NOT be listed - a terminal already at its duct. DRAFT, so no proof moved. Compiles on all eight releases. NEEDS-CHECKING AJ1 **REPRODUCED 2026-09-23 on the owner's PC, Revit 2024 - [NEEDS-CHECKING AJ1](../needs-checking/group-aj.md):** all three terminals lifted onto the duct's underside, 550.0, 101.6 and 550.0 mm, and `moved` matched the owner's spot elevations exactly; no tap fitting; a supply diffuser joined to a return duct. Row 5b-187. The owner accepted the proof drafted on that arrangement. |
| **174** | **`AUTO_SIZE_PIPE` READ ITS SIZE BACK BEFORE THE REGENERATION THAT APPLIES THE SNAP, SO ITS OWN SNAPPED-SIZE REFUSAL COULD NEVER FIRE.** Found 2026-09-23 while adding the plan's row 2 count. `brain/fragments/auto-size-pipe/impl/any/fragment.cs` set the diameter and read it straight back with no `doc.Regenerate()` between. `SET_MEP_SIZE`'s header records that *"immediately after the set, the parameter still reports WHAT WAS ASKED FOR"* and that *"the snap happens at REGENERATION"*, and `AUTO_SIZE_MEP` regenerates before its read-back for that reason. So the case this fragment's own `tests/cases.yaml` declares - a segment offering 32 and 65, asked for 50, reported in `refused` - could not come back. **Not checked**: the timing is SET_MEP_SIZE's header, not something measured here. | **FIXED 2026-09-23** - one regeneration per pipe before the read-back, as AUTO_SIZE_MEP does. DRAFT and never proved, so no proof moved. NEEDS-CHECKING AJ2 |
| **175** | **THREE FRAGMENTS ANSWER "IS IT IN THE ROOM" TWO DIFFERENT WAYS, AND ONE CARD SAYS THE OPPOSITE OF ITS OWN CODE.** Found 2026-09-23 checking the plan's row 5, which the plan marked *reported*: a device in the ceiling void is outside its room's VOLUME (observed elsewhere, mostly on Revit 2020). In the files: `count-by-spatial-container` tests the element's X and Y at the container's own Z (`impl/any/fragment.cs:84-88`), so a ceiling-void diffuser counts IN the room below; `check-room-mep-completeness` calls `room.IsPointInRoom(point.Point)` at the device's own insertion point (`:94-99`) - the volume test - while its purpose says *"one in the ceiling void above the room still counts"*; `filter-elements-in-room` asks the volume test too (`:65-71`), and its purpose says an element above the ceiling *"can be genuinely inside"*. Where a room's volume stops at the ceiling, the first counts the diffuser in and the other two leave it out. **AND THE SIGNED PROOF OF `check-room-mep-completeness` NEVER REACHED THE ROOM TEST.** Its positive handed in 8 ducts as the devices; a duct has a location CURVE and the code counts only a location POINT (`:94-95`), so every room came back *"short of: Ducts (0 of 1)"* whatever the model held. **Not checked**: which answer Revit gives on a real room - that needs a model. | **OPEN - recorded, not settled.** The plan says settle each with evidence, not by picking one: all three cards carry a note pointing here, and NEEDS-CHECKING AJ5 is the arrangement. The proof is the owner's signed record and is not touched - whether to re-take it with point-based devices is the owner's call **RUN 2026-09-23 on the owner's PC - [NEEDS-CHECKING AJ5](../needs-checking/group-aj.md):** exactly as the code predicts - with the room's top at the ceiling, `count-by-spatial-container` counted the void diffuser IN and the other two OUTSIDE; with its Upper Limit at the level above, all three inside. Which meaning a room should have is the owner's; asked once, not answered. |
