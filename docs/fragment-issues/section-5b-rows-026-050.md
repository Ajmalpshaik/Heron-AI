# Fragment issues — section 5b, rows 26 to 50

> Rows 26 to 50 of section 5b of [the register](../FRAGMENT-ISSUES.md), in their own file since 2026-09-23 so that
> they can be read alone. **The section's own rules are on that page, under its heading.** A new
> row goes at the end of the section's last file, whatever its number, and the next run of
> [`tools/split-register.py`](../../tools/split-register.py) moves it to the file its number
> belongs to. [`tools/register-text.py`](../../tools/register-text.py) reads every row back into
> the register for every tool that reads it, so a row here is seen exactly as it was seen there.

| # | Defect | State |
|---|---|---|
| **26** | THE "NEVER COMPILED, NEVER RUN" SENTENCE IS IN FOUR FILES, AND THE TWO HARSHEST COPIES WERE THE TWO… — [full row](../fragment-issues-archive/reading-defects-5b-026-050.md#row-5b-26) | **FIXED 2026-09-21, all four, in the sitting that found the third.** |
| **27** | **THE RISK AND THE PROVIDER PRINTED BESIDE IT CAME FROM TWO DIFFERENT FRAGMENTS.** Found 2026-09-21 reading `mcp/server/heron_runtime.py` in the sweep. `_judge` computed `risk = _worst_risk(rows)` - correctly the HIGHEST across every provider, with its own docstring giving the reason - and then set `provider = rows[0].get("id")`, whichever happened to sort first. **SO A CAPABILITY ANSWERED BY A READ FRAGMENT AND A MODIFY ONE REPORTED `risk: MODIFY` BESIDE THE READ FRAGMENT'S NAME.** In the `BLOCKED_BY_TRUST` verdict that is the worst version of it: the sentence says *"MODIFY, and the ceiling here is EXECUTE"* and names a fragment that is not the one being blocked, so a planner - or a person reading the snapshot - would go and look at the wrong fragment. The same pair is returned for `NEEDS_REVIT` and `AVAILABLE`. **IT IS LATENT, AND THAT WAS CHECKED RATHER THAN ASSUMED**: `tools/check-reachable.py` records `snapshot` as deliberately unwired - *"the provider for planning code that does not exist ... PROPOSALS.md F1"* - so nothing in production reads either field today. It is [row 1](../FRAGMENT-ISSUES.md#)'s shape exactly: a public function whose whole job is to be the answer, and the next caller will not know. **AND THE SUITE HAD THE CASE AND HALF THE CHECK.** `tests/test_runtime_snapshot.py` already builds the mixed READ/MODIFY capability under the heading *"The risk of a capability is its worst provider, never its first"* and asserted only the verdict. The missing half was the name. **THE REPAIR MAKES IT UNGETTABLE-WRONG RATHER THAN CORRECT ONCE:** `_worst_risk` becomes `_worst_provider` and returns the ROW, so the risk and the id are read off the same object and a caller cannot pair them up wrongly again; `_worst_risk` stays as a one-line wrapper for the two verdicts that want the figure alone. **MEASURED:** the new check fails against the previous implementation - reverting the file leaves the verdict green and the provider check red - and passes after. **WHAT WAS NOT CHECKED:** whether `brain/heron_capability.py`, which this file says it copies the rule from, pairs them correctly; it was not read in this sitting. | **FIXED 2026-09-21, in the sitting that found it.** Nothing is broken today, and the reason to fix it now rather than record it is that the repair is four lines and the alternative is a correct-looking pair of fields waiting for its first reader |
| **28** | EIGHTEEN OF THE FORTY-SIX ERROR CODES THE ADD-IN CAN PRODUCE WERE IN NO TABLE, SO EVERY ONE OF THEM… — [full row](../fragment-issues-archive/reading-defects-5b-026-050.md#row-5b-28) | **FIXED 2026-09-21, in the sitting that found it.** |
| **29** | ONE SETTINGS FILE, TWO READERS, AND AS OF THIS MORNING THEY DISAGREED. — [full row](../fragment-issues-archive/reading-defects-5b-026-050.md#row-5b-29) | FIXED 2026-09-21, in the sitting that found it, and it is the one defect in this section… |
| **30** | TWENTY BLANK LINES SPLIT THE REGISTER INTO ABOUT TEN SEPARATE TABLES, AND NO TOOL COULD SEE IT… — [full row](../fragment-issues-archive/reading-defects-5b-026-050.md#row-5b-30) | **FIXED 2026-09-21.** |
| **31** | THE CHECKPOINT SAVE DELETED THE OLD FILE BEFORE WRITING THE NEW ONE, AND ITS OWN COMMENT SAID IT… — [full row](../fragment-issues-archive/reading-defects-5b-026-050.md#row-5b-31) | **FIXED 2026-09-21, in the sitting that found it.** |
| **32** | A STAGE THAT THREW WAS RETRIED THREE TIMES ON THE WRITING PATH, IN THE FILE WHOSE HEADER SAYS IT… — [full row](../fragment-issues-archive/reading-defects-5b-026-050.md#row-5b-32) | **FIXED 2026-09-21, in the sitting that found it.** |
| **33** | MINOR, AND BOTH IN THE ONE FILE THAT REFUSES SOFT FAILURES EVERYWHERE ELSE. — [full row](../fragment-issues-archive/reading-defects-5b-026-050.md#row-5b-33) | **FIXED 2026-09-21, in the sitting that found it.** |
| **34** | A PROBE WHOSE DOCSTRING ASKS A QUESTION ITS BODY NEVER ASKED, IN THE REPORT THAT ENDS "A CHECK THAT… — [full row](../fragment-issues-archive/reading-defects-5b-026-050.md#row-5b-34) | **FIXED 2026-09-21, in the sitting that found it.** |
| **35** | THE ONE BLOCK WRITTEN TO MAKE A RISK CROSSING VISIBLE WENT SILENT EXACTLY WHEN IT COULD NOT SEE. — [full row](../fragment-issues-archive/reading-defects-5b-026-050.md#row-5b-35) | **FIXED 2026-09-21, in the sitting that found it.** |
| **36** | THREE OF THE SEVEN REFUSALS DID NOT CARRY THE SENTENCE THIS FILE SAYS EVERY REFUSAL CARRIES. — [full row](../fragment-issues-archive/reading-defects-5b-026-050.md#row-5b-36) | **FIXED 2026-09-21.** |
| **37** | **THE SERVER'S TWO `NOT PROVEN` BANNERS WERE BOTH FALSE, AND ONE OF THEM IS QUOTED BACK AT IT IN THE DOCUMENT THAT DISPROVED IT.** Found 2026-09-21 reading `mcp/server/heron_mcp_server.py` top to bottom. Its module docstring carried two boxed banners. **The first** said `revit_change` *"has never been run against a real Revit, and `apply` has never been sent by anything"* — this same file sends `"apply": "true"` at line 839, and NEEDS-CHECKING E11-E15 record it running on **2026-09-15**, including `CREATE_LEVEL ran in Project2`, a level **kept** in a model. **The second** said *"the add-in code behind `revit_apply_move` has never been compiled or run"* — it compiles on Revit 2020 through 2027 in CI, and NEEDS-CHECKING **B8** moved **3 ducts up 200 mm** on **2026-09-07**, the first time Heron changed a Revit model. That document quotes this exact sentence and answers *"It has now, and this is what it found"* — the run found the token collision that made the write path structurally impossible. **THIS IS [ROW 26](../FRAGMENT-ISSUES.md#)'s SENTENCE, FIFTH AND SIXTH COPY.** That row found the never-compiled claim in four files and fixed four. **THE COST IS NOT OVER-WARNING, IT IS THE OTHER BANNERS.** A `NOT PROVEN` box is worth reading only while every one of them is true; two that are provably false teach a reader to scroll past the one that is not | **FIXED 2026-09-21, AND WHAT IS STILL OWED IS NAMED IN EACH.** Neither banner was deleted — a box that says nothing is worse than one that is wrong — both now say what happened, when, and what is still missing. `revit_change`: RUN, **and E16 still open** — the 2026-09-15 run also wrote a level into the model the chat was NOT pointed at, because `ProjectKey` is `ProjectInformation.UniqueId` and comes from the TEMPLATE, so it is one string for every project made from it; the fix is in and E16 is the re-run that would prove it. `revit_apply_move`: RUN, **not measured** — [D3](../FRAGMENT-ISSUES.md#) is still open, *move them, then MEASURE one*, and nobody has put a tape on what it did. **THE DATES ARE IN THE BANNERS ON PURPOSE**, including the date the false sentence was removed, so the next reader can see how long it stood: six days for one, fourteen for the other |
| **38** | FIVE PLACES THAT RUN A FRAGMENT TYPED THEIR OWN DEADLINE, AND IT IS THE EXACT DEFECT THE SUITE ONE… — [full row](../fragment-issues-archive/reading-defects-5b-026-050.md#row-5b-38) | **FIXED 2026-09-21. ONE DERIVATION, FLOORED AT THE NUMBER THEY CHOSE.** |
| **39** | THE ONE TOOL WHOSE JOB IS TO SWITCH MODELS THREW AWAY THE REASON IT COULD NOT. — [full row](../fragment-issues-archive/reading-defects-5b-026-050.md#row-5b-39) | **FIXED 2026-09-21.** |
| **40** | FIVE OF THE SEVEN COMMANDS HID A PROTOCOL MISMATCH ON THE ONE BRANCH WHERE IT IS THE ANSWER. — [full row](../fragment-issues-archive/reading-defects-5b-026-050.md#row-5b-40) | **FIXED 2026-09-21, ALL FIVE.** |
| **41** | `count` WITH NO SESSION NAMED TOOK EVERY REVIT THE USER HAD OPEN, AND IT IS THE DEFECT THE SERVER… — [full row](../fragment-issues-archive/reading-defects-5b-026-050.md#row-5b-41) | **FIXED 2026-09-21, THE SAME WAY THE SERVER WAS.** |
| **42** | **THE TOOL THAT ASKS WHETHER THE HOMEWORK WAS DONE COUNTED A GATE RECORDED AS `NOT RUN` AS DONE.** Found 2026-09-21 by watching it happen to this branch's own evidence. `tools/change-evidence.py` keeps **three** states apart on purpose - `PASS`, `FAIL`, `NOT RUN` with `exit: null` - and `tests/test_change_gate.py` already pinned the bottom half of that: *"no suites at all is NOT RUN, not PASS"*. Then `tools/check-change.py` asked `g not in ran` - **the presence of the KEY, never its VALUE** - so a gate the record carried as NOT RUN satisfied the obligation. **MEASURED, NOT ARGUED**: a capture whose `--tests` argument matched no suite recorded `tests: NOT RUN`, and this tool printed **PASS - "Scope matches the declared intent and every gate it owes was run"**. The line a reader sees was wrong in the same way: `Gates actually run:` listed every key in the record, `tests` among them. **THIS IS THE ONE TOOL THAT CANNOT HAVE THIS DEFECT** - every other gate answers a question about the code, and this one answers *did anybody ask the others*. It is the repository's own standing lesson at the top of the stack: a guard that fails open and says nothing is indistinguishable from a guard that passed | **FIXED 2026-09-21.** `unmet` now reads the RESULT: a gate counts as run only when it is `PASS` or `FAIL`, so NOT RUN is owed and unmet and the verdict is **REVISE**, naming it. The report separates the three: `Gates actually run` lists only what ran, a new `In the record, NOT RUN` line names what the record carries as unrun, and `Owed and not run` names what that costs. **A GATE THAT RAN AND FAILED IS STILL REVISE** - checked, because the obvious form of this fix quietly turns a failure into a missing gate and the two want different answers. **PINNED IN `tests/test_change_gate.py`**, which is where the bottom half of the rule already lived: four checks built on a written evidence record - every gate PASS, then `tests` NOT RUN, then `tests` FAIL. **Proved by making it fail**: with the previous `check-change.py` stashed back in, **3 of the new checks fail**; with the fix, all pass. **AND THE FIRST THING IT CAUGHT WAS THIS BRANCH** - the evidence record behind rows 37 to 41 said `tests: NOT RUN`, the gate had called it PASS, and after the fix it correctly demanded the suites be run. **THE TEST ITSELF WAS THEN WRONG IN A WAY WORTH RECORDING.** Its first version typed the five gates a medium-risk change owes into the synthetic record - and *owed* is not a constant: a diff touching `revit/` raises the `revit-version` signal, which owes `check-compile` and `check-api-surface` as well. So the suite went red on the very next commit to edit an add-in file, reporting *owed and not run* about gates the test had simply never listed. **A test whose verdict depends on the diff it happens to be run beside is not testing the thing it names.** The record is now built from `ALWAYS`, `BY_RISK` and `BY_SIGNAL` read out of the tool, so it holds every gate that could ever be owed whatever the tree looks like |
| **43** | THE CHECK THAT STOPS A FRAGMENT CONSUMING THE WRONG SET OF ELEMENTS COMPARED WITH A SUBSTRING… — [full row](../fragment-issues-archive/reading-defects-5b-026-050.md#row-5b-43) | **FIXED 2026-09-21.** |
| **44** | **THE LAST COPY OF THE NEVER-COMPILED SENTENCE, AND IT CARRIED AN INSTRUCTION TO DELETE ITSELF THAT NOBODY COULD ACT ON.** Found 2026-09-21 reading `platform/Heron.Core/HeronPermissions.cs`. Its class summary said the write path *"has never compiled, never loaded and never moved anything"* and closed with *"Delete this paragraph when the write path has been proven against a real model."* It has been: NEEDS-CHECKING **B8**, 2026-09-07, Revit 2024, **three ducts moved up 200 mm** - and the add-in compiles on 2020 through 2027 in CI. **THE SAME FILE RECORDS THE SAME EXPIRY ONE METHOD DOWN**: `Explain()` carries a comment headed *"THE REASON HAD GONE STALE IN THE TEXT A USER READS"* about this exact claim. The user-facing string was corrected and the paragraph three screens above it was not. [Row 26](../FRAGMENT-ISSUES.md#) found four copies and fixed four; [row 37](../FRAGMENT-ISSUES.md#) found the fifth and sixth; this is the seventh | **FIXED 2026-09-21, AND THE INSTRUCTION IS WITHDRAWN WITH IT.** The paragraph now records what happened and when, and separates the two things it had run together: **why the default is off never expires** - changing somebody's model is their decision - while the evidence about the path does. **A PARAGRAPH WHOSE REMOVAL IS CONDITIONAL ON A MEASUREMENT IS A PARAGRAPH THAT GOES STALE THE DAY THE MEASUREMENT LANDS**, which is exactly what happened, so the delete-when instruction is gone rather than reworded. What is still owed is named: **D3** - move them, then MEASURE one. `grep` across `platform/`, `revit/` and `mcp/` returns nothing for the sentence now |
| **45** | THE PARAGRAPH EXPLAINING WHY AN IN-REVIT COMPILE FAILURE OUTRANKS THE GATE SPENT ITS LIFE ABOVE THE… — [full row](../fragment-issues-archive/reading-defects-5b-026-050.md#row-5b-45) | **FIXED 2026-09-21.** |
| **46** | A RETRY LOOP THAT SLEPT THROUGH 300 ms OF GUARANTEED FAILURE, PER LINE, UNDER TWO COMMENTS THAT… — [full row](../fragment-issues-archive/reading-defects-5b-026-050.md#row-5b-46) | **FIXED 2026-09-21.** |
| **47** | THE HOOK WHOSE SUBJECT IS "A HOOK THAT LOOKS LIKE IT WORKS AND REFUSES NOTHING" CLAIMED SOMETHING… — [full row](../fragment-issues-archive/reading-defects-5b-026-050.md#row-5b-47) | **FIXED 2026-09-21, IN BOTH COPIES.** |
| **48** | **THE SKILL THAT EXISTS TO STOP A PROSE COUNT GOING STALE HAD FOUR OF THEM, AND ITS OWN PARAGRAPH SAYS THIS IS THE THIRD TIME.** Found 2026-09-21 reading `.claude/skills/heron-ship/SKILL.md`. That file carries the rule twice - *"derive the number, do not read a pass total here"* and *"a prose total is a cache with no invalidation"* - beside a warning that it had already drifted on 2026-09-12 and again on 2026-09-17. **FOUR MORE HAD DRIFTED UNDER IT.** (1) A table of the **four** suites that cannot run without an optional dependency, when `test_kernel.py`, `test_binding_note.py` and `test_stack_guard.py` each build and run a C# test host and each exits 3 without an SDK - **eight** suites can say *could not run* today. (2) The sentence under it, *"a fourth failure is probably yours"*, which would send somebody hunting their own change over a missing `dotnet`. (3) `check-revit-gate` *"reports 62 for links and 59 for refusal reporting"* - **measured today: 67 and 64**, because both move with every fragment added. (4) `check-compile` *"all 5 projects"* - it is **8**, three rows above a row that already says to derive a total *"because this row said 372 for three days after it was 395"* | **FIXED 2026-09-21, AND EVERY ONE REPLACED BY A COMMAND OR BY NOTHING.** The four-suite table is gone: what replaces it is the **two installs** that cover all of them - the MCP SDK and a .NET SDK, which do not change when a suite is added - and two commands that DERIVE which suites are waiting. *"A fourth failure is probably yours"* stops counting and says the thing that does not go stale: **exit 3 is waiting on a dependency, exit 1 is a finding**. The gate's two figures are now *read from the tool*, and `check-compile`'s row says *every project* and points at the count the tool prints. **EACH CORRECTION CARRIES THE DATE AND THE OLD NUMBER**, because this file's value is that it records its own drift - deleting the evidence would make the fourth time look like the first |
| **49** | **THE FOUR STATES SURVIVE EVERYWHERE EXCEPT THE ONE PLACE THE WHOLE REPOSITORY IS JUDGED.** Found 2026-09-21 reading `.github/workflows/gates.yml`. [`AGENTS.md`](../../AGENTS.md) states the rule - *"Separate the four states and never merge them: PASS · FAIL · NOT RUN (say why) · NEEDS REAL REVIT"* - and `brain/heron_unit_test.py` was written to keep it, under a heading that says so twice over: **THREE EXIT CODES, NOT TWO, AND THE THIRD IS THE POINT**, and *"a sweep that collapsed 3 into 'failed' would report a machine without an optional package as a repository with a regression in it - and the person reading it would go looking for a bug that is not there."* **CI'S SWEEP IS A SECOND COPY OF THAT LOOP, WRITTEN IN BASH, AND IT COLLAPSES 3 INTO FAILED.** `python "$t" > /dev/null 2>&1 \|\| basename "$t"` takes any non-zero exit, so `test_bridge_roundtrip.py` printing *"COULD NOT RUN - Build it first ... This is exit 3, which is NOT a pass: nothing was checked"* arrived on the failure list beside a suite that ran and broke - and the comment four lines above that loop names exactly the confusion it was causing: *"a lump pass total is how a real regression hides, so the build compares the SET of failures instead of counting them."* **THE SET CANNOT TELL "DID NOT RUN" FROM "RAN AND FAILED" EITHER.** Three suites are on the known-failure list for needing the MCP SDK or a built .NET host, and all three exit 3; **once a suite is excused, a genuine failure in it is invisible.** MEASURED BOTH WAYS on a three-suite fixture: flip the excused suite from exit 3 to exit 1 and today's logic reports nothing about it, while the three-way split puts it on the failure list AND names it as one that ran after all. **AND THE AGENT'S OWN DOCSTRING CLAIMED THE OPPOSITE** - *"the workflow's loop and this agent run the same suites the same way, and neither is generated from the other, which is a duplication worth naming rather than hiding"* - honest about the duplication and wrong about the one dimension the file exists for | **FIXED 2026-09-21 BY DELETING THE SECOND COPY RATHER THAN CORRECTING IT.** The job now runs `brain/heron_unit_test.py --json`, which is the sweep, and splits four ways: **failed** (red, whatever list it is on), **could not run** (compared against a list that is now a known-NOT-RUNNABLE list, not a known-failure one), **hung** (its own state - not a pass, not a failure, it never said anything), and passed. `--json` is new: the printed report is for a person and drops the two things a caller needs most, which is why the bash existed at all. **NOTHING RE-RUNS ANYTHING NOW.** The old job re-ran each new failure to recover the reason it had sent to `/dev/null` (rows 160 and 166); the sweep already keeps every failure's last line, so the reason arrives with the verdict. Gated in `tests/test_unit_test.py`: `--json` is valid JSON, carries the three lists, and a WAITING suite never appears among the failures - proved by making a fixture suite exit 3 and watching it stay out of `failed`. The docstring says what is now true: **one sweep, one place** |
| **50** | THE FILE AN OUTSIDE CONTRIBUTOR OPENS FIRST SAID 193 FRAGMENTS HAD NEVER MET A MODEL. IT IS 68… — [full row](../fragment-issues-archive/reading-defects-5b-026-050.md#row-5b-50) | **FIXED 2026-09-21, AND WRITTEN IN THE ONE FORM THE GATE CAN VERIFY** |
