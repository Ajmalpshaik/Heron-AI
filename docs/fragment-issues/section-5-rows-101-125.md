# Fragment issues — section 5, rows 101 to 125

> Rows 101 to 125 of section 5 of [the register](../FRAGMENT-ISSUES.md), in their own file since 2026-09-23 so that
> they can be read alone. **The section's own rules are on that page, under its heading.** A new
> row goes at the end of the section's last file, whatever its number, and the next run of
> [`tools/split-register.py`](../../tools/split-register.py) moves it to the file its number
> belongs to. [`tools/register-text.py`](../../tools/register-text.py) reads every row back into
> the register for every tool that reads it, so a row here is seen exactly as it was seen there.

| # | Defect | State |
|---|---|---|
| **101** | **`optional: true` IS DECLARED ON THREE NEEDS AND READ BY NOTHING AT ALL.** Found 2026-09-15 by the same sweep as row 95 — the library puts five keys on a need (`name`, `type`, `source`, `binds`, `optional`) and the executor read three of them. `copy-view-filters`, `create-line` and `create-sheet-list` each mark one need `optional: true`; all three are `source: request`, so `BindNeeds` reports them as values the caller must supply and the fragment cannot run without one. Neither `RevitFragment.cs` nor `heron_fragment.py` mentions the key. **Not fixed, deliberately:** "a need that may be absent" is a decision about refusals, and this library refuses rather than quietly supplying an empty value on purpose — `BindNeeds` says so in its own header. Recorded rather than invented. | Open — listed in `UNREAD_KEYS` in `tests/test_fragment_needs.py` so it is accounted for rather than forgotten, and the test fails if a SIXTH key appears **RE-VERIFIED 2026-09-19 and unchanged in every particular.** Still exactly **three** needs carry the key - `copy-view-filters.onlyFilterNamed`, `create-line.view`, `create-sheet-list.sortByField` - all three still `source: request`, and a grep across `brain/heron_fragment.py`, `revit/Heron.Revit.Addin/RevitFragment.cs` and `mcp/client/heron_bridge_client.py` still finds no reader: the single hit is a doc comment at `RevitFragment.cs:3242` using the English word about an unrelated need. Recorded so nobody re-runs this sweep to learn nothing moved |
| **102** | **`check-minimum-clearance` USED ITS PER-CATEGORY CLEARANCES RAW, IN FEET — AND ONLY THE PAIRS A RULE JUDGED.** The D-71 units pass recorded in [row 51](../FRAGMENT-ISSUES.md#) converted one length per fragment and walked past the second; this is the third instance and the first in a DICTIONARY, which is why that sweep could not have found it — it looked for a `double` request input with no conversion, and `rules` is an `IDictionary<string, double>`. `defaultClearance` was converted on line 29 and every value inside `rules` was used exactly as typed against a gap measured in feet, so `rules="Walls=150"` asked for 150 mm and demanded **150 FEET**. The shape of the wrong answer is the dangerous part: the default-judged pairs stayed correct while the rule-judged ones were 304.8× out, so the report reads as a model full of clashes with the rule column beside them looking like the explanation rather than the fault. **Found by reading, not by a run**, and it could not have been found by a run — see row 98. | Fixed — `rules` is converted into a local `clearances` dictionary once, before use, copied rather than written back so a re-run on the same values cannot divide twice. **Unverified against a model**: `rules` cannot be typed at all (row 98), so no arrangement on any model reaches the line |
| **103** | **`check-minimum-clearance` CANNOT BE PROVED AT ALL, BY ANY ARRANGEMENT, AND IT IS `rules` THAT STOPS IT.** Asked for 2026-09-15 alongside `find-nearest-elements`; run both ways on Project1, Revit 2020, session 23804, and refused both ways. Without the value: `[needs_request_values]` — *"'rules (IDictionary<string, double>)' is a value the CALLER supplies"*. With one: `--set "rules=Walls=150"` gives `[bad_request_value]` — *"\"IDictionary<string, double>\" is not one of them yet, so this fragment still has no way to receive it"*. `FromRequest` refuses `IDictionary` by name, and the need is `source: request` with no way to omit it (row 96), so the two refusals close the loop on each other. `tools/jobs/defect-finders-2.yaml` predicted exactly this in its own note — *"`rules` is a dictionary and is left unset — if the fragment demands it, that is a D-54 gap worth recording rather than working around"* — and it demands it. **This is the machinery, not the model:** no second model, view, category or threshold changes it. | Open — a syntax decision of the same class as [D-72](../DECISIONS.md)'s four, and the owner's to make. Section 6 below now counts it **THE DECISION WAS MADE THE SAME DAY THIS ROW WAS WRITTEN, AND NOBODY CAME BACK TO THE ROW.** Checked 2026-09-19. [D-73](../DECISIONS.md) is dated **2026-09-15** - the date in this row - and `RevitFragment.FromRequest` has accepted `IDictionary<string,double>` since: `revit/Heron.Revit.Addin/RevitFragment.cs:3468` matches it by name, and the XML comment above it at `:2237` names **`check-minimum-clearance.rules`** as one of the two needs it was built for. `tools/generate-jobs.py`'s `RECEIVABLE` carries the same three spellings, and `tests/test_generate_jobs.py` reads the accepted types out of the add-in and fails when the two disagree - it passes. **So `rules` can be typed in**, and this row's headline - *cannot be proved at all, by any arrangement, and it is `rules` that stops it* - stopped being true four days ago. **THE FRAGMENT IS STILL UNPROVABLE AND THE REASON IS A DIFFERENT ONE.** `python tools/generate-jobs.py` now lists it under *"two needs are both filled by 'elements', and the setup chain leaves one"*, which is [row 135](../FRAGMENT-ISSUES.md#), a different problem with a different fix and a different owner-decision. **ONE THING IS NARROWER THAN IT LOOKS:** D-73's status line still reads *Proposed - owner has not read this back*, so the CODE exists and the DECISION is unsigned. That is a real distinction and not a quibble - the syntax this row was waiting on can be used today and could still be sent back |
| **104** | A FRAGMENT THAT RECURSES WITHOUT A FLOOR DOES NOT RETURN AN ERROR - IT ENDS REVIT. — [full row](../fragment-issues-archive/proving-defects-101-125.md#row-104) | **FIXED AND PROVED IN FRONT OF REVIT 2026-09-16 - J9 PASSED.** |
| **105** | TWO WRITES THAT LOOKED ATOMIC AND WERE WORSE THAN A PLAIN OVERWRITE. — [full row](../fragment-issues-archive/proving-defects-101-125.md#row-105) | **FIXED 2026-09-16.** |
| **106** | **FOUR CLEARANCE FRAGMENTS COMPARE EVERY ELEMENT AGAINST EVERY ELEMENT WHILE FIVE OTHERS ASK REVIT.** `find-clashes`, `check-equipment-clearance`, `check-valve-accessibility`, `select-in-region` and `select-touching` all narrow through Revit's own spatial index (`BoundingBoxIntersectsFilter` and its siblings). **`check-minimum-clearance`, `check-vertical-clearance`, `check-insulation-clearance` and `find-nearest-elements` do not** - they nest two `foreach` loops over bounding boxes with no spatial pruning. The good pattern is already in the library; four fragments do not use it. Found 2026-09-16 | **MEASURED 2026-09-16, AND THE MEASUREMENT REFINES THE ROW RATHER THAN CONFIRMING IT. STILL OPEN.** Revit 2024 session 33812, `test projject.rvt`. **`pairsChecked` is honest** - it reported **9,506**, and a separate probe counting the same thing independently derived 9,506, to the unit. **THE LOOP ALREADY PRUNES, which this row did not know.** It pairs only elements that HAVE a bounding box, and on this model that is **98 of 3,565 placed elements** - so of 12,709,225 naive pairs it skipped **12,699,719**. The n-squared is over GEOMETRY, not over everything handed in. **AND THIS MODEL CANNOT ANSWER THE SCALING QUESTION.** Its largest physical category is **28 Walls**; the biggest categories are settings and definitions - 180 Electrical Load Classification Parameter Elements, 125 Space Type Settings, 91 Legend Components. There are no ducts and no pipes at all (the 17 `Pipe Segments` are segment definitions). **The loop cost is not merely small here, it is unmeasurable:** the 9,506-pair run finished in **1.52 s** total and the 756-pair run in **2.21 s** - the bigger one was FASTER, so both are entirely process start-up and one bridge round trip. **WHAT WOULD ANSWER IT:** a real MEP model, where nearly every element has geometry and the pruning above stops helping. Until one is open this row cannot be closed or dismissed, and no claim is made here that it is slow, because nothing has yet timed it at a size where it could be - and `check-minimum-clearance` **already reports `pairsChecked`**, so one run on a real model answers it without new tooling. **The fix is not a swap.** A spatial filter finds what intersects; clearance is about what does NOT touch yet, so the outline has to be grown by the threshold first. And it edits fragment files, so **those four fingerprints change and those four proofs go stale and need re-signing** - a real bill, and the reason this is recorded rather than done |
| **107** | REVIT CANNOT RENAME A PHASE AT ALL - TWO DOORS, TWO RELEASES, SAME REFUSAL - AND THE COMPILE AND… — [full row](../fragment-issues-archive/proving-defects-101-125.md#row-107) | **CLOSED - not a defect, a limit in Revit.** |
| **108** | **A REFUSAL RETURNED AS A `IList<string>` IS CUT TO 60 CHARACTERS, AND IT HID THE ONE FACT THE RUN EXISTED TO FIND.** Same run as [row 107](../FRAGMENT-ISSUES.md#). `RENAME_PHASE` declared `refused` as `IList<string>` - the shape `RENAME_WORKSET` and most of the library use - and the proof record came back `refused: 1 item(s) ['Existing' could not be renamed to 'HERON PROOF PHASE' - Thi...]`. **Three characters of the answer.** `RevitFragment.Describe` returns a `string` WHOLE and passes every item INSIDE a collection through `Shorten`, which is `text.Length > 60 ? Substring(0, 60) + "..."`. Both behaviours are right on their own: a list preview exists so a proof can see WHAT came back without printing forty rows. **THE MISTAKE IS DECLARING A ONE-OF-ONE AS A LIST.** The fragment's checks are a single if/else chain, so at most ONE reason is ever produced - the list described the fragment wrongly before it truncated anything. Changed to `string`, and Revit's two refusals then arrived in full and in one line. **THE RULE WORTH CARRYING: where the MESSAGE is the answer rather than a sample of many, declare it a string.** A count is honest about a list; its contents are not. Worth a sweep of the other fragments whose only refusal is a single reason in a one-item list. | **OPEN as a library-wide question.** It was fixed in `RENAME_PHASE`, and that fragment has since been removed with [row 107](../FRAGMENT-ISSUES.md#) - so the LESSON is all that survives, and it is the part worth keeping. The executor was not touched and should not be: the 60-character sample is right for a real list. Worth a sweep of the fragments whose only refusal is a single reason in a one-item list |
| **109** | **A REQUEST TO *LOOK* AT PIPES RESOLVED TO A WRITE THAT CHANGES PIPE GEOMETRY, AND THE MEANING ROUTE HAD IT RIGHT.** Measured 2026-09-16. `heron_lookup` was asked *"isolate all the pipes in the current view but leave out the condensate drain system"* and answered **`SET_MEP_SLOPE`** - `FRG-MEP-019`, which re-slopes pipework - **2.4 ranks clear of the runner-up**. `ISOLATE_ELEMENTS`, a view-only operation that changes nothing, came fourth. **THE TWO ROUTES DISAGREED AND THE WRONG ONE WON.** `SET_MEP_SLOPE` was **words #1, nearness #7**; `ISOLATE_ELEMENTS` was **words #10, nearness #1**. The trained model - the one that understands meaning - ranked the correct answer FIRST. The exact-word route ranked it TENTH, because the sentence carries *pipes*, *drain* and *condensate*, and slope is precisely what makes a drain drain. Fusion then let the word winner beat the meaning winner. **THIS IS THE RISK LADDER CROSSING IN THE DIRECTION `check-routing.py` ALREADY SAYS IS NOT A JUDGEMENT CALL** - *"the user asked a question and the model changed"* - except that tool only compares a fragment's OWN declared utterances, and no fragment declares this sentence. So nothing in the repository would have caught it. **IT IS THE SECOND OF THIS SHAPE IN ONE DAY**: [row 107](../FRAGMENT-ISSUES.md#) records *"change the phase name"* landing on `SET_ELEMENT_PHASE`, which MOVES ELEMENTS. **WHAT MAKES IT DANGEROUS IS THE PAIRING WITH ROW 111** - had the lookup's answer been run, `revit_change` would have printed only *"SET_MEP_SLOPE ran in ..."* and shown nothing about what it did. | **SUPERSEDED by [row 116](../FRAGMENT-ISSUES.md#) as the row that carries this, 2026-09-17 - and NOT because this sentence was fixed.** Found 2026-09-16; nothing was run, the answer was read and rejected by hand. [Row 113](../FRAGMENT-ISSUES.md#) generalised it to 14 crossings in 45 questions and [row 116](../FRAGMENT-ISSUES.md#) found the cause and took that to 7, which is why 116 is the live count and three rows stating one fact is the two-marks-one-fact failure [row 10](../FRAGMENT-ISSUES.md#)'s own cell names. **What this row is still owed, and it is not a count:** the four sentences 116 repaired do not include this one, and `tools/check-risk-crossings.py` quoted it at the top of its docstring as the reason it exists while never putting it in its question list - so every sweep since has measured 45 questions that excluded the one that started this. **Added to that list 2026-09-17**, which is what the tool asks each session to do - **and it registers as a crossing, which was measured rather than assumed.** `SET_MEP_SLOPE` (MODIFY), beating `CHECK_FIXTURE_CONNECTIVITY`, in both runs that asked it. **So this row's defect is now reproducible on demand instead of being one session's hand measurement, and it is still live.** It was written here first that it might NOT register, because the answer this sentence deserves is `ISOLATE_ELEMENTS`, an `EXECUTE` the discriminator excludes; that guess was wrong, and the reason is worth keeping - the gate fires because a DIFFERENT read happened to be close, not because anyone designed it to catch this. The real fix this row named stands and is 116's to carry: a MODIFY should not outrank a READ on word overlap alone when the meaning route disagrees. **[Row 116](../FRAGMENT-ISSUES.md#) also now records that the sweep is not deterministic** - 7, 9 and 8 crossings across three runs - so treat any single result for this sentence as a sample |
| **110** | HERON CAN FIND PIPES AND CANNOT SELECT THEM, AND THE TWO ANSWERS COME FROM DIFFERENT TABLES. — [full row](../fragment-issues-archive/proving-defects-101-125.md#row-110) | **FIXED, AND FOUND TO BE SO ON 2026-09-19 BY A ROUTE THAT WAS NOT LOOKING FOR IT.** |
| **111** | `revit_change` HAS NEVER REPORTED WHAT A WRITE DID, BECAUSE IT READS A KEY NOTHING EMITS. — [full row](../fragment-issues-archive/proving-defects-101-125.md#row-111) | FIXED 2026-09-17, and this cell still said `OPEN` while row 117 was using the fix WORKING… |
| **112** | **A CONTRACT PROMISES `EMPTY MEANS EVERY CATEGORY` AND THE BINDER REFUSES EMPTY, SO THE FEATURE CANNOT BE REACHED.** Measured 2026-09-16. `select-types` declares `categories` with the comment *"Null or empty means every category"*. Called with `categories=` it returns **`bad_request_value: No categories were named. Separate them with commas.`** - and that refusal is not the fragment's. It is the BINDER, `RevitFragment.cs:3165`, which rejects an empty `IList<Category>` before the fragment's own code runs. **BOTH SIDES ARE DEFENSIBLE AND THE PAIR IS NOT.** A binder that refuses to guess a category is right - guessing one is how a job runs against the wrong thing. A fragment documenting a behaviour its caller can never trigger is wrong. **THE CONTRACT IS THE HALF THAT IS FALSE**, and it was believed: it is why the empty call was made at all. Worth checking whether other fragments taking `IList<Category>` carry the same promise. | **CLOSED 2026-09-19 - the contract was fixed, and the sweep this row asked for comes back clean.** The fix landed in [PR #165](https://github.com/Ajmalpshaik/Heron-AI/pull/165) (`5669e7e`, found with `git log -S "AT LEAST ONE IS REQUIRED"`) and the comment in `brain/fragments/select-types/fragment.yaml` now reads *"AT LEAST ONE IS REQUIRED, AND THIS COMMENT USED TO SAY THE OPPOSITE... The binder is right to: guessing a category is how a job runs against the wrong thing. The comment was the false half"* - the repair this row asked for, in the half this row said was false, citing this row. **THE ROW'S OWN REMAINING QUESTION IS ANSWERED AND THE ANSWER IS NONE.** *"Worth checking whether other fragments taking `IList<Category>` carry the same promise"*: **26 fragments** declare a category list (`IList<Category>` or `IList<BuiltInCategory>`), and a text sweep of every `fragment.yaml` for an empty-means-all promise returns **exactly one hit - the corrected comment in `select-types` itself**. No other fragment ever made it. **The binder was not touched**, as the row directed. Re-read from disk rather than trusted: this row said OPEN for three days after it had been fixed, which is `open-defects.py`'s own printed warning - *"a row can say OPEN after a later row has closed it"* - happening to the row that warned about it |
| **113** | **ASKING HOW MANY PIPES THERE ARE RESOLVES TO CAPPING THEM. FOURTEEN CROSSINGS IN FORTY-FIVE ORDINARY QUESTIONS.** [Row 109](../FRAGMENT-ISSUES.md#) was found by hand and looked like one bad sentence. Asked systematically - 45 things a modeller says on a normal day, through `brain.lookup`, the same seam the host calls - **17 reached a capability above READ and 14 of those beat a READ that was right there.** Measured 2026-09-16, Revit 2020. The worst are not subtle: **`how many pipes are there` -> `CAP_OPEN_PIPE_ENDS`**, **`count the air terminals` -> `CONNECT_AIR_TERMINALS`**, **`which parameters are empty` -> `REMOVE_PARAMETER_VALUE`**, **`what workset is this on` -> `CREATE_WORKSET` (ADMIN)**, **`select all the pipes in this view` -> `CAP_OPEN_PIPE_ENDS`**. A count, a question and a selection, each answered by a write. **WHY `check-routing.py` REPORTED ONE AND NOT FOURTEEN:** it asks each fragment its OWN declared utterances, and not one of these 45 sentences is declared by anybody. The gate was right about everything it was allowed to ask. The hole was the question list, never the ranking. **NEITHER ROUTE IS THE CULPRIT, WHICH IS THE FINDING THAT DECIDES THE FIX.** On row 109's sentence the keyword route was wrong and the meaning route had the right answer first; on `can I edit these` the keyword route was RIGHT and the meaning route put the READ **eleventh**. A rule preferring either route fixes one and causes the other - which is `heron_retrieve`'s own argument for fusing rather than picking a winner, met again from the opposite direction. **THE DISCRIMINATOR THAT WORKS IS 'WAS A READ BEATEN'**: the 3 requests that reached a write with no read close were `isolate all the pipes`, `hide everything except the walls` and `how long is this run` - all three CORRECT, all three imperatives asking for the thing they got. 14 wrong, 3 right, no hand-tuning. | **SUPERSEDED by [row 116](../FRAGMENT-ISSUES.md#), 2026-09-17, and its closing number had already gone stale.** Found 2026-09-16. **This cell says *"14 questions still resolve to writes"* and row 116 measured SEVEN the next day** - a prose total outliving what it counted, in the register that exists to catch exactly that. 116 carries the live figure; do not read one from here. **What this row built is not superseded and is still load-bearing:** `heron_lookup` carries each candidate's risk and WARNS when a write wins with a read close, at the seam a host uses, changing no ranking so it cannot break a correct answer; and `tools/check-risk-crossings.py` makes the sweep repeatable. Its own instruction - that the question list must GROW whenever a session produces one - was acted on 2026-09-17, when [row 109's](../FRAGMENT-ISSUES.md#) sentence was found to be missing from it; the list is 46 now, so this row's 45 and 116's *"7 of 14"* are both figures against the list **as it stood that day**. The judgement this row declined to make is unchanged and is 116's: whether the repair is the re-ranker, the wording, or a risk-aware fusion is the owner's call |
| **114** | **"ZERO ON THE NAMED SYSTEM" IS NOT "ZERO OF THAT SYSTEM TYPE", AND I TOLD THE OWNER TWICE THERE WAS NO CONDENSATE DRAIN WHILE HE WAS LOOKING AT IT.** Asked on 2026-09-16 to isolate the pipes in `4355-BHVD-3D-50C10-BL001A` except the condensate drain, I answered that there was none to exclude. **TWO CHECKS, BOTH REAL, BOTH ANSWERING THE WRONG QUESTION.** `REPORT_SYSTEMS` showed the named systems `PCD 3`, `PCD 5`, `CDP 1`, `CDP 2` holding **0 elements each** - true, and about system INSTANCES. Then `SELECT_TYPES` found **0 pipe TYPE names** containing `Cond`, `Drain`, `PCD` or `CDP` - also true, and about the wrong PROPERTY: a pipe's type is `TCM_PIP_T031_Copper_Tee_Refrigerant` while its **System Type** is `TCM_M_CDN_PCD - Condensate Drain`. **THE OWNER SAW THEM ON SCREEN AND SAID SO.** Reading `System Type` gave the answer in one call: **105 Refrigerant Condensate, 103 Refrigerant Supply, 63 Condensate Drain** out of 271. **THE TRAP IS GENERAL IN THIS MODEL**: `REPORT_SYSTEMS` also reports **688 MEP elements on NO SYSTEM**, and every one of them still carries a System Type. So an empty named system says nothing about how much of that service exists. **AND THE NEEDLE MATTERS**: `Condensate` alone would have excluded `Refrigerant Condensate` too - `PCD` is the discriminating one. | **Method, not a defect in any fragment.** Found 2026-09-16 by the owner, from the model, after two confident wrong answers. The rule: for "which service is this", read `System Type` on the ELEMENT. `REPORT_SYSTEMS` answers a different question - which named systems exist and what is connected to them |
| **115** | **THE CHAIN EXPECTATION MET A REAL MODEL AND BOTH HALVES HELD, ON THE VERY CHAIN THE PROVING SKILL RECORDS AS IMPOSSIBLE.** [docs/36](../36-remembering-between-steps.md) built and run against `4355-BHVD-3D-50C10-BL001A` (26,632 elements), Revit 2020, session 50652, 2026-09-16. **POSITIVE:** `select-by-categories` (Pipes, Pipe Fittings, in `{3D - ajmal.al}`) left 271 elements and `set-selection --expect-from "select-by-categories"` consumed all **271**. **NEGATIVE, AND IT IS THE PAIR THAT MATTERS:** with the SAME carried values and nothing else changed, `--expect-from "select-by-categories where categories=Ducts"` returned **`chain_inputs_differ`** - *"The right fragment left these, with different inputs - so they are a different set of elements. NOTHING WAS BOUND and no fragment ran"* - and `where categories=Pipes` then bound **143**. A fragment-name-only check would have accepted both. **`chain_mismatch` ALSO FIRED FOR REAL**, when the carried values had since been left by `set-selection`. **AND IT DID ACTUAL WORK THE SAME HOUR:** `read-element-parameters --expect-from select-by-categories` then `group-and-count --expect-from read-element-parameters` produced the 105/103/63 split in row 114 - the exact hand-over `.claude/skills/fragment-proving/SKILL.md` describes failing with *"no earlier fragment in this session left a value of that name"*. **TWO OF THE FOUR REFUSALS WERE NOT EXERCISED**: `chain_empty` and `chain_contradiction` are guarded in code and covered by `tests/test_chain_expectation.py`, and neither could be produced from a command line. | **Proved on both legs, awaiting signature.** The skill's `--keep-chain` paragraph describes a DIFFERENT route (batch-prove's setup chain) and is still accurate about it; `--expect-from` is the new one and the skill does not mention it yet |
| **116** | **THE FOURTEEN CROSSINGS WERE NEVER A RANKING PROBLEM - THEY NEVER REACHED THE RANKING'S SAFETY NET AT ALL.** [Row 113](../FRAGMENT-ISSUES.md#) recorded 14 questions answered by a write and proposed no fix, on the grounds that neither retrieval route was at fault. Both observations were right and the conclusion was wrong. `heron_retrieve.find` tries `SEARCH.short_circuit` FIRST: an exactly declared sentence resolves by IDENTITY and no ranking runs. **TWO REASONS A SENTENCE MISSES THAT ROUTE, AND THE LIBRARY HAD BOTH.** (1) **AMBIGUITY.** `identities` stores `__ambiguous__` when two fragments declare one phrase, and that is a deliberate MISS - so the request falls through to ranking. Only **7 of 2,442** phrases were ambiguous, and two were dangerous: *"can I edit these"* was claimed by `read-element-ownership` AND `report-element-ownership`, so it fell through and was answered by **`UPDATE_SAVED_SET`** - and `report-element-ownership`'s own identity is literally *"whether these elements can be edited"*. *"zoom to these"* was claimed by `zoom-to-elements` and by **`set-view-section-box`**, a MODIFY. (2) **NOBODY DECLARED IT.** The other 12 are sentences no fragment claims, so identity cannot fire and ranking decides - which is the same fact [row 113](../FRAGMENT-ISSUES.md#) found from the other end. **THE FIX IS DECLARING, NOT DEMOTING**, and a nudge could never have done it: `heron_retrieve`'s quality nudge is deliberately smaller than ONE fusion rank (0.00026) while these writes won by **2.4 to 2.6 ranks**. Resolving the two ambiguities and claiming six sentences for `COUNT_ELEMENTS`, `MEASURE_MEP_SLOPE`, `DESCRIBE_BLANK_PARAMETERS` and `REPORT_SHEET_TITLE_BLOCKS` took the measured count from **14 to 7**, and the four worst are gone: *"how many pipes are there"* no longer caps open pipe ends, *"count the air terminals"* no longer connects them, *"which parameters are empty"* no longer removes values, and *"what is the slope of this pipe"* no longer re-slopes it. **THE TOOL WAS ALSO OVER-REPORTING.** It flagged anything that was not `READ`; docs/12 s2 gives `ANALYZE` side effects **none**. Corrected to `MODIFY`/`PUBLISH`/`ADMIN`, with `EXECUTE` counted separately as a VIEW change - which immediately named `DESCRIBE_BLANK_PARAMETERS` as the right owner it had previously hidden. | **OPEN - 7 of 14 fixed, and THE SWEEP THAT MEASURES IT IS NOT DETERMINISTIC, found 2026-09-17 while trying to reproduce this very number. THIS IS THE LIVE ROW for the whole thread; [109](../FRAGMENT-ISSUES.md#) and [113](../FRAGMENT-ISSUES.md#) were folded into it the same day.** Three runs of `check-risk-crossings.py`, same code, same `%APPDATA%` store, same machine, minutes apart, returned **7, then 9, then 8** crossings - and **the second and third had byte-identical inputs**. So a count from this tool is a sample, not a measurement, and *"measured both times"* above is two samples that happened to agree. **The seven this row names are the stable part**: they appear in all three runs, by name. **What moves is `how many pipes are there` -> `CAP_OPEN_PIPE_ENDS`, which this row lists among the four worst as FIXED** - it came back in one run of three. An intermittent crossing is worse than a steady one, because the fix looks like it held. **DIAGNOSED the same day, and it is not the ranker.** Ruled out by measurement rather than by reading: *the ranking* - one process answered 5/5 identically and three separate processes 3/3, and every sort ends `(-score, keyword_rank, c.id)` so a tie cannot break two ways; *the cache* - `utterances` holds **0** rows, so `recall()` cannot fire, and `remember()` refuses anything but `RAN` evidence exactly so it cannot; *a swallowed lock* - every `sqlite3.OperationalError` handler in the read path narrows to `"no such table"` and re-raises (D-52 holding), and no sweep printed a `NOT RESOLVED`; *a half-read rebuild* - `heron_search.index()` commits **once**, so a reader sees the old index or the new one, never a partial; *a different fragment tree* - all five worktrees on this PC declare the sentence. **WHAT IT IS: the sentence that moves is the only one of the three whose right answer never goes through ranking at all.** `how many pipes are there` -> `FRG-ELE-002` = `COUNT_ELEMENTS` is a DECLARED IDENTITY - which is precisely how this row fixed it - so it is answered by `short_circuit` in one lookup. When the sweep saw `CAP_OPEN_PIPE_ENDS`, **the identity route MISSED and the ranked search answered instead**, and ranking is the thing that answers a question with a write. So the instability lives in the INDEX, not the ranker - which is why the seven crossings above, all sentences nobody declares, are rock-steady, and the one this row calls FIXED is the one that wobbles. **A fix that works by declaring a phrase is only as stable as the table that holds it, and `index()` DELETEs `identities` and `fragment_text` wholesale and rebuilds them.** **AND THE SWEEP IS NOT READ-ONLY, WHICH ITS DOCSTRING CLAIMS IT IS**: run against a private copy of the store that nothing else on the machine can reach, `check-risk-crossings.py` changed that copy's md5 - **`63e333e9a28e...` before, `9d132778d8f7...` after, one sweep, no other writer**. So a sweep perturbs the index it is measuring. **That controlled run also returned the STABLE eight** (the seven, plus row 109's sentence), which is the fourth run in the set: 7, 9, 8 against the shared store and 8 against a copy only this session could touch - **the outlier is the one that shared the store with somebody**. The shared store ALSO moved while this session was pointed at the copy, so another session writes it too - `%APPDATA%\Heron\knowledge\global.db` is one file for every worktree. **NOT ESTABLISHED, and it is the remaining question:** which write made the identity miss on that one run. Comparing the shared store against the copy showed **only FTS5 shadow tables** differing - `fragments` 372, `identities` 2448, `vectors` 374, `capabilities` 373, `utterances` 0, all equal - so the write that was caught in the act was structural and does not by itself explain a miss. **The four runs are kept** in [work-notes/investigations](../work-notes/investigations/2026-09-17-risk-crossings-not-deterministic.md), because a conclusion is only re-checkable if the output it came from is. **The cheap next step is in the tool, not the ranker:** make the sweep print a fingerprint of the index it ran against - the `identities` count and a store hash - so that two runs can be compared at all. Until then, read the NAMES and not the number. The remaining seven need an owner chosen per sentence and none was obvious enough to claim at speed: duct size, pipe diameter, an element's workset, what is missing its Mark, views on a sheet, insulation thickness, and selecting pipes in a view. **Five ambiguous phrases also remain**, all read-vs-read so none is dangerous, each needing a judgement about which fragment owns the sentence. **THAT SENTENCE WAS WRONG WHEN IT WAS WRITTEN AND IS TRUE NOW BY REPAIR, 2026-09-20.** It was **six**, and two were MODIFY against MODIFY - [row 134](../FRAGMENT-ISSUES.md#) measured it two ways the day after this was written. Both are repaired: *"change the insulation thickness"* now belongs to `SET_MEP_INSULATION` alone, and the `DUPLICATE_VIEW` / `DUPLICATE_VIEWS` pair carries the number in its words. **Four remain and every one of them really is READ against READ** - `FIND_SHEETS` / `LIST_SHEETS` twice, `READ_ELEMENT_PARAMETERS` / `REPORT_PARAMETER_INVENTORY`, and `FIND_OVERLAPPING_TAGS` / `REPORT_TAGS_AND_TARGETS` - so *none is dangerous* is now a measurement rather than an assumption. The judgement about which fragment owns each of the four is still owed and still the owner's. **Two things arrived with the fold-in.** *"7 of 14"* was measured against a question list of **45**, which is now **46**: row 109's own sentence had never been in it, though `check-risk-crossings.py` quotes that sentence at the top of its docstring as the reason it exists. So a later sweep is not comparable to this figure by construction, and that is the list doing its job rather than drifting. **And row 109's sentence DOES register - measured, not predicted.** It was first written here that it might not, on the reasoning that the discriminator is *"was a READ beaten"* while the right answer is `ISOLATE_ELEMENTS`, an `EXECUTE` the tool excludes. **That reasoning was wrong and the run says so**: it comes back `SET_MEP_SLOPE` (MODIFY) beating `CHECK_FIXTURE_CONNECTIVITY`, a READ that was close for reasons nobody designed - so the gate fires, by a route nobody chose. It appeared in **both** runs that asked it. The wider question the guess raised is still real and still the owner's: a crossing whose only safe neighbour is an `EXECUTE` would be invisible here, and that must not be bought by quietly widening `reads` **MEASURED AGAIN 2026-09-20 ON THE OWNER'S PC, AND IT IS FOURTEEN, WITH THE BEATEN READ NAMED FOR EVERY ONE.** `python tools/check-risk-crossings.py`, exit 0, against `global.db` md5 `858ac50c...`, `fragments 395, identities 2582, vectors 397`, `built by` this worktree. **TWO INDEPENDENT ROUTES AGREE ON THE SAME FOURTEEN NAMES** - the tool, and a separate pass asking its own `QUESTIONS` list through `mcp/server/heron_brain.lookup` and reading `risk` off the store - which is the standard [row 134](../FRAGMENT-ISSUES.md#) set and the answer to [row 120](../FRAGMENT-ISSUES.md#)'s warning about a single detector. **THE FOURTEEN, question -> what answered -> what it beat:** *what size is this duct* -> `AUTO_SIZE_MEP` beat `REPORT_DUCT_WEIGHT`; *what is the diameter of this pipe* -> `CREATE_PIPE` beat `REPORT_PIPE_SEGMENTS`; *what workset is this on* -> `CREATE_WORKSET` (**ADMIN**) beat `SELECT_BY_WORKSET`; *what is missing its mark* -> `REPOINT_VIEW_REFERENCE` beat `FIND_UNTAGGED_ELEMENTS`; *what views are on this sheet* -> `PLACE_VIEWS_ON_SHEET` beat `FIND_VIEWS_WITHOUT_TEMPLATE`; *what is the insulation thickness* -> `SET_MEP_INSULATION` beat `CHECK_INSULATION_CLEARANCE`; *select all the pipes in this view* -> `CAP_OPEN_PIPE_ENDS` beat `FILTER_ELEMENTS_BY_CATEGORY`; row 109's own sentence -> `SET_MEP_SLOPE` beat `CHECK_FIXTURE_CONNECTIVITY`; *which rooms have no name* -> `PLACE_ROOMS` beat `REPORT_ROOM_SPACE_DATA`; *what is the area of this room* -> `PLACE_ROOM_AT_POINT` beat `REPORT_ROOM_SPACE_DATA`; *are any of these tags overlapping* -> `ARRANGE_TAGS` beat `FIND_OVERLAPPING_TAGS`; *what is the scale of this view* -> `SET_VIEW_SCALE` beat `REPORT_VIEW_TEMPLATE_CONTROL`; *what is the airflow in this duct* -> `AUTO_SIZE_MEP` beat `READ_SPACE_LOADS`; *what is the flow rate in this pipe* -> `AUTO_SIZE_PIPE` beat `MEASURE_MEP_VELOCITY`. **WHAT THE BEATEN READ SETTLES, AND WHAT IT DOES NOT.** It disproves *there is no READ to give the sentence to* for all fourteen - there is one, and it was close enough to lose. It does NOT make the owner obvious, and this row's *none was obvious enough to claim at speed* holds on inspection: `FIND_OVERLAPPING_TAGS` for *are any of these tags overlapping* is exactly right, `REPORT_ROOM_SPACE_DATA` for *what is the area of this room* is right, and `REPORT_DUCT_WEIGHT` for *what size is this duct* is **not** - a weight is not a size. So the sentences split into ones that can be declared today and ones that still need a capability or a judgement, and nothing here was declared. **THE STORE MOVED WHILE THE TOOL RAN** and the tool says so itself - md5 `858ac50c...` before, `b0a6a6ec...` after - so this is a sample, exactly as this row has said three times. **AND THE TWO IMPERATIVES ARE NOT IN THE FOURTEEN**: *isolate all the pipes* -> `ISOLATE_ELEMENTS` (EXECUTE) and *hide everything except the walls* -> `HIDE_ELEMENTS` are reported apart, because an instruction reaching a write is not a question reaching one **FIVE OF THE FOURTEEN ARE DECLARED, 2026-09-20, AND NINE ARE DELIBERATELY NOT.** He asked for the obvious ones and the split is the work: **the beaten READ is the right owner for some of these and measurably the WRONG owner for others**, which is why *the tool names a READ* was never the same as *the owner is obvious*. **DECLARED, each verified resolving by `identity` to a READ afterwards:** *what is the area of this room* and *which rooms have no name* -> `REPORT_ROOM_SPACE_DATA`, which reports number, name, level, area and volume and already declared *"what is the area of each space"*; *are any of these tags overlapping* -> `FIND_OVERLAPPING_TAGS`, whose whole identity is *the tags are printing on top of each other*; *what is missing its mark* -> `READ_ELEMENT_PARAMETERS`, **not** the `FIND_UNTAGGED_ELEMENTS` the tool named, because a tag is an annotation and a Mark is an instance parameter and an element can have one without the other - no capability in the library reads Mark by name, and this one declares *"what is the Mark on these"* and *"which ones have no value"*, which together are the question; and *select all the pipes in this view* -> `SELECT_BY_CATEGORY_NAME`, **not** `FILTER_ELEMENTS_BY_CATEGORY`, which is scoped by LEVEL rather than by view - its needs are `category` and `levelId` - where this one is view-scoped and takes one named category. That last was the sharpest of the fourteen: asking to SELECT pipes reached `CAP_OPEN_PIPE_ENDS`. **THE RUNNER-UP IS NAMED IN THE FILE** for that one, `SELECT_BY_CATEGORIES`, so it can be moved in one line. **NOT DECLARED, AND WHY, ONE BY ONE:** *what size is this duct* (the beaten READ reports a **weight**, not a size), *what is the diameter of this pipe* (the beaten READ lists the pipe SEGMENT TYPES a project has), *what workset is this on* (`SELECT_BY_WORKSET` answers the INVERSE - workset in, elements out), *what is the airflow in this duct* (the beaten READ is about SPACES), *what is the flow rate in this pipe* (the beaten READ measures VELOCITY, in DUCTS) - all five point at `READ_ELEMENT_PARAMETERS`, which [row 146](../FRAGMENT-ISSUES.md#) said to TRY, and it is generic enough that owning five questions at once is his call and not a session's; *what is the insulation thickness*, which [row 146](../FRAGMENT-ISSUES.md#) warns may be a real gap because insulation is a separate element wrapping the host; *what views are on this sheet*, where `LIST_SHEETS` declares *"what is on the sheets"* but `LIST_SHEETS` / `FIND_SHEETS` are already **two of the four remaining ambiguous pairs**, so adding to that territory before resolving it would be sloppy; *what is the scale of this view*, which is [row 146](../FRAGMENT-ISSUES.md#)'s one CERTAIN capability gap and is also [row 158](../FRAGMENT-ISSUES.md#)'s case of a WRITE declaring the question, so it is blocked on `Q-57`; and row 109's own sentence, whose right answer is an `EXECUTE` and which this row already says must not be bought by widening `reads`. **NO NEW AMBIGUITY WAS CREATED** - read from disk after the five, still exactly four phrases claimed twice, the same four. **AND THE MEASUREMENT CARRIES ITS PROVENANCE, BECAUSE THE FIRST ATTEMPT AT IT WAS WORTHLESS.** A full `check-risk-crossings.py` run straight after the edit reported ELEVEN and was **discarded**: its own header said `built by ...worktrees/heron-tab-ui-redesign-088b6c/brain/fragments`, another session's tree, which had reindexed the shared store between the rebuild and the read. That is [row 131](../FRAGMENT-ISSUES.md#) happening in the open, and the `built_from` half built for it is what caught it. The five were then measured with `SEARCH.indexed_from(store)` read BEFORE and AFTER the lookups, both naming this worktree - so the answer is about this library and is known to be **AND THE FIVE THAT WERE HIS TO DECIDE ARE DECIDED: `READ_ELEMENT_PARAMETERS` OWNS THEM, DECLARED 2026-09-20.** Asked as one question rather than five, and answered yes: *what size is this duct*, *what is the diameter of this pipe*, *what workset is this on*, *what is the airflow in this duct* and *what is the flow rate in this pipe*. **The fragment's own `purpose` is the argument for it** - version 2 exists because version 1 answered `absent` for TYPE parameters, and **SIZE is the first example it gives**; instance is asked first and type second, and `resolvedFrom` records which answered. **Verified with the provenance read both sides and both naming this worktree: all five resolve by `identity` to `READ_ELEMENT_PARAMETERS`, risk READ.** **WHAT IT DOES NOT DO IS WRITTEN INTO THE FILE**: it reads a parameter the caller NAMES, so these sentences now land on a READ that needs `parameterName` bound - `Size`, `Diameter`, `Workset`, `Flow`. That is a binding step rather than an answer, and it is the whole point - an unbound READ ASKS, where the writer it used to reach ACTED. **THE FOURTEEN ARE NOW FOUR**, and every one of the four is blocked on something named rather than on nobody looking: *insulation thickness* ([row 146](../FRAGMENT-ISSUES.md#) - it lives on a separate element wrapping the host and may be a genuine capability gap), *what views are on this sheet* (`LIST_SHEETS` declares *"what is on the sheets"* but `LIST_SHEETS` / `FIND_SHEETS` are two of the four remaining ambiguous pairs, so that pair wants settling first), *what is the scale of this view* (row 146's one CERTAIN gap, and [row 158](../FRAGMENT-ISSUES.md#)'s case of a WRITE declaring the question, so it waits on `Q-57`), and row 109's own sentence, whose right answer is an `EXECUTE` and which this row already says must not be bought by widening `reads` |
| **117** | **A WRITE THAT RENAMED NOTHING SAID "THE MODEL WAS CHANGED", AND [ROW 111's](../FRAGMENT-ISSUES.md#) FIX IS WHY ANYONE CAN SEE IT.** Measured 2026-09-17 on `test projject`, Revit 2024, verifying row 111 live. `RENAME_ELEMENTS` was run through `revit_change` with `find=ZZZNOTHINGMATCHESTHIS` against a 10-element selection. It reported `notMatched 10`, `planned 0`, **`renamed 0`** - correct, nothing matched - and then: **"the model was CHANGED and this is one undo step - Ctrl+Z in Revit puts it back"**. Nothing was changed. `WithVerdict` writes that sentence from **`applied`**, which is the caller's REQUEST to keep the transaction, not a statement that any work was done. **IT IS THE SAME FAMILY AS ROW 111 AND THE SAME FAMILY AS THE ROLLBACK FAILURES**: a write describing its INTENT where a reader will take it as an OUTCOME. `WithVerdict`'s own comment records the last time that cost something - *"on 2026-09-09 a rollback did not hold and this sentence claimed it had"* - and the `rolledBack` branch was fixed then to report what Revit said rather than what was asked. **THE `applied` BRANCH WAS LEFT ON THE REQUEST.** Harmless where work happened, misleading where none did, and a modeller reading only the last line would go looking for an undo step that has nothing under it. **IT IS ONLY VISIBLE BECAUSE ROW 111 SHIPPED THE SAME DAY** - before that, this whole reply was the single sentence *"RENAME_ELEMENTS ran in test projject."* and the contradiction had nowhere to show. | **FIXED 2026-09-19 AND THE TEST FAILS ON THE OLD WORDING**, which is the only thing that makes it a test. `WithVerdict`'s `applied` branch no longer says *"the model was CHANGED"*; it says **'X' was KEPT: Revit accepted the transaction rather than rolling it back**, then sends the reader to the counts that are already in the same reply. **READING THE COUNTS AND DECIDING WAS CONSIDERED AND REFUSED.** Which number means WORK and which means bookkeeping is [D-52](../DECISIONS.md)'s `role`, and that lives in `fragment.yaml` on the brain side - the add-in is handed the C# and the bindings and never the contract, checked by grep before it was ruled out. **"Every number is zero" would have got the measured case wrong too**, because `notMatched 10` was non-zero in the very run that changed nothing. **THE UNDO SENTENCE IS NOW CONDITIONAL AND THAT IS THE HALF THAT WAS DANGEROUS** rather than merely untrue: told to press Ctrl+Z after a run that did nothing, a modeller undoes whatever they did BEFORE it. `tests/test_change_reporting.py` gained four checks and all four FAIL on the old code, verified by reverting the file and running them. Compiles on 2020 to 2027. **NOT YET SEEN IN FRONT OF A MODEL** - the add-in needs a rebuild and a Revit restart, so this is DEPLOYED-PENDING rather than proved. The original state line follows. **OPEN.** Found 2026-09-17, by the row 111 fix working. The repair is to say what was KEPT rather than what was asked - the counts are already in the same reply, so the verdict can read them. Low severity and worth doing: every argument in `WithVerdict`'s own comment applies to it |
| **118** | TWO DEFECTS SHARED ONE LINE OF THE TABLE, SO ONE OF THEM WAS INVISIBLE TO EVERY SWEEP FOR SEVEN… — [full row](../fragment-issues-archive/proving-defects-101-125.md#row-118) | **FIXED 2026-09-19.** |
| **119** | **SEVEN ROWS READ `OPEN` AFTER A LATER ROW HAD CLOSED THEM, AND FOUR OF THE SEVEN WERE CLOSED BY EVIDENCE FROM A REAL MODEL.** The failure `open-defects.py`'s docstring names - *a closure written in a different row, in prose* - measured again on 2026-09-19 by reading all 31 listed rows and then every later row that names one of them. **Corrected in place, each with what closed it:** [16](../FRAGMENT-ISSUES.md#) and [79](../FRAGMENT-ISSUES.md#) are ONE defect recorded twice six days apart, both closed by [row 80](../FRAGMENT-ISSUES.md#)'s empty-list fix and verified on a real Revit by [row 81](../FRAGMENT-ISSUES.md#); [17](../FRAGMENT-ISSUES.md#) fixed in #138 and proved in #139 - `cleared 4` on the `Comments` it called impossible; [22](../FRAGMENT-ISSUES.md#) fixed and RE-PROVED in one commit, the new proof recording `spacingX 1.96850393700787` where the old recorded `600`; [26](../FRAGMENT-ISSUES.md#) closed by a JUDGE rule - `_result_appeared` - with the fragment never touched; [39](../FRAGMENT-ISSUES.md#) answered by [row 53](../FRAGMENT-ISSUES.md#)'s four tracking runs; [40](../FRAGMENT-ISSUES.md#) closed by [row 85](../FRAGMENT-ISSUES.md#), whose negative arrived as `elements from find-schedules (0)` - a route [row 40](../FRAGMENT-ISSUES.md#) had ruled out by looking only for a WRONG element rather than for NO element. **THE SHAPE IS WORTH MORE THAN THE SEVEN.** Three of them were closed by a change somewhere else entirely - a judge rule, an executor fix, a rule landing in the same commit - so **reading the fragment would have suggested nothing had changed**, and in [row 26](../FRAGMENT-ISSUES.md#)'s case the implementation still reads exactly as its defect row describes it. A row whose fix lives outside the thing it names cannot be closed by re-reading the thing it names | **FIXED 2026-09-19** - all seven state cells corrected where they are written, and the stale tails of [39](../FRAGMENT-ISSUES.md#) (*the decision owed is one rule for all four*) and [40](../FRAGMENT-ISSUES.md#) (*leaving it DRAFT is the honest outcome*) corrected with them, because a corrected opening over a stale tail is the same two-marks-one-fact failure in one cell |
| **120** | **[ROW 108](../FRAGMENT-ISSUES.md#)'s SWEEP, RUN TWICE AND WRONG BOTH TIMES - AND THE PART THAT SURVIVES IS THE MEASUREMENT, NOT THE CLASSIFICATION.** Row 108 asked for *a sweep of the fragments whose only refusal is a single reason in a one-item list*. **THE FIRST RUN RETURNED 189** and was thrown away: its brace tracking counted an `.Add(` inside a `foreach` as outside one, which `unload-links` and `set-category-visibility` showed at a glance. **THE SECOND RETURNED 15 AND WAS ALSO WRONG**, caught by Codex on PR #191 rather than by me, and the counter-examples are flat contradictions rather than edge cases: `set-schedule-filters` appends at `:97` and `:106` from **two independent `if` blocks, not an else-chain**, so a bad match type AND an unnamed column both land in one request; `assign-location-data` appends at `:124`, `:130`, `:135` independently and then **unconditionally** at `:141`; and `place-family-on-face`'s adds read `"entry " + (i + 1)`, which is a per-entry LOOP the detector again failed to see. **SO THE DETECTOR HAS BEEN WRONG ABOUT LOOPS TWICE, IN TWO DIFFERENT WAYS, AND ITS OUTPUT IS NOT EVIDENCE.** Any fragment in the list below is a CANDIDATE and nothing more. **WHAT DOES SURVIVE, BECAUSE IT DOES NOT DEPEND ON THE DETECTOR AT ALL:** the refusal string literals were measured directly, and the longest in each of these is far over the 60 characters `Describe` allows a list item - `set-view-section-box` **451**, `set-schedule-filters` **335**, `create-electrical-circuit` **293**, `set-view-template-control` **278**, `assign-location-data` **268**, `create-mep-system-type` **238**, `add-revision-cloud` **227**, `create-view-filters-by-value` **213**, `create-flex-duct` **199**, `rename-workset` **196**, `create-grids` **190**, `create-room-elevations` **183**, `create-view-template-from-view` **173**, `place-family-on-face` **105**, `array-elements-radial` **70**. **TWO WERE READ BY HAND AND ARE GENUINE ONE-OF-ONES**: `rename-workset` is a strict if/else chain of eight, and `set-view-section-box` a chain of four with a mutually exclusive pair nested in its last branch | **OPEN, and it needs READING rather than another sweep - that is the lesson.** Row 108's question is *can only one reason ever fire*, and that is a question about mutually exclusive branches, which no regex over braces has answered correctly in two attempts. **Thirteen of the fifteen are unverified and three are disproved.** The honest next step is to read the thirteen, not to write a third detector - [row 116](../FRAGMENT-ISSUES.md#) is the standing warning about trusting a tool's count over the names it prints, and this row earned it twice. **Nothing was changed in any fragment**: the repair is `IList<string>` to `string` in `fragment.yaml`, 13 of the 15 are PROVEN, and a change there stales a proof for a message improvement - the owner's call, and worth it for `set-view-section-box` and `set-schedule-filters` alone **THE TEN IT ASKED FOR HAVE NOW BEEN READ, ONE FILE AT A TIME, AND THE DETECTOR WAS WRONG ABOUT SIX MORE.** Done 2026-09-19 by reading each `impl/any/fragment.cs` and tracing which branches can reach a `refused.Add`. **THREE ARE GENUINE ONE-OF-ONES.** `create-electrical-circuit` - a strict `if` / `else if` / `else` with the create path's five sites each ending its own branch; `create-flex-duct` - three guards in one `else if` chain, then `badPoint` / `else if` over-length / `else` the create path, where the loop `break`s on the first empty point so even that one cannot repeat; and `set-view-template-control`, which **says so in the code**: `if (refused.Count == 0)` guards the read-back block, so 38, 42, 55, 111, 129, 142 and 162 are seven sites of which exactly one can fire. **SIX ARE DISPROVED, and every one is the loop or the unconditional append the detector has now missed five times.** `array-elements-radial` appends `"Copy " + i` inside the copy loop; `create-grids` appends per wanted grid and then a summary at `:136`; `create-room-elevations` appends per slot at `:75` and `:85` and a summary at `:91`; `create-view-filters-by-value` appends per filter name at `:124` and `:169` and summaries at `:62` and `:173`; `create-view-template-from-view` can refuse a **rename** at `:75` and an **apply-back** at `:99` or `:105` in the same run, which are independent; and `create-mep-system-type` is the worst of them - `:103` (the abbreviation would not set), `:108` (the abbreviation already belongs to another type) and then `:114` **unconditionally**, so three land in one request. **THE FIFTEENTH WAS NEVER IN SCOPE AND THE MEASUREMENT IS WHY.** `add-revision-cloud`'s `refused` is declared **`IList<ElementId>`**, not `IList<string>` - it collects the ids of elements it could not cloud, four sites all inside loops, by design. Its 227-character literal is real and goes somewhere else in the file: the string lengths above were measured **per file** rather than per provide, and that is the one way the surviving half of this row was itself too wide. **RUNNING TOTAL FOR ALL FIFTEEN: five confirmed one-of-ones** (`rename-workset` and `set-view-section-box` from this row, plus the three above), **nine disproved, one out of scope** - the detector was wrong about nine of fifteen, and reading was what settled every one. **A SECOND THING TURNED UP THAT ROW 108 DOES NOT COVER:** `refused` is being used as a NOTES channel. `create-mep-system-type:114` appends *"'X' was copied from 'Y' and is classified Z"* after a SUCCESSFUL duplicate; `create-view-filters-by-value` appends two counts; `create-room-elevations:91` appends *"{0} elevation(s) made"*. None of those is a refusal, and all of them are declared `role: accounting` on a name a reader takes to mean something went wrong. **THE REPAIR STILL NEEDS THE OWNER, and one of the three is now cheap:** `create-electrical-circuit` is **DRAFT**, so changing its `refused` to `string` stales no proof. The other two are PROVEN and the trade in this row's own last sentence stands. **Nothing was changed in any fragment** |
| **121** | **[ROW 112](../FRAGMENT-ISSUES.md#)'s FIX WAS APPLIED IN ONE OF THE FOUR PLACES THAT CARRY THE FALSE PROMISE, AND I FIRST WROTE THAT THERE WERE THREE.** `select-types` promises *null or empty means every category* in four separate texts: the `needs:` comment in `fragment.yaml`, the `purpose:` prose above it, the header of `impl/any/fragment.cs`, and - found by Codex on PR #191, after I had counted - **`tests/cases.yaml`, which declares BOTH a positive case (*"categories null and both name filters empty"*) and a negative (*"categories left null... the WHOLE type table comes back"*)**. Row 112 was recorded 2026-09-16 and only the `needs:` comment was corrected then. **THE CASES FILE IS THE WORST OF THE FOUR AND I MISSED IT BY GREPPING FOR A PHRASE INSTEAD OF READING THE CONTRACT**: `heron_validate.declared_negative()` reads it when planning proof work, so it does not merely misinform a reader - **it would plan a proof run for a case the binder makes unreachable**, which is a session wasted arranging something that cannot happen. **THE BEHAVIOUR IS THE HALF THAT IS RIGHT**: the loop genuinely treats an empty set as every category (`if (wantedCategoryIds.Count > 0)`), and the binder genuinely refuses to let a caller reach it. Nothing is broken; four texts describe one contract and one of them has been corrected | **PARTLY FIXED 2026-09-19, AND THE COUNT IN THIS ROW WAS ITSELF WRONG ONCE - which is the row.** `impl/any/fragment.cs:12` rewritten to say at least one category is required, why the binder is right to refuse, and that the empty branch below it is deliberate but unreachable. **THREE TEXTS ARE STILL FALSE AND NONE WAS THIS SESSION'S TO EDIT**: `fragment.yaml:39` (the purpose prose), and both declared cases in `tests/cases.yaml`. **The general lesson is the row, twice over**: a contract written in four places gets corrected in one, grep finds the texts that share a phrase and misses the one that states the same promise in different words, and only reading the declared cases finds that |
| **122** | ROW 23 REPAIRED, AND HALF PROVED 2026-09-19 - THE HALF A MODEL COULD ANSWER WITHOUT BEING… — [full row](../fragment-issues-archive/proving-defects-101-125.md#row-122) | FIXED AND PROVED 2026-09-19, BOTH LEGS - AND THE SECOND LEG EXISTS BECAUSE THE OWNER SAID… |
| **123** | PROVED 2026-09-19 ON `Project1 work_ajmal.al`, BOTH DIRECTIONS, AND THE SECOND DIRECTION IS WHY… — [full row](../fragment-issues-archive/proving-defects-101-125.md#row-123) | FIXED AND PROVED 2026-09-19 on `Project1 work_ajmal.al`, Revit 2024 session 20472 - both… |
| **124** | I WROTE THAT A `PROVEN` FRAGMENT'S PROOF CAN GO STALE IN SILENCE. IT CANNOT, AND CI SAID SO TWENTY… — [full row](../fragment-issues-archive/proving-defects-101-125.md#row-124) | NOT A DEFECT - WITHDRAWN 2026-09-19, the same day it was written, by the gate it said did… |
| **125** | THE SECTION-MARK REPAIR CHANGED THE MECHANISM AND LEFT THE FRAGMENT'S OWN DESCRIPTION SAYING THE… — [full row](../fragment-issues-archive/proving-defects-101-125.md#row-125) | **FIXED 2026-09-20 - ROW 122 HAS LANDED, SO THE CONDITION THIS CELL SET IS MET.** |
