# FRAGMENT-ISSUES archive — section 5, rows 151 to 175

> **Finished rows, moved out of the live register.** These are rows from section 5 of
> [`FRAGMENT-ISSUES.md`](../FRAGMENT-ISSUES.md) — Heron's own defects found by proving — whose state said they were
> finished when they were moved. Each still has its line in the register: the same number, a
> one-line title, the opening of its state, and a link to its full text here.
>
> **Nothing below was rewritten** except its links, which were re-pointed one folder deeper and
> each checked to reach the same file it reached before. Where a row disagrees with
> [DECISIONS.md](../DECISIONS.md), the [Golden Rules](../14-golden-rules.md) or the
> [Constitution](../../HERON_CONSTITUTION.md), **those win** — a row records what was true on
> the day it was written.
>
> **Nothing new is written here.** A new defect goes in the register, and a row here that turns
> out not to be finished is reopened **there**, on its own line.
>
> Rows 151 to 175 of section 5 belong in this file. A number in that band with no entry below
> was never moved — it is still in the register, in full. Written by
> [`tools/archive-fragment-issues.py`](../../tools/archive-fragment-issues.py).

---

### Row 152

*Moved from the register on 2026-09-23.*

**Defect.** **BOTH TOOLS THAT HANDLE A ROUTING RECORDING PRINTED THE STALENESS FINGERPRINT AND COMPARED NOTHING, SO A CARD WENT ON SHOWING `words 1 of 4 reach` - AND `prove-skill.py` WENT ON SAYING `UNDERSTOOD` - ABOUT FOUR SENTENCES THAT HAD SINCE BEEN EDITED.** Measured 2026-09-19. [Row 150](../FRAGMENT-ISSUES.md#) added the WORDS half and had it print the date and the index fingerprint the recording was taken against - [row 116](../FRAGMENT-ISSUES.md#)'s rule, that two measurements are comparable only when that block matches. **It PRINTS the block and COMPARES nothing.** `routing()` reads `doc["index"]["md5"]` into `meta["index"]`, the subtitle prints its first eight characters, and no line anywhere asks whether that is the store the page is being rendered against - the comparison was left to a reader's eye, on a page whose whole purpose is that a card can say anything. And the fingerprint is the WRONG THING TO HANG IT ON in any case: a routing measurement is a statement about WHICH SENTENCES a skill says, and a skill's utterances are edited in `brain/skills/*.yaml` without the store changing at all. `words 1 of 4 reach` survives all four being rewritten. **AND THE TOOL THAT WRITES THE RECORDINGS HAS THE SAME HOLE, WHERE IT COSTS MORE.** `tools/prove-skill.py --routing-from` exists because the understanding half is twenty-five minutes; it checks which SKILLS the recording is missing and prints *"NOT IN THE RECORDING, so no verdict for"* - and never asks whether a skill it DOES cover still says the same sentences. `verdict()` would return **`UNDERSTOOD`** from a recording about a different set of words, which is the one word in that vocabulary meaning *half 1 held*

**State.** **FIXED 2026-09-19, and the comparison is made against the PHRASES rather than the store.** `words_moved(rows, utterances)` returns `(gone, fresh)` - measured and no longer said, said today and never measured - and either one makes the count a statement about a different set of sentences. **THE STORE IS DELIBERATELY NOT THE TEST**, and this is the whole design: `global.db` is one file every worktree writes ([row 116](../FRAGMENT-ISSUES.md#), [row 136](../FRAGMENT-ISSUES.md#)), so its md5 differs between two renders of the same page for reasons that change nothing, and a rule hung on it would print STALE every time - noise a reader learns to ignore, which is worse than silence. **The phrases are under version control and a difference in them is a real one.** It also needs no store at all, so it holds in CI, where there is none. A card whose words have moved reads **`OUT OF DATE`**, names how many were never measured and how many are no longer said, lists both in its details with the command to re-take, and is counted as **NEITHER whole NOR short** - a word added since the recording cannot have reached anything, so folding it into either number would be the same mistake one layer down. **AND A CROSSING IS NEVER SUPPRESSED BY THE STALENESS**: `crossing_live` keeps a crossing found on a sentence the skill STILL says - the danger is real whatever the rest of the recording is worth - and drops one found on a sentence nobody says any more, which is not a danger. `collect(recordings=...)` takes the folder so the WHOLE path is exercised against a recording written for the test: **nothing in `tests/test_skill_catalog.py` edits `brain/skills`** - [row 142](../FRAGMENT-ISSUES.md#) is what a suite that rewrites the real library costs, and this checkout is shared. The case that pins the exclusion is a recording where **every sentence it holds reaches** - perfect on its own, out of date against a skill that has since grown three more - and the page must count it as whole **zero** times. The suite also caught the test itself writing the real `skill-catalog.html`, because `OUT` is read at import and the redirect was set after it: fixed, and pinned. **THE RULE LIVES IN ONE PLACE, BESIDE `classify()`.** `tools/check-skill-routing.py` already owns what a crossing IS, shared for the reason its own docstring gives - two copies would start disagreeing about the same skill on the same day - and now owns what a STALE recording is, read from there by the catalogue and by `prove-skill.py` alike. It sits directly below `_index_fingerprint()`, the thing it does not use, so the next reader finds both questions in one place: the fingerprint answers WHICH LIBRARY was asked, the phrases answer WHICH SENTENCES. **`prove-skill.py` gains a sixth verdict, `OUT OF DATE`**, and a skill whose words have moved can no longer reach `UNDERSTOOD` - measured by dropping one sentence from `count-elements` in the saved recording and adding one it never says: the only skill the run called UNDERSTOOD becomes `OUT OF DATE`, and the totals go from *UNDERSTOOD 1* to *OUT OF DATE 1*. The same two rules hold there as on the page: a crossing on a sentence the skill STILL says outranks the staleness, and one on a sentence nobody says any more is dropped rather than reported

---

### Row 154

*Moved from the register on 2026-09-23.*

**Defect.** **TWO SWEEPS OVER THE SAME STORE EACH KEPT THEIR OWN COPY OF THE FINGERPRINT BLOCK, AND FOUR PLACES PRINTED IT.** Found 2026-09-19 while teaching that block to name the tree ([row 131](../FRAGMENT-ISSUES.md#)). `tools/check-skill-routing.py` and `tools/check-risk-crossings.py` each carried a `_index_fingerprint()` - **same behaviour, different comments**, the routing one saying *"the same block `check-risk-crossings.py` prints"* beside a second implementation of it. Four call sites printed the six lines by hand: those two, and `prove-skill.py` twice, once for a live run and once for a recording. **THE COST IS NOT DUPLICATION, IT IS THE NEXT CHANGE.** Row 131's repair adds one field to that block, so it would have had to be written into two functions and four printers on the same afternoon - and the comment already claiming the two were one copy is how the second would have been missed. It is the rule `AGENTS.md` opens with: *two copies of a rule is how one of them goes stale*

**State.** **FIXED 2026-09-19, in the direction the repository already runs.** `check-skill-routing.py` is the shared home for rules about a routing measurement - its own docstring has said so since `classify()` was pulled out of `main()`, and [row 152](../FRAGMENT-ISSUES.md#) put `words_moved()` there the same day - so `_index_fingerprint()` stays there and `check-risk-crossings.py` imports it by path, its own copy deleted along with the `pathlib` and `sqlite3` imports that existed only to serve it. **`fingerprint_lines(index, lead)` returns the block as LINES rather than printing it**, which is what lets all four sites share it - one wraps its output, one prints inside a `GJ.wrap` paragraph, and a test can read them. **THE TEST NEEDS NO STORE**, so it holds in CI where there is none: the five cases are built as dictionaries - unreadable store, same tree, another tree, a fingerprint taken before the field existed, and a store asked that has no answer. The last two are the D-52 pair and the reason they are separate: a missing key claims nothing about a tree, and a recorded `None` says **`NOT RECORDED`**. Verified afterwards: `check-risk-crossings.py --list` runs, both of `prove-skill.py`'s blocks render, and the saved recording from before this change correctly prints no `built by` line at all

---

### Row 157

*Moved from the register on 2026-09-23.*

**Defect.** **THE RETRIEVER HAS BEEN SAYING "NO ROUTE PREFERRED THIS" IN PLAIN WORDS AT THE SEAM EVERY SWEEP READS, AND EVERY SWEEP THREW THE SENTENCE AWAY.** Found 2026-09-19 while looking for a way to see past [row 137](../FRAGMENT-ISSUES.md#)'s blind spot. That row's finding is that a question inside a **MODIFY** skill reaching an unrelated MODIFY is invisible **by construction**: a crossing is decided by comparing a sentence's reach against the SKILL's declared risk, so a skill declared MODIFY can never register one. It closed with *"the ranking found nothing and said so - it won on words #17 and nearness #17, both last"*, read off the screen by a person. **THAT READING IS AVAILABLE TO A PROGRAM AND ALWAYS HAS BEEN.** `heron_retrieve.Contest` computes it, `Contest.sentence()` writes it in English, `heron_retrieve` appends it to `note`, and `heron_brain.lookup` returns `note` across the seam. Four sentences are in there: **`A COIN TOSS`** - *"their order carries no preference at all"*, and its sibling *"could have come from status alone, not from either route preferring one"*; **`ranked the library rather than selecting from it`** - the words route matched at least as much as the filter left, so a position in it is not a claim on the sentence; **`a shortlist of one is not a ranking`**; and **`'both agree' means nothing here yet`**. Measured at the seam: *"what colour is the sky"* resolves to **`COLOR_BY_PARAMETER`, a MODIFY**, and the note says the words route *"matched 392 fragment(s) - at least as many as the 392 the filter left - so it ranked the library rather than selecting from it"*. **Heron said it had nothing, and `check-skill-routing.py`, `prove-skill.py` and the catalogue all recorded `(phrase, capability, risk, route)` and dropped the sentence**

**State.** **FIXED 2026-09-19, by READING what was already there rather than deriving anything.** `ROUTING.unsettled(note)` matches those four phrases and returns them as short tags; an empty result is **the absence of a complaint and not a clean answer** (D-52), and a note that could not be read is empty for the same reason. **NOTHING IS RE-DERIVED, AND THAT IS THE DESIGN.** A second implementation of *was this contested* would be a second opinion about the retriever's own measurement, which [row 154](../FRAGMENT-ISSUES.md#) is the cost of. **THE PHRASES ARE PINNED AGAINST `Contest.sentence`'s SOURCE** in `tests/test_skill_proving.py`, so a reword there fails loudly and names the phrase rather than quietly turning the section off - and the pin closes Python's adjacent-string-literal seams first, because a sentence a reader sees whole is broken across lines in the source and a literal search would report a reword that never happened. **IT CUTS ACROSS THE FIVE LISTS RATHER THAN BECOMING A SIXTH**, which is the whole point: an unsettled answer can be a reach, a miss, a crossing or an escalation, and a sixth bucket would make a reader choose. `check-skill-routing.py` prints it as its own section and says in that section why it exists; `prove-skill.py` records it as a **sixth field** and names it under every verdict. **A RECORDING WRITTEN BEFORE THIS STILL READS**: every consumer pads and slices rather than unpacking a fixed width, which `tests/test_skill_catalog.py` already relied on and now matters. **IT DOES NOT CHANGE A VERDICT WORD.** The retriever saying it found nothing is a question for a person, not a failure - the rule every report in `tools/` is written to - and a tool that downgraded a skill on it would be ranking confidence, which is R-56 and R-58's floor problem with a new name - both are blocked on W-8 for exactly that reason, as `tests/test_contest.py` and `tests/test_conflict.py` both record: *retrieval has no floor*, and R-60 says a floor is derived from a measurement or it is not set at all. **MEASURED ACROSS ALL FORTY-THREE SENTENCES, 2026-09-19**, by re-taking the routing recording (`routing-2026-09-19b.json`, twenty-five minutes, no Revit): **24 of 43 carry a complaint**, 21 of them *the words route ranked the library rather than selecting from it* and 9 *a coin toss*, some both. **The number that matters is where they landed**: 24 sentences reach a capability their skill declares, and **12 of those 24 carry a complaint** - half of the page's good news. **BOTH CROSSINGS CARRY ONE TOO.** The starkest is Heron's own opening example: *"select all the ducts"* reaches `SELECT_BY_CATEGORIES`, which `select-elements` declares, **by `hybrid` with the words route having ranked the library rather than selecting from it** - the right answer from a route with no claim on the sentence. *"select the 300x300 dampers"* carries **both** complaints. *"show me the ones you found"*, the one that resolves by **identity**, carries none - which is the argument for declaring a sentence rather than ranking for it, stated in data. **THE TWO COMPLAINTS ARE NOT THE SAME STRENGTH AND THE PAGE MUST NOT SAY THEY ARE**: *a coin toss* means NEITHER route preferred the winner; the words one means only that THAT route had no claim, and nearness may still have. So the card says **the retriever complained**, never that the answer is wrong - a first draft read *with no confidence* and was corrected before it landed. **AND THE SAME RE-TAKE SETTLED [ROW 152](../FRAGMENT-ISSUES.md#)'s DESIGN CHOICE WITH A MEASUREMENT.** The two recordings were taken against stores whose md5 **differs** (`c1193203...` against `e38e1298...`) and **not one of the 43 answers moved** - same capability, same risk, same route, every sentence. A staleness rule hung on the fingerprint would have called that recording stale and been wrong about all 43; the phrases, which had not moved either, called it current and were right. **AND THE SIXTH FIELD BROKE `--jobs`, WHICH NO TEST COVERED AND RUNNING IT FOUND IN ONE COMMAND.** `job_file()` unpacked FIVE values from a measured row, so `--jobs` **with** `--routing-from` died with *`ValueError: too many values to unpack (expected 5)`* while `--jobs --plan-only` was fine - every job case in `tests/test_skill_proving.py` passed `measured=None`, which is exactly why nothing caught it. **THREE FUNCTIONS UNPACK A MEASURED ROW AND ONLY TWO WERE PADDED**: `verdict`'s `said()` and the catalogue's `said()` were, `job_file` was not. (`load_routing` is not one of them - it passes each row through as a tuple and unpacks nothing, which is why it needed no change and why an earlier draft of this sentence naming it as padded was wrong; corrected 2026-09-20.) It now pads and slices like the others, and the suite emits a job file from a SIX-field measurement and from a FIVE-field one, asserting both parse. **The ten job files are re-emitted against the new recording**, so each now carries what the retriever said beside where each sentence landed - `check-connectivity`'s two crossings both do. **AND THE SUBTITLE COMPUTED THE HEADLINE NUMBER AND DROPPED IT, INSIDE THE CHANGE THAT ADDED THIS ROW.** The two caveats were written as `shaky + stale if stale else ""`, which Python reads as `(shaky + stale) if stale else ""` - so with no stale recording the COMPLAINT sentence vanished with it, and the page said *"1 have every word reaching"* while holding, unprinted, that **12 sentences across 7 skills reached and the retriever complained**. A precedence bug, and the exact failure this row is about, committed by the row's own author an hour after writing it. **Found by reading the RENDERED subtitle rather than the code** - which is the only way it could have been found, because the code looks right. The caveats are built as a plain variable now, and `tests/test_skill_catalog.py` pins the case that was missing: a complaint with **no** staleness beside it, which is the combination that vanished. **THIS ROW WAS ANNOUNCED AS 155 AND IS 157 ON PURPOSE, AND 156 IS RESERVED RATHER THAN MISSING** - two other branches had taken the numbers in between by the time this one was written. **155 is now on `main`** as [PR #203](https://github.com/Ajmalpshaik/Heron-AI/pull/203)'s *a test suite breaks 55 real `fragment.yaml` files*, merged while this branch was open; **[PR #204](https://github.com/Ajmalpshaik/Heron-AI/pull/204) announced 155 and 156 too** and will need one of its own moved when it rebases. So the later row moved - twice, honestly, each time off the `main` in front of it. That branch has already paid for the other resolution once: its own commit *"My renumbering made their row look missing, so I inserted a second copy of it"*. A gap is harmless and a duplicate is not, which is the rule [NEEDS-CHECKING](../NEEDS-CHECKING.md) states - keep both, renumber the later one, check for duplicates afterwards. Verified: no duplicate number in section 5

---

### Row 159

*Moved from the register on 2026-09-23.*

**Defect.** **THE VERSION WALL SLID ONTO A RELEASE THE USER NEVER CHOSE, WHEN THE ONE THEY DID CHOOSE CLOSED.** Found 2026-09-20 by reading `_revit_version` while repairing [row 130](../FRAGMENT-ISSUES.md#) - **not by that row, which names a different defect in the same eight lines**. The old body tried the chosen pid against the live sessions, and when no session matched it **fell through** to `if len(live) == 1: return live[0].revit_version, "the only Revit connected"`. So a modeller who chose Revit 2024, with 2020 still open, had 2024 close under them and the version filter move to **2020**, described as *the only Revit connected* - a release they never picked, deciding which fragments were offered. **THE RULE IT BREAKS IS FOUR LINES FROM THE CODE, IN `tests/test_session_binding.py`'s OWN TABLE**: *CHOSEN + it closed → **STOP. Never slide onto another***, against *ASSUMED + it closed → quietly take the remaining one*. The two rows are a deliberate distinction this file was built around - the suite calls the first *"the dangerous one"* and says a naive sticky binding *"silently rebinds to whatever is left, and the user's next command lands on someone else's model"* - and the version wall was doing exactly that. **IT IS THE SOFTER HALF OF THAT HAZARD AND STILL THE SAME SHAPE**: this path decides which fragments are OFFERED, never which model is touched, so nothing was written to the wrong project. What it costs is that fragments are filtered against a release nobody named, and the sentence printed calls that an assumption about *the only Revit connected* when a choice had been made and lost

**State.** **FIXED 2026-09-20 in the same change as [row 130](../FRAGMENT-ISSUES.md#), and the two are reported apart because they are two defects.** `version_for_filter` returns `(None, "the Revit you chose (pid N) has closed")` when an **explicit** choice is no longer live, so the wall stops rather than sliding; an **assumed** binding whose session closed still quietly takes the remaining one, which is the other row of the same table and must not be collapsed into the first. Both are pinned, and the slide has its own check asserting the survivor's release is **not** returned - a test that would pass on a function that merely refused everything is not a test of this. **THE RULE IS PURE AND LIVES IN `mcp/server/heron_session.py`**, which imports no MCP SDK: CI cannot import `heron_mcp_server` at all, so a test beside the caller would never run on the machine that matters. `_revit_version` keeps only the plumbing - gather, ask, close. **AND ONE MORE WAS FOUND IN MY OWN FIRST DRAFT**, before it landed: the caller used `.capitalize()`, which lower-cases everything after the first letter, so *"2 Revits are connected"* printed as *"2 revits"* - the product's own name in lower case, in a sentence a modeller reads. Raised by the first character only

---

### Row 160

*Moved from the register on 2026-09-23.*

**Defect.** **A TEST THAT PASSES ON WINDOWS, IN A CLEAN CLONE, WITHOUT `%APPDATA%` AND WITH A STRIPPED ENVIRONMENT, AND FAILS ON THE LINUX RUNNER EVERY TIME.** `tests/test_fragment_tracking.py`, added 2026-09-20 with [row 156](../FRAGMENT-ISSUES.md#)'s `--vary`. Three commits, three red runs, and the suite gate records only WHICH file failed - it runs `python "$t" > /dev/null 2>&1` - so the reason has never been visible. **WHAT WAS RULED OUT, each by doing it rather than by reasoning**: a missing `%APPDATA%` (passes with it unset); a stale file in the working checkout (passes in a fresh `git clone` of the same commit); the subprocess section (made skippable, and CI failed again on the very next commit - which is what narrowed it to the pure-Python half); the `DRAFTS_DIR` monkeypatch (verified it takes effect, by pointing it at an EMPTY folder and getting *"no run record at ..."* back); and any non-ASCII byte in the test file or in `heron_validate`'s source - zero in both, so it is not an encoding fault on a C locale. **ONE HINT IS RECORDED AND NOT FOLLOWED UP**: `heron_validate`'s own usage line offers `restamp - fix platform-skewed fingerprints`, so this repository already knows fingerprints differ between Windows and Linux. The test uses a FAKE fragment whose fingerprint is a constant, so it should not be reachable - but whoever picks this up should start there, because it is the one platform difference the repository has already written down

**State.** **FIXED 2026-09-21, AND THE ANSWER IS ONE SENTENCE.** The suite is back in `tests/`, under its own name, green on Linux. **The check that was genuinely false was section 3's**, which runs the client as a subprocess and reads WHAT IT PRINTS: `main()` in `heron_bridge_client.py` opened with `if os.name != "nt": print("Heron's bridge uses Windows named pipes..."); return 2`, and **exit 2 is the code a refusal uses**, so the runner's own guard saw a well-behaved refusal and checked the text. The text was about Windows. No exception was ever thrown for a guard to catch, which is exactly why every guard added over six commits changed nothing. **THIS ROW'S OWN PREDICTION WAS RIGHT** - *"some assertion about the text ... is not true there"* - and its one recorded hint, the `restamp` fingerprint line, was a red herring, which is worth saying because it was followed first. **THE FIX IS IN THE CLIENT, NOT THE TEST**: `refuse_off_windows()` now sits at the head of each command that needs a pipe, so a command line that is wrong on every platform is refused on its own terms first. `validate --vary` with no `--vary-field` now says so on Linux; `ping` still says Revit is Windows-only. **The skips stay** - they earn their place whatever their origin. **AND THE REASON IT TOOK SIX CYCLES IS ITS OWN DEFECT, now [row 166](../FRAGMENT-ISSUES.md#)**: the sweep printed a filename and threw the output away. Both halves show it now

---

### Row 161

*Moved from the register on 2026-09-23.*

**Defect.** **THE COMPILE GATE SAID "EVERY PROJECT" WHILE NAMING FIVE OF SIX, AND THE SIXTH'S ABSENCE WAS A DECISION NOBODY COULD FIND.** Found 2026-09-20 while adding a seventh project and noticing the gate never named it. `brain/heron_dotnet.py` carried `PROJECTS` under the comment *"Every project, in dependency order"*, and `tests/Heron.Banner.TestHost` was not in it - so `tools/check-compile.py`, the gate whose whole job is proving the C# builds on every Revit release, **had never built it on any of them**, while `gates.yml` ran that gate on every push. **THE FIRST READING OF THIS WAS WRONG AND IS WORTH RECORDING AS SUCH.** It looked exactly like an oversight. It was not: `5669e7e` (2026-09-17) left it out **on purpose**, and the reasoning is sound - Banner is a `WinExe` WPF project, CI builds on Linux with `EnableWindowsTargeting`, and that combination had never been watched go green. That commit even said where it belonged: *"which is where it belongs - but ... Adding the line is trivial once someone has watched one green run."* **THE DEFECT IS NOT THE DECISION, IT IS WHERE THE DECISION LIVED.** The reason was in a commit message. Nothing in `heron_dotnet.py`, nothing in the project file, and nothing in the gate's output said a project was being held back - so the next reader saw a list calling itself complete and a project missing from it, which reads as a mistake and was nearly recorded as one here. **A HELD-BACK PROJECT AND A FORGOTTEN ONE LOOKED IDENTICAL**, and so did `tools/api-surface`, which is genuinely out because it is a tool that reads the Revit assemblies rather than something Heron ships. **WHAT WAS MEASURED:** the condition `5669e7e` set has now been met. `tools/check-compile.py` on **Linux**, the **10.0.x** SDK, `-p:EnableWindowsTargeting=true` - the same command, operating system, SDK line and flag `gates.yml` itself uses - built **all seven projects on all eight releases, 2020 through 2027, Banner included**, exit 0. So it is listed now, on that evidence rather than on a tidy-up. **AND THEN CI WATCHED IT ITSELF, WHICH IS THE THING `5669e7e` ACTUALLY ASKED FOR.** The run above was a local one arranged to match the runner; the condition was *"once someone has watched one green run"*, and the runner has now had one - GitHub Actions run `35481172661` on head `8829728`, job *"The C# compiles, 2020 to 2027"*, **success**, with `Heron.Banner.TestHost.csproj` printing `ok` on every one of the eight and the tool's own closing line reading *"All 7 projects compile on every version tried, and no .csproj on disk is missing from that list."* A `WinExe` WPF project has now been built on Linux with `EnableWindowsTargeting` **on the runner that was the worry**, which no local run could establish. **THE REPAIR IS NOT THE TWO LINES.** `heron_dotnet.unlisted()` asks **git** which `.csproj` are tracked and **fails the gate** on any that neither `PROJECTS` nor the named `NOT_SHIPPED` accounts for, so the next exclusion has to be written down **where the gate is** rather than in a message that scrolls away. It reads the source tree and not the build, so it runs **before** the .NET check and holds on a machine with no .NET at all. **IT ASKS GIT RATHER THAN WALKING, AND THAT WAS A REVIEW FINDING.** The first version walked the disk excluding `bin` and `obj`, and a Codex review on PR #212 pointed out what that does to a documented command: `check-fragments-compile.py --keep` deliberately leaves `build/<version>/FragmentCheck.csproj` behind for inspection, `build/` is in `.gitignore`, and the next run of the gate would have called that generated project forgotten and exited 1 - **a gate failing because somebody used a flag the tool documents.** A tracked file is the right test, since a project nobody committed is not one this gate compiles; the walk survives only as the fallback for a tree that is not a checkout, and there the generated directories are **read from `.gitignore`** rather than typed - which a THIRD round then forced and improved. `tests/test_dotnet.py` guards that this module starts no build, by scanning it for the quoted word, and a hardcoded exclusion list containing `"build"` tripped it. **The test was not touched** - Golden Rule: a red test is a claim about the code. Reading `.gitignore` removes the literal honestly AND is the better rule, since a second list of generated directories beside the one git already has is the drift this repository keeps paying for. **BOTH LEGS MEASURED (D-30), AND RE-MEASURED AFTER THAT CHANGE:** a generated `build/fragment-check/2024/FragmentCheck.csproj` is **not** reported and the gate still exits 0; a tracked `.csproj` nobody listed **is** reported by name and the gate exits 1. And the **gate** was run, not only the function - a stray `.csproj` in `tests/` makes `check-compile.py` exit **1** with a message naming the file and both places it may be declared. **WHAT IS NOT ESTABLISHED:** whether Banner would have compiled on every release during the three days it was unlisted - it compiles today, which is a measurement of today and not a claim about the past. **And compiling is not behaving.** Banner's `shots` and `freeze` modes need a screen; nothing here ran them, and D3 in NEEDS-CHECKING is still the line that catches a unit error.

**State.** **FIXED 2026-09-20 - and the fix is the check, not the list.** The list was wrong for three days because nothing could tell it was. A hand-written list is only worth keeping if something checks it, which is the argument `disagreements()` already makes one level down about the mirrored runtime tables - this is the same argument about the projects

---

### Row 163

*Moved from the register on 2026-09-23.*

**Defect.** **A TOOL CRASHED INSTEAD OF REPORTING A MISSING FOLDER, ON THE OWNER'S PC ONLY, AND CI CAN NEVER SEE IT.** Found 2026-09-20 - it was the ONE entry in `tools/check-gaps.py`'s UNFINISHED list, the list whose exit code that tool follows. `tests/test_skill_catalog.py` died at its sixth section with **`ValueError: path is on mount 'C:', start on mount 'D:'`**, raised inside `tools/generate-skill-catalog.py:199`: `os.path.relpath(folder, ROOT)`. **On Windows `os.path.relpath` RAISES across drives rather than returning something useless**, the checkout is on `D:` and `%TEMP%` is on `C:`, and the test hands `routing()` a scratch folder to prove it reports an absent recording. **THE LINE THAT CRASHES IS THE ONE THAT REPORTS ABSENCE** - the branch reached only when the folder is missing, which is the branch nobody exercises and the reason it survived. **IT IS INVISIBLE TO THE LINUX RUNNER BY CONSTRUCTION**: one mount, so `relpath` always returns and the raise cannot happen, which is [row 160](../FRAGMENT-ISSUES.md#)'s shape with the platforms swapped - that row is a test green on Windows and red on Linux, this is a tool green on Linux and red on Windows. **AND THE REPOSITORY ALREADY OWNED THE ANSWER, TWICE.** `brain/heron_fragment.repo_relative()` does try/except and falls back to the absolute path, and `tools/check-licence.py` carries a deliberate second copy whose docstring names this exact hazard in these words: *"on Windows os.path.relpath RAISES across drives"*. **`generate-skill-catalog.py` already imports `heron_fragment as FRAG` and did not call it**

**State.** **FIXED 2026-09-20.** All three `os.path.relpath` calls in `routing()` became `FRAG.repo_relative(...)` - the module that owns the rule, already imported, so nothing new is depended on and there is no third copy of the answer. `python tests/test_skill_catalog.py` exits **0** where it exited 1, and `python tools/generate-skill-catalog.py` exits 0 and writes `skill-catalog.html` (37 KB) with no change to any tracked file. **THE SWEEP HAS NOT BEEN RE-RUN AND THAT IS SAID RATHER THAN ASSUMED** - `check-gaps.py` costs about fifty minutes here; its UNFINISHED list held exactly this one item and the suite now passes alone, so the list SHOULD be empty, which is an inference and not a measurement. **THE SUITE WAS RUN ALONE BEFORE BEING BELIEVED**, because two suites read FAIL inside a serial sweep on 2026-09-19 and passed on their own; this one fails both ways and the traceback is the same. **THE OTHER RELPATH SITES WERE NOT SWEPT AND ARE NOT CLAIMED SAFE.** A scan for the shape - a path reaching `relpath` from a function PARAMETER rather than a module constant - names three candidates and nothing more: `tools/check-fragments-compile.py:290`, `tools/new-agent.py:259` and `:266`, and `tools/prove-agent.py:177`. **That is a candidate list, not evidence** - [row 120](../FRAGMENT-ISSUES.md#) is the standing warning about a home-made detector's output being read as a finding, and it was wrong about loops five times. Whether any caller ever hands one of those an off-drive path is unread **THE SWEEP WAS RE-RUN AND THE INFERENCE IS NOW A MEASUREMENT, 2026-09-20.** `python tools/check-gaps.py`, full pass, **exit 0** where it exited **1** before this fix - and the exit code of that tool follows the UNFINISHED list alone, which is the whole reason it is worth reading. It prints **`UNFINISHED - nothing. Everything buildable here is built, every test passes, every checker is clean.`** 1,278 seconds of suites, no `FAIL`, no `HUNG` and no `wait`; `check-docs`, `check-metadata` and `check-structure` all clean in the same pass. **WAITING stands at 125** and none of it is work anybody can do here. **The exit code was read from the command and not through a pipe** - the first attempt at this sweep reported `EXIT=0` because `$?` had been taken after a `\| tail`, which is the trap `docs/HANDOVER.md` records for `check-docs.py`, and it hid an exit of 1

---

### Row 164

*Moved from the register on 2026-09-23.*

**Defect.** **THE FIX FOR [ROW 5b-23](../FRAGMENT-ISSUES.md#) STOPPED THE BRIDGE STARTING ANYWHERE BUT WINDOWS, AND THE SUITE THAT PROVES THE PROTOCOL WITHOUT REVIT WENT RED THE SAME COMMIT.** Found 2026-09-21 by running the suites on a Linux container after installing what they need. `tests/test_bridge_roundtrip.py` exited **1**, not 3, and printed the host's own dying words: **`System.PlatformNotSupportedException: Access Control List (ACL) APIs are part of resource management on Windows and are not supported on this platform`**, raised at `BridgeServer.cs:313` inside `CreatePipe`, called from `Start()`. **THE CAUSE IS THE PREVIOUS FIX AND IT WAS RIGHT TO MAKE.** Row 5b-23 measured that the .NET 8+ branch's default ACL grants READ to the world group, and repaired it by asking for the ACL explicitly through `NamedPipeServerStreamAcl.Create`. What it did not carry is that **`new PipeSecurity()` throws before a single rule is added on any non-Windows runtime** - so the unguarded call does not harden the pipe off Windows, it stops the server existing. **WHY THAT MATTERS WHEN REVIT IS WINDOWS-ONLY:** the add-in never takes that path, but `tests/Heron.Bridge.TestHost` does. It runs this exact server with no Revit and no Windows to prove the framing, the token check, the newest-connection handover and the lease - the one thing in this repository that exercises the bridge end to end without a modeller's PC. A suite that cannot start the host proves none of them. **THE REPAIR:** the ACL is now asked for under `if (OperatingSystem.IsWindows())`, and the other path builds the same pipe with the same arguments and no `PipeSecurity`. Windows behaviour is unchanged - same one rule, same principal, same `CreateNewInstance`, same constructor - and the compiler agrees: the seven `CA1416` warnings this method used to emit are gone, because the guard is the form the platform analyser is asking for. **MEASURED, BOTH SIDES:** `test_bridge_roundtrip.py` **passes** (*"Step 1 PASSED - the bridge works, without Revit"*); `tests/test_authentication.py` passes unchanged, including its per-branch reading that each branch names the current user, adds exactly one rule and one `Allow`, and names no group; and `python tools/check-compile.py` builds all seven projects on **2020 through 2027**. **WHAT IS NOT ESTABLISHED:** nothing here re-measures row 5b-23's finding on a real Windows box - the ACL the Windows path asks for was not read back again, and A4 still means the Windows run. The Unix path is a **test transport**: what it gives up is the ACL, and what still guards it is the per-session token on every request plus a socket created under the process umask.

**State.** **FIXED 2026-09-21, in the sitting that found it.** Recorded rather than fixed-in-passing would have left the only Revit-free proof of the bridge red, and the repair is four lines and a guard. The thing to watch is that **a Windows-only API added to shared code fails at RUN time and not at compile time** - `check-compile.py` was green on all eight releases the whole time this was broken

---

### Row 165

*Moved from the register on 2026-09-23.*

**Defect.** **A LITERAL PIPE IN A ROW'S TEXT MAKES THE REGISTER'S OWN SWEEP READ A FRAGMENT OF A SENTENCE AS THE STATE, AND FOUR ROWS WERE DOING IT.** Found 2026-09-21 while closing [row 162](../FRAGMENT-ISSUES.md#) and noticing the tool reported it as neither open nor settled, on a day its cell began with the word **OPEN**. `tools/open-defects.py` reads the state as `line.split("\|")[-2]`, and Markdown lets a cell hold a literal pipe written `\|`. It also lets one through **unescaped inside a code span**, which is where every instance here hid. **THE FOUR, EACH READ RATHER THAN INFERRED.** [Row 162](../FRAGMENT-ISSUES.md#) wrote `` `ls tests/test_*.py \| wc -l` `` in both its cells - correctly escaped, and the tool did not know the escape, so the state read as *"wc -l against the count the sweep prints"* and the row was invisible. [Row 15](../FRAGMENT-ISSUES.md#) used a bare `\|` as a sentence break between the OPEN history and the fix that followed: the tool read the SECOND half, reported **FIXED**, and was right by accident - reversed, the same shape hides a fix. [Row 163](../FRAGMENT-ISSUES.md#) holds `` `\| tail` `` in its state cell, so the state read as *"tail, which is the trap docs/HANDOVER.md records"*. [Row 5b-13](../FRAGMENT-ISSUES.md#) holds `` `identity + "\|" + doc.PathName + "\|" + doc.Title` `` in its defect cell - two pipes, and cells[-2] landed on the state only by luck. **AND ALL FOUR RENDER WRONG ON GITHUB TOO**, as a four- or five-column row in a three-column table; this was not only a tool's problem. **THE REPAIR IS IN THREE PARTS, because any one alone comes back.** The tool splits on an unescaped pipe (`CELL = re.compile(r"(?<!\\)\|")`) and **complains by name** when a row does not have exactly three cells, rather than silently reading the wrong one. The four rows are repaired in place - escapes added, and row 15's cell reordered so its first word is `FIXED`, keeping every word of what it said while it was open ([row 153](../FRAGMENT-ISSUES.md#)'s convention: the FIRST word of the cell is the state). And `tests/test_open_defects.py` gains a **gate** - not a report - that every row of sections 5 and 5b has three cells. **MEASURED:** before, `rows()` returned the wrong state for 15, 156 and 163; after, all three read `FIXED`/`BUILT` from their own first words, 5b-13 reads `OPEN`, and the open count is unchanged at the figure `python tools/open-defects.py` prints. The new gate was **made to fail on purpose** - a synthetic row with a bare pipe - then the register restored. **[ROW 156](../FRAGMENT-ISSUES.md#) WAS A DIFFERENT FAULT IN THE SAME FAMILY and is fixed here too:** its line did not end with a `\|` at all, so it had one cell fewer than every other row and the tool read its **defect text** as its state. **WHAT IS NOT ESTABLISHED:** `tools/owner-queue.py`, `tools/agent-count.py`, `tools/prove-agent.py`, `tools/check-metadata.py` and `brain/heron_validation.py` all split table rows the same naive way. None is wrong today - they read cells from the LEFT, which an escape further right cannot move - and none was changed. **Recorded, not fixed**, because widening this into five tools would have made it unreviewable.

**State.** **FIXED 2026-09-21, in the sitting that found it.** The durable half is the gate in the suite, not the four escapes: a register whose own sweep cannot see one of its rows is the single failure this register exists to prevent, and it had been true for at least a day. The number to watch is the `MALFORMED` line the tool now prints - it should never appear

---

### Row 166

*Moved from the register on 2026-09-23.*

**Defect.** **THE SUITE SWEEP THROWS AWAY THE ONE THING A FAILING SUITE SAYS, AND THAT COST SIX RED CI RUNS, A WRONG RENAME AND A DELETED TEST.** Found 2026-09-21 while answering [row 160](../FRAGMENT-ISSUES.md#) - which is not this defect, but is entirely a consequence of it. `tools/check-gaps.py` ran every suite with `stdout=DEVNULL, stderr=DEVNULL` and printed `FAIL <name>`; `.github/workflows/gates.yml` runs `python "$t" > /dev/null 2>&1 \|\| basename "$t"` and compares a list of FILENAMES. **Right for the two hundred that pass. Catastrophic for the one that does not**, because the exit code is then the only thing that crosses the boundary, and an exit code cannot say which check was false. **WHAT IT ACTUALLY COST, all of it recorded in [row 160](../FRAGMENT-ISSUES.md#)'s own cell:** three sections of `test_fragment_tracking.py` made skippable one at a time to bisect a failure by pushing; the file renamed out of the suite, which `tests/test_naming.py` refused within minutes; six commits and six red runs; and finally the suite deleted rather than left blocking a feature that was already proved on a model. **THE ANSWER TOOK ONE COMMAND** - `python tests/test_fragment_tracking.py` on a Linux machine - and it was one line of the output the sweep had been discarding all along. **THE REPAIR IS IN BOTH PLACES, because either one alone leaves the other blind.** `check-gaps.py` captures the output and prints the last 25 lines under any `FAIL` - the tail is where every suite here puts its verdict, which is the convention `tests/README.md` states - and says *"(it printed nothing at all)"* when there is none. `gates.yml` re-runs **only the new failures, only when the job is already red**, and puts the last 40 lines into the step summary inside a `<details>` block as well as into the log. **MEASURED:** a suite that fails on purpose was dropped into `tests/`, the sweep run, and its `FAILED - 1 check(s)` block appeared under the `FAIL` line; the file was then removed. **WHAT IS NOT ESTABLISHED:** the `gates.yml` half has not run on a real runner - it is shell, reviewed and YAML-parsed here, and the first new failure will be its first execution. A suite that fails non-deterministically may print something different on the re-run, which is itself worth knowing and is said in the comment beside it. **AND THE COST OF THE CAPTURE WAS NOT MEASURED**: holding a passing suite's output in memory is bytes, and nothing here timed the sweep before and after.

**State.** **FIXED 2026-09-21, in the sitting that found it.** The rule worth keeping is the one [row 162](../FRAGMENT-ISSUES.md#) states from the other direction: **a check that cannot say what it found is a check nobody hears.** An exit code is not a diagnosis, and a sweep that prints only a filename asks every future reader to reproduce the failure before they can start on it

---

### Row 167

*Moved from the register on 2026-09-23.*

**Defect.** **`main` HAS BEEN RED SINCE #221, AND #223 WAS MERGED ON TOP OF IT.** Found 2026-09-21 when a pull request's own gates went red for a reason that was not in it. **WHAT ACTUALLY FAILED, read from the job log rather than guessed:** the `check-docs` step, on seven **DRIFT** lines. [#221](https://github.com/Ajmalpshaik/Heron-AI/pull/221) put `tag-elements-in-view` and `force-tag-leader-lshape` back to `DRAFT` - correctly, their code changed under a signed proof and `check-signatures` reports both as STALE, which is D-30 working - and did not carry the change into the seven sentences that state the totals: `329 PROVEN` is **327**, `66 DRAFT` is **68**, and `168 MODIFY PROVEN` is **166**, across `README.md` (five), `docs/README.md` and `brain/README.md`. **THE GATE WORKED AND THE MERGE DID NOT WAIT FOR IT.** Gates run 580 on `main` is a FAILURE at `c043cbc8`, and run 600 - #223 - is the next thing on `main` after it. So the repository's own rule, *a stated count is a claim and a derived count is a fact*, was enforced by the tool and then merged past twice. **AND IT IS INVISIBLE FROM A BRANCH.** A pull request opened before #221 passes every gate locally, because the two fragments it never saw are still PROVEN in its own tree; the red only appears when GitHub builds the MERGE of the branch with `main`. That is what made this look at first like a defect in the branch. **FIXED HERE** by merging `main` and correcting all seven sentences from the library rather than from each other - derived with the command each line names. `check-docs` exits 0 again. **WHAT IS NOT FIXED AND CANNOT BE HERE:** the two STALE signatures. Re-proving needs Revit and a model, and `restamp` is for a platform-skewed fingerprint, not for code that genuinely changed - using it here would forge a proof. They stay in `check-signatures`' STALE list, which is where D-30 wants them.

**State.** **FIXED 2026-09-21 for the drift; the two proofs are the owner's, on a PC with Revit.** The durable point is the one the register keeps making about itself: **a green branch is not a green merge.** Nothing in this repository currently checks the base branch before merging, and two changes went in on top of a red one

---

### Row 168

*Moved from the register on 2026-09-23.*

**Defect.** **THE REGISTER'S OWN SWEEP READ A DATED *DENIAL* AS A DATED FIX, SO THE ONE QUESTION IT ASKS WAS THE ONE A READER HAD TO LEARN TO IGNORE.** Found 2026-09-21 while reading the 32 rows it still calls open. `tools/open-defects.py` prints a short list headed *"ROWS THAT ARGUE WITH THEMSELVES"* — a row whose state begins with OPEN and also carries a dated `FIXED` or `CLOSED` claim, which is the shape [row 127](../FRAGMENT-ISSUES.md#) had when a fix was appended under the sentence saying it was open. The whole value of that list is that it is short enough to read every time it prints. **IT MATCHED `\b(FIXED\|CLOSED)\b` WITH NOTHING IN FRONT OF IT**, and [row 75](../FRAGMENT-ISSUES.md#)'s state reads *"THE LOSS IS VISIBLE NOW AND THE WRONG BINDING IS **NOT FIXED**, 2026-09-20"* — a row saying, in writing, that it is still open and when it was last looked at. So from 2026-09-20 the sweep reported row 75 as arguing with itself, and the tool's own docstring went on claiming the test was *"measured against every row in the register: no false ones"* the whole time. **A CHECK WHOSE ONE FINDING IS ALWAYS WRONG IS A CHECK NOBODY READS**, which is [row 166](../FRAGMENT-ISSUES.md#)'s lesson from the other end — that one threw away what a failing suite said, this one prints a sentence that is not true

**State.** **FIXED 2026-09-21, AND MEASURED RATHER THAN IMAGINED.** Every `FIXED`/`CLOSED`-plus-date in the register was printed with the 34 characters in front of it: **exactly one is a denial**, and it is row 75. `SETTLED` now carries four fixed-width lookbehinds — `NOT `, `not `, `NEVER `, `never `, spelled out in both cases because the next one will be written by somebody else — and the docstring's *no false ones* claim is gone, because it was the claim rather than the pattern that made this survive a day. **PINNED BOTH WAYS in `tests/test_open_defects.py`**: section 1 still proves the detector fires on row 127's real shape, and section 2 now carries row 75's own words and a `NEVER FIXED` line beside the four fixes-in-passing it already held. **The count did not move and could not have** — this tool asks and never re-counts, so 32 rows were open before and 32 after

---

### Row 170

*Moved from the register on 2026-09-23.*

**Defect.** **882 CHECKS COMPILED EVERY DAY AND RAN ON NO DAY.** Found 2026-09-21 while reading the add-in, by asking which test hosts have a runner. `tests/Heron.StackGuard.TestHost` runs `HeronStackGuard`'s rewriter over the WHOLE fragment library and checks the three things that decide whether it is safe to deploy: that **no fragment's line COUNT moves** (so a compile error still points at the right line of `fragment.cs`), that **no new diagnostic appears**, and that **every block-bodied lambda ends up guarded**. Its own project file says what it is for - *"Proves the stack guard WITHOUT Revit"* - and how to run it - *"Run it with `dotnet run`, pointed at this folder"*. **NOTHING IN THE REPOSITORY RAN IT.** It was added to `heron_dotnet.PROJECTS` so that it COMPILES on all eight releases, and HANDOVER calls that *"the 'a gate nobody runs' shape caught before it could set"* - **the half it caught was the compiling half**. `check-gaps.py` sweeps `tests/test_*.py`, so no suite built it, no suite executed it, and 882 checks sat outside every count of *every test passes*. **ITS THREE SIBLINGS ALL HAVE ONE**: Kernel, BindingNote and Bridge each link C# by source and each has a runner in `tests/`. This was the fourth and it had none. **AND THE STALENESS IS THE PROOF**: HANDOVER described it as running *"over all 372 fragments: 836 checks"*, and the host reports **395 fragments and 882 checks** - the host was right the whole time and everything written ABOUT it had drifted, which is what happens to a thing nobody runs

**State.** **FIXED 2026-09-21 - `tests/test_stack_guard.py`, the missing runner.** Written in the same shape as `tests/test_kernel.py`, which is the sibling that had to solve the same problem: it works out the newest framework an installed SDK can BUILD and an installed runtime can RUN, builds the host with `HeronTfm` overridden to a plain **`net10.0`**, runs it, and passes the host's own exit code through. **THE OVERRIDE IS THE WHOLE TRICK AND IT WAS MEASURED, NOT ASSUMED**: `dotnet run` as the project file suggests FAILS here, because `RevitVersion=2024` maps to `net48` and there is no Mono - the error is *"An error occurred trying to start process 'mono'"*, which reads like a broken machine rather than a wrong target framework. Overridden, it builds in **1.73 s** and prints **PASSED - 882 checks, 395 fragments, 91 carrying a guard**. **IT EXITS 3 WHERE THERE IS NO .NET, WHICH IS NOT A PASS** - the fourth state `check-gaps.py` and `change-evidence.py` both read. The suite count goes **207 to 208**, and HANDOVER's paragraph now carries both measurements with their dates and says which day each was taken. **WHAT IT STILL DOES NOT SAY**, in the host's own closing words: that a guarded fragment survives runaway recursion in front of Revit. That needs a model and is [J9](../NEEDS-CHECKING.md)'s to hold

---

### Row 171

*Moved from the register on 2026-09-23.*

**Defect.** **`revit_systems` COUNTED A SYSTEM'S TERMINALS AND CALLED THEM THE SYSTEM, SO EVERY DUCT, PIPE AND FITTING ON EVERY MODEL READ AS "ON NO SYSTEM".** Measured 2026-09-22 on `Project1` (3,420 elements), Revit 2024, session 17492: `duct  Mechanical Supply Air 1  0 element(s)` and `3 MEP element(s) are on NO SYSTEM`, while `revit_parameters` read `System Name = Mechanical Supply Air 1` on 2 of 2 ducts, and the third MEP element was the elbow between them (`M_Rectangular Elbow - Radius: 1.5 W`). **THE CAUSE IS IN AUTODESK'S OWN WORDS**, the same in the API notes of every release from 2020 to 2027: `MEPSystem.Elements` is *"Terminal elements in the system ... doesn't include the base equipment or panel"*. The run is `MechanicalSystem.DuctNetwork` or `PipingSystem.PipingNetwork`, and the equipment is `BaseEquipment`. `RevitSystems.cs` read the first alone. **THE SIGNED PROOF ALREADY CARRIED IT**: the 2026-09-19 record in [`HERON-REVIT-SYS-030.yaml`](../../brain/agent-proofs/HERON-REVIT-SYS-030.yaml) read `onNoSystem: 34 -> 16` beside `mepElementsExamined: 34 -> 16` - every MEP element off every system in BOTH models, with 2 and 10 systems present. D-53 read that as movement, which it was: tracking asks whether an answer follows the model and cannot ask whether it is right. **[Row 114](../FRAGMENT-ISSUES.md#)'s `688 MEP elements on NO SYSTEM` was, at least in part, this.** That row stands as written - its rule, read `System Type` on the element, is still right - and this one says what it could not: the 688 was never evidence about the model. The file also said *"an element connected to nothing is on no system by definition"*, and the tool's own description repeated it. Nothing ever counted that.

**State.** **FIXED 2026-09-22, and proved the same day** on two models - `test projject` (Revit 2024, session 45408, 3,565 elements) and `PIPE` (Revit 2020, session 12684, 3,332 elements), fingerprint `7ad8b6c5570fce82`. Each system now counts its run, its terminals and its base equipment, each element once, and reports `network` and `components` beside `elements`. **Positive**: `PIPE`'s nine systems hold 37 elements, and the `System Name` of its 25 pipes and 12 fittings, read separately, gives the same count for every one of the nine. **Negative**: `onNoSystem 0` on `PIPE`, where every element names a system; on `test projject`, `onNoSystem 6` - exactly the six mechanical equipment units `read-mep-system` puts on no system. `tests/test_systems_answer.py` holds the wording to the counts. [NEEDS-CHECKING](../NEEDS-CHECKING.md) Group N has the rows

---

### Row 172

*Moved from the register on 2026-09-23.*

**Defect.** **THE SAME TOOL COUNTED A DUCT'S LINK TO ITS OWN SYSTEM AS A PHYSICAL JOIN, SO EVERY OPEN END ON A SYSTEM READ AS JOINED.** Same model, same session: `withAnOpenConnector: 0` on a run of two ducts and one elbow whose two far ends are open. The proven `find-dead-ends` fragment, run on the same two ducts minutes later, answered `openEndsFound 2`, `deadEnds [925754, 925761]`. `Connector.AllRefs` holds *"both physical connection and logical connection"* (Autodesk's API notes), and `IsJoined` counted the logical reference a connector on a duct or pipe system holds to that system. **THE LIBRARY ALREADY KNEW**: `report-connectors` - the same agent, and `PROVEN` - says in its purpose *"A SYSTEM IS NOT A PARTNER"* and drops those references before it counts. The add-in half never got the rule. **What it hid**: NEEDS-CHECKING N4 (a lone duct appears in `connectedToNothing`) and N5 (a connected run counts in `withAnOpenConnector`) could not pass on any run that is on a system. **Deliberately NOT extended to electrical**: a circuit's reference still counts as a join, because dropping it would list every light fitting on a circuit as connected to nothing - a decision about electrical, which this agent does not read.

**State.** **FIXED 2026-09-22** in the same change, and proved on the same two runs. **`PIPE`**: `withAnOpenConnector 19`, and `find-dead-ends` over the same 37 pipes and fittings counted 19 elements with an open end, asking each connector's own `IsConnected` instead. **`test projject`**: its ten single ducts and pipes each sit on a system Revit made for that one element, and all ten are now in `connectedToNothing` - 16 in all, where the 2026-09-19 record, on the same file at 3,594 elements, counted 6

---
