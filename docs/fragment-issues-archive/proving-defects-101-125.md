# FRAGMENT-ISSUES archive — section 5, rows 101 to 125

> **Finished rows, moved out of the live register.** These are rows from section 5 of
> [`FRAGMENT-ISSUES.md`](../FRAGMENT-ISSUES.md) — Heron's own defects found by proving — whose state said they were
> finished when they were moved. Each still has its line in the register: the same number, a
> one-line title, the opening of its state, and a link to its full text here.
>
> **Nothing below was rewritten** except its links, which were re-pointed one folder deeper and
> each checked to reach the same file it reached before. Where a row disagrees with
> [DECISIONS.md](../DECISIONS.md), the [Golden Rules](../14-golden-rules.md) or the
> [Constitution](../../HERON_CONSTITUTION.md), **those win** — a row records what was true on
> the day it was written.
>
> **Nothing new is written here.** A new defect goes in the register, and a row here that turns
> out not to be finished is reopened **there**, on its own line.
>
> Rows 101 to 125 of section 5 belong in this file. A number in that band with no entry below
> was never moved — it is still in the register, in full. Written by
> [`tools/archive-fragment-issues.py`](../../tools/archive-fragment-issues.py).

---

### Row 104

*Moved from the register on 2026-09-23.*

**Defect.** **A FRAGMENT THAT RECURSES WITHOUT A FLOOR DOES NOT RETURN AN ERROR - IT ENDS REVIT.** `RunScript` catches `Exception` and its own comment reads *"a fragment that throws is a finding, not a crash"*. True of every exception but one: **`StackOverflowException` cannot be caught.** Since .NET 2.0 the runtime fails fast - no handler, no `finally`, nothing written, nothing asked - and the process it ends is **Revit, holding the user's unsaved model**. Nothing in the add-in guarded against it. Found 2026-09-16 reading an unrelated open-source WPF app that had solved the same problem for its own scripting host. **Zero of the 372 fragments recurse today**, so this was a door standing open rather than a fire - but a fragment's `.cs` is LIVE, read off disk and sent on every call, so the door is one edit wide

**State.** **FIXED AND PROVED IN FRONT OF REVIT 2026-09-16 - [J9](../NEEDS-CHECKING.md) PASSED.** A fragment recursing with no floor came back as `fragment_threw`, *"Insufficient stack to continue executing the program safely"*, and **Revit lived** - same PID, 1 ms ping, and a real read of 3,565 elements afterwards. The uncatchable failure is now an ordinary one. `HeronStackGuard` injects `RuntimeHelpers.EnsureSufficientExecutionStack()`, which throws a **catchable** exception just before the stack runs out, so `RunScript`'s existing catch handles it. **THE IDEA WAS TAKEN AND THE CODE WAS NOT, and that distinction is the whole row.** The usual form of this rewriter guards named members and deliberately skips lambdas, on the stated grounds that recursion through anonymous functions alone is not a real shape. **Measured here: 0 of 372 fragments declare a method or a local function** - a fragment is a script of top-level statements and `Func<>` lambdas - so the received rule, applied unexamined, would have shipped a guard that compiles, reviews well and protects **nothing**. Guarding block-bodied lambdas instead covers **91**. An EXPRESSION-bodied lambda is still unguarded and that is deliberate: rewriting one can change `Func` to `Expression` and pick a different overload. Proof: `tests/Heron.StackGuard.TestHost`, 836 checks over the whole library - line counts unchanged, no new diagnostic, and every one of the 91 guarded

---

### Row 105

*Moved from the register on 2026-09-23.*

**Defect.** **TWO WRITES THAT LOOKED ATOMIC AND WERE WORSE THAN A PLAIN OVERWRITE.** `HeronConfig.Save` and `BridgeIdentity` both wrote a sibling `.tmp` and then did `if (File.Exists(target)) File.Delete(target); File.Move(tmp, target);`. A plain write leaves a **truncated** file if interrupted; delete-then-move leaves **no file at all**, through a window made wider by the extra syscall inside it. **In `HeronConfig` it does not corrupt the settings, it erases them, silently:** `Load` answers a missing file with `Defaults` and swallows `IOException` without a word, and the next `Save` writes those defaults back - so the file whose own header says *"Owned by you. Never overwritten by an update"* is gone with no message. In `BridgeIdentity` the comment read *"so a client never reads a half-written file"*, which is true and does not cover a client reading **no** file and concluding no bridge is running. Found 2026-09-16

**State.** **FIXED 2026-09-16.** `HeronAtomicWrite` - `File.Replace` when the target exists (atomic on NTFS, and available since .NET Framework 2.0, unlike `File.Move(overwrite:)` which arrived in .NET Core 3.0 and is no use to net472/net48), a plain rename when it does not. Both call sites now go through it. **Neither the fix nor the bug changes any fragment, so no fingerprint moved and no proof went stale.** It needs the add-in deployed to take effect

---

### Row 107

*Moved from the register on 2026-09-23.*

**Defect.** **REVIT CANNOT RENAME A PHASE AT ALL - TWO DOORS, TWO RELEASES, SAME REFUSAL - AND THE COMPILE AND THE SURFACE CHECK BOTH SAID IT COULD.** A `RENAME_PHASE` fragment was written, compiled on 2020-2027, and run against real models on 2026-09-16: `PIPE` (3,332 elements) on **Revit 2020**, and `Snowdon-scratch_ajmal.al` (9,638 elements) on **Revit 2024**. Both refused, and the message is identical on both: `Element.Name` throws **"This element does not support assignment of a user-specified name"**, and `BuiltInParameter.PHASE_NAME` - the second door every workaround reaches for - reports **`IsReadOnly`**. **SO IT IS THE OBJECT AND NOT THE RELEASE.** A phase is not an editable object from the API. **NOTHING SHORT OF RUNNING IT COULD HAVE FOUND THIS.** `check-fragments-compile` passes on 2020-2027 and `check-api-surface` passes on 2020-2027, and both are CORRECT - the member genuinely exists; a member that exists and throws is exactly what `tools/api-surface/changes.json` lists under `cannotSee`. **IT IS THE SAME SHAPE AS A FACT `REPORT_PHASES` ALREADY CARRIED** and nobody read across: Revit exposes no CREATE call for a phase on any release either. That routing note now carries a rename note beside it, in the same words. **THE NEGATIVE LEG WAS CLEAN ON BOTH RELEASES** - `no phase called 'ZZZNoSuchPhaseAnywhere'. This model has: Existing, New Construction` - so D-30's second leg held and its FIRST can never be non-empty anywhere. Neither model was altered; both were re-read afterwards, and `PIPE`'s names survived a full Revit restart, which is a stronger check than a rollback anybody has to trust. **RENAMING BY HAND WORKS AND WAS DEMONSTRATED THE SAME DAY** - the owner renamed `Existing` in `PIPE` through Manage, Phases while this was being measured, and the model came back with 2 phases, 0 design options and 3,332 elements unchanged. Revit's dialog can do what Revit's API will not.

**State.** **CLOSED - not a defect, a limit in Revit.** Found and settled 2026-09-16. The fragment was REMOVED: it could never be proved on any release, and a permanently unprovable fragment is noise in the DRAFT count. What replaces it is a routing note in `REPORT_PHASES`, which is where the identical fact about CREATE already lived

---

### Row 110

*Moved from the register on 2026-09-23.*

**Defect.** **HERON CAN FIND PIPES AND CANNOT SELECT THEM, AND THE TWO ANSWERS COME FROM DIFFERENT TABLES.** Measured 2026-09-16 on `4355-BHVD-3D-50C10-BL001A`. `select-by-category-name` - a FRAGMENT - was asked for `Pipes` in `{3D - ajmal.al}` and returned **120 elements, `resolvedTo: Pipes`, 0 near misses**. `revit_parameters` and `revit_select_by_category` - the MCP TOOLS - refused the same word: *"Heron does not know the category 'pipes' yet. It understands: duct, duct curves, ducts, ductwork."* **THE REFUSAL IS CORRECT AND THE INCONSISTENCY IS THE DEFECT.** The tools read a hard-coded four-row table in `RevitOperations.cs` (`Categories`), whose own comment says *"adding a row is how the rest arrive, once each has been tried against a real model"* - the rule is right and pipes have now been tried. The fragments resolve categories a different way entirely and always could. **SO A MODELLER IS TOLD 'I DO NOT KNOW PIPES' BY A SYSTEM THAT HAS JUST COUNTED 120 OF THEM.**

**State.** **FIXED, AND FOUND TO BE SO ON 2026-09-19 BY A ROUTE THAT WAS NOT LOOKING FOR IT.** The four-row table is now **27 names** and `pipes` is among them: `RevitOperations.cs:351-352` map `pipe` and `pipes` to `OST_PipeCurves`, and the DEPLOYED add-in answers for them - `category="pipes"` returns *"No pipes in `Project1 work_ajmal.al`"*, naming the category rather than refusing to know it. **So this row's own measurement can no longer be reproduced**, and it was found only because [row 126](../FRAGMENT-ISSUES.md#) sent me to read the table for a different reason. **WHAT SURVIVES, AND IT IS THE HALF THIS ROW CALLED DEEPER:** the 27 are ALL MEP - ducts, pipes, fittings, valves, insulation, air terminals, dampers - so `title blocks` is still refused by the TOOL while the FRAGMENT `select-by-category-name` resolves it in the same minute (`resolvedTo Title Blocks`, `nearMisses 0`, measured 2026-09-19). **Two category resolvers still exist and still disagree**; what changed is only where the line falls. Found 2026-09-16

---

### Row 111

*Moved from the register on 2026-09-23.*

**Defect.** **`revit_change` HAS NEVER REPORTED WHAT A WRITE DID, BECAUSE IT READS A KEY NOTHING EMITS.** Found 2026-09-16 by reading the model back after a write that claimed to run. `heron_mcp_server.py:901` does `answer = reply.get("answer")` and prints it under *"X ran in Y"*. **THE ADD-IN NEVER EMITS `answer`.** `RevitFragment.Report` returns `provides`, `providesCount`, `bound`, `ran`, `document`; `WithVerdict` adds `applied`, `rolledBack` and **`verdict`**. Grep for `"answer"` across `mcp/` returns exactly one hit - the read. Nothing writes it, on either side. **SO THE VERDICT IS ALWAYS `None` AND EVERY WRITE THROUGH THIS TOOL REPORTS ONLY THAT IT RAN.** Measured: `RENAME_PHASE` returned *"RENAME_PHASE ran in PIPE."* while the fragment had actually produced `renamed: false` and a refusal naming Revit's own words. The model was unchanged and the tool said nothing about it either way. **THE IRONY IS THAT THE COMMENT DIRECTLY ABOVE THE BUG IS RIGHT**: *"THE VERDICT IS THE ADD-IN'S TO WRITE, NOT THIS TOOL'S ... on 2026-09-09 a rollback did not hold while this side claimed it had."* The lesson was learned and written down; the key name was wrong. **THIS IS THE WORST OF THE FOUR** - it is the failure this repository most fears, a write reporting success with no evidence, and it silently disarms `verdict`'s warning that a rollback may NOT have held.

**State.** **FIXED 2026-09-17, and this cell still said `OPEN` while [row 117](../FRAGMENT-ISSUES.md#) was using the fix WORKING as its own evidence.** Found 2026-09-16. Every part this cell asked for exists: `heron_mcp_server.py:979` reads `reply.get("verdict")` where it read `answer`, `:966` renders `provides` so a refusal is visible as a refusal, it was Python only and needed no Revit restart, and the test that FAILS on the old key is [`tests/test_change_reporting.py`](../../tests/test_change_reporting.py) - which asserts on the field NAME as text, for precisely the reason this cell gives. **Row 117 is the proof it shipped**: its own text says the contradiction it records *"is ONLY visible because row 111 shipped the same day"* - before that, the whole reply was the one sentence this row is about. Row 117 is a **separate** defect (`WithVerdict` wording `applied`, a request, as an outcome) and is correctly open

---

### Row 118

*Moved from the register on 2026-09-23.*

**Defect.** **TWO DEFECTS SHARED ONE LINE OF THE TABLE, SO ONE OF THEM WAS INVISIBLE TO EVERY SWEEP FOR SEVEN DAYS - AND THE OTHER WAS DISPLAYED WEARING ITS NEIGHBOUR'S STATE.** Rows 16 and 17 were written as a single physical line: row 16 opened the line, its state cell closed, and **row 17 began again in the same line** - two cell separators back to back where a line break belonged. `tools/open-defects.py` matches an id at the START of a line and then takes `cells[-2]` as the state - the second-to-last cell of the WHOLE line - so it reported **row 16 carrying row 17's state cell**, and reported no row 17 at all. The register read `116 rows, 31 open`; it was **117 rows and 32 open**. **THE TOOL IS NOT AT FAULT AND WAS NOT CHANGED.** It reads a markdown table correctly; the table was malformed. A reader taught to cope with two rows on one line would then be a reader that cannot tell a row boundary from a cell boundary, which is worse. **THE DEFECT THAT HID THERE HAD BEEN FIXED AND PROVED SIX DAYS EARLIER** ([row 17](../FRAGMENT-ISSUES.md#)), so nothing was lost this time - which is the only reason this is a near miss rather than an incident. Split 2026-09-19; the tool now reports 117 rows without being touched

**State.** **FIXED 2026-09-19.** The check that would have caught it is one line and is not written: **no line in the section may contain two cell separators back to back followed by a row id**. Left unwritten deliberately - this is the second class of thing `open-defects.py`'s own docstring says it cannot see, and a second pattern guarding the first is how a register grows a tool nobody re-reads. The cure that works is the one already in that docstring: **print the ids and read them**

---

### Row 122

*Moved from the register on 2026-09-23.*

**Defect.** **[ROW 23](../FRAGMENT-ISSUES.md#) REPAIRED, AND HALF PROVED 2026-09-19 - THE HALF A MODEL COULD ANSWER WITHOUT BEING REARRANGED.** On `Project1 work_ajmal.al` (Revit 2024, session 20472), `1 - Mech`, `onSheetsOnly: true`, rolled back: **`markers 5`, `hidden 5`, `shown 0`, `unmatched 0`, `refused` empty** - where [row 23](../FRAGMENT-ISSUES.md#) measured `hidden 0` on the same five marks in the same view. The five are the owner's section and four elevations, named in the reply: `North - Mech`, `East - Mech`, `West - Mech` and two more. **THIS IS NOT YET A PROOF AND THE ROW STAYS OPEN**, because a fragment that answered *not on a sheet* for EVERYTHING would produce exactly this number. The original row follows. **[ROW 23](../FRAGMENT-ISSUES.md#) REPAIRED IN FRAGMENT SOURCE.** `set-section-mark-visibility` read `BuiltInParameter.VIEWER_SHEET_NUMBER` and treated *not empty* as *on a sheet*, under a comment claiming it *"gets the empty case right"*. Revit writes its placeholder `---` there for a view on no sheet, which is not empty, so every view read as placed and every mark was skipped. **The repair does not recognise the placeholder** - matching `---` would be guessing at a string Revit formats, and a different language build could spell it differently. It asks Revit instead: collect every `Viewport`, take its `ViewId`, and a section is on a sheet when a viewport points at it. The empty case then comes out right for free, with nothing to translate. `Viewport.ViewId` exists in 2020 through 2027 and no `IntegerValue` is read, so it is version-safe by construction. **A fragment's `.cs` is sent to Revit on every call, so this costs no rebuild and no restart**

**State.** **FIXED AND PROVED 2026-09-19, BOTH LEGS - AND THE SECOND LEG EXISTS BECAUSE THE OWNER SAID TO BUILD IT RATHER THAN WAIT FOR IT.** This cell said the negative needed a view placed on a sheet and called that HIS job. He answered: *"we have already fragments to add views in the sheet... that is the easy way, you don't want to wait for me"* - which is [row 85](../FRAGMENT-ISSUES.md#)'s ruling arriving a second time, and he was right both times. `place-view-on-sheet` put `North - Mech` on `A101` (`placed 1 [926181]`, applied), and the same call then returned **`markers 5`, `hidden 4`, `unmatched 0`** - every mark found, and exactly the one whose view is now on a sheet left alone. **5 then 4, by placing one view, is what separates the repair from a fragment that answers *not on a sheet* for everything** - the failure mode this cell was holding out for. The earlier reading stands as the positive leg: `hidden 5` where [row 23](../FRAGMENT-ISSUES.md#) had `hidden 0`, on the same five marks in the same view. Not marked FIXED, and the fragment stays DRAFT, because **a fragment answering *not on a sheet* for EVERY view would return exactly that 5** - which is the bug this repaired, with its answer inverted. Only a view genuinely on a sheet separates the two. **WHAT WOULD PROVE IT, and it is the arrangement [row 23](../FRAGMENT-ISSUES.md#) already half-built:** on `Project1 work_ajmal.al`, whose `1 - Mech` holds the owner's section plus four elevations and whose `list-sheets` reports **0 sheets**, run with `onSheetsOnly: true` - every mark is on no sheet, so the positive is `hidden 5, unmatched 0`, where the defect gave `hidden 0`. **The negative needs the opposite and needs a sheet**: place those sections on one, then the same call must give `hidden 0` because every view is now genuinely placed. **That second leg is the one this model cannot do today** and it is what the run is for - a positive alone would not separate the fix from a fragment that hides everything

---

### Row 123

*Moved from the register on 2026-09-23.*

**Defect.** **PROVED 2026-09-19 ON `Project1 work_ajmal.al`, BOTH DIRECTIONS, AND THE SECOND DIRECTION IS WHY CODEX'S REVIEW WAS WORTH MORE THAN THE FIRST FIX.** Revit 2024, session 20472, 3,529 elements, sheet `A101` (name `Unnamed`), all runs `--write` with no `--apply` and Revit reporting the group ROLLED BACK. **`hide-elements`**: 18 model ducts with `view = A101` gave **`hidden 0`, `cannotHide 18`, `viewRefused false`**; the single `A1 metric` title block that IS on that sheet gave **`hidden 1`, `cannotHide 0`, `viewRefused false`**. **`isolate-elements`**: the 9 ducts of `1 - Mech` gave **`isolated 0`, `viewRefused false`**; the title block gave **`isolated 1`**. **`set-view-crop`**: **`applied false`, `enclosed 0`, `viewRefused true`**, where it gave `applied true, enclosed 9, viewRefused false`. **THE TWO `1`s ARE THE RESULT THAT MATTERS.** The first repair refused any `ViewSheet` outright and would have returned `viewRefused true` for that title block - a real request, silently blocked, and no amount of testing off-sheet ducts would have shown it. Codex named it on PR #191 before a model was ever asked, and the model then agreed. The original row follows. **THREE FRAGMENTS ACCEPTED WORK ON A SHEET AND REPORTED SUCCESS, AND THE GUARD THAT SHOULD HAVE STOPPED THEM WAS ALREADY THERE IN ALL THREE.** [Rows 20](../FRAGMENT-ISSUES.md#) and [24](../FRAGMENT-ISSUES.md#) measured it: `hide-elements` returned **`hidden 191`** on `Cover Sheet` of `Snowdon-scratch_ajmal.al` for 191 ducts that live in another view; `isolate-elements` returned **`isolated 9`** and `set-view-crop` **`applied true, enclosed 9`** on sheet `A101` of `Project1 work_ajmal.al` for 9 ducts not on it. All three declare `viewRefused` for precisely this case and a sheet tripped none of them. **THE THREE TESTS WERE EACH RIGHT ABOUT A DIFFERENT QUESTION, WHICH IS WHY THIS WAS NOT AN OBVIOUS BUG.** `CanUseTemporaryVisibilityModes()` asks *do temporary modes work here* - `isolate-elements`' own comment calls it *"Revit answering for itself"* and Revit answers yes for a sheet. `CanBePrinted` asks *is this drawable* and a sheet is the most printable thing in the model. **Neither asks whether the view can show the elements handed to it, and no API property does.** Each now tests `view is ViewSheet` FIRST. One line each, the fragment's own declared field, no new result and no contract change. **A READ-BACK WAS CONSIDERED AND DELIBERATELY NOT WRITTEN**, which is the part worth arguing with. [Row 42](../FRAGMENT-ISSUES.md#) fixed the same *counted the call, not the change* shape in `set-category-visibility` by reading `GetCategoryHidden` before and after, and that is the better repair where it can be had. It cannot be had here blind: `Element.IsHidden(view)` reports PERMANENT hiding only, so it cannot see the temporary path that `hide-elements` and `isolate-elements` both use by default, and which call does see it is a Revit fact this session cannot test. **[Row 31](../FRAGMENT-ISSUES.md#) and [row 18](../FRAGMENT-ISSUES.md#) are the precedent for not guessing** - the same `Regenerate` hypothesis fixed one and did nothing for the other, and the file's own ruling is that *"add a Regenerate is not a rule, it is a thing to test each time"*

**State.** **FIXED AND PROVED 2026-09-19 on `Project1 work_ajmal.al`, Revit 2024 session 20472 - both directions, for all three.** The measurements are in the left cell. Compiles on all eight releases. **THE THREE FRAGMENTS REMAIN `DRAFT` ON PURPOSE**: these were single `fragment` runs, not a recorded `validate` draft with a signature, so the DEFECT is closed and the PROMOTION is not. Whoever takes them needs a recorded proof per D-30, and the arrangement is now known to work. **WHAT WOULD PROVE IT, and it is the rig Ajmal already built**: sheet `A101` of `Project1 work_ajmal.al` exists for this. Hand each of the three the same 9 ducts from `1 - Mech` with `view = A101`. **THE THREE EXPECTED ANSWERS ARE NOT THE SAME, AND THAT IS THE SECOND REPAIR** - refusing the whole sheet was the first fix and Codex showed it was too broad, because a text note or a title block ON the sheet is genuinely hideable. So: `hide-elements` must return **`hidden 0`, `cannotHide 9`, `viewRefused false`** - the ELEMENTS were rejected, not the view; `isolate-elements` must return **`isolated 0`, `viewRefused false`**; `set-view-crop` alone must return **`viewRefused true`**, because a sheet has no crop region and there is nothing narrower to refuse. **A fourth case is worth one minute and is the one that proves the repair is not just the refusal**: hand `hide-elements` a text note that IS on `A101` - it must come back `hidden 1`, which the first version of this fix would have refused. **Then the same call against `1 - Mech` itself**, which must still work exactly as before - that leg is the one that matters, because a guard that refuses everything would pass the first test and break all three fragments. **THREE PROOFS ARE NOW STALE AND THIS SESSION COULD NOT DEMOTE THEM**: all three are PROVEN, [row 69](../FRAGMENT-ISSUES.md#)'s rule says a proof describing code that no longer runs is replaced rather than kept, and `fragment.yaml` was not this session's to edit. **They must be set to DRAFT before anyone trusts the PROVEN mark on them, and the repository REFUSES TO LOAD THEM UNTIL SOMEBODY DOES** - `heron_fragment.py` fingerprints the implementation against the proof and fails all three, which is [row 124](../FRAGMENT-ISSUES.md#) and is the gate working rather than a gap. **DEMOTED 2026-09-19, on the owner's explicit word after the rule reserving `fragment.yaml` was put to him rather than worked around.** All four this session edited are now DRAFT - `hide-elements`, `isolate-elements`, `set-view-crop` and `select-types` - with their proofs KEPT, and `check-signatures.py` now sees them and reports them STALE rather than UNUSED, which is the DRAFT-side half of the same fact the fingerprint enforces on the PROVEN side. **Four derived counts moved with them and six README claims had to follow**: 316 PROVEN to 312, 79 DRAFT to 83, 163 MODIFY PROVEN to 161, 137 READ PROVEN to 136 - `check-docs.py` section 7 caught every one, which is a stated count being a claim and a derived count being a fact

---

### Row 124

*Moved from the register on 2026-09-23.*

**Defect.** **I WROTE THAT A `PROVEN` FRAGMENT'S PROOF CAN GO STALE IN SILENCE. IT CANNOT, AND CI SAID SO TWENTY MINUTES LATER.** The claim was that [row 69](../FRAGMENT-ISSUES.md#)'s demotion rule has no gate: I changed three PROVEN fragments in [row 123](../FRAGMENT-ISSUES.md#), ran `check-docs.py`, saw it report exactly one stale signature - `set-wall-constraints`, stale before this session began - and concluded nothing was watching. **`check-signatures.py` really does only inspect DRAFT fragments** (`if (frag.status or "") != "DRAFT": continue`), and that part was correct. **THE CONCLUSION DRAWN FROM IT WAS NOT.** A DIFFERENT mechanism catches it, and catches it harder: `heron_fragment.py` holds a FINGERPRINT of the implementation the proof was taken against, and a PROVEN fragment whose bytes have moved does not load at all - *"status is PROVEN but the proof does not match this implementation - no fingerprint, or one taken against different bytes... Re-take it here (D-30), or set the status back to DRAFT"*. CI runs it through `check-routing.py`, which cannot build its store from a fragment that will not load, and **PR #191 went red on exactly the four this session edited**: FRG-SEL-027 `select-types`, FRG-VIEW-002 `isolate-elements`, FRG-VIEW-003 `hide-elements`, FRG-VIEW-014 `set-view-crop`. **`set-section-mark-visibility` was edited too and passed, because it is DRAFT and has no proof to contradict** - which is the fingerprint discriminating correctly, not luck. **THE LESSON IS THE ROW, AND IT IS THIS FILE'S OWN RECURRING ONE:** *one tool's silence is not the system's silence*. [Row 116](../FRAGMENT-ISSUES.md#) records a count read from one sweep and called a measurement; [row 8](../FRAGMENT-ISSUES.md#) records a renderer blamed for what an undeclared role did; [row 87](../FRAGMENT-ISSUES.md#) records a stale record read as a fresh result. I checked the tool whose docstring used the word STALE, got silence, and did not ask what else runs. **The register nearly gained a row asserting a missing safety net that has existed all along**

**State.** **NOT A DEFECT - WITHDRAWN 2026-09-19, the same day it was written, by the gate it said did not exist.** Kept rather than deleted, because a withdrawn row is the only durable record of how the wrong conclusion was reached, and this file's header says the shape returns. **The one true observation inside it survives and is worth nothing on its own**: `check-signatures.py` is DRAFT-only, which is right for the question it was built to answer - *thirteen signed fragments are sitting at DRAFT, why*. It is not the staleness gate and does not claim to be; the fingerprint is

---

### Row 125

*Moved from the register on 2026-09-23.*

**Defect.** **THE SECTION-MARK REPAIR CHANGED THE MECHANISM AND LEFT THE FRAGMENT'S OWN DESCRIPTION SAYING THE OPPOSITE - INCLUDING A SENTENCE THAT NOW ARGUES AGAINST THE FIX.** [Row 122](../FRAGMENT-ISSUES.md#) changed `set-section-mark-visibility` from reading `VIEWER_SHEET_NUMBER` to collecting `Viewport.ViewId`. **`fragment.yaml` was not this session's to edit and still describes the old route**, and its wording is not merely stale: it claims the sheet-number read *"gets the empty case right, which hunting through viewports does not"* - **the exact reverse of what was measured**, since the parameter is what got the empty case wrong (`---` is not empty) and the viewport is what gets it right. The compatibility note names the same parameter. Found by Codex on PR #191. **THIS IS NOT COSMETIC, AND THAT IS WHY IT IS A ROW**: `purpose` is what `heron_search` indexes, so the sentence a modeller's question is matched against still describes a mechanism the fragment no longer uses, and the next person reading it would be told the repair was the wrong idea

**State.** **FIXED 2026-09-20 - [ROW 122](../FRAGMENT-ISSUES.md#) HAS LANDED, SO THE CONDITION THIS CELL SET IS MET.** `purpose` now says **a view is on a sheet when a viewport points at it**, in the implementation's own words, and the sentence claiming the parameter *"gets the empty case right, which hunting through viewports does not"* - the one that argued against the measured fix - is gone, replaced by what was actually measured: the placeholder Revit writes for a view on no sheet is not empty, which is why every mark was skipped. The `compatibility-note` names `Viewport.ViewId` in place of the sheet-number parameter and records that no `ElementId.IntegerValue` is read, so the 2024 widening does not reach it. **The fragment is still DRAFT and no proof was at stake, exactly as this cell said** - and the ownership boundary it names was that session's instruction rather than a rule about the file. **THE ORIGINAL ACCOUNT FOLLOWS.** **OPEN - one file, not this session's, and it must move WITH [row 122](../FRAGMENT-ISSUES.md#) rather than after it.** `set-section-mark-visibility` is DRAFT, so no proof is at stake and nothing is blocked; the edit is the purpose prose and the compatibility note, and it should be made by whoever takes row 122's proof. **The general shape is [row 121](../FRAGMENT-ISSUES.md#)'s, arriving the same day in a second fragment**: a contract written in more than one place is corrected where the code is and left standing everywhere else, and it takes another reader to find the rest **RE-VERIFIED 2026-09-19 AND STILL LIVE, WITH THE LINE.** `brain/fragments/set-section-mark-visibility/fragment.yaml:27-29` still reads *"**WHETHER IT IS ON A SHEET IS ONE PARAMETER READ.** Revit fills the sheet number in on the view when it is placed. Hunting through viewports for the answer is slower and **gets the empty case wrong**"* - and hunting through viewports is what the code does now, precisely because the parameter is what got the empty case wrong. The fragment is still `heron-status: DRAFT`, so this row's *"no proof is at stake"* still holds and the edit is still free. Recorded rather than made: the file is another session's, and this row already says it must move WITH [row 122](../FRAGMENT-ISSUES.md#)

---
