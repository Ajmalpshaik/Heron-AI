# FRAGMENT-ISSUES archive — section 5b, rows 76 to 100

> **Finished rows, moved out of the live register.** These are rows from section 5b of
> [`FRAGMENT-ISSUES.md`](../FRAGMENT-ISSUES.md) — Heron's own defects found by reading the repository — whose state said they were
> finished when they were moved. Each still has its line in the register: the same number, a
> one-line title, the opening of its state, and a link to its full text here.
>
> **Nothing below was rewritten** except its links, which were re-pointed one folder deeper and
> each checked to reach the same file it reached before. Where a row disagrees with
> [DECISIONS.md](../DECISIONS.md), the [Golden Rules](../14-golden-rules.md) or the
> [Constitution](../../HERON_CONSTITUTION.md), **those win** — a row records what was true on
> the day it was written.
>
> **Nothing new is written here.** A new defect goes in the register, and a row here that turns
> out not to be finished is reopened **there**, on its own line.
>
> Rows 76 to 100 of section 5b belong in this file. A number in that band with no entry below
> was never moved — it is still in the register, in full. Written by
> [`tools/archive-fragment-issues.py`](../../tools/archive-fragment-issues.py).

---

### Row 5b-76

*Moved from the register on 2026-09-23.*

**Defect.** **THE PRODUCT MANIFEST TELLS ITS READER THE OWNER HAS NOT RULED ON SOMETHING HE RULED ON, AND THE RULING CAME WITH A CONSEQUENCE FOR EVERY BUTTON HERON SHIPS.** Found 2026-09-21 reading `platform/heron-products.json`. Its `readThisFirst` block says *"`requires` is EMPTY everywhere on purpose. Whether Heron Doc and Heron MEP work without the AI connector is **Q-PE-2 and the owner has not ruled**. An empty list is 'not decided'; a filled one would be a guess wearing a decision's clothes."* **HE RULED THAT SAME DAY.** `docs/work-notes/plans/plugin-extension/03-open-questions.md` carries Q-PE-2 **struck through** and *ANSWERED 2026-09-21*: ***"No. The AI connector is optional everywhere, and the tools work without it"***, in his own words - *"if they do not tick the AI connector, it will not be installed"*. **THE EMPTY LIST IS STILL RIGHT AND THAT IS WHY IT IS EASY TO MISS**: `requires: []` was correct as *undecided* and is correct as *decided*, so nothing looks wrong. What is wrong is the **reason** given for it, which now says the opposite of the truth - and a reader deciding whether to fill that field is told to wait for a ruling that has already happened. **AND THE RULING CARRIES A CONSEQUENCE THE MANIFEST DOES NOT MENTION**, recorded in the answer: ***"A tool's button cannot assume the bridge is there. Every button must work with the connector absent ... a button that errors without the bridge would make the connector required in practice while the installer says it is optional."*** That is a rule for every button in Heron Doc and Heron MEP, and it was written where the buttons' author would not look

**State.** **FIXED 2026-09-21.** The block says the list is empty because **nothing requires anything**, quotes the owner, and carries the consequence next to the field it follows from - a button must work with the connector absent and may offer the AI only as EXTRA. The old sentence is kept with its date, because *it was true when it was written* is the part worth keeping. **Nothing about the data changed**: `requires` is still `[]` in all five products, and `tools/check-products.py` still exits 0

---

### Row 5b-77

*Moved from the register on 2026-09-23.*

**Defect.** **THE ONE GATE THAT READS THE FILE A MODELLER'S INSTALLER READS HAS NEVER RUN ANYWHERE.** Found 2026-09-21 checking the previous row's fix. `platform/heron-products.json` ends its own instructions with ***"Checked by `tools/check-products.py`. Run it after every edit to this file."*** - and that instruction was addressed to whoever happened to remember. **MEASURED**: `check-products` appears in **no** job in `.github/workflows/gates.yml` - not among the ten commands of *"The gates that must pass"*, not among the four of *"The reports"* - and not in `tools/check-gaps.py`'s checker list either. **IT IS A GATE, NOT A REPORT**: it returns **1** on a finding, five times in its `main`. **AND WHAT IT GUARDS IS THE WORST KIND OF DEFECT THIS REPOSITORY HAS**, in its own words: the manifest is *"a file nothing compiles: a typo in it reaches a modeller's machine untouched by every other gate in this folder"*; a **duplicate `addInId`** is *"a LOAD FAILURE, not a warning, and nothing in source ever looks wrong"*; a **`partOf` typo** makes *"a tick disappear from the installer window with no error anywhere"*; and a **stale revit list** is the one that already happened elsewhere - *"the installer SILENTLY installed nothing at all on three releases while the document advertised them. **Nothing failed. It just skipped.**"* **THIS IS [ROW 170](../FRAGMENT-ISSUES.md#)'s SHAPE AGAIN** - 882 checks that compiled every day and ran on no day - and [row 5b-58](../FRAGMENT-ISSUES.md#)'s, where `check-gaps` ran three of four. A gate nobody runs is a gate that is right about a repository nobody is checking

**State.** **FIXED 2026-09-21 - it runs on every push.** Added to *"The gates that must pass"* with a comment naming the three mistakes only it can see, so the next person to wonder why it is there does not have to find this row. **It passes today, exit 0**, which is the honest state: nothing was broken, and nothing was watching. **AND THE COUNT MOVED AGAIN, ONE DAY AFTER [ROW 70](../FRAGMENT-ISSUES.md#) FIXED IT**: four documents said *nine decide the pull request* and it is now **ten**. `AGENTS.md`, `heron-ship/SKILL.md`, `.claude/skills/README.md`, `docs/PROJECT-MAP.md` and `check-gaps.py`'s comment all say ten and name it - because shipping a tenth gate while five files say nine is the defect row 70 exists to stop, committed by the fix for row 77. **AND A SIXTH PLACE CAUGHT ME THAT FIVE DID NOT**: `tests/test_tag.py` pins the ordered list `heron_tag.required_gates()` derives from `gates.yml` - the **release** gate list - and CI went red with *"it is the eleven CI runs"* against a derived twelve. I had swept for the word *nine* and this suite says *eleven*, because it counts the whole workflow rather than one job. **The suite was right to stop the push**: adding a step to that job makes the gate **release-blocking**, which for this one is correct and is now said in its comment - a duplicate `addInId` should stop a release. `AGENTS.md` also still said *one of the other **five***; it is six

---

### Row 5b-80

*Moved from the register on 2026-09-23.*

**Defect.** **THE INSTALLER TELLS A MODELLER TO INSTALL A REVIT THAT WOULD NOT HELP, AND THE SUITE THAT COVERED IT PINNED THAT EXACT SENTENCE.** Found 2026-09-21 reading `platform/Heron.Installer/InstallPlan.cs`, new in #233. For each ticked release `Build()` asked **is this Revit on the PC** before it asked **does the product even run on it** - so a release that is **both** absent and unsupported came back as `ReleaseNotInstalled`, with *“Revit 2026 is not installed on this PC, so 'Piece One' has nowhere to go. **Install Revit 2026 first**, or untick it.”* **The product does not support 2026.** Installing Revit 2026 would change nothing, and the person has been sent to do a two-hour install for an answer that was never going to arrive. `docs/14`'s wording rule is *say what to do next*; this says the wrong next thing, confidently. **AND THE SISTER FILE ALREADY HAD IT RIGHT**, in its own words: `InstallerScreen.WhyNotOffered` puts the state test first and says why - ***“STATE FIRST, because it is the stronger reason. Telling somebody which Revit releases a product that does not exist yet would support is an answer to a question they cannot have asked.”*** Two places deciding the same kind of thing and only one of them knew the rule. **WHAT MAKES IT A ROW RATHER THAN A NIT IS THE TEST.** `tests/Heron.Installer.TestHost` covered `ReleaseNotInstalled` with `piece-one` - which supports 2024 and 2025 - asked for **2026** on a PC holding 2024 and 2025. That is precisely the both-wrong case, and the suite asserted the wrong reason and passed. **A guard whose test demonstrates the defect is worse than no test**, because the next reader takes the green as an answer

**State.** **FIXED 2026-09-21 - the support question is asked first, and the case that used to be wrong is now three checks.** Order swapped, with the reason written beside it and `InstallerScreen`'s sentence quoted, so nobody restores the old order tidying up. The `ReleaseNotInstalled` enum comment now says when it fires - *the release is absent AND the product supports it, so installing that Revit really would make this work* - because a name that describes only half its condition is how the two got confused. **The suite was rebuilt rather than patched**: one plan, `piece-one` ticked for 2024, 2025 and 2026 on a PC holding 2024 and 2027, so **one of each case falls out of a single call** - 2025 absent but supported, 2026 neither, 2027 present and unticked, 2024 the one step that survives. The 2026 row is asserted three ways: the reason, that the message names what the product **does** support, and that the words *Install Revit 2026 first* **do not appear at all**. **Shown to fail**: with the order put back, those three go red and the rest stay green

---

### Row 5b-81

*Moved from the register on 2026-09-23.*

**Defect.** **A TYPED COUNT IN A README THAT MATCHES NOTHING MEASURABLE, AND A RANGE MY OWN CHANGE MADE STALE AN HOUR AFTER IT WAS WRITTEN.** Found 2026-09-21 finishing `platform/`. `platform/README.md`'s installer section says ***“Everything it DECIDES is tested; nothing it DOES has run. **38 checks** pass against a fake Revit and a fake deployer.”*** **MEASURED, AND NOTHING IS 38.** `tests/Heron.Installer.TestHost` - which is the fake Revit and the fake deployer that sentence names - prints **84**. `tests/test_installer_window.py` prints 43 and `tests/test_installer_engine.py` prints 86. #233's own commit message says **79** for a third thing. No section of the test host sums to 38 either; the closest reading is the engine half before [row 5b-80](../FRAGMENT-ISSUES.md#) added four, which is not what the sentence says. **THIS IS THE FIRST RULE IN `AGENTS.md`, IN THE FILE THAT DOCUMENTS THE MOST CAREFUL CODE IN THE REPOSITORY**: *“Never type a number a command can derive. Say where it comes from instead. Three READMEs in this repository once carried a fragment count wrong by more than a hundred, each with a sentence beside it admitting the number goes stale.”* **No gate catches this one** - `check-docs.py` derives fragment counts, and a count of checks is not one of the shapes it knows. **AND THE SECOND HALF IS MINE.** Four lines below, the same section said the window is owed *`AB1` to `AB7`* in NEEDS-CHECKING. **Row 5b-78 added `AB8` earlier the same day**, so that range was wrong within the hour - by my own hand, in a session whose other three rows are all about a sentence that was true when it was written

**State.** **FIXED 2026-09-21 - one number replaced by the command that derives it, one range replaced by the name of the group.** The checks line now carries `dotnet run --project tests/Heron.Installer.TestHost -c Release` piped into `grep -c '^  ok '` and records that it said 38 and nothing matched. The Windows line names **Group `AB`** rather than a range, and says why: a range has to be re-counted every time a row is added and a group name never does. **THE HABIT WORTH KEEPING is the one that found it**: the only reason this surfaced is that finishing the `platform/` folder meant re-reading a file my own change had touched the edges of. [Row 5b-70](../FRAGMENT-ISSUES.md#) is the same lesson at five files; this is it at four lines. **AND THIS ROW TURNED THE PULL REQUEST RED ON ITS WAY IN, TWICE.** The derive command was written with a real pipe character in it, which ends a markdown cell, and `tests/test_open_defects.py` said so - *no row holds a literal pipe (row 81 has 4 cells)*. I had run `check-gaps` **before** writing rows 81 and 82 and not after, so the one suite that reads this file never saw them. Then the sentence written to record that went red the same way, because it quoted the character it was about. **Both times the suite caught it and I did not**, and the fix is to name the character in words rather than type it: an escaped one inside a code span renders as a backslash

---

### Row 5b-82

*Moved from the register on 2026-09-23.*

**Defect.** **THE SEAM BETWEEN THE MCP SIDE AND THE BRAIN SAYS THREE TOOLS STAND ON IT. TEN DO.** Found 2026-09-21 reading the last file in `mcp/`. `mcp/README.md` describes `server/heron_brain.py` as ***“The one place this side reaches `brain/`”*** - which is still true and is the valuable part - and then says ***“Three tools stand on it: what Heron knows how to do, who provides a capability, and which capability a sentence needs.”*** **MEASURED with an AST walk over the 34 `@server.tool()` functions in `heron_mcp_server.py`: ten of them call `brain.*`** - `heron_capabilities`, `heron_resolve`, `heron_lookup`, `heron_context`, `heron_check`, `heron_standards`, `heron_research`, `heron_research_check`, `heron_gaps` and `heron_compatibility`. The three named are `catalogue`, `resolve` and `lookup`; the other seven arrived afterwards and nobody came back to the sentence. **WHY IT MATTERS MORE THAN A WRONG NUMBER USUALLY WOULD.** That paragraph is the argument for the seam existing at all - it says the rule was previously kept *“by having no route at all: eight brain modules, seven fragments and ten skills, imported by nothing but their own tests”* - and the size of what now depends on it **is** the argument. Three tools reads like a narrow door somebody could still reason about; ten is a third of the whole server. A reader deciding whether the seam is holding is given a figure that makes the question look smaller than it is. **AND IT IS THE SESSION'S THREAD AGAIN** - [rows 5b-78 to 5b-81](../FRAGMENT-ISSUES.md#) - *a sentence that was checked the day it was written and never again*. This one is the fourth in a row, and the third where the honest repair is to delete the number rather than correct it

**State.** **FIXED 2026-09-21 by deleting the figure and keeping the history.** The row now says it *started* with those three, that there are more, and how to count them - `grep -c 'brain\.' server/heron_mcp_server.py`, with the tools being the `@server.tool()` functions around those calls. **The old figure is kept with its date**, because *it was true when it was written* is the part worth keeping and is what tells the next reader the file grew rather than that somebody miscounted. **The seam claim itself was checked and stands**: `heron_diagnose.py` also imports `heron_brain`, which is not a second route - it goes THROUGH the seam, which is the seam working

---

### Row 5b-84

*Moved from the register on 2026-09-23.*

**Defect.** **TWO PUBLIC METHODS ON ONE CLASS DISAGREE ABOUT THE SAME INPUT, AND THE ONE THAT DISAGREES THROWS A BARE `KeyError` OUT OF A MODULE WHOSE WHOLE POINT IS THAT A REFUSAL IS DATA.** Found 2026-09-21 reading `brain/heron_router.py`, the Model Router - the first of the 53 live-path brain modules to be read. `route()` upper-cases the intent it is handed; `candidates()`, public and beside it, did not. **Measured, on one router with one adapter registered:** `route("classify")` answers `claude-code`; `candidates("classify")` raises **`KeyError: 'classify'`**. `route("nonsense")` returns `{refused: UNKNOWN_INTENT, why: ...}` - a sentence a modeller can read; `candidates("nonsense")` raises **`KeyError: 'nonsense'`**, with no message at all. **THE MODULE'S OWN DOCSTRING SAYS WHY THAT IS WRONG**, on the very next method: ***“A refusal is data rather than an exception because the caller's next move is to tell a modeller what is missing, and **an exception two layers down arrives there as a stack trace**.”*** `candidates()` is the method a caller reaches for to SHOW the options, and it is the one that arrives as a stack trace. **NO GATE COVERS THIS SHAPE.** `tools/check-narrow-errors.py` is about D-52's PLAUSIBLE ZERO - an error swallowed and returned as an empty list. This is its mirror: an error escaping where the module promised data, from a file that has the discipline everywhere else. **AND THE FIRST VERSION OF THIS ROW WAS WRONG ABOUT THE SECOND HALF, WHICH IS WHY IT READS AS IT DOES NOW.** It said the bare `KeyError` was simply unhandled. It is not: `brain/heron_availability.py`'s `resolve()` catches it **by name** and turns it into the router's own refusal, with a comment saying so - *“an unknown intent is its refusal to word, not this module's”*. So the `KeyError` was **load-bearing**, and changing it without changing that caller turned `tests/test_availability.py` red - green before the edit, two checks failing after it. **I had not looked for a caller before writing the row.** The case mismatch above is untouched by that and is the harder half of the finding; the `KeyError` half is a poor contract rather than an oversight, and the row says which is which now instead of rounding both up. **AND IT IS IN THE MODULE THAT ENFORCES CONFIDENTIALITY.** `candidates()` is where `if confidential and kind != LOCAL: continue` lives - the one rule this file says it exists to protect. Checked and it holds, in both directions: confidential only ever REMOVES candidates, there is no value of the flag that adds one, and a confidential request with no local adapter is refused by name rather than falling back to the host

**State.** **FIXED 2026-09-21, and the fix answers the mistake one way instead of two.** `candidates()` now upper-cases the intent exactly as `route()` does, and an undeclared one raises **`ValueError` carrying the whole sentence** - which is what `register()` already does for the same mistake, so the module has one answer rather than three. **IT DOES NOT RETURN `[]`, AND THAT IS THE DECISION WORTH RECORDING.** An empty list would read as *no adapter offers this* when the truth is *that is not an intent*, and keeping those two apart is what this module is for. A list has nowhere to put a reason, so the honest options were a raise with a message or a different return type, and a raise is the smaller change. The message also names `route()` as the method that returns the same refusal as data. **THE ONE CALLER MOVED WITH IT, AND NOT ONE MORE.** `brain/heron_availability.py` line 121 catches `ValueError` now, with the whole story in a comment above it - it is the only use of `candidates()` outside the router itself, measured across `brain`, `mcp`, `tools` and `tests`. Its second call, further down, needs no guard: it runs only after the first has already established the intent is real. **A RED SUITE IS A CLAIM ABOUT THE CODE AND WAS TREATED AS ONE** - `test_availability.py` was not edited, the code was changed until it passed. **Shown to fail**: `tests/test_router.py` section 11 has four checks, and **three go red** against the file as it stood - the fourth was already true and is kept so the pair is asserted together rather than one side of it. **Read word by word and clean otherwise**, with two claims verified rather than taken: no model id appears anywhere in the file, and the confidentiality rule narrows in both directions as its docstring says

---

### Row 5b-87

*Moved from the register on 2026-09-23.*

**Defect.** **THE TABLE THAT DEFINES A BREAKING CONTRACT CHANGE LISTS SEVEN RULES. THE CODE BENEATH IT ENFORCES EIGHT, AND THE MISSING ONE IS THE DIRECTION THAT BREAKS CALLERS.** Found 2026-09-21 reading `brain/heron_contract.py`, the fifth live-path brain module. Its docstring opens *“A change is BREAKING when it can make a caller that was correct yesterday wrong today”* and lists seven: a removed input, an added required input, an optional input made required, a changed type, a removed output, a removed failure state, an added failure state. **`compare()` enforces all seven and a shortened timeout as well**, with its own argument beside it: *“a call that legitimately finished under the old promise can time out under the new one”*. **MEASURED**: 60s → 30s comes back **BREAKING**; 60s → 120s comes back **COMPATIBLE**. The asymmetry is right - raising a promise costs nobody anything, so only the downward direction counts - and it is entirely absent from the table a reader reads. **WHY IT IS WORTH A ROW RATHER THAN A SHRUG.** This is the module all 249 other agents declare themselves to, the table is the definition of the word BREAKING for this repository, and somebody lowering a timeout on the strength of seven rules would expect `COMPATIBLE` and raise a MINOR version. The gate would then refuse the change they had already planned around - or, worse, they would not run it. **AND IT IS THE SESSION'S OWN THREAD AGAIN** - [rows 5b-81, 5b-82 and 5b-85](../FRAGMENT-ISSUES.md#) - *a sentence that was true when it was written and the code moved past it*. Four of today's rows are that shape, in four different files

**State.** **FIXED 2026-09-21 - the eighth row is in the table, and the table is now held against the code.** The entry reads *the timeout fell — a call that finished inside the old promise can fail under the new one*, with a paragraph recording that it was missing while `compare()` enforced it, and the measurement both ways. **THE TEST IS THE PART THAT LASTS.** `tests/test_contract.py` asserts the behaviour **and** that the module's own `__doc__` carries the rule - **against the docstring itself, not a copy of it** - so the table cannot drift from the code again without going red. Copying the table into a test would have created the second home this module's own docstring refuses for `tier` and `risk`. **Shown to fail**: run against the docstring as it stood, the documentation check goes red and the two behaviour checks stay green, which is how the trio proves the code was already right and only the sentence was wrong

---

### Row 5b-88

*Moved from the register on 2026-09-23.*

**Defect.** **THE AGENT WHOSE WHOLE ARGUMENT IS COST READS EVERY TEXT FILE IN FULL TO ASK A QUESTION THE FIRST CHARACTER ANSWERS.** Found 2026-09-21 reading `brain/heron_classify.py`, the sixth live-path brain module. `_shape()` reads `SNIFF = 8192` bytes to decide text or binary, under a comment saying the bound is there because it is *“small enough that a folder of large binaries costs nothing”* - and then, for every file that came back text, calls `json.load` on the **whole file**. `json.load` reads all of it before it looks at the first character. **SO THE BOUND PROTECTED BINARIES AND NOT TEXT, WHICH IS THE HALF THIS AGENT ACTUALLY WALKS.** **MEASURED, the pathological case**: 120 MB of plain prose beginning with the letter `t` was read in full and held in memory - a peak of **240 MB**, 0.12s warm - to be rejected on its first character. The sniff above it costs 8 KB. **MEASURED, this repository**: 2,127 of 2,148 files reach the JSON check; the old path reads **30.27 MB** and opens **2,127** files a second time. **AND COST IS NOT AN INCIDENTAL PROPERTY OF THIS FILE - IT IS THE FILE'S ARGUMENT.** Its own docstring is headed *“THE COST PROBLEM IS THE DESIGN PROBLEM, AND IT IS WRITTEN DOWN”*, quotes [19 §96](../19-context-and-cost.md) - *“Batch classification of 20,000 fragments should not run on a frontier model”* - and groups files so a repository of sixty-one thousand becomes a handful of questions. It counts the questions so nobody has to guess what a run will cost, and then reads the corpus twice without saying so. **AND THE INPUT IS SOMEBODY ELSE'S REPOSITORY**, which is the one place a 120 MB log or SQL dump is ordinary rather than pathological

**State.** **FIXED 2026-09-21 - the answer is identical and the reading is bounded, and the test proves the first half rather than assuming it.** **THE HEAD TEST IS SOUND, NOT A HEURISTIC**, which is the only reason it may decide anything: RFC 8259 says a JSON text is one value, and every value begins with `{`, `[`, a quote, `-`, a digit, or the exact words `true`, `false`, `null`. Anything else cannot parse, whatever the other 120 MB hold. So: a file that fits inside the sniff is **already read** and is parsed from the bytes in hand, with no second open; a larger one whose head cannot begin a JSON value is rejected on that head; **only a large file that really does start like JSON is read in full**, and that is the one case where reading it is the only way to know. **THE SAME REPOSITORY NOW READS 11.50 MB AND OPENS 7 FILES TWICE**, down from 30.27 MB and 2,127. **EQUIVALENCE WAS PROVED BEFORE THE COST WAS**: 22 cases - small and large objects, arrays, bare scalars, all three bare literals, the near-misses `truthy` and `nullify` and `for the record`, broken JSON, empty, whitespace, markup, prose, a leading zero - run through both paths, and **cases where the answer changed: 0**. **THE TEST COUNTS OPENS RATHER THAN SECONDS.** A timing assertion for this would be a flaky test on somebody else's machine; `tests/test_classify.py` §8 swaps a counting `io` into the module and asserts how many times each fixture is opened, end to end through `_shape`, alongside the answer and the same answer read the old way. **Shown to fail: 20 checks go red** against the module as it stood - five files opened twice that now cost one, and the fifteen that ask whether the head test covers every character RFC 8259 allows. **AND THE FIRST DRAFT OF THAT SECTION REPEATED [ROW 5b-85](../FRAGMENT-ISSUES.md#)'S MISTAKE THREE ROWS LATER.** It read `CLS.JSON_STARTS` directly, so against the old module it raised `AttributeError` and the remaining checks never ran - **one traceback where there were twenty failures to report**. `getattr` with a default now, and the reason is written beside it, because knowing the lesson and reaching for it are evidently two different things

---

### Row 5b-89

*Moved from the register on 2026-09-23.*

**Defect.** **THE ONE CHECK THAT STOPS A DOCUMENT ASKING FOR AN ADMIN ACTION IS SHARED BY NINE SURFACES, AND IT TOLD ALL NINE THE SAME WRONG THING.** Found 2026-09-21 reading `brain/heron_flags.py`, the seventh live-path brain module. `origin_allowed()` is Golden Rule 19's gate - *“permission comes from the user, through Heron's own UI, per action”* - and it fails closed correctly on everything that is not the user. **MEASURED with an AST walk over `brain` and `mcp`: it is called in NINE places.** `heron_configuration`, `heron_update`, `heron_dependencies`, `heron_brain_init`, `heron_rag_init`, `heron_tooling`, `heron_safemode` and `mcp/server/heron_register` all borrow it rather than keeping a second copy - which is right, and `tests/test_safemode.py` §6 says so in as many words: *“it is the flag agent's own check, not a second copy that would drift from it”*. **WHAT CAME BACK WITH IT WAS A SENTENCE ABOUT FLIPPING A FLAG.** All three of its refusal branches named a flag: *“a flag flip is the shortest path from a sentence somebody else wrote to a write in a live model”*, *“the one rule that stops a document flipping a flag”*, and *“Configuration is a security boundary”* for surfaces that are not editing configuration. **RUN END TO END**: entering Safe Mode from a document is refused with *“...and a flag flip is the shortest path...”*. Nothing was flipping a flag; Safe Mode undoes work and switches components off. **ALL EIGHT BORROWERS PASS THAT SENTENCE STRAIGHT THROUGH AS THEIR `why`** - each writes a correct `proposal` naming its own act and then hands the reader a `why` about somebody else's. One of the nine is `mcp/server/heron_register.py`, so the wrong sentence reaches a modeller's conversation. **AND THE SUITES CHECKED THE HALF THAT WAS RIGHT.** `tests/test_flags.py` §3 asserts the refusal CODE and the words *“data, never instruction”* - the ORIGIN half, correct throughout. `tests/test_safemode.py` §6 asserts the code alone. **Nothing anywhere read the rest of the sentence**, which is [row 5b-80](../FRAGMENT-ISSUES.md#) and [row 5b-85](../FRAGMENT-ISSUES.md#) again: a suite that pins the part that never broke

**State.** **FIXED 2026-09-21 - the gate now takes the act it is being asked about, and nine call sites say what theirs is.** `origin_allowed(origin, action=None)`, with `action` a short noun phrase - *a flag flip*, *Safe Mode*, *installing a package*, *registering with a host*, *an edit to configuration*, *an update*, *setting up the brain's store*, *choosing a retrieval backend*, *detecting an external tool*. **THE DEFAULT IS VAGUE RATHER THAN WRONG**: a caller that says nothing gets `an ADMIN action`, because naming the wrong act is worse than naming none - it sends the reader off to check something they were never doing. **The behaviour is untouched**: the same origins are allowed and refused, and the user still goes through. **THE DOCSTRING NOW SAYS IT IS A SHARED GATE**, with a `grep` rather than a count, because a typed nine is wrong the first time a surface is added. **THE CHECK THAT STOPS IT COMING BACK IS DERIVED.** `tests/test_flags.py` §3b walks `brain` and `mcp` with `ast`, finds every call to `origin_allowed`, and asserts each one names its act - so a new ADMIN surface that forgets goes red on the day it is added, and the count is measured rather than typed. §3a asserts the sentence carries the act for all four kinds of refused origin, and `tests/test_safemode.py` §6 now reads the words end to end. **Shown to fail: 13 checks go red** across the two suites. **AND THE FIRST DRAFT CRASHED INSTEAD OF FAILING - THE THIRD TIME TODAY.** Calling the two-argument form against the old module raised `TypeError`, so sections 4 to 8 never ran at all. It now asks `__code__.co_argcount` first and skips the dependent checks with one clean failure. [Row 5b-85](../FRAGMENT-ISSUES.md#), [row 5b-88](../FRAGMENT-ISSUES.md#), this one: **a check written against a name or a signature the module may not have must ask before it calls**, and three rows in one day say that is a habit and not an accident

---

### Row 5b-90

*Moved from the register on 2026-09-23.*

**Defect.** **THE LEDGER SAID 142 FILES HAD BEEN READ. 113 HAD.** Found 2026-09-21 while marking eight modules for [row 5b-89](../FRAGMENT-ISSUES.md#) that had only been read at one function. **MEASURED over `docs/REVIEW-LEDGER.tsv`: thirty of the 142 files counted as read carried notes opening *“PARTIAL READ and said so”*** - honest prose, written by sessions that did the right thing, **in a column nothing counts**. `AGENTS.md` sends people to `python tools/review-ledger.py` for how much of the repository has been read, and section 5b of this file closes with *“A short table means nothing without the second number: it cannot tell you whether little was found or little was looked at”* - **that second number is this one, and it was soft by 21%**. **AND A COUNT DERIVED FROM PROSE IS GUESSED, NOT DERIVED**, which is the same failure `tools/open-defects.py` names one layer down about prose totals. **THE TWO FACTS CANNOT SHARE THE VERDICT COLUMN**: **nineteen of the thirty also carry a defect row**, so `partial` as a third verdict would have dropped nineteen real findings out of *read, issue found* to fix a count. How much was read and what was found are separate questions about the same file

**State.** **FIXED 2026-09-21 - a seventh column, `scope`, holding `full` or `part`.** Orthogonal to the verdict, so a file can be part-read AND carry a 5b row, which nineteen of them are. `--part` on `--mark`; the file keeps its verdict, stays in the `--next` queue tagged `PART`, and the summary now separates **opened at all** from **READ WORD BY WORD**, which is the line that says *the number AGENTS.md asks for*. **It reads 113 of 1180, not 142.** **`--part` REFUSES WITHOUT A `--note`** saying which part, for the reason `issue` refuses without a row: a part-read mark nobody can resume is **worse** than no mark, because it takes the file out of the never-opened queue and puts nothing in its place. **NOTHING WAS REWRITTEN.** The ledger is append-only and `tests/test_review_ledger.py` §3 holds that, so each of the 29 still in scope got a NEW row naming whose read it was - re-classifying somebody's mark is not reading the file again and must not read as if it were - and `--history` keeps the chain. **A row written before the column has six cells and means `full`**, which is what it claimed at the time; a scope nobody defined is **malformed rather than assumed safe**, the same rule the verdict column already had after a Codex review on PR 219. **Shown to fail: 6 checks** in a new §7. **Two of its nine pass against the old tool for the wrong reason** - a seven-cell row is refused there as the wrong cell count, which happens to give the right answer - and that is said here rather than counted as proof. **AND THE FIRST DRAFT CRASHED INSTEAD OF FAILING FOR THE FOURTH TIME IN ONE DAY.** It named `RL.PART`, which the old tool has not got. [Rows 5b-85](../FRAGMENT-ISSUES.md#), [5b-88](../FRAGMENT-ISSUES.md#), [5b-89](../FRAGMENT-ISSUES.md#) and this one are one mistake made four times, twice after writing the lesson down - **so this time it went into a house rule rather than another paragraph**: [`.claude/skills/heron-ship`](../../.claude/skills/heron-ship/SKILL.md) §2a now carries *a fix is not proved until its test has been seen to FAIL*, the `getattr` and `co_argcount` forms that ask before they call, and the table of all four

---

### Row 5b-91

*Moved from the register on 2026-09-23.*

**Defect.** **THE BRAIN'S HALF OF THE AUDIT TRAIL HAD NO SUITE, AND THE ONE PROMISE IT MAKES IN CAPITALS IS BROKEN THREE WAYS.** Found 2026-09-21 reading `brain/heron_audit.py`, the eighth live-path brain module. **NOTHING UNDER `tests/` IMPORTED IT** - measured with a grep over the whole suite directory - and the BUILD ORDER check in `tools/check-gaps.py` covers the **fourteen steps docs/27 describes**, while this file carries `Heron-Step: 17`, so no gate noticed. **THIS IS NOT AN INCIDENTAL MODULE**: it is Golden Rule 14's requirement for the half of Heron that never reaches Revit, it is D-62's deliverable, and every line it writes goes into a file that is **append-only and never pruned**. Its own docstring argues at length about exactly that - about the day `ms` went in quoted and a reader compared *“9”* against *“6620”* as text, about never writing the user's sentence, about the refusal codes that stop a correct refusal reading as a fault. **None of that argument was held by anything.** **AND THE PROMISE IN CAPITALS IS `record()`'s: *NEVER FATAL, and never silent about being non-fatal*, with the reasoning beside it - *a trail that can take down the request it is recording is worse than no trail*.** Its `numbers` loop does `int(value)` **OUTSIDE the `try`**. **MEASURED**: `ms="fast"` raises **ValueError**, `float("nan")` raises **ValueError**, `float("inf")` raises **OverflowError** - which is not even in that except list - and a list raises **TypeError**. The `ValueError` sitting in the except clause is the tell: **the author anticipated the conversion failing and guarded the wrong statement**, because `json.dumps` of a dict of strings and ints does not raise. **NOT REACHABLE FROM ANY CALLER AS THINGS STAND** - all thirteen pass a `len()` or a `rowcount` - **and that is the dangerous half rather than the reassuring one**: the early `return False` when there is nowhere to write means this code only ever runs where the trail really writes, which is somebody's machine and never this container

**State.** **FIXED 2026-09-21, and the module has a suite for the first time.** The conversion is guarded per key. **A BAD NUMBER COSTS THAT ONE FIELD, NEVER THE LINE AND NEVER THE REQUEST** - and it is **not** written as a string instead, because *NUMBERS ARE WRITTEN AS NUMBERS* is this file's founding lesson and the log is never pruned, so a quoted number is permanent. **Golden Rule 14 does not allow a silent discard**, so a `dropped` field names the key on the line itself. **`tests/test_audit.py` IS THE LARGER HALF OF THIS ROW.** Eight sections, and they hold the arguments the docstring makes rather than the lines it is made of: that nothing escapes; that `ms` reads back as an **int** and `ok` as a **boolean**; that none of the four writers takes a parameter a sentence could arrive in; that a refusal keeps its reason AND its code; that `excluded` records what the version wall turned down (**D-52**); and that no brain line invents a `workflow` id. **THE CENTRAL ONE RUNS END TO END THROUGH THE READER.** `heron_gaps.analyse()` buckets a failed row by its `error` and drops an unknown one into `unclassified` - **its own founding mistake was that its loudest error was the executor behaving correctly** - so §4 writes three real refusals and asserts they come back as **three CORRECT REFUSALS, none unclassified, none a defect**, rather than asserting the strings and hoping. §6 proves the answer to **Q-44** the same way: an add-in file and a brain file in one directory come back as **one list sorted by `at`**, with a truncated line costing one entry and not the report. **Shown to fail: 9 checks** against the module as it stood, **and the suite still ran to the end** - each call sits in its own `try` so an escape is reported as the failure it is. **Two of them were vacuous in the first draft** - `all()` over an empty list is True, so they passed while saying nothing; both now require the list to be non-empty first, which is [row 5b-79](../FRAGMENT-ISSUES.md#)'s false green in a different shape

---

### Row 5b-93

*Moved from the register on 2026-09-23.*

**Defect.** **THE REPORT BUILT NOT TO LIE ABOUT THE TRAIL TELLS A MODELLER *“HERON HAS NO RECORD OF DOING ANYTHING YET”* WHEN THE TRAIL IS FULL.** Found 2026-09-21 reading `brain/heron_gaps.py`, the ninth live-path brain module. `since(entries, days)` takes a window with **no floor**. **MEASURED on a trail of three entries**: `--days 7` gives 1, `--days 30` gives 3, `--days 0` and `None` give 3 - and **`--days -7` gives 0**. `report()` then prints *“The audit trail is empty. Nothing has been asked of Heron yet”*, and the MCP tool prints *“Heron has no record of doing anything yet... it means nobody has asked it for anything”*. **BOTH ARE FALSE CLAIMS ABOUT SOMEBODY'S OWN HISTORY**, and the second is the one a modeller reads: `heron_gaps(days: int = 0)` in `heron_mcp_server.py` declares an `int` with no lower bound and `heron_brain.gaps()` passes it straight to `since()`. **THIS IS `tests/test_brain_reachable.py` §7's OWN ARGUMENT, ONE TOOL ALONG** - *“'Heron knows how to do nothing' and 'Heron cannot read what it knows' send a user in opposite directions”* - and the module's own `duration()` makes the same argument about timings: it returns `None` rather than 0 because *“averaging the two together is how a timing report starts lying”*. **AND THE SUITE PINNED THE HALF THAT WAS RIGHT.** `tests/test_gaps.py` §7 is headed *“An empty trail says nothing rather than 'no gaps'”* and empties the directory to prove it. The message is correct for an empty trail; nothing ever asked whether a FULL one could reach it. **THE SECOND HALF IS IN `_stat()`**, four lines away: it takes `ordered[len(ordered) // 2]`, which for an even count is the upper of the middle pair - **so the median of two runs is the slower of them**. **MEASURED**: `_stat([10, 100])` returns **`(100, 100)`**, and the report prints *median 100 ms, worst 100 ms* for a fragment that ran at 10 and 100. **Two runs is the ordinary case for most of the library**, which is where a reader has the least context to notice

**State.** **FIXED 2026-09-21 - the window is refused where the semantics live, and the median is between the two.** `since()` **raises `ValueError` for a window below 1**, naming what is wrong and what it costs; `None` and `0` still mean the whole trail and are documented as doing so. **It is refused at `since()` and not at each caller** because there are two callers and the rule belongs to neither - the same reasoning as [row 5b-84](../FRAGMENT-ISSUES.md#)'s `candidates()`. **The command line says it in its own words and exits 2**, the repository's *could not* code. **`heron_brain.gaps()` TURNS IT INTO DATA** - `{"refused": "NOT_A_WINDOW", "why": ...}` - and the MCP tool returns that sentence, so a modeller gets the reason rather than an exception or a wrong claim. **THE REFUSAL DOES NOT COME BACK LOOKING LIKE AN ANSWER**, and a check says so: no `found` key, because *an answer with nothing in it* is the exact shape that read as *never been asked*. **`_stat()` averages the middle pair for an even count** and is unchanged for an odd one; `_stat([10, 100])` is now `(55, 100)`. **Shown to fail: 7 checks** across the two suites - 4 in `test_gaps.py` §9 and §10, 3 in `test_brain_reachable.py` §6b - **and the three checks that a real window still answers stay green in both**, which is how the pair proves the change was narrow. **The new sections ask before they call** - a `try` round the refusal rather than naming a symbol the old module has not got - so nothing crashed this time, which is [`heron-ship`](../../.claude/skills/heron-ship/SKILL.md) §2a working the first time it was needed

---

### Row 5b-94

*Moved from the register on 2026-09-23.*

**Defect.** **THE FUNCTION THAT TURNS A SILENCE INTO A FINDING KEPT ONLY THE LAST CALLER'S REASON, AND THE ONE THAT SURVIVED WAS THE LEAST USEFUL OF THEM.** Found 2026-09-21 finishing `brain/heron_capability.py` - the [ledger](../REVIEW-LEDGER.tsv) said `rebuild`, the gap report and `main` were still owed from an earlier partial read, which is [row 5b-90](../FRAGMENT-ISSUES.md#)'s new column doing its job. `want(store, name, why)` did `INSERT ... ON CONFLICT(name) DO UPDATE SET why = ?`, so **the last writer won and every earlier reason was gone**. **MEASURED**: three different things wanting `TRACE_DUCT_SYSTEM` - skill `trace-system`, skill `size-check`, and a user asking by name - left **ONE** row and **ONE** sentence. **AND THE SENTENCE THAT SURVIVES IN PRODUCTION IS THE GENERIC ONE.** `mcp/server/heron_brain.resolve()` writes *“asked for by name and no fragment provides it”* every time somebody asks for a capability nobody provides, so **one person asking erases which skills were blocked** - and `tests/test_skills.py` loops over every skill calling `want()`, so two skills needing one capability already lose one of the two. **THE MORE PEOPLE ASK, THE LESS THE GAP REPORT CAN SAY ABOUT WHO NEEDS IT**, which is backwards for a function whose own docstring says it *“turns 'we have no fragment for that' from a silence into a finding”*. **Golden Rule 14**: nothing is discarded silently, and this discarded the evidence that tells somebody whether a gap is worth closing

**State.** **FIXED 2026-09-21 - every distinct reason is kept, and a repeat is not a new one.** The table still holds **one row per capability**, because docs/18 says the gap IS the name and that is the finding; the reasons share the cell rather than the row, joined by `WHY_JOIN`. `wanted_by(why)` reads them back as a list, in the order they arrived. **DE-DUPLICATION IS NOT COSMETIC HERE**: the common caller is a loop over every skill, which would otherwise write the same sentence into the cell on every run and grow it without bound. The distinct reasons are bounded by the number of distinct callers, which is a small real number. **AN EMPTY `why` NO LONGER BLANKS SOMEBODY ELSE'S** - it records *wanted, with no reason recorded*, because the NAME is still the finding and a caller with nothing to say must not erase a caller that had something. **Shown to fail: 3 checks** in a new §3b of `tests/test_capability.py`, and the fourth - that a repeated reason does not grow the cell - stays green in both, which is how the set proves the change was narrow. **The section asked before it called** (`getattr` for `wanted_by`), so it reported three failures rather than a traceback: [`heron-ship`](../../.claude/skills/heron-ship/SKILL.md) §2a for the second row running

---

### Row 5b-96

*Moved from the register on 2026-09-23.*

**Defect.** **A REAL REVIT DISPROVED A SENTENCE I WROTE, AND THE RULING IT TRIGGERED LEFT ONE LINE LOAD-BEARING WITH NOTHING HOLDING IT.** Found 2026-09-21 reading what `AA9`'s run on the owner's PC brought back (#235, merged into `main` while this branch was open). **TWO HALVES, AND THE FIRST IS MINE.** [Row 5b-79](../FRAGMENT-ISSUES.md#) moved the backup path to `install-backup\<version>\<folder>` and I wrote beside it *“No old backup is deleted or moved - reaching into a path this script no longer owns to tidy it would be a worse risk than the one it fixes”*. **ON DISK, ON A REAL MACHINE, THAT IS FALSE FOR `heron-bridge`** - its `productFolder` is `Heron`, so its new `backupDir` is `install-backup\<version>\Heron`, which is **exactly where the old layout put the backed-up folder**. The first new deploy writes over it and leaves the old `Heron.addin` and `replaced.json` orphaned one level up. **It is true for every other product**, which is why reasoning about it missed it and a machine did not. Nothing `-Rollback` could have used was lost - that backup was already unreachable, which the same paragraph says - but the sentence was a claim and the claim was wrong. **AND THE SECOND HALF IS WHAT `AA9` ACTUALLY FOUND.** A Heron downloaded as a zip carries `ZoneId=3` on **all 2130 files**, and PowerShell at the Windows default of `RemoteSigned` refuses an unsigned downloaded script **before its first line runs** - so `Unblock-File`, which lives *inside* `deploy-addin.ps1`, cannot clear the mark that stops `deploy-addin.ps1` running. The owner ruled the same day ([D-96](../DECISIONS.md)): **the installer is the only supported route for a downloaded copy**, signing considered and refused. **THAT RULING MADE ONE LINE LOAD-BEARING** - `-ExecutionPolicy Bypass` in `platform/Heron.Installer/WindowsAdapters.cs`, which is what lets the installer run the script a downloaded copy cannot. **MEASURED: it appears in exactly one place in the repository and NOTHING under `tests/` mentions it.** Its comment argued that the flag is *safe* and said nothing about the install breaking without it, so a later reader tidying a permissive-looking flag would have had every reason to and no warning at all

**State.** **FIXED 2026-09-21, and the corrections are held rather than trusted.** **The ps1 note now says what the machine saw**: it names `heron-bridge` as the one product whose new path lands on the old backup's contents, says what is orphaned and that nothing `-Rollback` could have used is lost, and keeps the old sentence **as a quote with its history** rather than deleting it - [row 5b-82](../FRAGMENT-ISSUES.md#)'s habit, because *it was true when it was written* is the part worth keeping. **The code was deliberately NOT changed**: tidying the orphans means reaching into a path this script no longer owns to delete files, which is the worse risk the paragraph already names. **The `WindowsAdapters.cs` comment now records D-96 and says the flag is load-bearing, not a convenience**, with the 2130 files and the `RemoteSigned` refusal beside it. **`tests/test_deploy_script.py` HOLDS BOTH HALVES**: a new section reads `WindowsAdapters.cs` and asserts the flag, its value, their order, `-NoProfile`, and that the file names D-96 - so removing the line goes red the day it happens. **Shown to fail: 3 checks** against the files as they stood. **AND THE FIRST DRAFT OF THE PROSE CHECK REPORTED THE OLD FILE AS ALREADY FIXED.** It asked whether the claim was absent from `text`, and a comment WRAPS - the sentence sits across two lines with `# ` between them, so a plain `in` saw **zero** of them and the failure message read *appears once, not twice (0)*. It joins the comment bodies into one run of words first now. A check that cannot see the thing it is about is worse than no check, and this one was one line from passing for that reason

---

### Row 5b-97

*Moved from the register on 2026-09-23.*

**Defect.** **`AB1` WAS FOUND BY A REAL MACHINE AND FIXED THE SAME DAY, AND NOTHING STOPS IT BEING UNDONE.** Found 2026-09-21 reading what #235 brought back. The installer window is a fixed **620 wide** with `ResizeMode.CanMinimize`, the tick box carried a **bare string**, and on the first drawing of that window the AI Bridge row lost its last word - *“The three buttons that exist today.”* rendered as *“...that exist”* - **with no ellipsis, so the truncated line still read as a finished sentence.** Neither escape existed: the window cannot be dragged wider, and the tooltip carries `WhyNot` rather than the description. #235 fixed it properly - a wrapping `TextBlock` rather than widening, because widening clips again just as silently the first time a longer product is added and **R-3** promises that is only ever a line in a file. **THE FIX IS RIGHT. WHAT IS MISSING IS ANYTHING HOLDING IT.** `tests/test_installer_window.py` says at the top that whether the window *looks* right needs the owner's PC, which is true - but **whether the content can wrap at all is a text fact, checkable here, and nothing checked it.** Measured: not one mention of `TextBlock`, `TextWrapping` or `AB1` anywhere in that suite. **So reverting to `Content = row.Name + "    " + row.Description` compiles, passes every gate and every suite, and clips again on a machine nobody is watching** - and the evidence that it was ever wrong is a screenshot in a document. **This is [row 5b-96](../FRAGMENT-ISSUES.md#)'s shape a second time, from the same run**: something proved once on a real machine, with nothing on this side to stop it being undone

**State.** **FIXED 2026-09-21 - the structural half of AB1 is now held, and the visual half still is not and says so.** A new section in `tests/test_installer_window.py` asserts that the tick box's own content is a `TextBlock`, that it wraps, that the wrap sits **between the `CheckBox` and its `IsEnabled`** rather than somewhere else in the file, that the block names `AB1`, and that it records why widening was refused - so the rejected fix is not tried again. **Shown to fail: 4 checks** against the file as it stood before #235, read with `git show 59c1f21:...` - **a `git stash` proves nothing here**, because the fix is already committed and the working tree has no change to stash. These checks guard against the fix being UNDONE; they do not go red on today's code and the evidence record agrees, which is why the KEEP for this change comes from `test_deploy_script.py` and not from this suite. **TWO OF THEM PASSED FOR THE WRONG REASON IN THE FIRST DRAFT AND WERE TIGHTENED.** `AB1` is named elsewhere in that window, so a plain `"AB1" in window` was true against the unfixed file too - **a check that is true either way is not a check** - and they ask of the tick block alone now. **AND A THIRD WAS RED AGAINST THE RIGHT ANSWER**: `find` for `TextWrapping` returned the FIRST one in the file, which sits above the tick box, because every other block in that window already wrapped - that was the whole point of #235's comment - so the search starts at the tick box now. **Three drafting mistakes in one short section, all of the same kind**: a check that does not look where the thing it is about actually is. [Row 5b-79](../FRAGMENT-ISSUES.md#) is the same lesson with `str.find` returning -1

---

### Row 5b-98

*Moved from the register on 2026-09-23.*

**Defect.** **TWO DOCSTRINGS IN ONE FILE DISAGREE ABOUT WHETHER THE CLAUSE STORE EXISTS, AND THE SUITE HOLDS ONLY THE STATE THAT IS GONE.** Found 2026-09-21 reading `brain/heron_context.py`, the eleventh live-path brain module and 1,278 lines that had **never been read**. Its module header says *“A scope store holds `fragments` and `meta` and no clause table - read from heron_scope, not assumed. So that path raises and says which source is missing”*. **MEASURED: a scope store holds FOUR tables** - `chunks`, `documents`, `fragments`, `meta` - because `heron_ingest.ensure_tables` creates the first two in that same store. **AND `_standard_parts`, 690 LINES FURTHER DOWN, ALREADY KNOWS**: its own docstring opens *“Before there was a clause store this path raised by name”* and implements R-45's narrowing in four branches. **The function followed the code and the header did not**, so one file states a fact and then disproves it. **THE SECOND HALF IS WHAT NOTHING HOLDS.** R-45 is the rule that as the store fills the refusal must **narrow** to *nothing indexed covers this*, must never **soften** into an answer - and, just as much, must **STOP** once there is a clause to cite. `tests/test_context.py` §3 tests **one** of the four states, the empty store, and nothing tested the rest. **A break that refused for ever would have passed every check in this suite**, and the STANDARDS path is the one docs/05 s8 says must carry a citation or be a bug. **AND ALL FOUR STATES ARE REACHABLE HERE** - measured on this Linux container with no Revit and no optional dependency: ingest a markdown clause, `heron_search.index_chunks`, `heron_embed.index_chunks`, and the route goes `empty` to `unindexed` to `documents`

**State.** **FIXED 2026-09-21 - the header says what the code does, and three of the four states are walked end to end.** The paragraph now names the four states as a table, **keeps what it used to say and why it no longer says it** ([row 5b-82](../FRAGMENT-ISSUES.md#)'s habit), and records that the store outgrew it rather than that somebody miscounted. **`tests/test_context.py` §9 ingests a real clause, indexes it, and asserts the refusal NARROWS and then STOPS**: an unindexed store still refuses and says *ingested but not indexed*, and an indexed one **answers**, with a STANDARD part, every part inside the STANDARDS budget, and **a citation on every clause**. It is last in the suite on purpose - it fills the store. **PROVED BY BREAKING IT, TWICE.** Regression A, the refusal never stops: **4 checks red**. Regression B, the refusal softens and tells the wrong nothing: **2 checks red**. **AND REGRESSION B WAS GREEN UNTIL THE CHECK WAS TIGHTENED.** It asked for `NOT INDEXED` in the message, and the **fallback** refusal appends `answer.note`, which says *ingested and NOT INDEXED* too - so the check matched the note rather than the branch and passed while the branch was disabled. It asks for `INGESTED BUT NOT INDEXED`, the branch's own words, and asserts the other two refusals are **absent**. **A check that matches a neighbour's text is not checking its own branch.** **AND THE FIRST DRAFT CRASHED INSTEAD OF FAILING, THE FIFTH TIME** - the *it stops refusing* step called `assemble` bare, and a refusal here is an exception, so the regression it exists for ended the run on a traceback. Caught now, and the failure quotes what it still said. [`heron-ship`](../../.claude/skills/heron-ship/SKILL.md) §2a, which I wrote today, and still had to learn again

---

### Row 5b-99

*Moved from the register on 2026-09-23.*

**Defect.** **A COMMENT POINTS AT A FILE THAT DOES NOT EXIST, AND IT IS THE EXACT DEAD END THE COMMIT BESIDE IT WROTE DOWN SO NOBODY WOULD WALK IT AGAIN.** Found 2026-09-21 reading [#242](https://github.com/Ajmalpshaik/Heron-AI/pull/242) - *one build folder per Revit release* - which landed while this branch was open and turned three ledger marks STALE. `tools/deploy-addin.ps1` says *“THE RELEASE IS PART OF THE PATH SINCE 2026-09-21, and that is the whole point of **Directory.Build.targets**”*. **MEASURED: there is no `Directory.Build.targets` in this repository** - only `Directory.Build.props`, which the same script names correctly **five other times**, one of them **five lines earlier**. **AND THE FILE IT SHOULD NAME SAYS IN CAPITALS WHY THE OTHER ONE IS WRONG**: `Directory.Build.props` carries *“IT MUST BE SET HERE, IN .props, AND NOT IN A .targets FILE”*, with the measurement that proved it - setting `OutputPath` from a `.targets` file left `OutDir` **and** `ProjectDepsFilePath` still pointing at the flat folder, so the build wrote its assemblies to the new folder and its `deps.json` to the old one and **Revit 2027 would not load**. That paragraph ends *“Both dead ends are written into the comment so the next person does not walk them again”*. **The one wrong pointer, added in the same commit, sends that next person straight down one of them** - and because the file does not exist, they find nothing there and have no way to tell a typo from a file somebody forgot to commit

**State.** **FIXED 2026-09-21 - one phrase, and a check so the next one cannot drift.** The comment now names *the `OutputPath` block in `Directory.Build.props`*, which is where the setting actually lives and what the rest of the file already says. **THE CHECK IS DERIVED RATHER THAN PINNED TO A NAME.** `tests/test_deploy_script.py` collects every `Directory.Build.<something>` the script mentions and asserts **each one exists in the repository** - so it catches this, and it catches the next pointer at a build file nobody wrote, without anybody having to remember which names are real. **Shown to fail: 1 check**, and it names the file: *Directory.Build.targets exists in the repository*. **THE REST OF #242 WAS READ AND IS SOUND**, and its three stale marks are cleared: the split is by **Revit release and not by target framework** - 2021 to 2024 all build `net48` but `DefineConstants` makes them different builds, and a per-framework split would let a 2021 build install into 2024 silently, which is [A12](../NEEDS-CHECKING.md)'s class of defect; the flat-layout fallback is safe because the runtime guard reads the assembly; and the `.gitignore` rule keeps the built installer out of history. **The comment recording that four installs failed in a row before this was fixed is the best part of that commit** and is untouched

---
