# FRAGMENT-ISSUES archive — section 5b, rows 26 to 50

> **Finished rows, moved out of the live register.** These are rows from section 5b of
> [`FRAGMENT-ISSUES.md`](../FRAGMENT-ISSUES.md) — Heron's own defects found by reading the repository — whose state said they were
> finished when they were moved. Each still has its line in the register: the same number, a
> one-line title, the opening of its state, and a link to its full text here.
>
> **Nothing below was rewritten** except its links, which were re-pointed one folder deeper and
> each checked to reach the same file it reached before. Where a row disagrees with
> [DECISIONS.md](../DECISIONS.md), the [Golden Rules](../14-golden-rules.md) or the
> [Constitution](../../HERON_CONSTITUTION.md), **those win** — a row records what was true on
> the day it was written.
>
> **Nothing new is written here.** A new defect goes in the register, and a row here that turns
> out not to be finished is reopened **there**, on its own line.
>
> Rows 26 to 50 of section 5b belong in this file. A number in that band with no entry below
> was never moved — it is still in the register, in full. Written by
> [`tools/archive-fragment-issues.py`](../../tools/archive-fragment-issues.py).

---

### Row 5b-26

*Moved from the register on 2026-09-23.*

**Defect.** **THE "NEVER COMPILED, NEVER RUN" SENTENCE IS IN FOUR FILES, AND THE TWO HARSHEST COPIES WERE THE TWO NOBODY HAD LOOKED AT.** Found 2026-09-21 reading `mcp/server/heron_health.py` in the sweep, straight after fixing [row 9](../FRAGMENT-ISSUES.md#), which is the same sentence in `RevitWrite.cs`. **A GREP FOR IT WAS THEN RUN ACROSS THE WHOLE TREE, WHICH IS THE PART WORTH COPYING** - fixing one copy of a stale claim and not looking for the others is how it reached four. **The four:** `HeronPermissions.cs` (corrected 2026-09-20), `RevitWrite.cs`'s banner ([row 9](../FRAGMENT-ISSUES.md#), corrected 2026-09-21), `heron_health.py`'s module docstring - *"While the write path has never been compiled or run (D-19)"* - and `tests/test_write_safety.py` - *"it has never been compiled, let alone executed"*. **BOTH HALVES OF THE LAST TWO ARE FALSE.** It compiles on all eight releases (`tools/check-compile.py`), and `NEEDS-CHECKING.md:484` records it moving three ducts under a single undo entry. **AND A FIFTH COPY WAS A TEST ASSERTING IT.** `tests/test_config_and_health.py` checked that the write-gate warning contains *"never been proven"*, describing that as *"the path has not met a real Revit"*. That is a test keeping an expired sentence alive: it would have gone red on any honest correction, which is exactly what it did. The claim is replaced rather than the wording loosened - the warning must now name **D3** and the missing MEASUREMENT, and must NOT contain either expired phrase. **WHAT IS STILL TRUE, and is what all four now say:** nobody has put a tape measure on the result. D3 - *"move them, then MEASURE one"* - is not struck through. **WHAT WAS NOT CHECKED:** whether the same claim appears in the numbered documents under `docs/` in a form the grep missed; it covered `.py`, `.cs` and `.md`, and `docs/NEEDS-CHECKING.md:471` already carries its own correction of it.

**State.** **FIXED 2026-09-21, all four, in the sitting that found the third.** The rule this earns: **a stale claim is never in one place.** Row 9 was filed as one file's banner; it was four files and a test, and the test was the one holding it in position

---

### Row 5b-28

*Moved from the register on 2026-09-23.*

**Defect.** **EIGHTEEN OF THE FORTY-SIX ERROR CODES THE ADD-IN CAN PRODUCE WERE IN NO TABLE, SO EVERY ONE OF THEM TOLD A MODELLER TO GO AND CHECK THEIR MODEL.** Found 2026-09-21 reading `mcp/server/heron_failure.py` in the sweep. Its `_KNOWN` table maps an error code to what happened and what may follow; anything absent falls to the fail-closed default, which on a **write** is `UNKNOWN` + `LOOK_AT_THE_MODEL`: *"Heron did not get an answer, so it cannot tell you whether that happened. Look at the model before trying again - repeating it could do the work twice."* **THAT DEFAULT IS RIGHT AND THE ANSWER WAS FALSE.** For `compile_failed` the fragment did not compile and nothing ran. For `needs_unbound` a value was missing and nothing ran. For `chain_empty` the add-in's own message says **"NOTHING WAS BOUND and no fragment ran"** - and the analyser then told the user the outcome was unknown and to go and look. That is the most frightening sentence Heron can produce, in the case where it is certainly untrue. **MEASURED, BOTH SIDES:** `analyse({'error': 'compile_failed'}, writes=True)` returned `unknown / look / touched=None` before and returns `refused / fix_first / touched=False` after. Same for `needs_unbound`, `request_too_long` and `fragment_threw`. **THE FILE ALREADY RECORDED THIS HAPPENING TWICE, ONE CODE AT A TIME.** Its own comments say `wrong_document` *"was classified as an unknown outcome on the write path and told the user to go and check the model - frightening, and false"*, and that `no_such_document` *"PREDATES all of this and was never in the table - the same gap, sitting unnoticed until its sibling was added beside it"*. Nobody then asked how many more there were. The answer was eighteen. **ONE OF THEM AROSE THIS MORNING:** `request_too_long`, added by [row 22](../FRAGMENT-ISSUES.md#) hours earlier. A refusal made before the line is even parsed, reported as a possible change to the model. **EACH CLASSIFICATION WAS READ AT ITS SITE, NOT GUESSED.** `fragment_threw` and `setup_failed` are `ROLLED_BACK` because both roll the transaction AND the group back before returning, which was checked in `RevitFragment.cs`; the chain refusals are `REFUSED`, two of them `START_OVER` because what is carried came from the wrong producer; `request_too_long` is `NEVER_RAN` because it is refused before the token is checked. **`handler_failed` IS DELIBERATELY LEFT OUT and that is the interesting one.** It is raised when an exception escapes `Dispatch` from anywhere at any depth, so whether the model was touched depends on what threw. A static row can say only one thing; the fail-closed default says the right thing **twice**, because it reads `writes`. Naming it would make one of the two answers wrong. **THE DURABLE HALF IS THE GATE**, in `tests/test_failure_analysis.py`: it reads every `Json.Error("code"` out of `revit/` and fails if one is unclassified, fails if a row classifies a code nothing can produce any more, and checks that the deliberately-open one is still open. Proved by reverting the table and watching it name all seventeen. **ALSO FIXED HERE:** `"stopped"` read *"the Emergency Stop is on"* - [row 2](../FRAGMENT-ISSUES.md#)'s removed button, in a second file nobody had looked at.

**State.** **FIXED 2026-09-21, in the sitting that found it.** A table of codes maintained by hand beside a program that emits them is the same shape as [row 29](../FRAGMENT-ISSUES.md#)'s two vocabularies and [row 6](../FRAGMENT-ISSUES.md#)'s two minters: the repair that lasts is the comparison, not the eighteen rows

---

### Row 5b-29

*Moved from the register on 2026-09-23.*

**Defect.** **ONE SETTINGS FILE, TWO READERS, AND AS OF THIS MORNING THEY DISAGREED.** (Written as 25, moved to 29 when `main` turned out to have taken 25 first - [row 149](../FRAGMENT-ISSUES.md#)'s hazard, and the half of its rule that nobody follows is the ANNOUNCE.) Found 2026-09-21 reading `mcp/client/heron_config.py` in the sweep, hours after [row 8](../FRAGMENT-ISSUES.md#) was fixed - and CAUSED by that fix, which is why it is worth a row of its own rather than a line in row 8. `HeronConfig.GetBool` learned `on` and `off`, learned to trim, and learned to return the **fallback** for a word it does not recognise. `heron_config.truthy` on the Python side still read `raw in ("true", "1", "yes")` - so `on` was **false** there and **true** in the add-in. **THE LIVE CASE IS `write.enabled = on`.** The add-in would permit a change to the model; `heron_health` and `revit_health`, which both call `writing_enabled`, would report writing as **off**. That is precisely the hazard `HeronPermissions.cs:150-154` names in its own words - *"the button reports a state the permission gate does not honour"* - arriving from the other direction. **AND IT WAS INVISIBLE TO THE CHECK THAT EXISTS**: `tests/test_config_and_health.py` already reads `HeronConfig.cs` and compares the declared KEYS, which is why the closed set has never drifted. Nothing compared the VOCABULARY. A key list is not a parser. **WHAT WAS CHECKED RATHER THAN ASSUMED:** every caller of `truthy` - it is `writing_enabled` only, reached from `heron_mcp_server.py:166` and `heron_diagnose.py:96`, so `write.enabled` is the one key affected today; `ui.activityBanner` is read by the C# alone, and would have been the worse case had anything on this side read it, because its default is **true** and an unrecognised value collapsed to false. **WHAT WAS NOT CHECKED:** whether any THIRD reader of this file exists outside `platform/` and `mcp/` - the grep covered those two and `brain/`, which D-48 forbids from importing the config at all. **THE REPAIR IS THE WORDS AND THE CHECK.** `TRUE_WORDS` and `FALSE_WORDS` mirror the C# exactly, an unrecognised value falls back to the key's declared default, and the suite now reads `GetBool`'s body out of the C# and fails if the two word lists differ - proved by deleting `on` from the Python list and watching two checks go red.

**State.** **FIXED 2026-09-21, in the sitting that found it, and it is the one defect in this section that this session caused.** A fix to one half of a mirrored pair is not finished until the other half is looked at, and the only reason this was caught within the hour is that the sweep happened to reach the file next. The check is the durable part: the keys were compared and the vocabulary was not, so the drift had a door standing open

---

### Row 5b-30

*Moved from the register on 2026-09-23.*

**Defect.** **TWENTY BLANK LINES SPLIT THE REGISTER INTO ABOUT TEN SEPARATE TABLES, AND NO TOOL COULD SEE IT BECAUSE EVERY TOOL READS IT LINE BY LINE.** Found 2026-09-21 while sorting the rows of this section and discovering they are not contiguous. A blank line ENDS a Markdown table: 16 of them sat between rows of section 5b and 4 between rows of section 5, so on GitHub this register renders as roughly ten one-row tables, most of them with no header above the columns. **EVERY CHECKER WAS HAPPY THE WHOLE TIME.** `open-defects.py`, `review-ledger.py` and `test_open_defects.py` all match a row with a regex per line, so a blank line between two of them is invisible to all three. The register is read by PEOPLE as well as by tools, and nothing was watching that half. **IT ALSO HID [ROW 149](../FRAGMENT-ISSUES.md#)'s HAZARD HAPPENING AGAIN.** `main` took **5b-25** in #221 and this branch took 25 as well; the collision only surfaced when the rows were sorted, which needed the blank lines gone first. This row's neighbour at 29 carries the note - written as 25, moved. **FIXED:** the blank lines are removed, the rows of section 5b are in numeric order, and `tests/test_open_defects.py` gains a sixth section that fails if a blank line ever sits between two table rows again - proved by putting one back and watching it name the section. **WHAT WAS NOT CHANGED:** a single word of any row. Only the empty lines between them.

**State.** **FIXED 2026-09-21.** Two rules, and the second is the one worth keeping: a register is a document before it is a data source, and **a check that reads it line by line cannot see its shape**

---

### Row 5b-31

*Moved from the register on 2026-09-23.*

**Defect.** **THE CHECKPOINT SAVE DELETED THE OLD FILE BEFORE WRITING THE NEW ONE, AND ITS OWN COMMENT SAID IT DID THE OPPOSITE.** Found 2026-09-21 reading `mcp/server/heron_workflow.py` in the sweep. `CheckpointStore.save` wrote a `.tmp` beside the file, then `os.remove(path)`, then `os.rename(temp, path)` - under a comment reading *"Write beside, then replace: a crash mid-write leaves the previous checkpoint intact rather than a truncated file that parses to 'nothing was ever done'."* **THE WINDOW BETWEEN THE REMOVE AND THE RENAME HAS NO CHECKPOINT FILE AT ALL**, and `load()` answers a missing file with `{"stages": {}}` - *"nothing done yet"*, deliberately, because repeating work is safer than refusing to run. So a crash in that window does not truncate the checkpoint: it silently discards **every finished stage**, which is the one loss the whole file exists to prevent. docs/23 calls resume *"the most practically valuable item in Part 4"* and the reason given is an 18-stage pipeline failing at stage 13. **THE C# SIDE HAD THIS EXACT DEFECT AND FIXED IT A WEEK EARLIER.** `HeronConfig.Save`'s own note reads *"This was write-tmp, DELETE, move until 2026-09-16, which left a window with no config file at all"*, and `HeronAtomicWrite` exists because of it. Nobody looked at the Python half. **THE FIX IS ONE CALL:** `os.replace(temp, path)`, atomic on POSIX and on Windows, and unlike `os.rename` it overwrites on both - so the remove goes away with it. **MEASURED:** `tests/test_workflow.py` now stops **both** `os.replace` and `os.rename` mid-save and checks the earlier stage is still on disk. Against the previous implementation it fails; against this one it passes. Stopping only `os.replace` would have passed against the defect, because the old code never called it - which is why the check stops both.

**State.** **FIXED 2026-09-21, in the sitting that found it.** The lesson is not about temp files: **the same defect was found, understood and written up on the C# side, and the Python half was never checked.** A fix recorded in one language is not a fix

---

### Row 5b-32

*Moved from the register on 2026-09-23.*

**Defect.** **A STAGE THAT THREW WAS RETRIED THREE TIMES ON THE WRITING PATH, IN THE FILE WHOSE HEADER SAYS IT NEVER RETRIES BLINDLY.** Found 2026-09-21 in the same read. `Workflow.run` catches any exception from the stage's action, records the text, and `continue`s - straight to the next of three attempts, with no reference to `heron_failure` and no regard for `writes`. **THE FILE'S OWN CLOSING PARAGRAPH IS THE CLAIM IT BREAKS:** *"IT NEVER RETRIES BLINDLY. The engine does not decide that for itself; it asks the Failure Analysis Agent, which is the one place that knows the difference between 'never reached the model' and 'was sent, and the answer was lost'. A workflow engine that retried on its own judgement would undo the whole point of that agent."* It asks the agent about a REPLY and never about an EXCEPTION - and an exception is the case with no reply at all, which is precisely the unknown outcome. **WHAT IT COSTS:** a write stage whose call throws after Revit took the request - a dropped pipe, a timeout inside the client - is repeated twice more. That is the *"moves the same ducts twice"* failure `heron_failure` was written to prevent, reached by the one path that never consults it. **THE AGENT ALREADY HELD THE ANSWER**, which is what makes the repair three lines: `analyse(None, writes=...)` is exactly this question, and it answers `NEVER_RAN` + `RETRY` for a read and `UNKNOWN` + `LOOK_AT_THE_MODEL` for a write. The exception path now asks it and obeys `may_retry`. **MEASURED, BOTH DIRECTIONS:** a throwing WRITE stage is attempted **once** and raises `WorkflowStopped` carrying a failure whose `touched_the_model` is `None` - unknown, not false; a throwing READ stage is still attempted three times, because repeating a question is free. Against the previous implementation the write case is attempted three times and the check fails. **WHAT IS NOT ESTABLISHED:** nothing calls this engine yet - the file says so at length and `check-reachable` agrees - so no modeller has met this. It is built for Phase 2's 18-stage pipeline, which is exactly when it would have mattered.

**State.** **FIXED 2026-09-21, in the sitting that found it.** Worth reading beside [row 28](../FRAGMENT-ISSUES.md#) from the same morning: both are the same shape, a careful rule with a door beside it that nobody checked. There, eighteen codes bypassed a table; here, one `except` bypassed the agent the file is built around

---

### Row 5b-33

*Moved from the register on 2026-09-23.*

**Defect.** **MINOR, AND BOTH IN THE ONE FILE THAT REFUSES SOFT FAILURES EVERYWHERE ELSE.** Found 2026-09-21 reading `mcp/server/heron_install_health.py` in the sweep. **(a)** A step whose check reported a word this agent does not know - `"PROBABLY FINE"` - falls to `UNCHECKED`, which is right and which the suite pins on purpose, and then says *"nothing checked this"*. Something did. The file's own docstring, one paragraph up, forbids exactly this shape: *"'nobody looked' and 'fine' are the two readings an install report must never merge"* - and *nobody looked* against *I cannot read what they said* is two more. The behaviour is unchanged (it still fails safe); the sentence now names the word it could not read. **(b)** A check handed in for **step 16**, `user-told`, passed the step-name validation and was then silently dropped, because `VERIFIED_BY_CHECKS` stops at fifteen. Accepting something and ignoring it is the soft failure every other refusal on that page exists to prevent, so it is refused by name now - *"step 16 is PRODUCED by complete(), not reported on. A check for it would be the install confirming that it told somebody it worked."* **MEASURED:** both new checks fail against the previous implementation and pass after. The suite's own last section - every refusal code in the source is reached by a test above - still passes with the new code added. **WHAT WAS NOT CHANGED, DELIBERATELY:** an unreadable state still counts as unchecked rather than being refused. It fails safe, `tests/test_install_health.py` asserts it, and turning a bad word into a hard refusal would make a shallow reporting mistake stop an install report that is otherwise complete.

**State.** **FIXED 2026-09-21, in the sitting that found it.** Both are wording-and-shape rather than behaviour, and worth a row only because of where they are: a file whose entire argument is that a soft answer is worse than a refusal had two soft answers in it

---

### Row 5b-34

*Moved from the register on 2026-09-23.*

**Defect.** **A PROBE WHOSE DOCSTRING ASKS A QUESTION ITS BODY NEVER ASKED, IN THE REPORT THAT ENDS "A CHECK THAT DID NOT RUN IS NOT A CHECK THAT PASSED".** Found 2026-09-21 reading `mcp/server/heron_diagnose.py` in the sweep. `_tools`'s docstring is one line - *"Is every declared tool actually being offered."* - and the body imported `heron_tools`, counted `len(TOOLS)`, counted how many write, and reported **HEALTHY**. It never looked at the server. **SO THE COMPONENT COULD NOT FAIL**, short of the registry being unimportable: the tool registry and the thing that serves it could disagree completely and *"diagnose Heron"* would print `tools HEALTHY, 34 declared`. That is the shape this whole file argues against - its own closing line tells the reader that a check which did not run is not a check that passed, and this was one. **THE TWO DIRECTIONS ARE NOT THE SAME AND ARE NOW NAMED SEPARATELY.** A tool the server **offers** and the registry does not **declare** is `FAILED`: `risk_of()` raises for an undeclared name, so it is offered and cannot be called at all. A tool declared and not offered is a `WARNING` - a dead entry, untidy rather than dangerous - and one of the two files is wrong about what Heron can do either way. **THE SERVER IS READ AS TEXT, NOT IMPORTED, AND THAT IS THE POINT OF THE REPAIR.** Importing `heron_mcp_server` needs the MCP SDK, which is exactly the thing that may be missing on the day somebody types *"diagnose Heron"* - and a probe that cannot run when the machine is broken is not a probe. It matches `@server.tool()` followed by a `def`, which is how `tests/test_tool_registry.py` and this session's read of `heron_tools.py` both counted them. **MEASURED ON THIS TREE:** 34 declared, 34 offered, 2 able to change a model - they agree today, which is why nobody noticed. Against the previous implementation all three new checks fail; a name removed from the registry now reads `FAILED` and names it, and one added reads `WARNING`. **WHAT WAS NOT CHANGED:** the probe still cannot tell whether an offered tool WORKS. It compares two lists, which is what its docstring claims and all it ever claimed.

**State.** **FIXED 2026-09-21, in the sitting that found it.** The rule is [row 28](../FRAGMENT-ISSUES.md#)'s from the other end: there, a table was missing entries nothing compared it against; here, the comparison was written down as the function's purpose and never coded. **A docstring is not a check**, and in a file of probes it reads exactly like one

---

### Row 5b-35

*Moved from the register on 2026-09-23.*

**Defect.** **THE ONE BLOCK WRITTEN TO MAKE A RISK CROSSING VISIBLE WENT SILENT EXACTLY WHEN IT COULD NOT SEE.** Found 2026-09-21 reading `mcp/server/heron_brain.py` in the sweep. `lookup()` reads every fragment's declared risk out of the store so that the answer can say whether a QUESTION was answered by something that CHANGES THE MODEL - the block carries twenty lines of comment explaining why, ending *"this only lets the crossing be SEEN"*, and cites [row 109](../FRAGMENT-ISSUES.md#), where *"can I edit these"* resolved to `UPDATE_SAVED_SET` and *"isolate all the pipes"* to `SET_MEP_SLOPE`. It ended `except Exception: risks = {}` and said nothing. **AN EMPTY RISK COLUMN IS NOT A SAFE ANSWER, IT IS AN UNREADABLE ONE.** `heron_mcp_server.heron_lookup` does `top_risk = (found.get("risk") or "").upper()` and warns only `if top_risk and top_risk != "READ"`. With no risks the condition is false, the warning is skipped, and what the modeller sees is **identical to a clean answer** - in the single case the block exists for. A silent pass that looks exactly like a clean one is the shape [rows 28](../FRAGMENT-ISSUES.md#), [34](../FRAGMENT-ISSUES.md#) and [162](../FRAGMENT-ISSUES.md#) are all about, met a fourth time. **THE ANSWER IS STILL RETURNED**, because a lookup that refuses because it could not read a risk column is worse than one that says so: `risks_unreadable` carries the reason on the answer, and the server prints *"Heron could not read what these fragments are allowed to do ... so nothing below says whether this reads the model or changes it. Check before you run it."* **THE READ IS NOW A NAMED FUNCTION**, `_risks(store)`, for a reason worth copying: a test that wants to see the failure cannot break `store.fragments()`, because retrieval calls it too and the whole lookup would fail instead of the one read. Patching a named helper tests the handler; breaking the store tests something else. **MEASURED:** an ordinary lookup reports `risks_unreadable: None` and resolves `"select all the ducts"` to `SELECT_BY_CATEGORIES`, risk READ, route hybrid; with `_risks` made to throw, the answer carries the reason and the suite checks both. **ALSO FIXED IN THE SAME FILE:** its module docstring's section *NOTHING BELOW IS PROVEN - every fragment and every skill is DRAFT*, which is [row 26](../FRAGMENT-ISSUES.md#)'s sentence in a fifth place. Every SKILL is still DRAFT; the fragments are mostly not, and the reader is sent to derive it. **AND ROW 27's OPEN QUESTION IS ANSWERED HERE:** `brain/heron_capability.resolve` sorts its rows by TRUST then id, so `rows[0]` is the MOST TRUSTED provider and naming it beside the capability's worst-case risk is a deliberate pairing rather than the accident row 27 found in `heron_runtime`.

**State.** **FIXED 2026-09-21, in the sitting that found it.** Fourth of the same shape in one day, and the shape is worth naming: **a guard that fails open and says nothing is indistinguishable from a guard that passed.** Every one of the four was written by somebody being careful

---

### Row 5b-36

*Moved from the register on 2026-09-23.*

**Defect.** **THREE OF THE SEVEN REFUSALS DID NOT CARRY THE SENTENCE THIS FILE SAYS EVERY REFUSAL CARRIES.** Found 2026-09-21 re-reading `mcp/server/heron_session.py`, which the ledger had marked STALE since #213 edited it. Its module docstring states the rule and the reason: *"Every refusal says 'nothing has been sent to Revit' in as many words. The user's first thought on any refusal is 'did it half-do something?', and answering that unasked is the difference between a safe stop and a frightening one."* Four of them do. The three built by `_nothing_connected` did not - **no Revit connected**, **the bridge is still starting**, and **a Revit is connected on another protocol**. **AND THE TWO THAT MATTER MOST WERE THE TWO IT READ LEAST LIKE.** With nothing connected at all, nothing having been sent is nearly self-evident. With a Revit **running** but not answering yet, or one connected and speaking a different protocol version, it is not self-evident at all: something is plainly there, and a modeller who reads either of those in the middle of a conversation has real reason to wonder whether the last thing they asked went through. **THE SUITE HAD THE RULE AND CHECKED IT CASE BY CASE.** `tests/test_session_binding.py` asserts the sentence on the two refusals that involve a choice and never on these three, so the docstring's *every* was broader than anything measured. **AND THE OBVIOUS GATE WOULD HAVE BEEN THE WRONG ONE, which is the part worth copying.** Counting the sentence in the SOURCE was tried first and reported **2 of 3**: two of these messages are built from a pair of adjacent string literals, so the phrase is not contiguous in the file even though it is in the answer. The gate counts the function's RETURNS instead and requires each to be exercised above, so a fourth branch added later fails until it is covered. **MEASURED:** all three checks fail against the previous wording and pass after.

**State.** **FIXED 2026-09-21.** Small, and it is [row 2](../FRAGMENT-ISSUES.md#)'s family a third time: a sentence a worried person reads, in a file that had already written down the rule it was breaking

---

### Row 5b-38

*Moved from the register on 2026-09-23.*

**Defect.** **FIVE PLACES THAT RUN A FRAGMENT TYPED THEIR OWN DEADLINE, AND IT IS THE EXACT DEFECT THE SUITE ONE FOLDER OVER CALLS BUG 2.** Found 2026-09-21 in the same read. `heron_config.response_timeout()` is **derived** — `operation_timeout() + 30` — and its docstring says why: *"the far side is the one that gets to answer, because only it knows whether Revit took the request, started it, or never saw it"*. `tests/test_config_and_health.py` pins that ordering **at every value, not just the default**, because the original bug was a constant 90 that was correct only by coincidence. **AND THEN FIVE CALL SITES WROTE `response_timeout=180.0` BY HAND** — `heron_mcp_server.py:932` (`run_fragment_write`) and four fragment runs in `heron_bridge_client.py`. A fragment is compiled by Roslyn inside Revit before a line of it executes, so a longer wait is right; a **literal** longer wait is beyond the add-in's deadline only while nobody changes it. **MEASURED**: at `revit.operationTimeoutSeconds = 300` the add-in waits 300 and these five gave up at 180 — **two minutes early**, reporting *no answer* for a request Revit was still working on. Raising that setting is exactly what a person does when a model is slow, and a slow model is when a fragment run is slow too. **ON THE WRITE PATH IT IS NOT A WRONG SENTENCE, IT IS AN UNANSWERABLE QUESTION**: `run_fragment_write` is sent `idempotent=False` precisely because a lost answer cannot be asked again, so the one call that most needs the far side to answer was the one most likely to be cut off

**State.** **FIXED 2026-09-21. ONE DERIVATION, FLOORED AT THE NUMBER THEY CHOSE.** `heron_config.fragment_timeout()` returns `max(FRAGMENT_FLOOR_S, response_timeout(values))`, so the 180 those sites wanted is kept at the default settings and the ordering holds at every value above it — **measured at 1, 10, 60, 120, 179, 180, 181, 300 and 3000**, add-in against client, each one higher. All five call sites now use it. **AND A GATE, BECAUSE A HELPER NOBODY USES IS NOT A FIX**: the suite reads both files and fails on any `response_timeout=<number>` that is not the deliberate 10-second one on `release`, which the add-in answers without touching Revit. **Proved by making it fail** — one site put back to `180.0` and the suite names `mcp/client/heron_bridge_client.py:1190` and exits 1. The sixth call site will be written by copying one of these five, and that is the one this catches

---

### Row 5b-39

*Moved from the register on 2026-09-23.*

**Defect.** **THE ONE TOOL WHOSE JOB IS TO SWITCH MODELS THREW AWAY THE REASON IT COULD NOT.** Found 2026-09-21 in the same read, by comparing all twenty Revit-touching tools against each other rather than reading each on its own. Nineteen of them answer a refusal with `reply.get("message") or reply.get("error")` - the add-in's own sentence, which for `revit_busy` is *dismiss the dialog and ask again* and for `no_document` is *open a model*. `revit_use_this_model` collapsed **no answer** and **refused** into one branch and replaced both with *"Heron could not read which model is open, so it has not moved the pin"*. True, and it sends the reader nowhere: the one thing they came to this tool to do is switch models, and the reason they cannot was the half thrown away. It is [row 36](../FRAGMENT-ISSUES.md#) inverted - that one found refusals missing the sentence this file adds, this one a refusal missing the sentence the ADD-IN wrote

**State.** **FIXED 2026-09-21.** The two cases are separated: no answer names the Revit and the session, the way every other tool does, and a refusal passes the add-in's own message through. **WHAT IS ADDED IS THE HALF THE ADD-IN CANNOT KNOW** - *"The pin has not moved"* - because the add-in has no idea this chat is pinned to anything, so the sentence a person needs is the two put together: why it failed, and that the next request still goes to the old model. Found by MEASURING the twenty tools against one another for four things (a `reply is None` branch, a not-ok branch, `except NotBound` and `session.close()`); the other nineteen were identical and the two write tools answer through `analyse()`, which fails closed on a lost reply by design

---

### Row 5b-40

*Moved from the register on 2026-09-23.*

**Defect.** **FIVE OF THE SEVEN COMMANDS HID A PROTOCOL MISMATCH ON THE ONE BRANCH WHERE IT IS THE ANSWER.** Found 2026-09-21 reading `mcp/client/heron_bridge_client.py`, by comparing the seven `cmd_*` functions against each other. `discover()` separates a bridge speaking another protocol - that separation is [row 163](../FRAGMENT-ISSUES.md#)'s fix - and `report_mismatched()` prints the only sentence that helps: *"Restart that Revit to finish updating. Heron will not talk to it until the two agree."* **ALL SEVEN CALLED IT ON THE `no live` BRANCH. TWO CALLED IT ON THE `still starting` BRANCH.** `cmd_count`, `cmd_fragment`, `cmd_prove`, `cmd_validate` and `cmd_release` printed *"Revit is still starting - try again shortly"* and stopped. **THAT IS THE BRANCH WHERE IT MATTERS MOST**: one Revit booting and another stuck on an old add-in reads as *wait a moment*, and the wait never ends, because the second one needs restarting and nothing said so. A person waits, retries, waits, and the tool tells them the same thing each time while holding the reason. **`cmd_list` AND `cmd_ping` ARE THE PROOF IT WAS MEANT ON BOTH** - the same two-line shape, written first, with the call on both branches

**State.** **FIXED 2026-09-21, ALL FIVE.** One line each, and the branch now says both things: one Revit is still coming up, and this other one will never come up until it is restarted. **GATED IN `tests/test_bridge_discovery.py`**, which is the suite this belongs to - it already exists for the defect that made a mismatched bridge invisible. It reads the client's source, finds every `not live and starting` branch, and fails when one reaches its `return` without `report_mismatched`. **It also fails if it finds fewer than seven**, so a command deleted or renamed makes the check say it has stopped looking rather than pass on an empty sweep - the [row 34](../FRAGMENT-ISSUES.md#) shape, guarded against in the guard itself. **Proved by making it fail**: one call removed and the suite names `cmd_validate (line 1610)` and exits 1

---

### Row 5b-41

*Moved from the register on 2026-09-23.*

**Defect.** **`count` WITH NO SESSION NAMED TOOK EVERY REVIT THE USER HAD OPEN, AND IT IS THE DEFECT THE SERVER FIXED ONE LAYER UP AND THE CLIENT DID NOT.** Found 2026-09-21 reading `cmd_count`. `BridgeServer.cs` exempts exactly two operations from the lease and says why in as many words: *"asking 'who has this?' must not be the act of claiming it"*. `count_elements` is not one of them. `cmd_count(pid=None)` looped over every connected Revit and asked it, **so each one was claimed for five minutes** - a modeller running `count` to see what was open would refuse the two chats about to use the other two Revits. **`heron_mcp_server.revit_health` CARRIES THIS EXACT COMMENT AND THIS EXACT FIX**, written one commit after the lease was built: *"a health check that changes who owns what is not a health check"*. The client half was never done, and it is the half a person runs by hand. **AND THE RULE WAS ALREADY WRITTEN DOWN**, which is the part worth sitting with: `.claude/skills/revit-addin-conventions/SKILL.md` states it as a house rule - *"A tool that reads EVERY session must use `info`, not `count_elements` - `revit_health` claimed a lease on every free Revit for one commit because it used the latter"* - and names the incident. The rule existed, the incident was recorded, and the other caller of the same operation went on doing it for a fortnight. A house rule is not a gate. **The help line says `count  # count elements in the open model`** - singular - so the loop did not even match what the tool says it does

**State.** **FIXED 2026-09-21, THE SAME WAY THE SERVER WAS.** With no session named, each Revit is asked `availability()` first - which uses `info`, is exempt, and costs nothing - and one held by another chat is printed with who has it and *not counted: counting would take it*, rather than being taken. **NAMING A SESSION IS STILL THE DELIBERATE ACT**: `count <pid>` narrows the list first and asks that one whatever its lease says, because somebody who typed a session id has decided. **GATED, AND THE EXEMPT LIST IS DERIVED FROM THE C#**: `tests/test_bridge_discovery.py` reads `BridgeServer.cs`, pulls the ops out of the lease-exemption block, and fails if it is ever anything but `ping` and `info` - so a third exemption makes the check say every loop over `live` needs reading again, instead of quietly going on describing two. It then fails if `cmd_count` reaches `bridge.request("count_elements")` without `lease_state` in front of it - **the FLAG, not the sentence**, because the first version of the guard read `"another chat" in availability(bridge)` and a reworded phrase would have turned it off in silence, which is the exact shape this whole sweep keeps finding. `availability` still builds the words a person reads. **Proved by making it fail** - the guard removed and the suite names it and exits 1. **AND THE FIRST VERSION OF THE GATE WAS WRONG IN THE OTHER DIRECTION**: it matched the bare word `count_elements`, which the comment explaining the rule uses several lines above the call, so it reported the FIXED code as broken. Watched doing it, then narrowed to the call

---

### Row 5b-43

*Moved from the register on 2026-09-23.*

**Defect.** **THE CHECK THAT STOPS A FRAGMENT CONSUMING THE WRONG SET OF ELEMENTS COMPARED WITH A SUBSTRING SEARCH, SO IT PASSED THE CASE IT EXISTS FOR.** Found 2026-09-21 reading `RevitFragment.ChainDisagrees`. `--expect-from "select-by-categories where categories=Pipes"` is how a caller says *these elements must be the ones THAT fragment left, and it must have run with THOSE inputs*, and the refusal's own closing sentence is **"The right fragment left these, with different inputs - so they are a different set of elements"**. What is carried is rendered by `DescribeSupplied` as `"categories=Pipes; view=Level 10"`, and the test was `actual.IndexOf(want, OrdinalIgnoreCase) >= 0` **over the whole string**. **MEASURED IN REAL C#, NOT ARGUED** - a throwaway console project holding the old line and the new one side by side, nine cases: the substring search wrongly PASSES **three of the four** it is for. `categories=Pipe` is inside `categories=Pipes`; `view=Level 1` is inside `view=Level 10`; and it is loose in the other direction too - `s=Pipes` is inside `categories=Pipes`, so a truncated NAME passed as well. Every one of those is a different set of elements. **THE SUITE COULD NOT SEE IT**: `tests/test_chain_expectation.py` reads the source as text and checks that all four refusals are NAMED and that the check is CALLED before the binding - both worth checking, and neither exercises the comparison

**State.** **FIXED 2026-09-21.** `SuppliedCarries(actual, want)` splits the rendered pairs on `;` and compares a whole segment with `string.Equals`, still `OrdinalIgnoreCase` because that half was never the defect. **SPLITTING IS WHAT THE OTHER SIDE ALREADY DID** - `wantedInputs` is split on `;` before it reaches here - so the two sides now agree about a semicolon instead of disagreeing quietly. **The same nine cases: the new comparison answers all nine correctly.** Compiles on Revit **2020 through 2027**. **GATED in `tests/test_chain_expectation.py`** - the check must call a NAMED comparison, `IndexOf(want` must be gone from `ChainDisagrees` itself, and `SuppliedCarries` must split and compare for equality. **Proved by making it fail**: the substring line put back, two checks fail and the suite exits 1. **AND THE GATE WAS WRONG ON THE FIRST ATTEMPT, in the direction worth recording** - it searched the WHOLE FILE for `actual.IndexOf(want`, which `SuppliedCarries`'s own docstring quotes on purpose so a reader in a year knows what it replaced, so it reported the FIXED code as broken. Scoped to the method body after watching it do exactly that

---

### Row 5b-45

*Moved from the register on 2026-09-23.*

**Defect.** **THE PARAGRAPH EXPLAINING WHY AN IN-REVIT COMPILE FAILURE OUTRANKS THE GATE SPENT ITS LIFE ABOVE THE WRONG METHOD.** Found 2026-09-21 reading `RevitFragment.cs`. Two `<summary>` blocks sat adjacent: the first describing `Compile` - *"A COMPILE FAILURE HERE IS NOT THE SAME AS A FAILURE IN THE GATE. check-fragments-compile builds against the reference assemblies for a release; this builds against the assemblies Revit has actually loaded"* - and the second describing `RunSetupSteps`, which is the method they were both in front of. C# accepts that (the last one wins) and the build is **0 warnings**, so `Compile` had no documentation at all while `RunSetupSteps` was headed by a paragraph about compiling. **IT IS THE MOST USEFUL PARAGRAPH IN THE FILE FOR THE READER MOST LIKELY TO NEED IT** - somebody looking at a compile error that the gate said could not happen. **AND THIS REPOSITORY HAS HAD THIS EXACT DEFECT BEFORE**: [row 16](../FRAGMENT-ISSUES.md#) moved a summary off `ProjectKey` in `RevitOperations.cs` for the same reason, and its own note says it sat *"stacked as a second summary on an unrelated method while the one it describes carried none - in a repository where the comment is the design record"*. Two adjacent summaries compile clean on all eight releases, so nothing catches either of them

**State.** **FIXED 2026-09-21.** Moved onto `Compile`, where it says what it always said, plus a line recording where it had been and that nothing catches this - it was found by reading. No behaviour changes and the eight-release compile is unaffected

---

### Row 5b-46

*Moved from the register on 2026-09-23.*

**Defect.** **A RETRY LOOP THAT SLEPT THROUGH 300 ms OF GUARANTEED FAILURE, PER LINE, UNDER TWO COMMENTS THAT TOLD THE TWO CASES APART.** Found 2026-09-21 reading `platform/Heron.Core/HeronAppend.cs`, which this sitting's predecessor wrote. `Write` catches three exceptions and labels them in its own source: `IOException` **"busy: worth another attempt"**, `UnauthorizedAccessException` **"permissions: retrying changes nothing"**, `ArgumentException` **"a path nobody can write to"** - and returned the same bare `false` for all three. The caller then tried five times with a 20/40/60/80/100 ms backoff whatever the reason. **MEASURED**: a path the framework refuses outright took **301 ms** to fail, and would have taken it again for the next line and the next. Not a correctness fault - it is a comment describing a distinction the code did not make, in the one file whose whole subject is telling two failures apart

**State.** **FIXED 2026-09-21.** `Write` gained `out bool worthRetrying`, set true only on the busy case, and the loop returns at once when it is false. **PINNED IN `tests/Heron.Kernel.TestHost`, WHICH RUNS THE CODE**: a path holding a NUL character is the one of the three this machine can produce - no permissions failure is reachable as root - and the check is the TIME, because that is what distinguishes *it gave up* from *it tried five times*. **Proved by making it fail**: with the early return removed the same call takes **301 ms** and the host names it; with the fix, **0 ms**. 72 checks in that host now. **WHAT IS STILL CLASSIFIED LOOSELY, said rather than hidden**: a path *under a file* raises `IOException` and is therefore still retried, though it can never clear. Splitting that from a genuinely busy file needs the exception's own detail and is not worth a guess

---

### Row 5b-47

*Moved from the register on 2026-09-23.*

**Defect.** **THE HOOK WHOSE SUBJECT IS "A HOOK THAT LOOKS LIKE IT WORKS AND REFUSES NOTHING" CLAIMED SOMETHING IT HAS NEVER DONE, IN BOTH OF ITS OWN FILES.** Found 2026-09-21 reading `.claude/skills/heron-guard/`. Its second trap is real and correctly implemented - *a hook that dies with nothing on stdout is read as PERMISSION*, so a crash prints a **deny**. But both the Python docstring and the SKILL.md row state it as ***"every path out of here prints a decision"***, and the ALLOW path prints nothing at all. `tests/test_heron_guard.py` pins that on purpose - *"an edit with nothing to say about is allowed silently"* - and the SKILL's own closing line says **"An allowed edit prints nothing at all"**, four paragraphs below the row claiming the opposite. **NOTHING IS BROKEN**: silence is exactly what the host reads as no objection. What is wrong is a file contradicting itself about the one mechanism it exists to explain, in a repository where the comment is the design record

**State.** **FIXED 2026-09-21, IN BOTH COPIES.** Each now says the crash prints a deny, that an ALLOW prints nothing, that silence is what the host reads as *no objection*, and that the suite pins both halves. The code is untouched and `tests/test_heron_guard.py` still passes; this was a claim, not a behaviour. **THE TWO COPIES ARE WORDED DIFFERENTLY ON PURPOSE, AND THAT IS WORTH KNOWING**: the Python docstring records what the sentence used to say and when it was removed, because in this repository a code comment IS the design record; the SKILL.md row does not, because `.claude/agents/skill-maintainer.md` says a skill is instructions rather than a changelog - *"Say what is true, not what went wrong"*. The first version of this fix put the changelog in both, and the agent definition read an hour later is what corrected it. `heron-ship` keeps its own this-said-X-until lines because recording its drift is that file's established and deliberate convention

---

### Row 5b-50

*Moved from the register on 2026-09-23.*

**Defect.** **THE FILE AN OUTSIDE CONTRIBUTOR OPENS FIRST SAID 193 FRAGMENTS HAD NEVER MET A MODEL. IT IS 68 - AND THE NEXT LINE IS AN INSTRUCTION TO DERIVE THE NUMBER RATHER THAN BELIEVE IT.** Found 2026-09-21 reading `CONTRIBUTING.md`. The paragraph read *"**167 of the 360** carry a recorded proof, 55 of them on the write path"* and *"**193 fragments** have still never met a model"*; derived the same minute: **395 fragments, 327 `PROVEN`, 68 `DRAFT`**, and 166 of the proven are `MODIFY`. Wrong by 160 in the direction that costs most - it tells somebody offering to help that there is nearly three times as much proving work as there is. **AND check-docs COULD NOT SEE IT, THOUGH IT CHECKS EXACTLY THIS.** Its fragment-status gate matches one form on purpose - `N of the M fragments are PROVEN\|DRAFT` - because a looser pattern *"matched every line of docs/HANDOVER.md and every execution record, which are logs"*. `README.md` states both figures in that form and is correct to the day. `CONTRIBUTING.md` says the same fact in different words - *carry a recorded proof*, *have never met a model* - and is three words outside the gate. **A SYNONYM IS A HOLE IN A GATE THAT MATCHES ON WORDING**, and the file carries the warning about typed numbers twice: this paragraph's *"derive both numbers rather than believing this line"*, and further down *"do not trust a number typed here - this line read about 70 for nine days after it stopped being true"*, above *"the agent registry lists 250 agents, and most of them are built by now"* - which `agent-count.py` settles as **239 built, 9 host-provided, 2 deferred, 0 left**. Also found in the same read: `.github/ISSUE_TEMPLATE/fragment_proposal.yml` cites **Golden Rule 4** for *"reuse before generating"*. Golden Rule 4 is *never break a working Revit version*; reuse is **Golden Rule 3**, which `CONTRIBUTING.md` gets right on the same subject

**State.** **FIXED 2026-09-21, AND WRITTEN IN THE ONE FORM THE GATE CAN VERIFY** so it cannot rot silently a second time: two separate claims, each complete on its own line, each now checked by `check-docs.py` on every run. The registry sentence stops typing a total at all and points at the tool, which prints built, host-provided, deferred and left in one line. **The old wording is kept as a quotation with its date**, which the gate already skips by design - a citation is not a claim - because deleting the evidence would make the next drift look like the first. The Golden Rule number is corrected to 3

---
