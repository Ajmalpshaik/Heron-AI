# FRAGMENT-ISSUES archive — section 5b, rows 51 to 75

> **Finished rows, moved out of the live register.** These are rows from section 5b of
> [`FRAGMENT-ISSUES.md`](../FRAGMENT-ISSUES.md) — Heron's own defects found by reading the repository — whose state said they were
> finished when they were moved. Each still has its line in the register: the same number, a
> one-line title, the opening of its state, and a link to its full text here.
>
> **Nothing below was rewritten** except its links, which were re-pointed one folder deeper and
> each checked to reach the same file it reached before. Where a row disagrees with
> [DECISIONS.md](../DECISIONS.md), the [Golden Rules](../14-golden-rules.md) or the
> [Constitution](../../HERON_CONSTITUTION.md), **those win** — a row records what was true on
> the day it was written.
>
> **Nothing new is written here.** A new defect goes in the register, and a row here that turns
> out not to be finished is reopened **there**, on its own line.
>
> Rows 51 to 75 of section 5b belong in this file. A number in that band with no entry below
> was never moved — it is still in the register, in full. Written by
> [`tools/archive-fragment-issues.py`](../../tools/archive-fragment-issues.py).

---

### Row 5b-51

*Moved from the register on 2026-09-23.*

**Defect.** **A SECOND DOOR ONTO THE SAME BRIDGE, WITHOUT THE ONE SETTING THE FIRST DOOR EXISTS TO CARRY.** Found 2026-09-21 reading `.codex/config.toml`. `PROJECT-MAP` names that file as *"the same four agents for a second host, pointing at the same skills"*, and it starts the same MCP server: `command = "python"`, `args = ["mcp/server/heron_mcp_server.py"]`. `.mcp.json` starts it too, and sets `HERON_CLIENT_ID`. **The Codex one does not**, so the server gets `uuid4().hex[:12]` - a fresh identity per process. **THE RULE IS STATED IN BOLD IN THE CODE THIS AFFECTS**, in `heron_bridge_client.py`: *"ONE ID PER PERSON, SET EVERYWHERE"*, and the reason is the case a fresh id per server does not cover - *"a single conversation that uses BOTH doors. An assistant holding the MCP server open and also running this client from a command line is one chat by any honest reading - and Heron saw two, refused the second, and said 'in use by another chat'. It cost five interruptions in one session on 2026-09-12."* That comment then lists where the setting lives - `.mcp.json`, the exported value for a command line, and `batch-prove.py`'s `setdefault` - and **the fourth door was already in the tree while that list was being written.** `.codex/config.toml` was committed 2026-09-06; `.mcp.json` got the pin on 2026-09-13, in the commit called *"One conversation was two chats, and the lease was right to refuse it"*. **The fix that said EVERYWHERE enumerated three places and walked past the one that had been sitting there for a week.** Nothing was looking: no test reads either file. The refusal a modeller would meet is correct and reads exactly like a fragment failing, which is the shape [ONE PERSON, ONE `HERON_CLIENT_ID`](../FRAGMENT-ISSUES.md#) already records costing an afternoon

**State.** **FIXED 2026-09-21** - `.codex/config.toml` pins the same id as `.mcp.json`, which is the documented trade rather than a new one: two of the owner's own chats share an id and will not refuse each other, and that stops being right the day a second person shares this Revit. **Gated in `tests/test_config_and_health.py`, and the gate FINDS the host configs rather than listing them** - every `.json` or `.toml` in the tree that mentions `heron_mcp_server.py` - so a third host added later is caught the day it arrives, which is the half that failed here. A config that starts the server and pins **no** id fails, because absence was the whole defect. **AND THE COMMIT THAT FIXED IT FOUND A SECOND HALF, BY BEING REFUSED.** `tools/check-structure.py`'s `PARTS` and `SUPPORT` tables name every folder the repository knows about, and `.codex/` was in neither - tracked since 2026-09-06 and named in `PROJECT-MAP` the whole time. So `check-change.py` classified every file in it as **unrelated** and returned **SPLIT**, which is reported first and therefore **masked the real verdict**: `tests/test_change_gate.py`'s four checks that an unevidenced change is REVISE all went red, each getting SPLIT instead. **A folder the tables cannot name is a folder the change gate cannot judge**, and no change to `.codex/` had ever been put through it. `.codex` is now SUPPORT beside `.claude`, which is what it is

---

### Row 5b-53

*Moved from the register on 2026-09-23.*

**Defect.** **THE DISCLAIMER TOLD A MODELLER UNDER AN NDA THAT HERON HAS A LOCAL-ONLY MODE. IT HAS NEVER HAD ONE.** Found 2026-09-21 in the same read. `DISCLAIMER.md` said *"If you work under a confidentiality agreement, **check your project's configuration** before use. Heron supports a **local-only mode** for restricted projects."* **There is no such mode and no such configuration.** [12 §4](../12-security-and-permissions.md) offered three positions - cloud only, **local only**, hybrid per project - and [D-26](../DECISIONS.md) took **none of them**, in the owner's own words: *"Any project name, data, typing, or content being in the cloud is not an issue ... The main thing is that we should not upload the model itself."* The line is drawn at **the file, not the information**. Verified three ways: the decision; **every setting Heron understands, declared once in `HeronConfig.Defaults` - eight keys, not one about egress**; and the fact that *local-only* everywhere else in this repository means the **named-pipe transport** (D-02), which is a statement about who can reach the add-in and says nothing about what reaches an AI provider. **THIS IS THE WORST PLACE IN THE REPOSITORY FOR THAT SENTENCE.** It is the file a BIM manager reads to answer *"does using Heron breach our NDA?"* - the question [D-26](../DECISIONS.md) records as the one the person deploying it will be asked - and it told them a switch exists. Also rounded up in the same file: *"Every Heron change is a single entry in Revit's undo stack"*, where [Golden Rule 16](../14-golden-rules.md) is **per document** and says in as many words that the distinction *"is kept rather than rounded up"*

**State.** **FIXED 2026-09-21.** The section now says what is true and is checkable line by line: **Heron's replies ARE the conversation**, so what it says about the model reaches the provider by design and no setting changes that; **the model file never leaves** - `.rvt`, `.rfa`, family and template ([D-26](../DECISIONS.md)), embeddings computed locally ([D-24](../DECISIONS.md)), and a tool answers a question rather than returning the model; **everything else about the work travels**, names, counts, sizes, parameter values, reasoning and code, **and identifiers are not redacted first** - [Q-40](../OPEN-QUESTIONS.md) asked and the answer was no. Then, in as many words: **there is no local-only mode and no per-project configuration to check**, with the reader sent to `Defaults` rather than a number typed here, and the old sentence kept with its date. The undo line says **per document**. **A confidentiality agreement is a decision about the project, not a setting to change** - which is the sentence the file should always have carried

---

### Row 5b-55

*Moved from the register on 2026-09-23.*

**Defect.** **A GATE THAT MATCHES ONE WORDING LEAVES EVERY SYNONYM OPEN, AND FOUR FRAGMENT COUNTS ROTTED IN THREE OTHER WORDINGS.** Found 2026-09-21, the same read. `check-docs.py`'s fragment-status gate matches `N of the M fragments are PROVEN\|DRAFT` and says why it is narrow - a looser pattern *"matched every line of docs/HANDOVER.md and every execution record, which are logs"*. It is right to be narrow and it is not enough. **FOUR LIVE CLAIMS SIT OUTSIDE IT:** `README.md` - *"**The other 66** have still never met a model"*, written beside a freshly re-derived **327 of the 395**, so 395 − 327 = **68** and the two halves of one sentence disagree by two, **in the paragraph that exists to record that they once disagreed by thirteen**; `docs/33-external-repository-research.md` and `docs/32-master-architecture-reconciliation.md` - *"**218 fragments** have never met a model, that is the critical path"*, stale by 150, in a sentence whose whole force is the number; and `CONTRIBUTING.md`'s two, already [row 50](../FRAGMENT-ISSUES.md#). **AND README's IS INVISIBLE TWICE OVER**: the claim is broken across a line ending in *"The other 66 have"*, so a per-line check cannot see it even with the right wording - the same shape as [row 5b-36](../FRAGMENT-ISSUES.md#), where a refusal's sentence was split across two adjacent string literals and a source grep reported 2 of 3

**State.** **FIXED 2026-09-21.** All four corrected, and each rewritten in the one form the gate can verify rather than only being given the right number. **`check-docs.py` gained two more wordings** - *N fragments have never met a model* and *N of the M carry a recorded proof* - both chosen because each is a sentence this repository actually wrote, and **it now reads a PARAGRAPH rather than a line**, so a claim split across a line break is seen. Measured before adding either: the two new patterns fire on exactly the four defects and on nothing correct, and the paragraph read adds one hit, which is README's. The existing quotation and history rules carry over unchanged, so a row quoting an old number is still not a claim

---

### Row 5b-57

*Moved from the register on 2026-09-23.*

**Defect.** **THE FILE THAT OWNS THE EXIT-CODE CONTRACT SAID TWO SUITES BREAK IT, AND NEITHER DOES.** Found 2026-09-21 reading `tests/README.md` - the section `AGENTS.md` and `PROJECT-MAP` both send a reader to first. It said *"Not every suite that needs something exits 3 - `test_bridge_roundtrip.py` and `test_served_claims.py` exit 1 when their dependency is missing."* **Both return 3**, and the first carries the reason in its own source: *"EXIT 3, NOT 1, AND THE DIFFERENCE IS THE WHOLE OF FRAGMENT-ISSUES ROW 162"*. The exception was written before row 162 fixed it and outlived it by a week - **and it is the sentence that would tell somebody the new CI split ([row 49](../FRAGMENT-ISSUES.md#)) cannot be trusted**. **AND THE SECTION BELOW IT IS WRONG IN BOTH HALVES.** *"`check-gaps.py` runs 40 of the 41, on purpose... it skips `test_bridge_roundtrip.py`"* - measured today, it runs **208 of 208 and skips none**, and `check-gaps.py` does not mention that suite anywhere. The closing sentence is the dangerous one: *"anyone reconciling one against the other should expect the difference of one and not go looking for a lost suite"* - **the difference is zero**, so anyone who followed it went looking for a suite that does not exist. **AND THE TABLE HEADED *WHAT IS HERE* NAMED ONE TEST HOST OF FIVE**: `Bridge`, while `Kernel`, `StackGuard`, `Banner` and `BindingNote` all sit beside it - and `StackGuard` is the one [row 170](../FRAGMENT-ISSUES.md#) found had compiled every day and run on no day. The file that says what is here never said it was here. **All of this under its own line four paragraphs up: *Derive the count; do not read one here*.** Also found in the same read: `brain/README.md`'s `fragments/` row carried *360 as of 2026-09-10 - 193 `DRAFT`, 167 `PROVEN`*, **the fourth copy of the pair [row 50](../FRAGMENT-ISSUES.md#) fixed in `CONTRIBUTING.md`**, in a table headed *what is here now*, above its own *"Do not trust those three numbers over the tools"*

**State.** **FIXED 2026-09-21.** The exception is replaced by what is true - every suite that cannot run says so with 3 - with the old sentence and its date kept, and with the consequence named: CI now splits on the exit code, so a suite returning 1 where 3 is expected is a failure rather than a skip. The `check-gaps` section says it skips none and gives the two commands instead of the two numbers. The test-host row covers all five, says which one has to be built first, and ends in `ls -d tests/Heron.*.TestHost` rather than a list that goes stale when a sixth arrives. `brain/README.md` stops typing the pair at all: the row already told the reader to take it from the tools, and now it only does that

---

### Row 5b-58

*Moved from the register on 2026-09-23.*

**Defect.** **THE REPOSITORY'S OWN GAP REPORT CHECKED THREE OF THE FOUR GATES THAT MUST PASS, AND THE MISSING ONE IS THE ONLY THING THAT READS `Heron.addin`.** Found 2026-09-21 reading `tools/check-gaps.py`. Its `CHECKERS - the repository checking itself` section ran `check-docs`, `check-metadata` and `check-structure`. `.claude/skills/heron-ship` calls them **the four that must pass** and the fourth is `check-package`, whose whole job is the manifest Revit reads before it reads anything else - and `gates.yml` says why that matters in its own words: *"a manifest fault costs a modeller their whole add-in while every other gate on this page stays green."* **It exits 0 in about a tenth of a second and needs nothing but Python**, so there was never a reason for it to be absent; it simply was. A gap report that leaves out the gate for the failure nobody else catches is the shape [row 5b-35](../FRAGMENT-ISSUES.md#) named: *a guard that fails open and says nothing is indistinguishable from a guard that passed*. **AND `gates.yml` MISQUOTED THE CHECKLIST IN THE SAME BREATH**, calling them *"the three the ship checklist calls 'the three that must pass'"* above a file that says four. **AND `check-gaps` EXPLAINED ITS OWN THIRD EXIT CODE WITH A REASON THAT HAD STOPPED BEING TRUE**: *"only the exit code crosses this boundary - stdout goes to DEVNULL"*, twenty lines below the `subprocess.run` that now captures stdout to `PIPE`, changed by [rows 160 and 166](../FRAGMENT-ISSUES.md#) with the cost of NOT capturing it recorded in the comment directly above. The conclusion survived the premise

**State.** **FIXED 2026-09-21.** `check-package.py` joins the other three, and the list stays typed rather than derived **with the reason written down**: these four are the ones where a non-zero exit means *your change*, which is a judgement the ship checklist makes and this file follows - the rest of CI is reports, and a report's finding is a question for a person. `gates.yml` says three of four and names where the fourth runs. The exit-code comment now says what is true - the exit code is the only thing a suite can say **without being read** - and keeps the DEVNULL history as history. **ALSO HERE, A GATE FOR A PAIR THAT AGREES TODAY**: `.claude/agents/*.md` and `.codex/agents/*.toml` are the same four agents for two hosts, neither generated from the other, **byte-identical in name, description and body** - and nothing compared them. `tests/test_host_agents.py` does, finding the pairs rather than listing them and printing the diff across two formats; proved by rewording one rule and by removing one file. Not a defect row of its own, because nothing was wrong - it is the answer `tests/test_heron_guard.py` already gives for the layering rule. **Checked and dismissed in the same sitting**: `A10` and `A15` cannot be closed here - `huggingface.co` answers `CONNECT tunnel failed, 403` from this container while `pypi.org` answers 200, so the package half is not what is missing

---

### Row 5b-60

*Moved from the register on 2026-09-23.*

**Defect.** **THE EVIDENCE TOOL'S OWN SUMMARY MERGED THE FOUR STATES, THREE LINES BELOW ITS GATE THAT DOES NOT - AND SEVEN DESCRIPTIONS OF CI WENT STALE UNDER [ROW 49](../FRAGMENT-ISSUES.md#) TWO HOURS AFTER IT LANDED, WHICH IS THIS SWEEP'S OWN RULE BROKEN BY THIS SWEEP.** Found 2026-09-21 reading `tools/change-evidence.py` for an unrelated reason. **THE MERGE, MEASURED**: with the MCP SDK made unimportable, one record printed `tests PASS  2 ran, 1 could not run (test_mcp_serves.py)` and, three lines below it, `2 ran, 1 did not pass`. `tests_gate()` excludes exit 3 correctly and `_code()` already returns *could not run*; only the headline counted them together, in the tool a record exists to be believed from - and in the repository whose `AGENTS.md` says *separate the four states and never merge them*. **A SECOND THING, FALSE IN BOTH HALVES, IN TWO FILES**: `check-change.py` and `tools/README.md` both justified the pre-existing-failure rule with *"two suites here exit 1 for want of the MCP SDK, so the tests gate reads FAIL on a plain container whatever anybody changed"*. Measured the same way: **both exit 3**, and the tests gate reads **PASS**. The rule is right and the example given for it is the one case it is not needed for. **AND THE THIRD IS MINE.** [Row 49](../FRAGMENT-ISSUES.md#) renamed CI's list from known-failure to known-NOT-RUNNABLE at 07:21 and **seven live descriptions of the old behaviour were still in the tree at 09:00**: `change-evidence.py`, `check-change.py`, `tools/README.md` twice, `.claude/skills/heron-ship/SKILL.md`, `README.md` and `tests/test_fragment_tracking.py`. [Row 26](../FRAGMENT-ISSUES.md#)'s rule is **a stale claim is never in one place**, and [row 54](../FRAGMENT-ISSUES.md#) is four hunts that each grepped only the wording they had just fixed. I enforced both all morning and then changed a mechanism without grepping for its description

**State.** **FIXED 2026-09-21.** The summary counts and names failed and could-not-run separately, and `tests/test_change_gate.py` pins it by capturing what `show()` prints for a record holding one of each - proved by putting the merge back and watching it go red. The two justifications are replaced by a **true** example - a gate already red for a reason nobody here caused - with the false one kept and dated, because a justification that was wrong is worth more as a record than as a deletion. All seven descriptions now say what the job does, each keeping the old name with its date. **WHAT THIS EARNS, AND IT IS THE HALF WORTH COPYING**: the rule was stated as *look for the other copies of a CLAIM*; it is really *look for the other descriptions of a MECHANISM you just changed*, which is the same rule and the harder half, because the descriptions are correct until the moment your change lands. Nothing announced them. **What is NOT touched**: `docs/HANDOVER.md`'s Tests row and the work-note under `docs/work-notes/plans/`, both dated logs that are supposed to hold what was true that morning - the distinction `check-docs.py` already draws by skipping them

---

### Row 5b-62

*Moved from the register on 2026-09-23.*

**Defect.** **THE TWO DOCUMENTS A BIM MANAGER IS SENT TO BOTH UNDERSTATE WHAT HAS BEEN CONFIRMED ABOUT REVIT 2027, AND ONE OF THEM STILL ASKS SOMEBODY TO CONFIRM IT.** Found 2026-09-21 finishing `docs/PROJECT-MAP.md`. Its **BIM manager** route - the one that exists to say what is *tested* rather than *declared* - says *"the compile gate covers 2020-2027; a real Revit has been used on 2020 and 2024."* **It is three.** `NEEDS-CHECKING` **A12** is struck through as *"DONE 2026-09-19 on the owner's PC - and on **ALL THREE releases** installed there, 2020, 2024 and 2027, **rather than on 2024 alone**"*, `A13` says the same of deploying over a previous install, and `HANDOVER` records *"Loads on 2020, 2024 and 2027"*. The record anticipated exactly this misreading and the route was never updated. **AND `docs/16-version-support-strategy.md` - the other link in that same route - STILL CARRIED AN OPEN ACTION THAT HAD CLOSED TWICE OVER**: its runtime table said of 2027 *".NET 10 ... Reported by a shipping project; **confirm against the SDK**"*. It is confirmed in four places - `heron_dotnet.py` pins `2027: 10`, `Directory.Build.props` maps it, CI compiles 2027 with the .NET 10 SDK on every push, and a real Revit 2027 loaded the add-in on 2026-09-19. **THE TWO LISTS ARE GENUINELY DIFFERENT AND THAT IS THE POINT**, which is why the fix is not simply *change two to three*: the ADD-IN has loaded on three releases; FRAGMENTS have been proven on two. Measured across every proof block in the library - 2020 and 2024 only, and none names 2027

**State.** **FIXED 2026-09-21, WITH THE TWO LISTS SAID SEPARATELY.** `PROJECT-MAP` now names the compile gate, the releases the add-in has loaded on with their register rows, and the releases fragments are proven on, and says a proof is a recorded run against a named model. Doc 16's cell says confirmed and names all four confirmations, keeping the old sentence with its date because an open action that closed is worth more as a record than as a deletion. **CHECKED AND DISCARDED IN THE SAME READ, BECAUSE THE FIRST COUNT WAS WRONG**: a sweep of the proof blocks reported a fragment proven on **Revit 2022**, which would have made three files understate the fragment list as well. It is a false positive - the 2022 is inside `create-floor`'s findings string, naming a family, and that proof's `model:` line says Revit 2024. The three files saying *2020 and 2024* about fragments are correct and are left alone

---

### Row 5b-63

*Moved from the register on 2026-09-23.*

**Defect.** **THE SWEEP'S OWN BOOKKEEPING TOOL REFUSED TWICE IN ONE SITTING AND EXITED 0 BOTH TIMES, SO NEITHER REFUSAL WAS VISIBLE TO THE SESSION THAT CAUSED IT.** Found 2026-09-21 by noticing that the read count had not moved after a mark reported success. `review-ledger.py --mark` refuses for four good reasons - an unknown verdict, a path outside scope, a section 5b it cannot read, and a `--note` naming no row that exists - and **every one of them was a bare `return`**, with `main()` doing `cmd_mark(...)` and then `return 0`. So the tool printed *"Nothing was recorded"* on stdout and told every caller **fine**. **MEASURED**: `--mark <path> issue --note "5b-99's bad note"` prints the refusal and exits **0**. **IT COST TWICE IN THE SAME SITTING, BOTH SILENT.** A note beginning *"5b-50's other half"* puts an apostrophe where the row check wants a delimiter, so the id does not parse and the mark is refused - and a shell line ending `>/dev/null 2>&1 && echo marked` printed **marked**. It happened again an hour later with *"5b-62's other half"*, and was caught only because the unique-path count had not moved. **THE REFUSALS ARE RIGHT AND ARE NOT THE DEFECT** - each was added on purpose, the row check after a Codex review on PR #219, because a mark whose row nobody has verified is worse than no mark. **A guard that refuses correctly and reports success is the same shape as [row 5b-35](../FRAGMENT-ISSUES.md#)**, one layer down: it fails *closed* and then says the word that means open

**State.** **FIXED 2026-09-21.** Every refusal returns a named `COULD_NOT = 2` and `main()` passes it through; the write path returns 0. **2 and not 1, and the constant says why**: nothing failed - the tool declined to write a mark it could not stand behind, which is the third state `check-package.py` and `change-evidence.py` both already name as *the tool could not do its job*. Measured after: the same refusal now exits **2** and a status run still exits 0. Gated in `tests/test_review_ledger.py`. **AND THE TOOL REFUSED THIS ROW'S OWN MARK WHILE IT WAS BEING WRITTEN** - *"No such row in section 5b: 5b-63. Write the defect there FIRST"* - which is the check doing exactly its job, and the first time this sitting that a refusal was visible

---

### Row 5b-64

*Moved from the register on 2026-09-23.*

**Defect.** **THE FILE EVERY AGENT READS FIRST SAYS `.claude/skills/` IS *"the ONLY skills tree"*, AND HERON'S OWN SKILLS - THE JOBS IT DOES FOR A MODELLER - LIVE IN `brain/skills/`.** Found 2026-09-21 reading `AGENTS.md`. There are three directories named `skills`: `.claude/skills/` (six house rules for whoever works on Heron), **`brain/skills/` (ten `.yaml` skills that ARE the product** - `count-elements`, `sprinkler-layout`, `trace-system` - loaded by name through `heron_skill.SKILLS_DIR`, counted by `heron_audit`, reported by an MCP tool and listed as row 2 of the balance of work), and `tools/jobs/skills/` (their model halves). **`docs/PROJECT-MAP.md` CARRIES THE SAME SENTENCE AND CONTRADICTS ITSELF TEN LINES ABOVE IT**: its `brain/` row reads *"Knowledge - fragments, **skills**, capabilities, retrieval, context, the graph"*, and its `.claude/` row ten lines below reads ***"The only skills tree"***. **WHY IT IS WORSE THAN A LOOSE WORD.** An agent that believes it then meets ten yaml files nobody mentioned, and has two readings available: the map is wrong, or `brain/skills/` is a stray to be tidied. One loses trust in the map; the other reaches for the product. Both are caused by one word doing a job it cannot do

**State.** **FIXED 2026-09-21, BY SAYING WHICH KIND OF SKILL.** Both files now read *the only tree of **house rules***, and each names `brain/skills/` beside it as the other thing - what Heron *does* for a modeller. **The two share a word and nothing else**, and the sentence says so rather than leaving a reader to work it out. Nothing was moved or renamed: the split is correct, only the description was not

---

### Row 5b-66

*Moved from the register on 2026-09-23.*

**Defect.** **THE SIZE OF THE FRAGMENT LIBRARY WAS TYPED AS A PRESENT-TENSE FACT IN ELEVEN LIVE PLACES, WRONG BY UP TO 52, IN THE REPOSITORY WHOSE SECOND *NEVER* IS *"never type a number a command can derive"*.** Found 2026-09-21 checking a citation in `docs/32`. The library has been 328, 329, 343, 349, 350, 360, 372 and is **395**; every one of those was true the day it was typed. **THE WORST IS A REPORT CONTRADICTING ITSELF ONE LINE LATER.** `tests/test_naming.py` prints its section heading and then its measurement: *"2. The derivation, against **all 360 fragments** on disk"*, and directly beneath it *"ok  **395 fragments** on disk carry a capability"*. **A report that names its own sample size must derive it**, and this one already had the number - it printed a typed one first. The others: `tests/test_taxonomy.py` (*"IT MEASURES THE REAL 360 FRAGMENTS"*), `tests/test_revit_gate.py` (*"A checklist over 360 fragments"*), `brain/heron_search.py` (*"reads all 360 fragment files"*), `brain/heron_validate.py` (*"279 of the 349 fragments"*), `revit/Heron.Revit.Addin/HeronFragmentGlobals.cs` (*"so did all 343 fragments"*), `brain/fragments/report-schedule-definition/impl/any/fragment.cs` (*"nothing in the 349 fragments"*) and `docs/32-master-architecture-reconciliation.md`'s §6.4 cell (*"360 fragments"*). **NOTHING COULD SEE ANY OF IT**: `check-docs.py` reads markdown only, and its fragment pattern is the deliberately narrow *N of the M fragments are `PROVEN`*, so six of the eight were in file types it never opens and the rest in wordings outside it. **AND THE SUBSTANTIVE CLAIM UNDER THE STALEST NUMBER WAS RE-MEASURED RATHER THAN ASSUMED STALE**: `report-schedule-definition` says nothing in the library provides a `ViewSchedule` to chain from, written of 349 - measured today against 395, **still none declares one**, so the finding stands and the count was the only part that had rotted

**State.** **FIXED 2026-09-21, AND HELD BY A NEW SUITE.** Where the number added nothing it is gone - *every fragment on disk*, *the whole library* - and where it records a measurement it is kept with its date, because a measurement edited to a number it was never taken at is worse than a stale one. `tests/test_library_total.py` finds every whole-library total across `.py .md .cs .yaml .yml .toml` and **requires each to be either today's count or dated in its own sentence**, with quotations excused (`test_stack_guard.py` cites HANDOVER's *372* in the paragraph that corrects it) and the five dated registers, `work-notes/` and `handover-archive/` skipped whole - the line `check-docs.py` already draws, for the reason it already gives. **THE DECLARATIONS ARE FOUND, NOT LISTED**, so a twelfth is caught the day it arrives; and an empty sweep is a failure, because a pattern that has stopped matching passes perfectly. 18 totals checked, and it was proved by putting a stale one back and watching it go red

---

### Row 5b-67

*Moved from the register on 2026-09-23.*

**Defect.** **THE SENTENCE SPLITTER IN THREE GATES DOES NOT BREAK ON `.**`, AND THIS PROSE ENDS SENTENCES THAT WAY CONSTANTLY - SO A HISTORY WORD IN ONE SENTENCE EXCUSED A STALE CLAIM IN THE NEXT.** Found 2026-09-21 by negative-testing `tests/test_library_total.py` - the new gate reported a failure and quoted **the wrong sentence**, the one before the claim. `check-docs.py`'s `BREAK` is `(?<=[.!?])\s+`, a lookbehind of exactly **one** character, so `...proving pass.** 68 of the 360 fragments...` is **one sentence**: the terminator is followed by `**`, not by whitespace. **THIS IS [ROW 5b-55](../FRAGMENT-ISSUES.md#)'s OWN FAILURE ONE SIZE SMALLER, AND IT IS MINE.** That row moved the HISTORY skip from the paragraph to the sentence because a markdown table is one paragraph and one historical row switched the check off for every row beside it. The sentence was the right unit; the splitter was too literal to find it, and this repository's house style - bold to the full stop - is precisely the case it misses. **THE CONSEQUENCE IS A GATE THAT PASSES**, which is the only kind of gate defect that never announces itself: the three claim checks rows 55 and 61 added all read a sentence that could silently be two

**State.** **FIXED 2026-09-21 IN BOTH FILES, WITH THE SAME REGEX.** `[.!?][*_"'\u2019)\]]*\s+` - the terminator, then any closing emphasis, quote or bracket, then the break. Measured: the same negative test now names the sentence the claim is actually in, and `check-docs.py` still exits 0 with `tests/test_docs_guard.py` green. **Found only because the new gate was negative-tested rather than trusted for going green**, which is the habit worth keeping: a gate that has never been shown to fail has not been shown to work

---

### Row 5b-68

*Moved from the register on 2026-09-23.*

**Defect.** **THE BUILD FILE EVERY C# TARGET INHERITS SAYS ONE REVIT VERSION IS PROVEN. EIGHT COMPILE ON EVERY PUSH AND THREE HAVE RUN A REAL REVIT.** Found 2026-09-21 reading `Directory.Build.props`. Its header comment reads *"The multi-targeting machinery is here from the first commit because it is cheap now and ruinous to retrofit (D-05, docs/16). **Step 1 proves ONE version; the structure already supports the rest.**"* - written at Step 1, in the present tense, and the metadata header on every file in this repository now reads **Heron-Step: 18**. **WHAT IS ACTUALLY TRUE, ALL OF IT CHECKABLE**: `tools/check-compile.py` builds every release in the table twelve lines below the sentence; `.github/workflows/gates.yml` runs it on every push in a job literally named ***The C# compiles, 2020 to 2027***; and [A12](../NEEDS-CHECKING.md) records a real Revit loading the add-in on **2020, 2024 and 2027**. **SAME FAMILY AS [ROW 66](../FRAGMENT-ISSUES.md#) AND [ROW 61](../FRAGMENT-ISSUES.md#)** - a sentence nobody changed, that the project walked past - but this one understates rather than overstates, which is the version that costs differently: it reads as *do not rely on the other seven*, in the file whose whole job is to build them

**State.** **FIXED 2026-09-21.** The comment now names the three things a reader can run or look up, and keeps the old sentence with its date, because *it was true at Step 1* is the part worth keeping. No build behaviour changed - the table it sits above was already right, and `tests/test_dotnet.py` already holds it against `heron_dotnet.RELEASES`

---

### Row 5b-70

*Moved from the register on 2026-09-23.*

**Defect.** **THE JOB CALLED "THE GATES THAT MUST PASS" RUNS NINE COMMANDS. FOUR DOCUMENTS SAY FOUR, AND ONE OF THEM CALLS THE OTHER FIVE *"reports"*.** Found 2026-09-21 by [row 69](../FRAGMENT-ISSUES.md#) turning a pull request red. **MEASURED, BOTH HALVES.** `.github/workflows/gates.yml`'s job runs `check-docs`, `check-metadata`, `check-structure`, **`check-signatures`, `check-licence`, `check-narrow-errors`**, `check-package`, **`check-routing`** and **`check-intrusion`** - nine, each under `bash -e`, so a non-zero from any one turns the PR red. `AGENTS.md`, `.claude/skills/heron-ship/SKILL.md` section 1, `.claude/skills/README.md` and `docs/PROJECT-MAP.md` all name **four**, and `tools/check-gaps.py` states the false half outright: ***"The other checkers in CI are reports, and a report's finding is a question for a person."*** **THE COST IS EXACTLY WHAT IT LOOKS LIKE.** All four documented gates green locally, every suite green, pushed - and `check-routing.py` exited **2**. (No total typed here: `check-docs.py` caught the first draft of this very row saying *210* an hour after the library became 211, which is the gate doing its job to the row complaining about typed counts.) Nothing a contributor is told to run before pushing could have caught it. **THIS IS [ROW 5b-58](../FRAGMENT-ISSUES.md#) ONE SIZE UP**: that row was `check-gaps` running three of the four; this is every document naming four of the nine

**State.** **FIXED 2026-09-21 IN ALL FOUR PLACES, BY NAMING THE OTHER FIVE AND WHAT EACH CATCHES.** `heron-ship` gains a section headed *these four are not what decides the pull request, nine do*, with the five as runnable lines - including `HERON_KNOWLEDGE=$(mktemp -d)` for the two that need a store - and the morning it cost. `AGENTS.md` carries one sentence and a pointer. `check-gaps.py`'s false sentence is replaced with the measurement and the old wording kept. **THE FIVE ARE DELIBERATELY NOT ADDED TO `check-gaps`'s SWEEP, AND THAT IS RECORDED RATHER THAN QUIET**: `check-routing` and `check-intrusion` rebuild a knowledge store when it is stale, which is minutes against tenths of a second for the other four, and a sweep people skip is worse than a short one. Adding them needs the same four-state care the tests got - an absent store is not a gap in the code - and that is its own change

---

### Row 5b-71

*Moved from the register on 2026-09-23.*

**Defect.** **CI'S OWN COMMENT SAYS TWO CHECKERS *"say so rather than failing"* WHEN THERE IS NOWHERE TO KEEP A KNOWLEDGE STORE. THEY DIED WITH A TRACEBACK.** Found 2026-09-21 measuring [row 70](../FRAGMENT-ISSUES.md#)'s nine one at a time. `.github/workflows/gates.yml` says of `check-routing` and `check-intrusion`: *"These two need somewhere to put a knowledge store, and **say so rather than failing** when there is none: `No %APPDATA% and no HERON_KNOWLEDGE`."* **Run on Linux with the variable unset**, both end in an unhandled `ValueError` four frames down in `brain/heron_scope.py` and exit **1**. The sentence quoted in the comment is real - it is the exception's own text - but it arrives **under a stack trace**, which is the opposite of saying so, and exit 1 is *your change is broken* rather than *this machine has nowhere to put the store*. **THIS IS WHERE IT BITES**: Windows has `%APPDATA%`, so the owner never sees it; CI sets `HERON_KNOWLEDGE`, so CI never sees it; **a contributor on Linux following the checklist is the only one who meets it**, and what they get looks like they broke Heron

**State.** **FIXED 2026-09-21 IN BOTH TOOLS, AND BOTH PATHS RUN.** `heron_scope.knowledge_dir()` already returns `None` rather than raising, so each tool now asks it before doing anything and prints three plain lines - what is missing, that an empty folder is enough, and to run it again - then exits **2**, the code this repository uses for *the tool could not do its job*. **2 and not 0, because nothing was checked and that is not a pass**; 2 and not 1, because nothing failed. Measured both ways: unset gives exit 2 and the three lines with no traceback, set gives exit 0 and the real report. The gates.yml comment now records what was measured and keeps its old claim with the date

---

### Row 5b-74

*Moved from the register on 2026-09-23.*

**Defect.** **AN MSBUILD TOOLCHAIN ERROR IS NOT A COMPILE ERROR, AND THE GATE'S CLOSING SENTENCE TOLD ME IT WAS. IT COST ME A WRONG DIAGNOSIS, WRITTEN UP AND THEN WITHDRAWN.** Found 2026-09-21 running `tools/check-compile.py` in a fresh container. It reported **`FAILED 2020`** - `error MSB3644: The reference assemblies for .NETFramework,Version=v4.7.2 were not found` - under its own closing line: ***"A failure is normal for code no compiler has read yet, and each one names its file and line. Fix it and run this again."*** **CI HAD COMPILED 2020 GREEN ON THE SAME COMMIT THROUGHOUT** (run 35586557310: `COMPILED 2020, 2021 ... 2027`). **WHAT I WROTE FIRST WAS WRONG, AND THAT IS HALF THE ROW.** I drafted this as a missing .NET Framework targeting pack and proposed adding `Microsoft.NETFramework.ReferenceAssemblies`. Then I measured instead of writing: a bare `net472` project built here **first try**; `Heron.Core` for 2020 built **immediately afterwards**; and a full re-run of the same command passed **all eight releases**. The reference assemblies were never missing - they arrive through NuGet restore, and the very first build in a fresh container raced it. **ONE UNREPRODUCIBLE FAILURE IS NOT A FINDING**, so that half is withdrawn and recorded here rather than quietly deleted. **THE PART THAT SURVIVES IS THE SENTENCE.** MSB3644 is raised **before a line of C# is read** - it can never be a statement about the code - and the gate rounded it up into *fix your code and run it again*. This gate already draws exactly that distinction ten lines above, where `why_unbuildable()` prints `Revit 2027 SKIPPED - <what is missing>` and `a skip is not a pass`; it just could not draw it for an error MSBuild raised mid-build

**State.** **FIXED 2026-09-21, AND NOT BY TURNING A FAILURE INTO A SKIP.** `only_the_toolchain()` is true when **EVERY** error is `MSB3644`, `MSB4019` or `NU1101` - every one, not any one, because a real compile error beside a missing targeting pack is still a real compile error and rounding the pair up would hide it. The release still counts as FAILED; what changes is that the report says *every error here is MSBuild or NuGet saying something is MISSING, raised before a line of C# was read - that is not this code*, and the closing lines tell the reader to run it again first and name `docs/30` if it persists. Checked on all four cases - empty, toolchain only, toolchain beside a `CS0103`, and a `CS0103` alone. **THE HABIT WORTH KEEPING IS THE ONE THAT KILLED MY OWN ROW**: the diagnosis was three sentences from being committed, and measuring it took four minutes

---
