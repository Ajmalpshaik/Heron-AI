# FRAGMENT-ISSUES archive — section 5b, rows 126 to 150

> **Finished rows, moved out of the live register.** These are rows from section 5b of
> [`FRAGMENT-ISSUES.md`](../FRAGMENT-ISSUES.md) — Heron's own defects found by reading the repository — whose state said they were
> finished when they were moved. Each still has its line in the register: the same number, a
> one-line title, the opening of its state, and a link to its full text here.
>
> **Nothing below was rewritten** except its links, which were re-pointed one folder deeper and
> each checked to reach the same file it reached before. Where a row disagrees with
> [DECISIONS.md](../DECISIONS.md), the [Golden Rules](../14-golden-rules.md) or the
> [Constitution](../../HERON_CONSTITUTION.md), **those win** — a row records what was true on
> the day it was written.
>
> **Nothing new is written here.** A new defect goes in the register, and a row here that turns
> out not to be finished is reopened **there**, on its own line.
>
> Rows 126 to 150 of section 5b belong in this file. A number in that band with no entry below
> was never moved — it is still in the register, in full. Written by
> [`tools/archive-fragment-issues.py`](../../tools/archive-fragment-issues.py).

---

### Row 5b-127

*Moved from the register on 2026-09-23.*

**Defect.** **A MEASUREMENT TOOL RAN THE WHOLE MEASUREMENT AT A SETTING NOBODY ASKED FOR, AND PRINTED THAT SETTING AS IF IT HAD BEEN CHOSEN.** Found 2026-09-22 reading `tools/measure-graph.py` - 394 lines, **no suite**, the LAST of the six tools in `tools/` that no suite names. It answers Q-52 by running it, and the numbers it prints are in docs/33 s5.16. Its settings were read by hand out of `argv` with an `in` test and an index. **MEASURED, by running it**: `--wieght 0.05 --sedes 1` ran the full measurement, printed `settings weight=0.30 seeds=5` and exited **0**; `--weight` alone died in `IndexError`, `--weight abc` in `ValueError`, and `--seeds 2.5` in `ValueError`. `--sweep` also ignores `--weight` and `--seeds` without a word, because it has its own list of six. **A number recorded against a setting nobody asked for is worse than no number, because it is quoted afterwards** - and the whole point of this tool is that Q-52 was run rather than argued. It is the same defect rows 5b-112 and 5b-114 fixed in `brain/`, in a tool, where the cost is a recorded figure rather than a confused answer

**State.** **FIXED 2026-09-22.** A `settings_from()` reads every flag **before the store is opened** and returns the reason instead of a number: a flag this tool does not have, a bare word, a flag with no value, a weight that is not a number, seeds that are not whole. It exits **2** and names what it does take, which is the house refusal `heron_company` and the rest already give. Asking for `--sweep` together with `--weight` or `--seeds` is refused too, because the sweep has its own six settings and one of the two was going to be ignored - which is this defect wearing a different hat. **Nothing about the measurement moved**: a real run at `--weight 0.05 --seeds 3` prints those settings and still answers *the graph cost something and returned nothing*, which is docs/33's recorded finding at the gentlest setting rather than the default. `tests/test_measure_graph.py` is the first suite this tool has ever had: **one red** against the module as found. **THE REST OF THE FILE IS A NEGATIVE RESULT AND THE SUITE RECORDS IT AS ONE** - the four query shapes and the two they decline to repeat, `rank_of` returning None rather than a large rank for an answer that never came back, the tally dividing MRR by everything asked including misses, the empty-ranking path keeping its two-value shape, and the fusion leaving the order alone at weight 0 while lifting a neighbour two seeds reach: **all green before the fix and after it**. **The suite never runs the measurement** - that opens the store, re-indexes it and asks 396 fragments four ways, and writes outside the repository - so `main()` is called only with argv it must refuse

---

### Row 5b-129

*Moved from the register on 2026-09-23.*

**Defect.** **A GERMAN WORD COUNTED AS A LICENCE, AND A LIST NOBODY COULD FINISH READING.** Found 2026-09-22 reading `tools/check-licence.py` - 288 lines, **no suite**, the second of the seven CI gates that had never been opened. It answers Q-53 as D-66 and its own docstring states the rule it must never break: *a file with no marker at all is reported as UNMARKED rather than as clean - 'no evidence of a problem' and 'evidence of no problem' are different findings, and a tool that merges them is the tool scientific-agent-skills already has.* **TWO WAYS IT MERGED THEM, both measured on trees written for the purpose.** **ONE**: `LICENCE_NAME` matched a bare `mit` case-insensitively, so an imported unit whose only occurrence was the German word - `gemessen mit dem Werkzeug` - came back **1 CLEAN**, while the same file without that word comes back **UNMARKED**, correctly. Heron reads standards and a standard is not always in English. **TWO**: an unmarked list longer than forty printed forty and said `..and 5 more (--all)`, and **`--all` printed the same forty** - so five units could not be seen by any documented means. **NOT AN R-82 BREACH AND NOT CLAIMED AS ONE** - rows 5b-100 and 5b-111 both recorded that distinction, and R-82 is about the scanner's WINDOW rather than a report's margin. The defect here is on the tool's own terms: it names a remedy, and the remedy does nothing. **NEITHER IS A LIVE FAILURE TODAY, and saying so is part of the finding**: all 406 units declare `source: OFFICIAL`, no file carries the word `mit`, and the tool reports 406 clean. Both holes are in the branch that the first **IMPORTED** unit lands on - the future docs/09's COMMUNITY PACKAGES plans for, and the whole reason this tool exists

**State.** **FIXED 2026-09-22.** A bare `MIT` is matched **case-sensitively** now, in its own pattern beside the case-insensitive list: the licence is written in capitals and the German word is not, and `MIT License` was already matched either way. Both real forms still work - `licence: MIT` and `The MIT License` are each still a licence, and both are checked. `--all` prints the whole unmarked list, and the *and N more* line is derived from what was actually shown rather than from a second copy of the number forty. **Nothing else moved, and the gate reads the same on the repository**: 406 units, 406 clean, 0 findings, 0 unmarked. `tests/test_check_licence.py` is the first suite this gate has ever had: **3 red** against the module as found, with everything around them green before and after - all three findings it exists to make (a reservation of rights, an incompatible licence, an OFFICIAL unit naming somebody else), both ways of being UNMARKED, and the two false positives an earlier round already took out of it, a C# cast read as a copyright holder and a year with no name after it. **ONE THING RECORDED RATHER THAN FIXED**: `--all` is the only flag and an unknown one is ignored in silence - the same shape as rows 5b-112 and 5b-127 - but here it costs only a missing CLEAN section and cannot move the exit code, so fixing it would widen a change that is about two holes in the UNMARKED branch

---

### Row 5b-130

*Moved from the register on 2026-09-23.*

**Defect.** **THE GATE THAT GUARDS THE OWNER'S SCARCEST INPUT DROPPED A FRAGMENT IT COULD NOT READ AND SAID NOTHING.** Found 2026-09-22 reading `tools/check-signatures.py` - 155 lines, **no suite**, the fourth of the seven CI gates that had never been opened. It exists because thirteen fragments the owner had already signed were sitting at DRAFT on 2026-09-13, so the next proving round offered them to him to prove **again** - his own complaint, in his own words, and *the worst possible thing to be right about*. `findings()` calls `F.load_all()` and **discards the problems it returns**. **MEASURED, on a library of three built by copying real fragments, one of them a `fragment.yaml` that will not parse**: the answer is **`Signatures in the library: 1`**, exit **0**, the broken fragment never named, and nothing anywhere saying a fragment could not be read. **If the one it cannot read is the one carrying an unused signature, this gate says nothing is waiting and the owner signs it again** - the exact failure it was built to prevent, one level up, and D-52's plausible zero wearing this tool's own face. **ALSO FOUND, and smaller**: its usage line documents `--all` as *including what is fine*, and `--all` adds exactly one sentence and lists nothing - measured by diffing the two runs

**State.** **FIXED 2026-09-22.** The problems are carried out of `findings()` beside the answer and printed under **COULD NOT BE READ**, naming each one, with the sentence that the count above is over fewer fragments than the library holds and that *invisible is how a signature gets spent twice*. The headline now reads **`Signatures in the library: 331, across 396 fragment(s) read`**, so the two numbers are together and neither stands alone. The gate **does not pass** while a fragment cannot be read. The `--all` line says what `--all` does. **Nothing else moved, and the gate reads 0 on the real library.** `tests/test_check_signatures.py` is the first suite this gate has ever had: **4 red** against the module as found, with the three cases either side green before and after - two signed and promoted fragments left alone, an unused signature failing the gate, and a declared `proof-held:` NOT failing it and printing its own reason. **ONE FIXTURE FAULT WAS CAUGHT AND FIXED WHILE WRITING IT**: every copied fragment reads STALE on arrival, because the fingerprint includes the file's path and a fragment read from outside the repository comes back with `..` segments - which `heron_fragment` states in terms. The copies are re-stamped against themselves now, so the cases about waste are about waste; the one case that is about an unreadable file mutates AFTER the re-stamp

---

### Row 5b-132

*Moved from the register on 2026-09-23.*

**Defect.** **THE THREE FORMAT PATTERNS GUARDING THE ONE FILE NOTHING COMPILES ALL END IN `$`, AND IN PYTHON `$` ALSO MATCHES JUST BEFORE A FINAL NEWLINE.** Found 2026-09-22 reading `tools/check-products.py` - 527 lines, **no suite**, the fifth of the seven CI gates that had never been opened and the largest of them. It is the gate on `platform/heron-products.json`, which D-93 makes the single list of what Heron installs, and its own words are why it exists: *the price of that is a file nothing compiles - a typo in it reaches a modeller's machine untouched by every other gate in this folder.* **MEASURED, on manifests built by mutating a copy of the real one**: `folder` ending in a newline was accepted and the gate exited **0**; `id` ending in a newline likewise; and `addInId` ending in one matched the GUID pattern too. **`FOLDER` carries a comment saying exactly why it exists** - *a separator here would write outside the release folder, and `..` would write outside Addins altogether* - so it is a safety check with a documented purpose and an input that walks past it. JSON carries a newline inside a string quite happily. **ONE OF THE THREE WAS CAUGHT DOWNSTREAM ANYWAY, and it is worth recording which**: the GUID case exited 1 regardless, because `check_assets` compares the manifest's `addInId` against the shipped `.addin`'s and that comparison is exact. The id and the folder had nothing behind them

**State.** **FIXED 2026-09-22.** All three patterns end in `\Z`, which matches the end and nothing else, with the measurement recorded beside them and in the docstring. **Nothing else moved, and the gate still answers every offline question on the real manifest**: 5 products, 2 shipped, 2 proving, 1 planned, exit 0. **THE REST OF THE FILE IS A NEGATIVE RESULT AND THE SUITE RECORDS IT AS ONE.** `tests/test_check_products.py` is the first suite this gate has ever had, and every other section was green before the fix and after it: a duplicate `addInId` refused - the thing D-88 says no compiler, test or gate in this repository can see; a `partOf` naming nothing, which is *the typo that shows up as a missing tab rather than as an error*; a release the repository does not build, refused with AJ Tools' L3 in the message; a release named twice; two products sharing a folder, which is R-41 and L5; a version disagreeing with `Directory.Build.props`; an unknown key, refused rather than ignored; a lower-case GUID; a manifest that will not parse; and `--file` with nothing after it. **Shown to have teeth three ways**: the anchors put back (2 red), the duplicate-GUID report removed (1 red), and a release the repository does not build accepted (1 red). **ONE THING RECORDED RATHER THAN FIXED**: `--file` is the only flag and an unknown one is ignored in silence - the same shape as rows 5b-112, 5b-127 and 5b-129 - and here it runs against the default manifest and exits 0. **Stated precisely rather than talked up**: the first line it prints is `Manifest: <path>`, so it does NAME the file it read, and a reader who looks is not misled. What is missing is the refusal, not the honesty

---

### Row 5b-135

*Moved from the register on 2026-09-23.*

**Defect.** **THE FILE THAT FORBIDS TWO NUMBERS FOR ONE MEASUREMENT PRINTED THE OTHER ONE.** Found 2026-09-22 reading `tools/check-intrusion.py` - 213 lines, **no suite**, the LAST of the seven CI gates that had never been opened. Its docstring records what the tool measured the first time it ran - **0.313 over 330 utterances** - and then says in terms: *Quote what THIS TOOL prints, never this line. An earlier hand-written probe on the same day gave 0.234 over 312 utterances - a different corpus one commit back, not a different finding. **Two numbers for one measurement is how a figure becomes a remembered composite that matches no run that ever happened**, which is a failure this repository has already had once and now has a checker for.* **And then the branch that fires when the correlation has gone strong printed the HAND PROBE'S number as what the tool measured** - at exactly the moment a reader needs the right baseline to compare against. **ALSO FOUND, the same shape as row 5b-127**: `--top` was read as `argv[argv.index('--top') + 1]`, so `--top` alone is an `IndexError`, `--top six` a `ValueError`, and a misspelt flag is ignored in silence - and this tool retrieves for **every utterance in the library**, measured today at 2,227 across 396 fragments, so a mistyped flag costs minutes before it costs a wrong answer

**State.** **FIXED 2026-09-22.** The branch quotes this tool's own **0.313**, naming the corpus it came from - 330 utterances from 59 fragments - so there is one number for one measurement. A `settings_from()` reads `--top` **before the brain modules are imported and before the store is opened**, refusing a flag this tool does not have, a bare word, a flag with no value and a count that is not a number, each at exit 2. **RUN, not just read**: it answers **0.117** today over 2,227 utterances from 396 fragments, with the lexical backend - weaker than the 0.313 of 59 fragments, so the conclusion the docstring draws holds more strongly than when it was written, and the strong branch has still never fired. `tests/test_check_intrusion.py` is the first suite this gate has ever had and **it never runs the measurement**: **2 red** against the module as found, teeth proved three ways - the hand probe's number put back (1 red), the settings guard removed (8 red), and `correlation`'s divide-by-zero guard made to return 1.0 (1 red). **A TRAP WAS FOUND WHILE PROVING THOSE TEETH AND IS NOW IN `.claude/skills/heron-ship/SKILL.md` 2a**: a break and its restore inside the same second, at the same file size, are served STALE BYTECODE - CPython validates a `.pyc` against the source's mtime at one-second resolution. Measured: the `.pyc` at `09:49:36.754` and the restored `.py` at `09:49:36.760`, and the restored run went red on the broken behaviour while `inspect.getsource` printed the correct source. **The dangerous direction is the other one** - a break served stale GOOD bytecode reads as no red, which is exactly what would be reported as *the suite does not cover this*

---

### Row 5b-139

*Moved from the register on 2026-09-23.*

**Defect.** **A SIGNATURE THE PROOF TOOL COULD NOT WRITE WAS RECORDED AS SIGNED, AND THE EVIDENCE WAS DELETED BEHIND IT.** Found 2026-09-22 reading `tools/prove-agent.py` - 934 lines, **and no suite loaded or ran it**: one of four tools measured that day that a suite NAMES in prose and never executes, and that CI does not run either. It is the only route out of DRAFT for the 26 add-in agents, and its own header states the rule it is built on - *"The machine never signs. `by:` is required and written EMPTY."* `as_yaml`'s `quoted()` escapes the apostrophe that single-quoted YAML needs **and nothing else**, and `read_yaml` **silently skips any line it does not recognise**. **MEASURED on a draft written by the tool itself**, `accept HERON-REVIT-LVL-027 --by "Ajmal<newline>PS"`: it printed `signed by Ajmal / PS`, **exited 0**, wrote `by: 'Ajmal` into the proof - a bare quote, not a name - **and deleted the draft**. The draft is the evidence, `accept` removes it on the way out, and a signature cannot simply be given again: it was given against two live Revit sessions with two particular models open. **AND THE SECOND LINE CAN BE A KEY.** `--by "Ajmal PS<newline>heron-status: PROVEN"` put **`PROVEN'`** into the proof's own status - three lines below where `cmd_accept` deliberately sets `DRAFT` and prints *"Promoting it is a separate, deliberate act, and it is yours."* Nothing automated acts on that field today, and saying so is part of the finding: **nothing but this tool reads `brain/agent-proofs/`**, measured across the repository; the files are read by people, and linked from `docs/NEEDS-CHECKING.md` as the evidence for a signed agent. **TRACED AND NOT RAISED, because it is the obvious guess and it is wrong**: a **colon** in the name is sound here - `--by "Ajmal PS, BIM Lead: Heron"` round-trips exactly - which is where [row 5b-122](../FRAGMENT-ISSUES.md#) found `resign-machine-proofs.py` broken. This writer got the apostrophe right and the line break wrong. **RECORDED, NOT FIXED**: `cmd_vary` puts its `tracking:` rows through `as_yaml` as **Python dict reprs inside single-quoted YAML strings** - `- '{''input'': ''category=Pipes'', ...}'`. Measured; it round-trips and a person can read it, but it is a Python repr in a proof, and fixing it would change the draft format rather than close a hole.

**State.** **FIXED 2026-09-22.** `read_yaml` is split into `parse_yaml(lines)` + `read_yaml(path)`, and an `unwritable(body)` renders the body and **reads it straight back in memory, before anything is written or deleted**, refusing by name any field that would not survive. Both writers go through it - `write_draft` raises and `cmd_accept` refuses with the draft untouched - because `track` builds a draft out of a model's own answers and a document title carries whatever the person who saved it typed. `cmd_accept` also **reads the file back off disk and confirms the signature before it removes the draft**, which covers what the in-memory guard cannot: a short write, a full disk, an encoding the platform would not take. Nothing guesses at an escape; a value this small writer cannot represent is refused. `tests/test_prove_agent.py` is the first suite this tool has ever had: **6 red** against the module exactly as found. It **never opens a bridge** - `track`, `vary` and `sessions` need two live Revit sessions with different models, which is row T1 and its neighbours in NEEDS-CHECKING - and every case points `ROOT`, `REGISTRY`, `DRAFTS` and `PROOFS` at a temp folder, because a case that signed into `brain/` would be writing a proof. **TWO BREAKS LEFT THE SUITE GREEN AND THAT WAS THE SUITE'S FAULT, NOT THE TOOL'S** - it never called `write_draft` with a bad value, and the read-back's failure path was unreachable while the in-memory guard held. Both have a case now. Teeth proved seven ways: the module as found (**6 red**), the in-memory guard alone (**2**), the read-back alone (**2**), `write_draft`'s guard (**2**), the `revit/` write refusal (**1**), the fingerprint's line-ending normalisation (**1**), and `elements` back in the envelope unconditionally (**1**). **SEVEN THINGS WERE MEASURED AND FOUND RIGHT**, and the suite records them so nobody re-reads: the refusal to write anywhere under `revit/`, which makes *"it never promotes an agent"* a property of the code's reach; the fingerprint normalising path separators and line endings, after hashing raw bytes made all sixteen proven fragments read STALE in a Linux container; `compare` treating the document's own name as the input rather than the answer; `elements` skipped as a scalar total but read as the answer inside a list; `parse_args` refusing a pair with no `=`; the empty `--by` refusal; and `vary` refusing fewer than three inputs, the same bar `brain/heron_validate.py` enforces for a fragment.

---

### Row 5b-140

*Moved from the register on 2026-09-23.*

**Defect.** **A SKILL WHOSE DECLARED RISK IS NOT A RISK IS EXEMPT FROM EVERY RISK CHECK, AND THE REPORT NAMES IT NOWHERE.** Found 2026-09-22 reading `tools/check-skill-routing.py` - 770 lines, and **no suite loads it**. `classify()` decides a crossing by comparing two declared risks. `rung()` answers **-1** for anything off the ladder and `ASKS_A_QUESTION` does not hold, so **every risk branch falls through**. **MEASURED**: `classify("", "ADMIN", ...)` and `classify("Modifies", "MODIFY", ...)` both answer `miss`. And such a skill **is** routed: `heron_skill.load_all()` handles a file it could not parse and a file with no id, but **does not call `heron_skill.validate()`** - validation is a separate function - measured on a fixture where a skill with no `risk:` and one with `risk: Modifies` both came back in `found` with **zero problems reported**. The report's only heading about skills it did not route says *"SKILLS THAT WOULD NOT LOAD"*, which such a skill is not one of, so the reader is shown **`CROSSINGS: 0`** for a skill whose risk nothing knows - D-52's plausible zero in the one tool whose whole subject is two declarations disagreeing. **NOT A LIVE FAILURE, and that is part of the finding**: all ten skills declare a risk on the ladder today, and `tests/test_skills.py` calls `validate()`, so CI would name such a file. What was missing is this tool saying so about the verdicts **it is the only one making**. **TRACED AND NOT RAISED**: `--revit` is not validated, but an unsupported release is not the row 5b-127 shape - measured at `2019` and at `banana`, all 43 utterances land in a loudly printed **NOT RESOLVED (43)** with every other list at zero, and the release used is the first line of the report. Argparse also refuses an unknown flag here, unlike rows 5b-112, 5b-127, 5b-129 and 5b-132. **LEFT FOR WHOEVER READS `prove-skill.py`**: `classify` is **imported** by `tools/prove-skill.py`, so the same silence is in its proof path. Changing `classify`'s five return values from inside a tool that has not been read would be widening someone else's contract, so the report was made honest instead and this sentence is the pointer.

**State.** **FIXED 2026-09-22.** `skills()` answers `(loaded, problems, unrated)`, and `main()` prints the third under its own heading: they **are** routed and their reach and miss lines are true, but **none of the risk lists can speak for them**, and it points at `python brain/heron_skill.py`, whose `validate()` names the file and the field. `tests/test_skill_routing.py` is the first suite to **load this tool directly**: **1 red** against the module as found. **A CORRECTION TO THIS SESSION'S OWN MEASUREMENT, recorded because two pull requests carried it.** This tool was called *"named by a suite only in prose"*. That was **wrong**: `classify`, `unsettled`, `words_moved`, `fingerprint_lines` and `NOTHING_FOUND` are all exercised by `tests/test_skill_proving.py` and `tests/test_skill_catalog.py` **through `prove-skill.py` and `generate-skill-catalog.py`, which import them** - a second hop the first scan did not follow. Re-measured counting that hop, exactly **two** never-read tools are run by nothing at all, not directly, not through another tool, and not by CI: **`check-risk-crossings.py` (440)** and **`api-changes.py` (208)**. What nothing touched here is the tool's **own** half - `skills`, `without_the_store`, `declared_by_fragments`, `margin`, `rung` - and that is what the new suite is. It deliberately does **not** re-assert the five verdicts `test_skill_proving.py` already pins, because two copies of a judgement is the thing this tool's own docstring argues against. Teeth proved five ways: `skills()` back to two answers (**1 red**), `rung()` answering 0 instead of -1 for an unknown risk (**2**), an OUT counted as inside the skill's plan (**1**), counts that could not be read reported as zero (**1**), and one sentence claimed by two skills no longer reported (**1**). **FOUR THINGS MEASURED AND FOUND RIGHT**: the identity/ranking split is read from disk so it cannot wobble between runs (row 116); an OUT is a guaranteed disagreement and is marked as one; `margin` reports the two routes' ranks rather than inventing a scalar the seam does not carry, which is why row 109's *"2.4 ranks clear"* is not reproducible here and was taken by hand; and a store whose counts could not be read says so under D-52 instead of reading as empty.

---

### Row 5b-141

*Moved from the register on 2026-09-23.*

**Defect.** **A QUARTER OF THE SWEEP'S QUESTIONS CANNOT PRODUCE THE FINDING IT EXISTS FOR, AND THE HEADLINE COUNTS THEM ANYWAY.** Found 2026-09-22 reading `tools/check-risk-crossings.py` - 440 lines, and, measured counting the second hop as well as the first, **one of exactly two never-read tools that NOTHING runs**: no suite directly, no suite through another tool, and not CI. Its list exists because `check-routing.py` can only ask what a fragment DECLARES, and its docstring is explicit: *"A question a fragment already declares is worth less here, not more: the point is the sentences nobody thought to claim."* The ten skill sentences added on 2026-09-19 were chosen because each *"is declared in a skill's `utterances:` and declared by NO fragment - measured, not judged - which is exactly the hole rows 113 and 116 name: identity cannot fire, so ranking decides, and ranking is the thing that answers a question with a write."* **THAT CRITERION HAS GONE STALE UNDER THE LIST.** Measured 2026-09-22 against the live store: of **78** questions, **26 now come back by the IDENTITY route** - a fragment declares them - and only **52** reach ranking at all. An identity answer is fixed at that fragment's own declared risk, so it leaves by the risk test's `continue` and **can never be a crossing here**; among the 26 are eight of the ten skill sentences the criterion was written for, including *"how many sprinklers"*, *"is the ductwork still connected"* and *"what sizes are the ducts"*. The report said `questions asked: 78`, which is the size of the LIST rather than the size of the QUESTION - and the sweep finds **7** crossings, which is 7 of 52, not 7 of 78. **ALSO FOUND, AND THE FILE ARGUES AGAINST IT ITSELF**: `CHANGES_THE_MODEL` and `CHANGES_A_VIEW` - the two tuples that decide the whole verdict - were a verbatim second copy of `check-skill-routing.py`'s, identical when measured. Three functions earlier the same file explains why it IMPORTS the store fingerprint instead of keeping one: *"This file carried its own copy ... two copies of one rule is how one of them goes stale."*

**State.** **FIXED 2026-09-22.** A `denominator_lines(asked, by_identity)` prints all three figures under **HOW MANY OF THEM RANKING ACTUALLY DECIDED**, naming the route so a reader knows why the 26 do not count; the tally is taken **before** the risk test skips them, which is how the size of the list came to stand in for the size of the question in the first place. The two tuples are imported from `_ROUTING` like the fingerprint. And `read_that_lost(found, winner)` lifts the discriminator out of `main()`, where nothing could reach it without opening a store - and a count taken from the store is a **sample**, not a measurement (row 116: three sweeps minutes apart gave 7, 9 and 8, two on byte-identical inputs), so a test that opened one would be asserting today's index. **THE EXTRACTION WAS PROVED TO CHANGE NO VERDICT, not asserted to**: both forms were run end to end against the live store and returned the **same seven crossings, in the same order, with the same beaten read** - `what views are on this sheet`, `what is the insulation thickness`, `how high is this off the floor`, `find the fire dampers`, `hide everything except the walls`, `what is the scale of this view`, `what levels are in this model`. The store's md5 differed between the two runs, which is **the tool's own documented behaviour and not a fault** - it prints *"Asking moved the index"* for exactly this. `tests/test_risk_crossings.py` is the first suite anything has ever pointed at this file, and it **never opens a store**: **4 red** against the module exactly as found. **ONE OF THE SUITE'S OWN CHECKS CRASHED RATHER THAN FAILED ON ITS FIRST RUN** - it called `denominator_lines` before it existed and got an `AttributeError`, which proves nothing (`heron-ship` section 2a). Both new names are read through `getattr` now, so the run against the module as found is clean red. Teeth proved five ways: the module exactly as found (**4 red**), the two tuples copied back alone (**2**), EXECUTE counted as a read that lost - row 145's mistake (**1**), the winner counted as the read it beat (**1**), and the denominator no longer naming the route (**1**). **TRACED AND NOT RAISED**: argparse refuses an unknown flag here; the list holds no duplicate and no stray whitespace; and the docstring's *"45 ordinary questions found 17"* is a dated historical measurement rather than a stale count, so it is left as written.

---

### Row 5b-144

*Moved from the register on 2026-09-23.*

**Defect.** **THE SCAFFOLDER THAT EXISTS TO STOP HEADER MISTAKES WROTE `Heron-Step: banana` INTO TWO FILES AND EXITED 0.** Found 2026-09-22 reading `tools/new-agent.py` - 410 lines. It puts three things into a header: the part's layer, the module name and the step. **`--part` is checked against a list, `--module` against a pattern, and `--step` was not checked at all.** **MEASURED on a tree built for the purpose**: `new-agent.py HERON-DEV-ARC-003 --step banana` printed *written* three times, exited **0**, and left `# Heron-Step:   banana` in both the module and the test. Its own docstring is why that matters: *"Done by hand 146 times that is 146 chances to mistype a layer, invent a field, or copy a header from a file in a different part - and **check-metadata.py finds the mistake after the work, not before it**."* **AND check-metadata WOULD NOT NAME IT**, traced rather than assumed: line 235 asks only that the five fields are PRESENT, and the step arithmetic at lines 188 and 276 is guarded by `.isdigit()` - so a file whose step is not a number is quietly left out of the counts rather than reported.

**State.** **FIXED 2026-09-22.** `--step` is refused before anything is written, the same shape as `--part` and `--module`, and the refusal says why check-metadata will not catch it. **`tests/test_new_agent.py` ALREADY OWNED THIS TOOL, and every one of its cases was a REFUSAL** - it says so itself: *"EVERY CASE BELOW WRITES NOTHING ... this runs inside the real repository, because the register and the module list are what it is asserting against."* So **nothing had ever asserted anything about the three files it writes**. Extended rather than duplicated, with the write cases pointed at a temp tree and `NA.ROOT`/`NA.register` put back afterwards - and a final check that the real repository is untouched. **5 red** against the module as found. **A NEGATIVE RESULT MADE PERMANENT, and it is the valuable half.** The module template argues at length that the stub must **not** claim the agent, because `agent-count.py` reads any commented `Heron-Agent` line in the first 40 lines *including one quoted inside a docstring*, so an EXAMPLE of the line would raise the built count for work nobody has done. **Nothing checked that the template obeys its own argument.** Measured: both templates declare exactly `Heron-Agent:  none`, and the agent id appears only in prose. It is asserted now, on both files. Teeth proved five ways: the module as found (**5 red**), the stub claiming the agent in its header (**2**), the layer no longer following the part (**1**), a stub claiming DRAFT rather than DISCOVERED (**1**), and writing one file at a time instead of all-three-or-none (**1**). **Nothing was written into the repository** - checked with `git status` after the run.

---

### Row 5b-146

*Moved from the register on 2026-09-23.*

**Defect.** **TWO GENERATORS TYPE A COUNT THEY DERIVE CORRECTLY ONE LINE LOWER, AND BOTH TYPED COUNTS ARE STALE.** Found 2026-09-22 reading the generator family. **The pages are right; their docstrings were not.** `tools/generate-fragment-catalog.py` (359 lines) says *"349 are catalogued and most have never met a model"* **in the present tense**, in the file whose own argument is that *"a generated artefact cannot lie about its source ... Every number and every row here comes off disk at the moment it runs"*. **Measured: the library holds 396** - count the `fragment.yaml` files under `brain/fragments/` - and the page itself prints `396 fragment(s), 331 with a proof, 19 whose negative case cannot run`. Wrong by 47. `tools/generate-api-docs.py` (338 lines) says importing the server *"defines **eighteen tools** as a side effect"*, also present tense. **Measured: 34** - `grep -c '^@server.tool()' mcp/server/heron_mcp_server.py` - and the page prints `34 tool(s)`. Wrong by 16. AGENTS.md has both the rule and the reason: *"Never type a number a command can derive. Say where it comes from instead. Three READMEs in this repository once carried a fragment count wrong by more than a hundred, each with a sentence beside it admitting the number goes stale."* **ONE SENTENCE IN EACH IS A DATED MEASUREMENT AND IS LEFT AS IT IS**: api-docs' *"Two were unexplained the first time this ran"* is a record of that run, and the fragment catalogue's opening now says 349 was what the library held **the day it was written**. A proof is not edited after the fact ([Golden Rule 4](../14-golden-rules.md)); a present-tense claim is not a proof.

**State.** **FIXED 2026-09-22.** Both present-tense counts are replaced by the command that derives them, and each says the page prints the figure it actually read. Nothing else in either file changed, and both pages produce byte-identical output - run into scratch paths, never over anything committed.

---

### Row 5b-147

*Moved from the register on 2026-09-23.*

**Defect.** **THE API PAGE'S ONE LIVE FINDING WAS FALSE, BECAUSE A PARAMETER NAMED IN CAPITALS READ AS UNEXPLAINED.** Found 2026-09-22 reading `tools/generate-api-docs.py`. Its stated conclusion is *"THE CONCLUSION IT CARRIES: A PARAMETER NOTHING EXPLAINS"* - every parameter checked against its own tool's docstring, on a word boundary - and the page reported exactly one: **`revit_change: nothing explains capability`**. **IT IS EXPLAINED.** `mcp/server/heron_mcp_server.py:806` says *"Ask for the **CAPABILITY**, never a fragment id: heron_lookup turns the user's own words..."* - capitals being this repository's house style for emphasis. `explains()` matched with `re.search(r"\b%s\b")` and **no `re.IGNORECASE`**, so `\bcapability\b` missed `CAPABILITY`. **A PAGE CARRYING ONE FINDING, WITH THAT FINDING WRONG, IS THE FAILURE ITS SIBLING ALREADY RECORDS**: `generate-contract-reference.py` says *"A page of 135 findings that are all wrong is worse than no page: it teaches the reader to skip the table, which is where the real ones are."* At one finding it teaches the same lesson faster.

**State.** **FIXED 2026-09-22.** `explains()` matches case-insensitively. **The word boundary is what does the work and is untouched** - `full` is still not explained by `FULLY`, which the suite now asserts in both cases. `tests/test_api_docs.py` already owned this function (its claim 2 is the boundary rule) and was **extended rather than duplicated**: **2 red** against the module as found. Teeth proved two ways - the case-sensitivity put back (**2 red**) and the word boundary dropped for a substring match (**4 red**). The page now reports **34 tools, 26 parameters, 2 that change the model, and no unexplained parameter** - because the one it was naming was explained all along.

---

### Row 5b-148

*Moved from the register on 2026-09-23.*

**Defect.** **ELEVEN OF THE FOURTEEN QUESTIONS REPORT `0`, AND THAT ZERO MEANS THREE DIFFERENT THINGS.** Found 2026-09-22 reading `tools/check-revit-gate.py` - 651 lines, a checker CI does not run. It asks the master architecture document's fourteen questions of every fragment and gives each one of four verdicts, and its docstring is emphatic about why there are four: they *"describe EVIDENCE, not lifecycle - docs/24's two axes are untouched and this adds no third vocabulary"*. **Then `sweep()` collected only the `LOOK` verdicts and threw the rest away**, so the summary a reader actually reads was fourteen rows of a single number. **MEASURED by tallying every verdict across all 396 fragments**: eleven of the fourteen rows read `0`, and among them - `Q11` is **ANSWERED for all 396**, so the unit check ran and found nothing; `Q9` is **NEEDS A RUN for all 396**, so nothing was checked at all; `Q13` is **BY DESIGN for all 396**, and this file's own docstring says at length that it **cannot** decide that question and that D-28's executor answers it instead. **One presentation, three meanings**, in the tool whose whole argument is that the four verdicts are different. AGENTS.md is blunter: *"Separate the four states and never merge them: PASS - FAIL - NOT RUN (say why) - NEEDS REAL REVIT."* A question nobody asked reading as a zero is that rule broken in the tool's own headline. **A LEFTOVER SAYS IT USED TO KNOW**: `labels = {LOOK: "worth a look"}` sat two lines above, assigned and never used.

**State.** **FIXED 2026-09-22.** `sweep()` tallies every verdict, and a row with no looks now carries the verdict that stood instead - `ANSWERED for all 396`, `BY DESIGN for all 396`, `NEEDS A RUN for all 396` - under a three-line legend saying what each means. The dead `labels` is gone. **Nothing else changed**: the fourteen questions, their order, the four verdicts and every judgement in `ask()` are untouched, and the tool still exits 0 because a finding here is a question for a person. `tests/test_revit_gate.py` already owned this tool - five claims covering the fourteen in order, the unit check, a fragment opening its own transaction, only four verdicts ever produced, and question 13 answering differently for a reader and a writer - and was **extended rather than duplicated**: **2 red** against the module as found. **THE FIRST DRAFT OF THE CASE WAS IN THE WRONG PLACE AND PASSED FOR THE WRONG REASON** - it sat inside the block where the library is a temp fixture, so `entries` was empty, every row had no verdict to report and the check read as a defect in the tool. Moved to the section that loads the real library. Teeth proved two ways: the note dropped from the row (**2 red**), and the note kept but no longer naming which verdict stood (**2 red**). **TRACED AND NOT RAISED**: every remedy the report names works - `--list links` prints 68 names and `--list reporting` 65, measured; the `> 20` spread threshold is a display choice with the count still printed beside it; and an unknown `--list` key is ignored in silence, the shape of rows 5b-112, 5b-127, 5b-129 and 5b-132, but here the output names the right key on the very line that sends you looking.

---

### Row 5b-149

*Moved from the register on 2026-09-23.*

**Defect.** **THE DISPATCH RULE STORED THE DICT KEY, AND THE LOOKUP ASKED BY FUNCTION NAME. IT NEVER ONCE SUPPRESSED A REAL DISPATCH, AND IT SUPPRESSED 52 PRODUCTION FUNCTIONS BY VOCABULARY.** Found 2026-09-22 reading `tools/check-reachable.py` - 334 lines, the report that asks which built function no production path calls. Its docstring is emphatic about precision: *"The precise test is a dict literal whose value is the function or a getattr with a literal name. Both are structures; neither can be produced by prose."* It then lists three heuristics that were fooled by text ABOUT the thing rather than the thing. **THREE findings, all measured by running `survey()` over this repository.** **ONE.** `survey()` recorded `dispatched[key.value]` - the KEY - while `hits()` asks `if name in dispatched` with `name` the FUNCTION's name. Measured: thirteen dict entries in the tree have a module-level function of their own file as the value, and in **every one the key differs** - `"accept": cmd_accept` in prove-agent.py, `"header_disagreements": disagreements` in heron_agents.py, `".md": _read_text` in heron_ingest.py. **The rule had never suppressed the thing it was written for.** **TWO.** Because it matched any dict mapping a string to a Name, it swallowed the RESULT DICT - the commonest shape in `brain/` - `{"check": name, "passed": True}` in heron_qa.py, `{"package": name, "version": version}` in heron_dotnet.py. `dispatched` held **636** names: `count`, `report`, `review`, `version`, `read`, `evidence`, `why`, `what`. **52 of the 400 public production function names were on it**, each one permanently invisible to this report - if `heron_evaluator.report` lost its last caller tomorrow the tool would say nothing. That is the tool's own stated failure, in the tool written to catch it. **THREE.** The getattr half read `node.args[-1]`, which in the three-argument form is the **DEFAULT**, not the name. **183 of the 191** getattr calls in the surveyed areas have three arguments, so it recorded `''`, `'?'`, `'2024'` and `'.txt'` as dispatched names and missed the real ones. **WHY THE SUITE DID NOT CATCH IT:** `tests/test_reachable.py` case 2 used `{'accept': accept}`, the one form where key and function name are the same word, which **no dispatch table in this repository uses**. It passed on a coincidence the real tree does not supply - the same shape as row 148's first draft. **FOUR, in the tool's page rather than its code.** `tools/README.md` repeated the same misleading example and then said, in the present tense, *"It found 12, of which 5 were already recorded and one - Q-47, `heron_capability.want()` - was not."* **Both halves had gone**: Q-47 was answered 2026-09-09 and marked so in OPEN-QUESTIONS, `want()` was wired up, and it is not reported at all now - measured, today's run is 8 hits, 2 recorded. A second typed number, *"It went 12 hits to 4"*, was stated flat as if current. **The tool carries a whole section for this failure** - a RECORDED excuse whose reason has gone, D-54 applied to itself - and its own page was the thing out of date.

**State.** **FIXED 2026-09-22.** The value's identifier goes in, and only when it names a module-level function of that same file; the getattr name is read from `args[1]`. An imported function used as a dict value needs no rule - a bare name the file imported was already counted a reference. **The `own` restriction earns its keep and is not belt-and-braces**: measured, taking any Name as the value moves the leak rather than closing it, and **37** production names still get in that way - `want` among them, which this tool's own comment calls Q-47's whole subject. **`tests/test_reachable.py` ALREADY OWNED THIS TOOL** - eight numbered cases, a good suite - so it was **extended, not duplicated**, with case 2b and a corrected claim 1. **3 red against the module exactly as found.** Teeth proved four ways: the module as found (**3 red**), the key-instead-of-the-value alone (**2**), `args[-1]` alone (**1**), and the value taken without the own-function restriction (**1**). **THE REPORT ITSELF IS UNCHANGED** - six unexplained hits and two recorded, the same names before and after - so this is a fix to the tool's evidence, not to its answer, and the register says so rather than claiming a finding it did not produce. **What was traced and NOT raised:** `decorated` is also a name-only set, and a decorated function in one file would silence a same-named function in another - measured, all 34 decorated names carry their own decorator, so it fires nowhere today and is a shape, not a behaviour. The one module-level `async def` in the surveyed areas is in `tests/`, which is never reported, so `ast.AsyncFunctionDef` being invisible costs nothing yet. No caller of any reported hit exists outside the five surveyed areas - checked, the only Python file outside them is the guard hook. **`tools/README.md` corrected in place** rather than recorded for later: it is the page of the file being read, its counts are derivable, and AGENTS.md's rule is that a number a command can derive is never typed. The historical figures are kept and dated; the live ones are replaced by the command. **Needs no Revit.**

---
